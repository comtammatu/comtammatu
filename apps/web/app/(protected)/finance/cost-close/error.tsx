"use client";

import { ErrorPanel, type ErrorPanelProps } from "@/components/error-panel";

export default function InventoryCostCloseError(props: ErrorPanelProps) {
  return <ErrorPanel {...props} />;
}
