{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "git": {
    "deploymentEnabled": {
      "**": false,
      "main": true
    },
    "ignoreCommand": "if git diff --name-only HEAD^ HEAD | grep -qE '^(apps/|packages/|scripts/|supabase/|package\\.json|pnpm-lock\\.yaml|turbo\\.json)'; then exit 1; else exit 0; fi"
  },
  "regions": ["sin1"],
  "crons": [
    {
      "path": "/api/cron/kds-maintenance",
      "schedule": "0 5,11,17,23 * * *"
    },
    {
      "path": "/api/cron/tax-invoice-issue",
      "schedule": "*/5 * * * *"
    }
  ]
}
