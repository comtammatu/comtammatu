# Regressions — Named Failure Rules

> Loaded at the start of every session. Untagged lines are active prevention rules; lines marked `SUPERSEDED`, `PILOT-ONLY`, or `BACKLOG` are context and must not override newer active rules.
> Format: [DATE] [RULE NAME] — short description

- [2026-05-09] UI-TABS-TABLE-BADGE-PROPS — `Tabs`, `Table`, and `Badge` MUST express repeated geometry through primitive props. `TabsList variant/size` owns tab chrome and trigger height; route code may set layout width/grid only. `Table density` owns cell padding/row rhythm; `TableHead variant="eyebrow"` owns uppercase metadata header typography. `Badge size` owns pill geometry. Detect: `rg -n -U '<TabsList[^>]*className=\\{?\"?[^>]*(h-|!h-|p-[0-9]|!p-|rounded|bg-muted|gap-)|<TabsTrigger[^>]*className=\\{?\"?[^>]*(px-|py-|bg-|data-\\[state=active\\]|!flex-none)|<TableHead[^>]*className=\\{?\"?[^>]*(px-6|py-4|uppercase tracking|text-xs font-(bold|semibold))|<TableCell[^>]*className=\\{?\"?[^>]*(px-6|py-4)|<Badge[^>]*className=\\{?\"?[^>]*(rounded-full px-|py-1\\.5)' apps/web/app packages/ui/src/components` MUST 0 or only primitive-internal definitions / documented layout exceptions.
- [2026-05-09] UI-ACTION-CONTROLS-PRIMITIVE-PROPS — Buttons, button groups, toggles, checkbox, switch, and slider MUST express repeated visual roles through primitive props before route-local Tailwind. POS payment/action tiles use `Button size="choice"`; served/done actions use `Button variant="success"`; mobile sheet actions use `Button size="touch"` instead of `min-h-11`; segmented state selectors use `ToggleGroup variant="segmented"` and semantic `spacing` values; checkbox/switch/slider hit targets use `size`. Detect: `rg -n -U '<Button[^>]*className="[^"]*(h-20 flex-col gap-2|min-h-11 w-full bg-success)|<Button[\\s\\S]{0,180}className="min-h-11 w-full"|<ToggleGroup[^>]*spacing=\\{[0-9]|<ToggleGroupItem[^>]*className="[^"]*(data-\\[state=on\\]:bg-primary|!rounded-none|h-full justify-center text-sm)|<Checkbox[^>]*className="[^"]*(size-|after:)|<Switch[^>]*className="[^"]*(h-\\[|w-\\[|after:)|<Slider[^>]*className="[^"]*(h-|w-|after:|rounded)' apps/web/app packages/ui/src/components` MUST 0 or only primitive-internal definitions.
- [2026-05-09] UI-INPUTGROUP-DOM-ORDER-AND-SIZE — `InputGroup` is for search, clear, unit, currency, percent, scanner, or compact helper-action fields; do not wrap every plain/legal/date/long-note field just to look compact. Render `InputGroupInput`, `InputGroupTextarea`, or custom controls with `data-slot="input-group-control"` first in DOM order, then render `InputGroupAddon`; use addon `align` to place leading/trailing visuals. Height MUST come from `InputGroup size` (`sm`, `default`, `touch`, `touch-lg`), not caller `h-10`, `h-12`, or `h-8!` overrides. Mobile optimization is valid only when it preserves a 44px touch target and keeps label/error semantics clear. Detect: `rg -n '<InputGroup[^\n]*h-10|<InputGroup[^\n]*h-12|isMobile && "h-12|h-8!' apps/web/app packages/ui/src/components` MUST 0; scan InputGroup blocks so no `InputGroupAddon` appears before the control.
- [2026-05-09] UI-FIELD-A11Y-AND-PRIMITIVE-HEIGHT — Reusable form wrappers MUST compose shadcn `Field` as the semantic shell: `Field` gets `data-invalid`, the actual control gets `aria-invalid`, and `FieldDescription` / `FieldError` IDs are joined into the control's `aria-describedby`. Required state uses `aria-required`; native `required` validation is opt-in per form. Form wrappers MUST NOT reapply primitive heights (`h-10`, `!h-10`, `min-h-24`) to `Input`, `Textarea`, `SelectTrigger`, `Button` combobox triggers, or formatted-number inputs; the primitive owns those heights. Mobile defaults to vertical `Field`; use horizontal/responsive only for binary settings or real container-aware desktop layouts. Detect: `rg -n 'className=\\{cn\\(\"(!?h-10|min-h-24)|className=\"!?h-10|className=\"min-h-24' apps/web/app/components/form` MUST 0.
- [2026-05-09] UI-SELECT-COMBOBOX-SIZE-AND-SEMANTICS — `Select` is for known finite choices; combobox wrappers are for search/autocomplete, object matching, or multi-select. Do not turn short finite selects into comboboxes just for visual richness. `SelectTrigger` height MUST come from `size` (`xs`, `sm`, `default`, `touch`), never route-local `h-7/h-8/h-9/h-10/min-h-*`. Combobox triggers compose `Button`; their height MUST come from `Button size`, not `className`. Detect: `rg -n -U '<SelectTrigger[^>]*className=\\{?\"?[^>]*(h-[0-9]|!h-|min-h-)|role=\"combobox\"[^>]*className=\\{cn\\([^)]*\"[^\"]*(h-[0-9]|!h-|min-h-)|CommandInput[^>]*className=\"[^\"]*(h-[0-9]|!h-|min-h-)' apps/web/app packages/ui/src/components` MUST 0.
- [2026-05-09] UI-COMMAND-POPOVER-GEOMETRY-AND-SELECTION — `Popover` is for short anchored contextual content; long or multi-step mobile flows belong in `Dialog`, `Sheet`, or `Drawer`. `PopoverContent` geometry MUST come from `width` (`default`, `trigger`, `auto`, `notification`) and `padding` (`default`, `compact`, `none`), not route-local `className`/inline `style`. `CommandList` height MUST come from `maxHeight`; selected state MUST use `CommandItem checked` instead of caller-drawn duplicate trailing check icons. Detect: `rg -n -U '<PopoverContent[^>]*className=\\{?\"?[^>]*(w-\\(|w-\\[|w-[0-9]|p-[0-9]|p-0|gap-[0-9])|<PopoverContent[^>]*style=|<CommandList[^>]*className=\\{?\"?[^>]*max-h-|<CommandEmpty[^>]*className=\\{?\"?[^>]*(py-|text-sm|text-center)|<CommandGroup[^>]*className=\\{?\"?[^>]*p-[0-9]|<CommandItem[^>]*className=\\{?\"?[^>]*(rounded|px-|py-|text-sm|cursor-pointer)' apps/web/app packages/ui/src/components` MUST 0.
- [2026-05-09] UI-MENU-GEOMETRY-DENSITY-AND-STATE — `DropdownMenu`, `ContextMenu`, and `Menubar` own action-menu geometry through content `width`/`density` props, not caller class strings. `width="action"` replaces local `w-56`; `density="touch"` is required for POS/KDS/frontline touch menus. Use checkbox/radio menu items for toggle or exclusive-choice state; never fake selected state with plain `data-active`. Use `variant="destructive"` for destructive menu items instead of `className="text-destructive"`. Icons inside menu items inherit primitive gap/size, so callers render bare `<Icon />` without `mr-*` or `size-*`. Detect: `rg -n -U '<(DropdownMenuContent|DropdownMenuSubContent|ContextMenuContent|ContextMenuSubContent|MenubarContent|MenubarSubContent)[^>]*className=\\{?\"?[^>]*(w-|min-w-|max-h-|p-|rounded|shadow|bg-|ring-)|<(DropdownMenuItem|DropdownMenuCheckboxItem|DropdownMenuRadioItem|DropdownMenuSubTrigger|ContextMenuItem|ContextMenuCheckboxItem|ContextMenuRadioItem|ContextMenuSubTrigger|MenubarItem|MenubarCheckboxItem|MenubarRadioItem|MenubarSubTrigger)[^>]*className=\\{?\"?[^>]*(text-destructive|px-|py-|min-h-|h-|rounded|text-xs|text-sm)|<DropdownMenuItem[^>]*data-active=' apps/web/app` MUST 0. Also run `rg -l "DropdownMenu" apps/web/app -g '*.tsx' | xargs rg -n '<Icon[A-Za-z0-9]+[^>]*className=\"[^\"]*(mr-2|size-4)[^\"]*\"'` and manually confirm hits are trigger/button/non-menu-item icons only.
- [2026-05-09] UI-NAV-SELECT-PAGINATION-SEMANTICS — `NativeSelect` is the only allowed native `<select>` surface in app code and owns height via `size` (`xs`, `sm`, `default`, `touch`) plus wrapper width via `width` (`fit`, `full`). `NavigationMenu` is only for horizontal destination navigation; never use it as tabs, sidebar, breadcrumb, or POS/KDS rail, and never override trigger/content geometry through route classes. Pagination MUST compose `Pagination` primitives: URL page changes use link components, client-state table pagination uses button components. Detect: `rg -n '<select\\b' apps/web/app` MUST 0; `rg -n -U '<NativeSelect[^>]*className=\\{?\"?[^>]*(h-[0-9]|min-h-|rounded|px-|py-)|<NavigationMenu(Trigger|Content|Link)[^>]*className=\\{?\"?[^>]*(h-[0-9]|min-h-|rounded|p-[0-9]|w-[0-9]|min-w-)|<Button[\\s\\S]{0,500}(previousPage|nextPage|onPageChange\\(currentPage [+-])' apps/web/app` MUST 0.
- [2026-05-09] UI-PROGRESS-RADIO-RESIZABLE-SCROLL-CONTRACT — `Progress` height comes from `size` (`xs`, `sm`, `default`, `lg`) and semantic color from `tone`; callers may set layout width/flex only, never `h-*`, radius, background, or indicator color. `RadioGroup` spacing and hit target come from `density` (`compact`, `default`, `touch`); POS/KDS/frontline choice lists use `density="touch"` plus full-row clickable labels. `ResizableHandle` target/grip geometry comes from `size`; route code does not style handle width/height/grip. `ScrollArea` owns `overflow-hidden`, scrollbar presence, size, and thumb styling; callers only supply layout bounds such as `min-h-0`, `flex-1`, or `max-h-*`. Detect: `rg -n -U '<Progress[^>]*className=\\{?\"?[^>]*(h-[0-9]|rounded|bg-|text-|\\[&|from-|to-)|<RadioGroup[^>]*className=\\{?\"?[^>]*(gap-|space-y-|grid-cols)|<RadioGroupItem[^>]*className=\\{?\"?[^>]*(size-|after:|ring-|border-)|<ResizableHandle[^>]*className=\\{?\"?[^>]*(w-|h-|after:|rounded|bg-)|<ScrollArea[^>]*className=\\{?\"?[^>]*(overflow-hidden|scrollbar|\\[&.*scroll|thumb)' apps/web/app` MUST 0.
- [2026-05-09] UI-MODAL-CONTENT-GEOMETRY-PROPS — `DialogContent`, `SheetContent`, and `DrawerContent` own modal geometry through props, not route-local class strings. `DialogContent`: `size`, `padding`, `scroll`, `placement`. `SheetContent`: `size`, `height`, `surface`, `scroll`. `DrawerContent`: `size`, `height`, `surface`. `AlertDialog` is reserved for interrupt/destructive decisions; do not use a normal `Dialog` for irreversible confirmation. Detect: `rg -n -U '<(DialogContent|SheetContent|DrawerContent)[^>]*className=\\{?\"?[^>]*(sm:max-w-|max-w-|w-full|w-\\[|p-0|p-[0-9]|h-dvh|max-h-dvh|gap-0|bg-background|before:)' apps/web/app packages/ui/src/components` MUST return only documented internal exceptions such as the mobile `Sidebar` sheet.
- [2026-05-09] UI-MATU-SUPERAPP-BASELINE-APP-WIDE — The matu-superapp operational baseline is app-wide, not an Inventory-only pilot. Runtime typography maps `font-sans`, `font-heading`, and `font-matu-body` to Be Vietnam Pro; `font-mono` remains JetBrains Mono for tabular operational data. `AppPage` owns page padding (`p-4 md:p-6 2xl:p-8`, compact `p-4 md:p-5`) and section rhythm (`gap-6`, compact `gap-4`). Primitive control heights follow the 40px default / 44px frontline rule: `Button default=h-10`, `sm=h-8`, `xs=h-7`, `lg=h-11`, `touch=min-h-11`, `touch-lg=min-h-14`, `icon=size-10`, `icon-lg=size-11`; Input/Select/InputGroup default `h-10`, compact `h-8`, frontline `min-h-11`; Textarea `min-h-24`. Cards are border-first (`border border-border`, no default shadow) and default to 8px radius. Supersedes older pilot-scoped typography, AppPage spacing, and button-height notes where they conflict.
- [2026-05-09] MULTI-KEY-PERMISSION-BATCH — Multi-key permission probes (`currentUserHasAnyPermissionAny` in `apps/web/app/_lib/permissions.ts`, `getAuthContextWithAnyPermission` + `getAuthContextWithPermissions` in `apps/web/app/_lib/auth.ts`) MUST prefer the batch RPCs from `20260602000000_auth_permission_batch_rpcs.sql` (`has_any_permissions_any`, `has_all_permissions_any`, `has_any_permissions_for_branch`, `has_all_permissions_for_branch`) so N keys cost 1 PostgREST call. Fallback to `Promise.all(keys.map(...))` is allowed ONLY for deploy-order safety when the migration is not applied yet or the batch RPC errors; NEVER use sequential `for…await` (sequential = N×RTT). Single-key helpers (`currentUserHasPermissionAny`, `currentUserHasPermission`, `hasPermissionGrant`) stay wrapped in React `cache()` — primitive args value-keyed → identical `(key)` or `(branchId, key)` tuples in one render dedupe to a single RPC. NEVER wrap multi-key helpers in `cache()` — array identity changes per render, memoization never hits. AND vs OR semantic distinction is critical: `getAuthContextWithAnyPermission` uses ANY semantics, `getAuthContextWithPermissions` uses ALL semantics. Detect: `grep -rnE "for\\s*\\(\\s*const\\s+\\w+\\s+of\\s+\\w+\\s*\\)\\s*\\{[^}]*await\\s+(currentUserHasPermissionAny|hasPermissionGrant)" apps/web/app/_lib/` MUST 0; helpers MUST call the batch RPC names above. Caught by: 4-agent debate 2026-05-09 on app-wide auth/RLS/RPC latency.
- [2026-05-08] PROXY-NEVER-CALL-GETUSER — `apps/web/proxy.ts` + `packages/database/src/supabase/middleware.ts` MUST use `supabase.auth.getSession()` (cookie decode + auto-refresh via `setAll` callback when access token is past `EXPIRY_MARGIN_MS`), NEVER `getUser()` (HTTP roundtrip to Supabase Auth ~80-300ms per nav). `getSession()` triggers refresh via `_callRefreshToken → _saveSession → setAll` (verified `@supabase/auth-js@2.101.1` GoTrueClient.js:2333,2361) so cookie rotation invariant is preserved. JWT signature is cryptographically verified — proxy doesn't need a second HTTP validation for routing. Banned-user revocation lives EXCLUSIVELY in Server Actions: `getAuthContext` family in `apps/web/app/_lib/auth.ts:36` calls `getUser()` (HTTP validation) — NEVER copy this optimization to those helpers. Avoid `session.user.*` reads in middleware/proxy — `@supabase/auth-js` wraps `session.user` in `insecureUserWarningProxy` on the server, which spams logs on property access; pivot routing checks onto `session` truthy + `claims` from `extractClaimsFromAccessToken(session?.access_token)`. Trade-off: banned user with a valid JWT cookie can navigate RSC pages (data still RLS-gated → empty rows) until next mutation, when `getAuthContext` rejects them. Acceptable per `BMIDL-RLS-INTENTIONAL-ROLE-FASTPATH` precedent (≤1h JWT TTL stale-revoke window for read-only ops). Detect: `grep -rnE "getUser\\(\\)" apps/web/proxy.ts packages/database/src/supabase/middleware.ts` MUST 0; `grep -rnE "getUser\\(\\)" apps/web/app/_lib/auth.ts` MUST keep ≥1 (Server Action defense-in-depth). Caught by: 4-agent debate 2026-05-08 (BA antithesis on refresh semantics → architect verified via auth-js source → QA accept-with-reservations).
- [2026-05-08] FEEDBACK-HOST-SPLIT-COOKIE-DOMAIN-MUST-BE-HOST-ONLY — Public feedback surface (`/r/*`) PHẢI chạy trên domain riêng (e.g. `feedback.comtammatu.com`) tách khỏi admin/POS host (`app.comtammatu.com`). Proxy host gate ở [apps/web/proxy.ts](apps/web/proxy.ts) (đầu hàm `proxy()`, trước `isPublicAppPath` short-circuit) routes by `Host` header qua `resolveHostSurface()` từ `@comtammatu/shared/auth`: feedback host chỉ cho `/r/*` (404 mọi path khác); app host nhận `/r/*` 308-redirect sang feedback host. Bypass auth + skip session refresh trên feedback host để KHÔNG bao giờ set `sb-*-auth-token` cookie trên public origin. Cookie Supabase SSR (`@supabase/ssr` `setAll`) MUST giữ host-only — KHÔNG được set `Domain=.comtammatu.com` trong bất kỳ caller nào (`packages/database/src/supabase/{middleware,server,client}.ts`). Nếu cookie có Domain attribute parent → admin session leak sang feedback origin → XSS trên `/r/*` ăn được session owner. SW (`/sw.js`) + manifest (`/manifest.webmanifest`) MUST 404 trên feedback host (host gate đã chặn — admin SW register lên feedback origin = cache poisoning). `ALLOWED_ORIGINS_FEEDBACK` env trong production MUST chứa feedback host (NOT admin host) — `submitFeedback` ở `apps/web/app/r/[token]/actions.ts:43` reject `Origin` mismatch với 403. Loud warning ONCE/Edge instance khi production miss `NEXT_PUBLIC_FEEDBACK_HOST` (mirrors POS network gate kill-switch). Vercel preview deploys (`*.vercel.app`) fallback `surface="unknown"` → gate no-op → behaviour pre-split (chấp nhận để smoke test). Detect: `grep -rnE 'cookieOptions\\.domain\\s*=|domain:\\s*[\"'\'']\\.comtammatu' packages/database` MUST 0; DevTools verify cookie `sb-*` không xuất hiện trong request `feedback.comtammatu.com/r/*`. Caught by: 4-agent debate 2026-05-08 (security-driven domain split for public feedback module).
- [2026-05-07] UI-RHYTHM-NO-AD-HOC-PADDING — Page outer padding PHẢI qua `AppPage` (`p-4 md:p-6 2xl:p-8`, compact `p-4 md:p-5`) and section rhythm PHẢI use `gap-6` / compact `gap-4`. Card inner padding PHẢI qua `Card` primitive (`px-4 py-4`, `size="sm"` cho `px-3 py-3`). KHÔNG set padding trực tiếp trên page root `<div>` hoặc trên `<CardContent>` để override primitive. Allowed gap scale trong app: `gap-1`, `gap-1.5`, `gap-2`, `gap-3`, `gap-4`, `gap-6`. KHÔNG `gap-5/7/8` cho horizontal flow. Lý do: audit 2026-05-07 đếm 116 unique spacing values trong `apps/web/app`; matu-superapp baseline 2026-05-09 promoted one rhythm app-wide. Detect: `grep -rnE 'className="[^"]*\b(p|px|py)-[0-9]' apps/web/app/**/page.tsx` — page.tsx KHÔNG được tự set padding ngoài `<AppPage>`. Caught by: design-system rhythm audit 2026-05-07. Reference: `docs/spec/design-system.md` Rhythm Contract A and [2026-05-09] UI-MATU-SUPERAPP-BASELINE-APP-WIDE.
- [2026-05-07] UI-HEADING-SCALE-LOCKED — Heading roles được lock theo bảng trong `docs/spec/design-system.md` Rhythm Contract B: Page H1 = `font-heading text-xl sm:text-2xl font-semibold` (chỉ qua `AppPageHeader`), Section title = `CardTitle` (`text-base`), eyebrow = `text-xs uppercase tracking-wide`, Numeric input echo = `text-3xl font-semibold tabular-nums` (number pad readout, scale display). CẤM `text-4xl`, `text-5xl` trong app surfaces (`apps/web/app/**` trừ `(auth)/login`, `access-denied`, `payment/momo/return`, `components/brand.tsx`). `text-3xl` CHỈ trong numeric-input-echo role với `tabular-nums` đi kèm — KHÔNG dùng cho heading. CẤM `font-black` trong app. `font-bold` chỉ cho receipt totals và print-mode header. Lý do: audit 2026-05-07 — POS có 8× `text-xl` + 13× `text-2xl` + 8× `text-3xl` cho cùng heading role, Inventory 8/17/1; mỗi module một thang. Number pad cashier readout (mobile, glove-on, noisy kitchen) cần đọc nhanh ở arms-length → numeric-input-echo carve-out là role hợp lệ. Detect: `grep -rnE 'text-(4xl|5xl)' apps/web/app | grep -v "(auth)/login\|access-denied\|payment/momo\|components/brand"` MUST 0; `grep -rnE 'text-3xl' apps/web/app` review — mỗi hit MUST kèm `tabular-nums` hoặc nằm trong allowlist routes. Caught by: design-system rhythm audit 2026-05-07. Reference: `docs/spec/design-system.md` Rhythm Contract B.
- [2026-05-07] UI-ICON-SIZE-BY-ROLE — Icon glyph Tailwind class chỉ được nằm trong tập `size-3` (badge/chip), `size-3.5` (button sm), `size-4` (default), `size-5` (CardTitle / section), `size-6` (page-header eyebrow). Cỡ lớn (`size-8` → `size-12`) CHỈ trong `EmptyMedia variant="icon"`, brand lockup, hoặc splash media. Image/document thumbnail (`<img>` preview, photo upload, supplier doc thumbnail, GRN evidence) cho phép `size-12` đến `size-16` với `object-cover` — đây là media role, KHÔNG phải icon. CẤM `size-7`, `size-9`, `size-11` mọi nơi trong app surfaces. CẤM `size-14`, `size-16` cho icon glyph; chỉ allowed cho image thumbnail. Lý do: audit 2026-05-07 — `apps/web/app` dùng 13 cỡ icon khác nhau, Inventory 32× `size-5` + 24× `size-3.5` cho cùng "list row glyph" role. Photo upload preview là media QC, không phải glyph rhythm → carve-out hợp lệ. Detect: `grep -rnE 'className="[^"]*\\bsize-(7|9|11)\\b' apps/web/app` MUST 0; `grep -rnE 'className="[^"]*\\bsize-(14|16)\\b' apps/web/app` mỗi hit MUST trên `<img>` hoặc placeholder cho `<img>` (verify by manual review). Caught by: design-system rhythm audit 2026-05-07. Reference: `docs/spec/design-system.md` Rhythm Contract C.
- [2026-05-07] UI-BUTTON-HEIGHT-VIA-PRIMITIVE — `Button` primitive (`packages/ui/src/components/button.tsx`) là single source of truth cho button height under the matu-superapp 40px default / 44px frontline baseline: `xs=h-7`, `sm=h-8`, `default=h-10`, `lg=h-11`, `touch=min-h-11`, `touch-lg=min-h-14`, `icon=size-10`, `icon-xs=size-7`, `icon-sm=size-8`, `icon-lg=size-11`. App callers PHẢI use these variants instead of ad-hoc fixed height classes. Touch CTAs dùng `min-h-` để wrapped labels tăng chiều cao mà không bị clip. Tier mới (e.g. tablet KDS chef glove oversize) PHẢI add variant vào Button cva một lần. Detect: `grep -rnE 'className="[^"]*\bh-(12|14|16)\b' apps/web/app` review hits; `grep -rnE 'className="[^"]*\bmin-h-(12|14|16)\b' apps/web/app` review hits — each hit must be a semantic container or an approved large touch role. Caught by: design-system rhythm audit 2026-05-07; superseded sizing by [2026-05-09] UI-MATU-SUPERAPP-BASELINE-APP-WIDE.
- [2026-05-07] UI-RADIUS-4-TOKEN-ONLY — App code chỉ được dùng 4 radius classes: `rounded-md` (default 8px controls/cards), `rounded-lg` (larger sheets/dialogs when primitive requires it), `rounded-full` (avatar/pill), `rounded-none` (explicit reset). CẤM `rounded` (no suffix — leak default), `rounded-sm`, `rounded-xl`, `rounded-2xl`, `rounded-3xl`, `rounded-4xl` trong `apps/web/app/**`. Primitive layer (`packages/ui/src/components/*`) được phép dùng full token surface từ globals.css; app surfaces consume gián tiếp qua Card/Sheet/etc. Cards are border-first and default to `rounded-md`. Detect: `grep -rnE 'rounded(-sm\|-xl\|-2xl\|-3xl\|-4xl)?\b(?!-(md\|lg\|full\|none\|t\|b\|l\|r))' apps/web/app` review tail values. Caught by: design-system rhythm audit 2026-05-07; refined by [2026-05-09] UI-MATU-SUPERAPP-BASELINE-APP-WIDE.
- [2026-05-07] RSC-LUCIDE-ICON-MUST-BE-JSX — `apps/web/app/components/surface.tsx` `AppSection.icon` and `AppLinkCard.icon` are typed `ReactNode`, not `ElementType`. Server Components passing a lucide icon as a function reference (`<AppLinkCard icon={IconClock}>`) trip Next.js 16 RSC serialization: lucide icons are `forwardRef` objects (`{$$typeof, render}`) and only React **elements** (not raw component refs) survive the Server→Client boundary. ALWAYS pass JSX form: `<AppLinkCard icon={<IconClock />}>`. When building dynamic icon arrays (e.g. tile lists) keep entries as JSX elements (`{ ..., icon: <IconLayoutGrid /> }`) rather than `ElementType` references and instantiating in `.map()`. Domain wrappers like `EmployeePanel` (`apps/web/app/employee/components/employee-page.tsx`) and same-file Server helpers like `StatCard` may keep their public `icon: ElementType` API as long as they render `<Icon />` themselves before crossing into a Client surface. Symptom (Console Error in Next 16.2 / Turbopack): `Only plain objects can be passed to Client Components from Server Components. Classes or other objects with methods are not supported. <... icon={{$$typeof: ..., render: ...}} ...>`. Detect: `grep -rEn "icon=\{[A-Z]" apps/web/app --include='*.tsx'` — any match feeding into `<AppSection>` / `<AppLinkCard>` is a violation. Caught by: 4-agent debate 2026-05-07 (architect+critic finding) on `/employee` page crash.
- [2026-05-07] RPC-DROP-MUST-SCAN-6-CHANNELS — Trước khi DROP FUNCTION migration, MUST run comprehensive caller scan ở 6 channels (JS-only grep INSUFFICIENT): (1) **JS callers** `grep -rEln 'supabase\.rpc\(\s*[\"'\'']<name>[\"'\'']' apps/ packages/`; (2) **Internal SQL `PERFORM`/`SELECT`** `grep -rEln '(PERFORM|SELECT|FROM|JOIN|=\s*)\s+(public\.)?<name>\s*\(' supabase/migrations/`; (3) **Trigger `EXECUTE FUNCTION/PROCEDURE`** `grep -rEln 'EXECUTE (FUNCTION|PROCEDURE)\s+(public\.)?<name>\b' supabase/migrations/`; (4) **RLS policy `USING/WITH CHECK`** `grep -rEln '(USING\|WITH CHECK)\s*\(.*\b<name>\s*\(' supabase/migrations/`; (5) **Column DEFAULT/CHECK** `grep -rEln 'DEFAULT\s+(public\.)?<name>\s*\(\|CHECK\s*\(.*<name>\s*\(' supabase/migrations/`; (6) **pg_cron schedules** `grep -rEln 'cron\.(schedule\|unschedule).*<name>' supabase/migrations/`. PLUS query `pg_proc` for actual existence + `pg_depend` for view/policy/trigger dependencies + `pg_stat_user_functions.calls` for runtime ground truth. Audit M7 (2026-05-07): earlier "67 dead RPCs" list từ JS-only grep had ≥3 verified false positives in 60s spot-check (`cleanup_abandoned_payments` = `pg_cron`, `weekly_*_report` = `pg_cron`, `transition_order_*_status` = trigger + RPC callers). Implied false positive rate UNBOUNDED. **HARD CAP wave size ≤ 10 RPCs** per drop migration (CLAUDE.md owner-apply blast radius). Pre-launch pilot: **drop ONLY Tier A** (one-shot backfills, `_compute__` private prefix, `seed__`) where 6-channel scan returns TOTAL=0. Tier B (reporting + scheduled), Tier C (lifecycle helpers), Tier D (ops emergency tools, auth hooks like `custom_access_token_hook`) — DEFER to post-pilot when `pg_stat_user_functions` provides usage telemetry. Detect: any new DROP FUNCTION migration MUST attach 6-channel scan output in PR description. Caught by: 4-agent security audit 2026-05-07 (M7 NO-GO verdict).
- [2026-05-07] RPC-ROLLBACK-MUST-INCLUDE-BODY — Rollback files cho DROP FUNCTION migrations PHẢI inline `CREATE OR REPLACE FUNCTION` body từ `pg_get_functiondef` snapshot, KHÔNG reference upstream migration archaeology. Lý do: many RPCs đã được amended multiple times (e.g. `admin_update_profile` có 4 historical signatures). "Re-apply original migrations X, Y, Z" assumes byte-identical history — false. Rollback file shape: full function body với security flags preserved (SECURITY DEFINER + SET search_path + REVOKE/GRANT). Document `pg_get_functiondef` source snapshot timestamp trong rollback header. Caught by: 4-agent debate 2026-05-07 (architect + critic both flag).
- [2026-05-07] SELECT-STAR-ACCEPTABLE-FOR-NON-AUDIT-TABLES — `.select("*")` trong Server Actions là **acceptable** cho non-audit tables nếu consumer cần most columns + table không chứa PII fields. Audit M6 wave (#7-L4) liệt kê 7 sites: `inventory/actions.ts:115,560`, `inventory/supplier-actions.ts:43`, `inventory/transfer-actions.ts:178`, `hr/contract-actions.ts:20`, `hr/payroll-actions.ts:23`, `finance/chart-of-accounts-actions.ts:45`. Đánh giá pragmatic: each consumer reads most columns (full ingredient catalog, supplier details, transfer ledger detail, contract terms, payroll period status, chart of accounts) — explicit column list sẽ duplicate `database.types.ts` Row type. Tradeoff: mechanical refactor cost (read 7 schemas + 7 consumers + craft 7 explicit lists + risk typecheck breakage) > marginal benefit (slight serialization cost). EXCEPTIONS — explicit columns REQUIRED khi: (a) table chứa PII fields nên không expose qua list view (xem **AUDIT-LOG-SELECT-EXPLICIT-COLUMNS** cho `audit_logs.ip_address/old_data/new_data`); (b) consumer chỉ cần subset nhỏ → narrow projection cải thiện perf; (c) future column additions có thể leak unintended data (e.g. internal notes column). Khi adding new column tới existing select-star table, audit consumers cho potential leak risk trước. Reference: 4-agent debate 2026-05-07 — anti-SLOP decision: don't refactor mechanical patterns without measurable benefit. Caught by: architect audit (#7-L4 finding rejected as overreach).
- [2026-05-07] EXCEPTION-WHEN-OTHERS-MUST-LOG-OR-RERAISE — Mọi `EXCEPTION WHEN OTHERS` block trong PL/pgSQL PHẢI có ít nhất 1 trong 3: (a) `RAISE EXCEPTION 'context: %', SQLERRM` (re-raise với context — preferred cho destructive ops), (b) `RAISE WARNING '[func_name] context: %', identifier, SQLERRM` (fail-soft với loud signal — preferred cho non-critical secondary ops), (c) `RAISE NOTICE` + counter accumulator (acceptable cho FOR LOOP per-item skips). KHÔNG silent swallow (assignment-only, e.g. `v_var := NULL` mà không log). Audit M5 (2026-05-07) verified 17 existing sites: 4 hard re-raise (complete_payment, auto_complete_paid_order, m4_reverse_payment_and_post, db_rls_rpc:423), 2 RAISE WARNING (payment_stock_consumption_failsoft:84, db_rls_rpc:155), 8 RAISE NOTICE acceptable (cancel_order, print_safety×2, pos_discount, pos_clear_discount, confirm_vietqr×2, create_payment), 3 documented fail-silent ACCEPTABLE vì non-critical metadata + caller fallback (submit_feedback v1+v2 dish_names lookup → NULL fallback OK; print_templates_document_ast trigger → RETURN NEW preserves legacy payload, agent has fallback). Critical finding: **audit_logs inserts are NEVER inside EXCEPTION WHEN OTHERS blocks** — catches surround stock/print/journal ops only, audit always happens in main path → audit data NOT being eaten. New EXCEPTION WHEN OTHERS code PHẢI follow rule above. Detect: `grep -rn -A 3 "EXCEPTION WHEN OTHERS" supabase/migrations/` — verify next 3 lines contain RAISE EXCEPTION/WARNING/NOTICE. Caught by: 4-agent security audit 2026-05-07 (M5 finding — verified clean).
- [2026-05-07] TENANT-OWNER-USER-ID-CANONICAL — `tenants.owner_user_id UUID NOT NULL FK auth.users(id) ON DELETE RESTRICT` là canonical auth identity column cho tenant owner (added H3b `20260601500000`). PHẢI giữ separate với `tenants.representative` (TEXT legal name CTCP docs) và `positions.code='owner'` (HR label, JWT user_role source). KHÔNG add UNIQUE constraint cho đến khi có business rule cấm same user owning multiple tenants. KHÔNG sync `representative` ↔ `owner_user_id` automatically — different concepts. Mọi mutation của `owner_user_id` PHẢI qua future `transfer_ownership` RPC (chưa ship); manual SQL UPDATE chỉ acceptable trong runbook + audit trail. has_permission() owner-bypass HIỆN TẠI vẫn dùng positions.code='owner' check (H3a NOT NULL invariant đảm bảo no silent demotion); column này là data foundation cho future ownership transfer UI/RPC, không phải runtime authz check yet. Nếu future flip dual-source: update has_permission/\_auth_v2_is_owner/has_permission_any thêm OR branch `auth.uid() = tenants.owner_user_id` — defense-in-depth có drift surface với positions check. Detect: `SELECT id FROM tenants WHERE owner_user_id IS NULL` MUST return 0 rows. Reference: `docs/plan/adr/0005-owner-identity-dual-source.md` + `supabase/migrations/20260601500000_h3b_tenants_owner_user_id.sql`. Caught by: 4-agent security audit 2026-05-07 (synthesis: minimum-regret path, architect antithesis accepted).
- [2026-05-07] RPC-ERROR-MUST-MAP-OR-LOG-UNMAPPED — Mọi Server Action gọi `supabase.rpc(...)` PHẢI route error qua mapper helper trả về Vietnamese localized message (tham khảo `mapPaymentRpcError`, `mapDiscountRpcError`, `mapRefundRpcError`, `mapReturnRpcError`, `mapTransitionError`, `mapRpcError` × 2, `mapStationDbError`, `mapZoneDbError`, `mapTableDbError`, `mapTerminalDbError`, `mapDbError`, `mapJournalMutationError`). NEVER `return { error: rpcError.message }` — vi phạm CLAUDE.md `NEVER return raw Supabase/Postgres error.message to client`. Pattern: try mapper → if matched, return mapped + log; if NULL, log unmapped (`console.error("[actionName] [unmapped] rpc error:", msg)`) THEN return generic fallback. Unmapped log là CRITICAL để iterate mapper coverage — không log → unmapped errors silently dropped, không có data để mở rộng vocabulary. Cập nhật mapper khi observed unmapped messages tích lũy. KHÔNG centralize mappers cross-domain (payment vocabulary ≠ refund ≠ inventory; mỗi domain có own RPC error keys). KHÔNG add patterns speculatively (SLOP — wait for log evidence). Inline `if (msg.includes("specific_keyword"))` checks BEFORE mapper là acceptable cho high-frequency known errors (e.g. `already_paid`, `payment_not_found`). Reference: `apps/web/app/br/[branchId]/pos/payment-actions.ts:65-90,590-595,795-801,1041-1054,1357-1363`. Caught by: 4-agent security audit 2026-05-07 — finding outdated (mapper pattern đã in place trước session) nhưng unmapped logging gap thật sự (silent drops).
- [2026-05-07] RLS-DESTRUCTIVE-MUST-USE-HAS-PERMISSION — RLS policies cho destructive UPDATE/DELETE PHẢI gate qua `has_permission(branch_id, key)` (queries `staff_permissions` live qua DB), KHÔNG dùng `auth_role()` (đọc JWT user_role claim, cache đến token refresh ~1h). Tới khi JWT refresh, user bị revoke quyền vẫn pass `auth_role()` check → 1-hour stale-revoke window cho destructive op. Refunds_update policy đã refactor trong H2a (`20260601200000_h2a_refunds_update_perm_gate.sql`): `auth_role() IN ('owner','super_manager')` → `has_permission(branch_id, 'orders:refund_approve')`. Owner bypass tự động qua `_auth_v2_is_owner` trong has_permission (`20260423040000:14-18`). EXCEPTIONS (intentional, documented): (a) **scope/side guards** trong RPC bodies như `inventory_transfer_rpc_role_scope_gates.sql` — branch_manager forbidden từ inter-site ship là semantics rule, không phải revokable permission; (b) **branch-scope cross-tenant view pattern** `branch_id = auth_branch_id() OR auth_role() IN HQ_ROLES` — standard "HQ sees all branches"; (c) **named ABAC helpers** như `is_inventory_production_operator()`; (d) **module-ACL fast-path** cho non-destructive read-mostly tables (xem BMIDL-RLS-INTENTIONAL-ROLE-FASTPATH). HR payroll (`20260416040000_hr_payroll.sql:31,38,42,123,130,134`) còn `auth_role() IN ('owner','super_manager')` patterns chưa migrate — backlog H2b. Detect: `grep -rnE "auth_role\(\) IN" supabase/migrations/` cho destructive policies; cross-check với 4 exceptions trên. Caught by: 4-agent security audit 2026-05-07 (architect H2 finding).
- [2026-05-07] BMIDL-RLS-INTENTIONAL-ROLE-FASTPATH — `branch_menu_item_daily_limits` (`20260510040000`) có 9 sites dùng `auth_role() IN ('owner','super_manager','area_manager','branch_manager','cashier','chef')` — list này EXACTLY mirror `module-acl.ts:131-141` `branch_menu_limits.allowedRoles`. Đây là intentional **module-ACL fast-path** (audience filter ⇒ access), KHÔNG phải permission gate. KHÔNG refactor sang `has_permission()` cho đến khi có sub-action distinction (ví dụ: "view limits" vs "edit limits", hay "set" vs "approve"). Nếu mechanically thêm `branch_menu_limits:write` permission key thì sẽ create bool gate identical với role list — zero functional gain, nhưng cost: catalog +1 entry, role_templates upsert, backfill staff_permissions cho all existing branch_manager/cashier/chef users (miss = lost POS access mid-shift). Defer cho đến khi business case justifies finer-grained split. Tradeoff hiện tại: 1h stale-revoke window cho daily-limit writes (ai bị demote khỏi role vẫn write được đến token refresh). Acceptable vì daily limits là branch ops control panel (POS workflow), không phải financial. Detect: nếu add column "approve_quota" hay role split → revisit. Caught by: 4-agent debate 2026-05-07 (architect+critic+PM all agree defer).
- [2026-05-07] PROFILES-POSITION-ID-MUST-NOT-NULL — `profiles.position_id` PHẢI NOT NULL (enforced by H3a migration `20260601100000_auth_v3_h3a_position_id_required.sql`). `has_permission()` / `has_permission_any()` / `_auth_v2_is_owner()` kiểm tra owner status qua INNER JOIN positions; nếu position_id NULL → owner check returns false silently (M5 đã drop legacy `profiles.role` fallback). Combined với hook fallback `COALESCE(po.legacy_role_code, 'office')` → owner null-position bị silently demote sang 'office'. Trigger `handle_new_user` PHẢI `RAISE EXCEPTION` khi `_auth_v2_position_id_from_role(role, tenant)` return NULL (tránh silent NULL insert + privilege erosion); KHÔNG fallback sang 'office' sentinel — sẽ hide root cause (role typo, tenant chưa seed positions) và là privilege-escalation vector vì handle_new_user default `v_role_text='owner'`. `admin_update_profile` cũng PHẢI có same NULL guard trước UPDATE. FK `ON DELETE SET NULL` đã đổi thành `RESTRICT` — admin xóa position khi còn profiles assigned sẽ bị block với SQLSTATE 23503. Detect: `SELECT count(*) FROM profiles WHERE position_id IS NULL` MUST 0; `pg_get_constraintdef(profiles_position_id_fkey)` MUST contain `ON DELETE RESTRICT`. Sibling rule: PLPGSQL-RECORD-IS-NOT-NULL (line 27). Caught by: 4-agent security audit 2026-05-07 (security-reviewer M1, architect H3 finding).
- [2026-05-07] LOGIN-MESSAGE-MUST-BE-GENERIC — `apps/web/app/(auth)/login/actions.ts` PHẢI return cùng generic message `"Email hoặc mật khẩu không đúng"` cho mọi failure mode sau Zod validate: wrong creds, no session post-signIn, no claims (hook không emit). Distinguishable copy (cũ: `"Tài khoản chưa được phân quyền. Liên hệ quản lý."`) leak credential validity → user enumeration oracle cho brute force. Khi `extractClaimsFromAccessToken` returns null hoặc `getSession` returns no session, MUST `await supabase.auth.signOut()` TRƯỚC khi return error — `signInWithPassword` đã set session cookie; thiếu signOut → user vào proxy bounce loop (`/access-denied?reason=missing-auth-context`). Pre-auth events (rate-limit fail-open) KHÔNG được dùng `log_audit` RPC: caller anonymous (auth.uid() NULL) → RPC raise `insufficient_privilege` (xem AUDIT-LOG-INSERT-RPC-ONLY). MVP dùng structured `console.error` với event name `auth.login.*` + Vercel log drain; persistent `security_events` table là follow-up wave. Detect: review code login + grep `apps/web/app/(auth)` cho hardcoded distinguishable copy. Caught by: 4-agent security audit 2026-05-07 (security-reviewer H1, architect M3 quality gate).
- [2026-05-07] POS-RESUME-MUST-REFETCH — POS/KDS poll-gated hook (`use-order-sync`, `use-kds-realtime`, `printer-status-badge`) PHẢI register `visibilitychange → "visible"` listener fire immediate refresh, song song với existing `visibilityState === "hidden" return` gate trong setInterval. iOS Safari kill WebSocket sau ~30s background → realtime events fired khi hidden có thể drop → reconnect KHÔNG replay. Pause poll mà thiếu resume-refetch = miss VietQR webhook → cashier thấy stale "chưa thanh toán" → click Cash confirm → POS-PAYMENT-REUSE-UNIQUE-SLOT replay. Pattern reference: `apps/web/app/_hooks/use-notifications.ts:131-139`. Detect: mọi `setInterval` trong `apps/web/app/br/[branchId]/{pos,kds}/**` có hidden-gate MUST có matching `addEventListener("visibilitychange", ...)` resume-refetch. Caught by: 4-agent debate POS perf optimization 2026-05-07 (Critic) addressing incident class 2026-04-24 17:37 (12× duplicate-key burning 7 payment IDs).
- [2026-05-07] UI-PERMISSION-FLAGS-THREADED-NOT-SERVER-ONLY — Mọi destructive/financial action UI (close-shift, refund-approve, cancel-invoice, void-journal, post-journal, period-close) PHẢI có `can*` boolean prop fetched ở page.tsx (server) và threaded qua component tree đến button render. KHÔNG fallback rejection 42501 từ RPC. Detect: code-review checklist — `<Button onClick={...}>` cho destructive action MUST có `disabled={!can*}` hoặc tương đương. Mở rộng rule POS-CLOSE-SHIFT-UI-GATED (2026-04-25) và POS-CONFIRM-CASH-GATED (2026-05-02) sang Admin/Finance/HR. Caught by: pre-rebuild contract.
- [2026-05-07] UI-AUDIT-LOG-EXPLICIT-COLUMNS-IN-UI — Mở rộng `AUDIT-LOG-SELECT-EXPLICIT-COLUMNS` (2026-04-28) sang UI: mọi component đọc `audit_logs` trong `Lịch sử` tab của detail page PHẢI select explicit cols `id, action, entity_type, entity_id, user_id, created_at` qua RPC helper, không `.select("*")` hoặc include `ip_address`, `old_data`, `new_data` blobs. Detect: `grep -rnE 'audit_logs.*select' apps/web/app` mọi caller phải dùng explicit list. Caught by: pre-rebuild audit trên `_lib/audit.ts`.
- [2026-05-07] UI-TOAST-VIA-SONNER-NEVER-URL-FLASH — Non-auth success/error feedback PHẢI qua `toast.*` từ `@comtammatu/ui/components/sonner`. URL flash params (`?success=`, `?error=`) chỉ dùng cho permission/auth/scope failure → `/access-denied?reason=`. Detect: `grep -rn 'redirect.*\?error=' apps/web/app` baseline 4 — phải giảm về 0 cuối Wave 3. Caught by: BA audit 2026-05-07.
- [2026-05-07] UI-FORM-VIA-FORMDIALOG-FOR-CRUD — Form CRUD có line array hoặc 4+ field PHẢI dùng `FormDialog` từ `apps/web/app/components/form/form-dialog.tsx` với Zod 4 `{ error: ... }`. Plain `<form action>` chỉ cho login, sign out, single-reason confirm. Long-form line-array không vào Dialog (dùng Page hoặc Sheet — overlay decision trong `docs/modules/ui.md`). Detect: review checklist — file dùng `<Dialog>` + `useForm` + `zodResolver` mà không dùng `FormDialog` flag là vi phạm.
- [2026-05-07] UI-NO-PALETTE-FOR-STATUS — Status (success/warning/info/destructive) phải dùng semantic token (`bg-success`, `bg-warning`, `bg-info`, `bg-destructive`, `text-success`, etc.). KHÔNG dùng raw Tailwind palette `amber-/emerald-/zinc-/sky-/rose-/teal-/orange-/red-/green-/yellow-/blue-/slate-` ngoài `packages/ui/src/styles/*.css`. Detect: `grep -rnE '(text|bg|border)-(amber|emerald|zinc|sky|rose|teal)-[0-9]' apps/web/app` MUST 0 kết quả. Caught by: design-system contract v14.2.0.
- [2026-05-07] UI-EMPTY-STATE-VIA-APP-EMPTY-STATE — Mọi empty/no-result/no-access/error UI ở app surface phải dùng `AppEmptyState` từ `surface.tsx` hoặc `Empty` primitive trực tiếp. Cấm pattern `<Empty className="border bg-card py-X">` hand-rolled. Detect: `grep -rln 'Empty className="border bg-card' apps/web/app` MUST 0 kết quả (baseline 6 vi phạm sẽ giảm dần qua wave). Caught by: BA audit 2026-05-07.
- [2026-05-07] UI-LOADER-VIA-SPINNER-PRIMITIVE — Cấm `Loader2 + animate-spin` ngoài `packages/ui/src/components/spinner.tsx`. Loading indicator phải dùng `Spinner` primitive. Detect: `grep -rn 'Loader2\|animate-spin' apps/web/app` MUST 0 trong app code. Caught by: BA audit 2026-05-07 — 1 vi phạm còn ở `finance/components/mv-staleness-banner.tsx:97` (sẽ fix Wave 4).
- [2026-05-07] UI-NO-RAW-TABLE-OUTSIDE-PRIMITIVE — Cấm element `<table>` raw ngoài `packages/ui/src/components/table.tsx` và `apps/web/app/components/data-table/`. Mọi tabular data phải dùng `Table` primitive hoặc `DataTable` adapter. Detect: `grep -rnE '<table\s' apps/web/app` MUST 0 kết quả. Caught by: design-system audit 2026-05.
- [2026-05-07] UI-NO-COMPETING-CHROME-PRIMITIVE — `apps/web/app/components/page-hero.tsx` là legacy wrapper; mọi caller mới phải dùng `AppPageHeader` trực tiếp. File `page-hero.tsx` sẽ bị xoá ở Wave 4 cleanup sau khi audit-trail unfreeze (M4 P0 đóng). KHÔNG thêm caller `PageHero` mới. Detect: `grep -rn 'from.*page-hero\|<PageHero' apps/web/app/ --include="*.tsx"` MUST không tăng so với baseline 16.
- [2026-05-07] UI-PAGE-HEADER-VIA-APP-PAGE-HEADER — Mọi `app/**/page.tsx` và `*-client.tsx` không phải `(auth)/login`, `access-denied`, `payment/momo/return` PHẢI dùng `AppPageHeader` từ `apps/web/app/components/surface.tsx`. KHÔNG được tự dựng `<header><h1>` hoặc `<h1 className="font-heading text-2xl/3xl">`. Detect: `grep -rln 'font-heading text-2xl\|font-heading text-3xl' apps/web/app | grep -v "components/\(surface\|app-shell\|page-hero\)"`. Caught by: BA audit 2026-05-07 — chỉ 5/114 pages dùng adapter.
- [2026-04-29] AUDIT-NEVER-LOG-CREDENTIALS — Every `log_audit()` / `logAudit()` call carrying any payload with secret fields (`apiKey`, `secretKey`, `accessKey`, `password`, `token`, `webhook_secret`, `signing_key`, top-level `credentials`) MUST run `oldData` and `newData` through `redactCredentials()` from `@comtammatu/shared/utils/redact-credentials` before passing to the audit helper. Otherwise raw secrets land in `audit_logs.new_data` JSONB and become readable by anyone with `finance:view`. The utility uses an exact case-insensitive key allowlist — substring matches are intentionally avoided so harmless fields like `partnerCode` (merchant ID, not a secret) are NOT redacted. Detect via grep for any `logAudit({` call site that touches `provider_configs` / payments credentials and verify `redactCredentials` is invoked first. Caught by: D011/m4 4-agent debate (QA finding), 2026-04-29.
- [2026-04-29] WEBHOOK-MUST-IDEMPOTENT — Every payment provider webhook handler (Momo today, VietQR/VNPay future) MUST INSERT into `public.webhook_events(provider, request_id, ...)` before calling any payment-state RPC. The `UNIQUE (provider, request_id)` constraint converts duplicate deliveries / replay into ON CONFLICT no-ops, so RPC side effects (journal entries, stock movements) cannot run twice. Detect via grep `from("payments").update` or `complete_payment_and_consume_stock` inside `apps/web/app/api/webhooks/*/route.ts` without a preceding `webhook_events` insert. Caught by: pre-pilot security review 2026-04-27.
- [2026-04-29] WEBHOOK-MUST-BIND-TENANT — Every payment webhook MUST resolve `tenant_id` from a SIGNED, server-validated source (HMAC-verified payload + decoded `extraData` JSON), not from the `provider_ref` lookup alone. The `payments` SELECT MUST include `.eq("tenant_id", extracted_tenant_id)` so a leaked partner secret in tenant A cannot be replayed against a forged `provider_ref` in tenant B. Detect via grep `from("payments")` in webhook routes for any `.eq("provider_ref", ...)` without `.eq("tenant_id", ...)`. Caught by: pre-pilot security review 2026-04-27 (M4 P0-2).
- [2026-04-29] STOCK-CONSUME-MUST-CHECK-RESULT — Any caller of `complete_payment_and_consume_stock` MUST inspect the returned `stock_consumed_status` (post-recompute migration: `ok | out_of_stock | recipe_missing | internal_error`). If the value is anything other than `ok`, the webhook handler MUST return HTTP 500 (not 200) so the provider retries, AND a `notifications` row of severity `high` MUST be created. The cash-confirm path surfaces the same status as a toast and notification. Silently swallowing `false` / non-`ok` is the bug that caused money-paid-zero-stock divergence. Caught by: pre-pilot security review 2026-04-27 (M4 P0-3).
- [2026-04-29] PAYMENT-AMOUNT-MUST-RECOMPUTE-SERVER — `confirm_cash_payment` and `complete_payment_and_consume_stock` MUST recompute the order total inside the RPC from `order_items.qty * order_items.price_at_order ± discount + service + tax` and reject when `abs(amount - recomputed) > 1 ₫` with code `amount_mismatch_recomputed`. Comp / staff-meal orders bypass via `is_comp = true`. The protection sits AT payment time inside SECURITY DEFINER so no client-side amount tampering wins. Detect via grep payment RPCs for absence of `SUM(qty * price_at_order)` recompute. Caught by: pre-pilot security review 2026-04-27 (M4 P0-4).
- [2026-04-29] REFUND-MUST-CHECK-PAYMENT-COMPLETED — `create_refund` (and the action wrapper `createRefund`) MUST verify `payment.status = 'completed'` BEFORE inserting the refund row. Refunds against `pending` or `failed` payments allow negative-amount injection or refund-of-nothing. The RPC is the source of truth. Detect via grep `create_refund` or `createRefund` for the precondition. Caught by: pre-pilot security review 2026-04-27 (M4 P0-5).
- [2026-04-29] REFUND-MUST-REVERSE-ATOMICALLY — Refund approval MUST go through the `reverse_payment_and_post(p_refund_id)` RPC (when shipped). The action `approveRefund` MUST NOT directly UPDATE `payments.status` or INSERT `journal_entries`. Either ALL of {payments.status flip, GL reversal journal, stock_movements refund_restore, orders.payment_status flip, audit_logs row} land in one transaction, or none do. Today's `approveRefund` flips status only — that's the bug. Detect via grep `approveRefund` for `.update("payments")` or `.insert("journal_entries")` outside the RPC call. Caught by: pre-pilot security review 2026-04-27 (M4 P0-1).
- [2026-04-23] INVENTORY-BRANCH-FILTER-URL-ONLY — Inventory scope ALWAYS flows through URL `?branchId=` (see `apps/web/app/inventory/_lib/inventory-scope.ts#resolveInventoryBranchScope`). Never persist selection to localStorage, cookies, or React context. Server Actions that mutate branch-scoped data MUST take an explicit `branchId` input and re-validate it against the resolver's `allowedBranches` — never trust URL params as a write target. Multi-item RPCs (GRN confirm, transfer commit, stocktake post, production yield) MUST receive a concrete `branchId`; never pass "all" or null. Reports and dashboard aggregation are the ONLY pages allowed to ignore the param.
- [2026-04-23] PO-STATUS-DERIVATION-IN-RPC — Purchase-order status derivation (`sent` → `partially_received` / `received`) triggered by GRN confirmation MUST live inside `confirm_goods_receipt_note` RPC, locked via `SELECT … FROM purchase_orders … FOR UPDATE`. Never re-introduce app-layer computation (pre-fetch `po_id` + read siblings + `UPDATE purchase_orders`): two concurrent confirms against the same PO race and can clobber each other's status. Tolerance is ±5% per ingredient (lower bound only) per `docs/ref/inventory.md §7`. The RPC now returns `{grn_id, status, journal_entry_id, po_id, po_status}` — callers read `po_id` from the response for revalidatePath.
- [2026-04-22] DESIGN-SYSTEM-CONTRACT-FIRST — Before any UI/UX rebuild, read `docs/spec/design-system.md` and `docs/modules/ui.md`, then state the surface, primary user job, route family, change type, and shadcn primitives used. Never start from a new theme, fake primitive, route-specific visual system, or vocabulary source.
- [2026-04-23] PLPGSQL-RECORD-IS-NOT-NULL — `IF record IS NOT NULL` in PL/pgSQL returns TRUE **only when every column is non-null**, not when a row was found. Tenant-level users with `branch_id IS NULL` silently fell out of the JWT hook because one nullable column tripped the check. Check a guaranteed-non-null column (`record.tenant_id IS NOT NULL`) or use `FOUND` after the SELECT. Caught by: area_manager / office / super_manager login returning empty `app_metadata` on 2026-04-23.
- [2026-04-22] JWT-CLAIMS-NOT-IN-APP-METADATA — `session.user.app_metadata` (supabase-js) reads from the `auth.users` row, NOT from JWT claims. Hook-injected fields (`user_role`, `position`) live inside the JWT payload only. Use `extractClaimsFromAccessToken(session.access_token)` when claims include hook-added fields; `extractClaims(user.app_metadata)` still works but only returns DB-mirrored fields (`tenant_id`, `branch_id`, `role`). Caught by: `position` claim missing from UI on 2026-04-22 despite hook emitting it correctly.
- [2026-04-22] RLS-PERMISSIVE-POLICIES-OR — PostgreSQL RLS combines multiple PERMISSIVE policies on the same command with **OR**. Two permissive SELECT policies `(user_id = auth.uid())` and `(tenant_id = X)` together let anyone in the tenant see every row, not just their own. Either collapse to a single policy with the combined predicate, or mark one policy `AS RESTRICTIVE`. When writing self-scope + admin-scope policies, always gate the admin policy behind a permission check (e.g. `has_permission(NULL, 'staff:assign_permission')`). Caught by: cashier seeing 192 tenant-wide + 61 branch grants at `/employee/permissions` on 2026-04-22.
- [2026-04-02] CONSTRAINT-BEFORE-CLEANUP — Never add CHECK constraints before cleaning up existing data that would violate them. ALTER TABLE ADD CONSTRAINT will fail on dirty data and block all subsequent statements in the migration.
- [2026-04-02] REVOKE-ALL-DML — When locking down a table to RPC-only, revoke INSERT + UPDATE + DELETE (not just UPDATE). Leftover INSERT/DELETE grants create bypass paths that orphan related rows (e.g. auth.users without profiles breaks JWT hook).
- [2026-04-02] ROLE-SCOPE-CONSISTENCY — When a role is defined as "HQ-wide" or "tenant-wide" in one place, ensure ALL references (RLS policies, docs, spec tables) agree. office was HQ-wide in role table but branch-scoped in SELECT policy.
- [2026-04-02] TYPE-STUB-BEFORE-CODE — When adding a new DB table, check `database.types.ts` first. If the table isn't in the generated types, add a type stub BEFORE writing any code that calls `.from("new_table")`. Otherwise typecheck passes locally but build fails.
- [2026-04-02] VERIFY-BEFORE-DONE — Never mark a task completed until `pnpm typecheck && pnpm lint && pnpm build` output is confirmed green. Generated types may have `| null` where manual stubs assumed non-null.
- [2026-04-02] LINT-BEFORE-COMMIT — Always run `pnpm lint` before committing. CI runs typecheck + lint + build; skipping lint locally causes CI failures on push. `/verify` must include all three.
- [2026-04-09] NO-ARBITRARY-DIMENSIONS — Never use arbitrary dimension values like `text-[10px]`, `w-[200px]`, `h-[3rem]` in Tailwind classes. ESLint rule `design-system/no-arbitrary-tailwind-value` enforces this at error level. Use standard Tailwind tokens or extend `@theme` in globals.css. For viewport-based values, add custom utilities instead.
- [2026-04-14] NO-SURFACE-THEME-IMPORTS — Never import per-domain `theme.css` files to restyle a surface. Route all surface differentiation through shared tokens, utilities, and recipe variants instead.
- [2026-04-14] NO-STATIC-UI-INLINE-STYLES — Never add static `style={{ ... }}` blocks to foundation, shell, auth, or mobile chrome files. Move static presentation into `globals.css`, shared utilities, or recipe helpers. Inline style is allowed only for truly dynamic runtime values.
- [2026-04-14] BLOCKED-STATE-UI-IS-PRESENTATION-ONLY — Shared blocked-state helpers may parse `forbidden` / `reason` and render copy, but they must never become a second auth policy layer or change redirect targets.
- [2026-04-15] TERMINOLOGY-SOURCE-OF-TRUTH — Never introduce new domain/UI wording by ad-hoc hardcoded copy when an existing canonical term already exists. Business meaning and naming rules live in `docs/ref/glossary.md`; shared Vietnamese labels live in `packages/shared/src/labels/vi.ts`; inventory-specific route/term adapters live in `apps/web/app/inventory/_lib/dictionary.ts`. When adding or changing copy, update one of those sources first or in the same PR, run `pnpm lint:copy`, and avoid reintroducing drift terms like `Employee Portal`, `Admin Shell`, `Báo cáo CEO`, `Bếp dùng`, `Kiểm kê kho`, `Danh sách phiếu xuất`, `Chủ hệ thống`, `Quản lý cấp cao`, `Trụ sở chính`, or `Ngừng`.
- [2026-04-16] PRESET-FIRST-UI — Never add UI rules, wrappers, docs, or utilities that outrun the current shadcn preset. The project preset is defined by `packages/ui/components.json`, `apps/web/components.json`, `packages/ui/src/styles/globals.css`, and `apps/web/app/layout.tsx`.
- [2026-04-16] NO-PRIMITIVE-DESIGN-OVERRIDE — Never redefine the visual contract of core primitives like `button`, `table`, `sidebar`, `card`, or `badge` through page-level wrappers or docs. Reuse the current preset-backed primitives instead of creating a parallel design system.
- [2026-04-16] DOCS-MUST-MATCH-RUNTIME — UI docs must describe the actual runtime configuration. When changing `components.json`, `globals.css`, or `layout.tsx`, update `docs/spec/design-system.md` and `docs/modules/ui.md` in the same change so docs do not drift from the live preset.
- [2026-04-16] NO-LEGACY-APP-HELPERS — Do not keep or introduce legacy helper classes such as `app-*`, `safe-top`, or `safe-bottom` in runtime UI. Replace them with direct shadcn primitive composition and standard utility classes.
- [2026-04-16] NO-FAKE-PRIMITIVES — Never use raw `div` / `span` / `p` markup to imitate preset primitives such as `Card`, `Badge`, `Button`, `Table`, `Tabs`, `Input`, or `Select`. If the UI reads like an existing primitive, use that primitive directly instead of hand-styling a clone.
- [2026-05-05] APP-SURFACE-ADAPTER-FIRST — Repeated app-level page/header/section/toolbar/empty-state/link-card patterns MUST go through `apps/web/app/components/surface.tsx`. Domain wrappers (Inventory, Employee, Admin, POS/KDS where appropriate) may keep their public API, but they must delegate to the app surface adapters instead of cloning `Card` chrome, empty-state markup, page width containers, or filter toolbars. If a new layout pattern is genuinely missing, add it to `surface.tsx` and update `docs/spec/design-system.md` / `docs/modules/ui.md` in the same change before broad rollout.
- [2026-04-24] RLS-NOT-APPLIED-ON-MV — Postgres RLS on the base table is NOT inherited by materialized views. Dashboard MVs like `mv_inventory_stock_current`, `mv_grn_price_baseline`, and `mv_inventory_value_ranking` bypass tenant/branch RLS if queried directly by role `authenticated`. Always `REVOKE ALL ... FROM authenticated` on the MV and expose access only through a `SECURITY DEFINER` function that re-checks `tenant_id`, `branch_id`, and `has_permission(branch, key)` in SQL. Caught during inventory redesign workshop 2026-04-24 before any leak. Reference: `docs/plan/inventory-redesign.md §Q5`.
- [2026-04-24] SHIFT-KEY-CANONICAL — "Ca làm việc" in inventory / GRN express / waste anti-splitting uses the canonical string `YYYY-MM-DD_{morning|afternoon|evening}` computed against `branches.timezone` with business-day cutoff 04:00 local. Never use `pos_sessions.id`, raw `created_at`, or `date_trunc('day', now())` — those drift with timezone and don't handle overnight shifts. Helper `inventory_shift_key(branch_id, at timestamptz)` is the single source of truth. Reference: `docs/plan/inventory-redesign.md §Q1 + §B2`.
- [2026-04-24] BASELINE-SAMPLE-MIN — GRN price variance baseline (`avg_30d_same_supplier_same_item`) MUST check `sample_n >= 3` before applying any tier rule. With fewer samples, the baseline = the only GRN itself → variance always 0 → every price "passes" including gaming the first GRN of a new supplier. Fallback order: `avg_30d_any_supplier_same_item` (n≥3) → QLV manual approval required. After a hard-block override, set `baseline_paused=true` for 30 days on `(supplier, ingredient)` to prevent outlier pricing from poisoning the average. Reference: `docs/plan/inventory-redesign.md §Q3`.
- [2026-04-24] PERIOD-CLOSE-SOFT-HARD — Inventory mutations (waste, adjustment, stocktake finalize) respect `accounting_periods` two-stage close: soft-close day 5 of next month (back-dated posts auto-shift to current period with `originally_dated_in_prior_period=true` flag); hard-close day 15 (reject any back-dated write, `period_reopen` permission + 2FA required to override). Never silently post to a closed period — trigger `enforce_period_close()` on all write paths. Never add `ON CONFLICT DO NOTHING` to bypass the trigger. Reference: `docs/plan/inventory-redesign.md §B8`.
- [2026-04-24] OFFLINE-NO-SILENT-CLIENTWINS — Offline stocktake sync conflicts MUST be materialized as `stocktake_conflicts` rows and resolved by QLV through the queue UI. Never auto-merge with "client-wins" or "last-write-wins" silently — stocktake adjustments drive accounting and shrink detection, and silent overwrite of a legitimate count destroys audit trail. Server validates `offline_created_at` within `[session_opened_at, now()]` window to block clock tamper. Reference: `docs/plan/inventory-redesign.md §Q7`.
- [2026-04-24] STOCKTAKE-BLIND-STRIP-SERVER-SIDE — Blind-mode counter UI MUST fetch lines through `get_stocktake_lines_blind` (SECURITY DEFINER) — never through `.from("stocktake_lines").select(...)`. The RPC strips `system_quantity` server-side before the payload crosses the RLS boundary. Client code must not re-query the base table or reference a `system_quantity` field — if the network payload contains that key, blind mode is broken and the whole monthly/quarterly/spot integrity chain collapses. Reference: S13a Day 4 count page `apps/web/app/inventory/stocktake/[id]/count/page.tsx`.
- [2026-04-24] DRAFT-AUTOSAVE-CASCADE — Stocktake draft auto-save (`stocktake_drafts`) must CASCADE-delete when the parent `stocktake_sessions` row is removed (migration `20260425180000_s13a_stocktake_drafts.sql` uses `ON DELETE CASCADE`). Never replace with `ON DELETE SET NULL` or a manual trigger — stale drafts attach to session ids that get reused by identity sequence reset, silently feeding old counts into a new session. Debounce stays at 30 s client-side (spec §Q2); re-saves skip when serialized counts match previous save to avoid write amplification.
- [2026-04-24] ZONE-LOCK-HEARTBEAT-THIRD-TTL — `heartbeat_zone_lock` must fire at ≤ 1/3 of the TTL window, not at the TTL itself. Default TTL 1800 s → heartbeat every 600 s. Firing at or near TTL means any single network hiccup drops the lock. `ZoneLockIndicator` implements this; `onLost` callback flips the page into read-only mode — never silently retry as the holder after losing ownership.
- [2026-04-24] PAYMENT-AUTO-COMPLETES-ORDER — POS payment confirm RPCs (`confirm_payment_and_post`, `complete_payment_and_consume_stock`, `create_payment` cash branch) MUST call `finalize_paid_order(order_id, actor_id)` after `payment_status='paid'` so `orders.status='completed'` and trigger `trg_release_table_on_order_status` releases the table. Semantic: `orders.status='completed'` means "paid + closed from POS view" — NOT "kitchen done". Helper MUST NOT force `order_items.status` or `kds_tickets.status` — chef keeps cooking via KDS after payment so pay-first-cook-after (takeaway rush, customer in a hurry) still works. Bill sheet UI MUST call `onClose()` after confirm success; never re-enable the old "Hoàn tất và trả bàn" button that required `status='served'` — completion is automatic now. When adding new payment RPCs, always `PERFORM public.finalize_paid_order(order_id, actor_id)` as the last step. Reference: `supabase/migrations/20260426030000_auto_complete_paid_order.sql`.
- [2026-04-24] POS-SERVED-NOT-TABLE-TERMINAL — `orders.status='served'` is a fulfillment/service marker only and MUST NOT release a dine-in table. `trg_release_table_on_order_status` terminal set is `completed/cancelled`; active-count must also treat `served` as active until payment closes the order. POS UI MUST NOT show "Trả bàn" or "Hoàn tất và trả bàn" as a normal cashier step. Reference: `supabase/migrations/20260424142658_pos_payment_close_table_release.sql`.
- [2026-05-02] POS-SERVED-REQUIRES-ITEMS-READY — `update_pos_order_status('served')` MUST refuse to flip the order while any active `order_items.status` is still `pending`/`preparing` (raises `22023 items not terminal`, same predicate as the `complete` branch). The aggregate "Phục vụ" action is the inverse mirror of PAYMENT-AUTO-COMPLETES-ORDER: just as payment must NOT force `kds_tickets='served'`, a cashier tap on order-level Phục vụ must NOT erase in-flight tickets the chef is still cooking. The post-guard UPDATEs only flip `status='ready' → 'served'`; `served` and `cancelled` rows are preserved. Per-item served continues via `mark_order_item_served`. When adding any new bulk-status RPC that touches `kds_tickets`, replicate the `items NOT IN ('ready','served','cancelled')` guard. Reference: `supabase/migrations/20260502000000_pos_served_requires_items_ready.sql`.
- [2026-05-02] POS-MOBILE-BUTTON-TOUCH-TARGET — POS surfaces that show in mobile/touch context (mobile action bar, order-item actions sheet, cart pane primary CTA, item customizer qty +/- and main CTA, bill-receipt sheet primary CTA) MUST have `min-height ≥ 44px` and `min-width ≥ 44px` per Apple HIG / Google MD3. Default shadcn button sizes top out at h-9 = 36px (size="lg") / size-9 = 36px (size="icon-lg"); they DO NOT meet this bar. Apply `min-h-11` (44px) override on lg buttons and `size-11` on icon buttons in mobile-touch contexts. Existing compliant patterns: `min-h-14 min-w-14` (mobile action bar), `min-h-12 w-full` (order detail primary CTA), `min-h-11 min-w-11 size-9` (cart pane delete). NEVER replace size primitive with raw inline `h-X` Tailwind — combine `size="lg"` (gives semantic font/padding) with explicit `min-h-11` override. When adding a new POS button on a surface that opens in a mobile sheet/drawer/action-bar, audit the rendered height before merging. Reference: `apps/web/app/br/[branchId]/pos/_components/order-detail/order-item-actions-sheet.tsx`, `apps/web/app/br/[branchId]/pos/item-customizer.tsx`.
- [2026-05-02] POS-KDS-RPC-SERVER-SIDE-ROLE-GATE — Every write-path RPC under POS/KDS (`void_order_item`, `cancel_order`, `edit_pending_order_item`, `update_pos_order_status`, `mark_order_item_served`, `bump_kds_ticket`, `recall_kds_ticket`, `transfer_order_table`, `reduce_order_item_quantity`, `merge_orders`, `split_order`, `confirm_payment_and_post`, etc) MUST gate authority server-side, never trust the UI button-disable. Two patterns coexist and both are valid: (a) legacy role-set check `v_prof_role NOT IN ('branch_manager','cashier','waiter') → 42501 forbidden` (used by M2 POS lifecycle RPCs), (b) canonical permission check `NOT has_permission_any('kds:recall') → 42501 not_allowed` (newer auth_v2 phase 2 pattern, preferred for new RPCs — see `supabase/migrations/20260423090000_auth_v2_phase2_rpc_gates.sql`). Both raise `42501` so client mapping stays uniform. Cashier without `kds:recall` calling `recall_kds_ticket` from devtools MUST get `42501 not_allowed` — verified by code review on 2026-05-02. When adding any new POS/KDS write RPC: pick pattern (b), add `auth.uid() IS NULL → 28000` first, then `has_permission_any(<key>)` second, before any state read or lock. UI `canRecall`/`canVoid`/etc gates are convenience only — defense in depth requires the RPC itself to refuse.
- [2026-05-03] DAILY-LIMIT-REDUCE-QTY-DECREMENT — `reduce_order_item_quantity` MUST explicitly decrement `branch_menu_item_daily_limits.sold_today` by the qty delta (`v_old_qty - p_new_quantity`) for the (branch_id, menu_item_id, today) row, INSIDE the same transaction as the `order_items` UPDATE. The enforce trigger is INSERT-only and the cancel trigger fires only on `status='cancelled'` — a pure quantity reduction (5 → 2) bypasses both, leaving 3 freed portions locked in `sold_today` until end of day. Use `GREATEST(sold_today - delta, 0)` to defend against any race producing transient negative (the column has CHECK >= 0 anyway). Skip the decrement when `v_item.menu_item_id IS NULL`. When introducing any other RPC that reduces (vs cancels) committed quantities, replicate this decrement. Reference: `supabase/migrations/20260503000000_reduce_qty_decrement_daily_limit.sql`.
- [2026-05-03] KDS-TRANSFER-TABLE-SYNC — KDS hook (`use-kds-realtime.ts`) MUST subscribe to `orders` UPDATE with `branch_id=eq.X` filter AND re-fetch order info when `payload.old.table_id !== payload.new.table_id` AND the order is already in the client's `ordersRef` cache. Without it, `transfer_order_table` updates `orders.table_id` but the kds_tickets channel sees nothing (it's keyed on tickets, not orders) and the OrderCard renders the stale `Bàn N` indefinitely — chef bưng nhầm bàn. The double-filter (table_id-actually-changed + cached-order) avoids re-fetching on every order mutation (status/total/payment); only the genuinely-relevant subset triggers a network call. orders is in `supabase_realtime` with REPLICA IDENTITY FULL (migration 20260425024802) so `payload.old.table_id` is reliable. When adding a new RPC that relocates kitchen work between physical contexts (table, station, branch), audit whether the KDS hook needs to react.
- [2026-05-03] KDS-ORDERCARD-MEMO-PROPS — `OrderCard` is wrapped in `React.memo` with a hand-written `arePropsEqual` because at peak hour (30+ active orders × 8 stations), every `kds_tickets` UPDATE re-creates `groupedOrders` array → without memo every card reconciles even when only 1 ticket flipped. The propsAreEqual MUST: (a) check same-identity short-circuit on `prev.order === next.order` (guarded by pendingTicketIds re-check), (b) deep-compare `order.tickets[].{id, status, bumped_at, kitchen_send_batch_id}` and `order.items[].{id, status, quantity}`, (c) restrict `pendingTicketIds` Set equality to THIS order's tickets only — board-wide Set identity changes on any optimistic mutate, but only this card's tickets matter. When extending `KdsOrder` / `KdsTicket` shape with a field that affects rendering, ADD it to propsAreEqual in the same diff. Reference: `apps/web/app/br/[branchId]/kds/order-card.tsx`.
- [2026-05-03] POS-DETAIL-REOPEN-AFTER-CUSTOMIZER — When cashier taps "Sửa món" inside `OrderDetailSheet`, the parent (pos-desktop-shell) closes the detail (mobile sheet stacking / focus-trap fight) AND opens the customizer. The detail close MUST snapshot `{id, orderNumber, summary}` into `pendingDetailReopenRef` BEFORE clearing — both `ItemCustomizer.onClose` (cancel) and the edit-sent confirm success branch route through `closeCustomizerAndMaybeReopenDetail`, which restores the detail by inlining the openDetail body (refetches the freshly-edited order; do not pass the stale pre-edit seed). Without this snapshot, every edit drops cashier to the menu — they must re-tap the order to verify the price changed. When introducing another flow that closes a detail/cart sheet to open a customizer, route teardown through this same wrapper so the reopen contract stays single-sourced. Reference: `apps/web/app/br/[branchId]/pos/pos-desktop-shell.tsx`.
- [2026-05-03] REALTIME-CHANNEL-RESUBSCRIBE-ON-TOKEN-REFRESH — `useRealtimeChannel` MUST listen to `supabase.auth.onAuthStateChange` and tear down + resubscribe on `TOKEN_REFRESHED` and `SIGNED_IN`. Reason: the per-channel `realtime.subscription.claims_role` is stamped at subscribe time and not re-evaluated when the JWT rotates (~1h default). After rotation, RLS-protected events stop reaching the channel SILENTLY — no error, just dead subscription. POS/KDS shifts span 8h+ so token rotates ≥1× during a session; previously, the second half of the shift would not see realtime broadcasts. The teardown also fires on `SIGNED_OUT` to release server-side state. Always pair this with explicit `realtime.setAuth(token)` BEFORE re-subscribe (defense-in-depth on the JOIN frame). When auditing realtime hooks, verify they go through `useRealtimeChannel` rather than calling `supabase.channel(...).subscribe()` directly. Reference: `apps/web/app/_hooks/use-realtime-channel.ts`.
- [2026-05-03] REALTIME-EVICT-CHANNEL-SYNC-AFTER-REMOVE — Inside `useRealtimeChannel`, every `removeChannel(stale)` MUST be followed by a synchronous eviction `supabase.realtime._remove(stale)` (mirrors the `_onClose` callback that `RealtimeChannel.js:90` registers). Reason: `removeChannel` is async — it awaits the server `phx_close` ACK before evicting from `socket.channels` (`RealtimeClient.js:221-229`). realtime-js's `channel(topic)` dedups by topic and returns the existing channel from that list (`RealtimeClient.js:297-308`). Any re-subscribe attempt that runs before the leave round-trip completes — auth-event race when `TOKEN_REFRESHED` fires immediately after the initial `getSession().then(subscribeWithToken)`, navigation remount, or React Strict Mode double-invoke against the singleton client returned by `@supabase/ssr`'s `createBrowserClient` — hands back the stale, already-subscribed channel. Calling `.on('postgres_changes', ...)` on it then throws `cannot add 'postgres_changes' callbacks for realtime:<topic> after subscribe()` (`RealtimeChannel.js:378-382`). The fix is contained in `subscribeWithToken` + cleanup; do not surface the internal `_remove` call to callers, and do not change the topic naming convention (filter dedup is per-purpose semantics). Tests must exercise: (a) cold mount with token-refresh-during-init, (b) navigation away→back faster than the leave RTT, (c) `SIGNED_IN`/`TOKEN_REFRESHED` mid-session. Reference: `apps/web/app/_hooks/use-realtime-channel.ts`.
- [2026-04-24] R4-ESCALATE-NOTE-MIN-CHARS — `escalate_round_4` RPC enforces `length(note) >= 20` server-side (raises `22023`). UI MUST mirror that check in both the Zod schema (`z.string().min(20, ...)`) AND the form submit guard before the action call so the counter never gets a stale 500. Drop to 10 chars or skip the note → next audit cannot reconstruct why QLV+Admin overrode R3 counts. When changing the threshold, update the RPC, the Zod schema, AND the `(N/20)` char counter label together. Reference: `escalate-client.tsx` + `supabase/migrations/20260425120000_s5_stocktake_blind_recount_abc.sql`.
- [2026-04-24] CONFLICT-RESOLVE-VIA-RPC-ONLY — Stocktake conflict resolution MUST go through `resolve_stocktake_conflict` RPC. Never direct-update `stocktake_lines.counted_quantity` from the queue UI to "apply client" — bypasses the conflict ledger update (`resolved_at`, `resolution`, `resolution_qty`, `resolution_note`), breaks audit trail, and lets a second QLV re-resolve the same row. The 4 resolution modes (`keep_server` / `apply_client` / `manual_value` / `reject`) are enumerated server-side; adding a 5th mode requires RPC change first, then client. `manual_value` without `p_manual_qty` raises `22023` — client must enforce before dispatch.
- [2026-04-24] FINALIZE-R1-FINAL-GATE — `finalize_stocktake` refuses to complete a session while any R1 line has `is_final=false` (raises `22023 cannot finalize: X round-1 line(s) still not final`). Never bypass by direct `UPDATE stocktake_sessions SET status='completed'` — R4 escalation is the ONLY legitimate path from R3 → R1 final. The escalate RPC writes a round-4 line AND sets the round-1 line's `is_final=true` with the escalated qty, so after all escalations the gate passes automatically. UI finalize button MUST be disabled while `needEscalation.length > 0`.
- [2026-04-24] AUTO-WASTE-NON-FATAL — POS void + KDS cancel auto-generated waste (`create_waste_from_order` RPC + `createWasteFromOrder` action) MUST NOT fail the parent void/cancel operation. The action always returns `{ success: true }`; failures surface via `data.softFailed=true` with `data.softError` message so the host shows a warning toast ("admin sẽ xử lý") while the underlying void/cancel completes. Never rewrite the action to raise / return `{ success: false }` — a cashier confirming payment void cannot be blocked because photo-EXIF validation or permission gate rejected the waste row. Pair with `showAutoWasteToast` for uniform messaging. Reference: `apps/web/app/inventory/waste-actions.ts#createWasteFromOrder`.
- [2026-04-24] KDS-CANCEL-STAGE-STEP-5 — `KdsCancelStagePicker` ratio slider for `mid_cook` uses **step=5**, not 1. Finer steps (1%) let a malicious chef split 30% into 6×5% fragments each under the anti-split rolling-sum threshold. Step=5 makes "split-into-smaller" still register as one event in the 15-minute rolling window. When adding new stage types (e.g. `parbaked`, `par_cooked`) keep the same step. Before `after_cook` is the 100% waste; `before_cook` emits `wasteSourceType=null` so the host short-circuits the RPC entirely (no phantom 0-qty rows).
- [2026-04-24] TRUST-SELF-VIEW-NO-GATE — Self-view of `user_trust_score` (`getMyTrustScore` in `apps/web/app/inventory/trust-actions.ts`) MUST use `getAuthContext([])` — no permission gate. Users always see their own score. Gating with `REPORTS_VIEW_BRANCH` (as the admin leaderboard does) hides the score from the very cashiers/chefs whose GRN activity drives it, defeating the transparency goal of §Q4b. The admin LEADERBOARD keeps the `reports:view_branch` gate to avoid peer ranking leakage. When adding trust-related actions, default to gate ONLY the cross-user view; always allow self.
- [2026-04-24] TRUST-COMPUTE-OPT-IN — `getTrustLeaderboard(branchId, { includeComputed: true })` fires up to 25 RPC calls to `compute_user_trust_score`. Default `includeComputed=false` — live compute MUST be opt-in per request via `?computed=1` URL param so dashboards don't hammer the RPC. Self-view (`getMyTrustScore`) calls RPC unconditionally since it's a single invocation. When bumping the 25-row cap, weigh RPC cost × 10 000 staff-branches; cap exists to keep RSC render under 2 s.
- [2026-04-24] GET-AUTH-CONTEXT-EMPTY-ARRAY-REJECTS — `getAuthContext([])` in `apps/web/app/_lib/auth.ts` rejects **every** authenticated user because `allowedRoles.includes(claims.user_role)` is false on an empty array. When an action only needs "authenticated, any role" (e.g. self-view trust score, read-only feature-flag matrix), pass `STAFF_ROLES` from `@comtammatu/shared/auth` — NOT `[]`. Symptom: correct-looking perm check but always returns `{ success: false, error: "Chưa đăng nhập" }` even for owners. Caught by: owner hitting `/admin/inventory/feature-flags` and seeing "Chưa đăng nhập" despite valid session on 2026-04-24.
- [2026-04-24] MCP-APPLY-VS-CLI-PUSH — Prefer `supabase db push` from CI for production deploys; reserve MCP `apply_migration` for dev exploration only. MCP auto-generates migration version from wall-clock `applied_at`, while local filename timestamps drive `supabase db push`. Mixing the two on the SAME database causes `schema_migrations` drift: local filename `20260425050000_s0a` can appear on dev as version `20260423172706_s0a`. Drift is cosmetic on prod (empty → push writes filename timestamps cleanly) but breaks `supabase db pull` and `db reset` on dev. Caught during Inventory Redesign post-ship audit 2026-04-24 — 15 migrations drifted on dev DB `iexwsuaqqenyjiskawoj`. Reference: `docs/plan/inventory-redesign.md` post-S9 review.
- [2026-04-25] INVENTORY-MOVEMENT-SUBTYPE-DERIVED — `confirm_stock_issue` RPC (migration `20260426100000_stock_issue_wac_strict_and_subtype.sql`) derives `stock_movements.movement_subtype` from `(stock_issues.issue_type, branches.branch_kind)` at confirm time. Mapping: `consumption` × `central_warehouse|central_kitchen` → `storage_loss`; `consumption` × `branch` → `sale_consumption`; `writeoff` → `writeoff`; `other` → `other`. Never hardcode the subtype in application code or store the label as a literal on `stock_issues` — the label is presentation, derived from this column. Reports that split food-cost by category MUST read `stock_movements.movement_subtype`, not `stock_movements.type` (which stays `consumption` for every issue flavor).
- [2026-04-25] INVENTORY-WAC-STRICT-OVERRIDE — `confirm_stock_issue` MUST read `stock_levels.avg_unit_cost` under `FOR UPDATE` keyed by `(tenant_id, branch_id, location_id, ingredient_id)` at confirm time and overwrite BOTH `stock_issue_items.unit_cost` AND the inserted `stock_movements.unit_cost` with that WAC. NULL WAC (ingredient never GRN'd) MUST raise `wac_not_ready_for_<ingredient_id>` with ERRCODE 22023 — NEVER silently fall back to 0 or to caller-provided unit_cost. UI MUST NOT expose an editable `unit_cost` field on issue lines; the draft DEFAULT 0 is only a placeholder overwritten at confirm. Reason: caller-provided costs drift food-cost reports and lose the audit tie between voucher and ledger.
- [2026-04-25] INVENTORY-ISSUE-TYPE-NO-KITCHEN-USE — `stock_issues.issue_type` CHECK constraint is now `consumption | writeoff | other` after migration `20260426100100_retire_kitchen_use_issue_type.sql`. Any PR reintroducing `kitchen_use` (Zod enum, UI dropdown option, SQL constraint seed, backfill) MUST be rejected at review. The Phase 1 replacement is an intra-branch `stock_transfer` with `from_location_id = warehouse`, `to_location_id = kitchen_line` within the same `branch_id` — the transfer dialog's "Nội bộ" tab already supports this flow. The guard trigger `reject_stock_issue_kitchen_use` has been retired; the CHECK constraint is now the sole enforcement point.
- [2026-04-27] INVENTORY-PILOT-CONTRACT-V2 — Inventory pilot readiness is the 4-point operating loop `Kho Tổng -> Bếp Trung Tâm -> Kho Chi Nhánh -> Bếp Chi Nhánh`, not full ERP route coverage. `Kho Chi Nhánh -> Bếp Chi Nhánh` MUST be an atomic one-step intra-branch transfer, not `stock_issue(kitchen_use)` and not the 5-step inter-site state machine. Supplier invoice/payment/AP aging, returns, credit notes, trust, auto-waste, stocktake conflicts/recount, cold-chain, barcode/bin, vendor portal, and WIP/labor costing are P1/deferred unless an owner decision explicitly reopens them. Docs, SOP, route CTA matrix, QA runbooks, and tests must agree with runtime before marking Inventory ready.
- [2026-04-25] INVENTORY-ISSUE-LABEL-BY-BRANCH-KIND — The UI label for `issue_type='consumption'` flips by `branches.branch_kind`: `central_warehouse|central_kitchen` → "Hao hụt kho"; `branch` → "Tiêu hao". Dialog titles, list columns, CSV exports, and sidebar headings MUST resolve the label through the helper (`issueTypeLabel(type, branchKind)` in `apps/web/app/inventory/issues/issues-client.tsx` + `getIssueSurface(issueType, branchKind)` in `apps/web/app/inventory/issues/[id]/issue-detail-client.tsx`). NEVER ship a static "Tiêu hao" label without branch_kind context — CW/CK "consumption" is shrinkage, not sales consumption, and conflating them misleads both operators and food-cost reports.
- [2026-04-25] POS-PAYMENT-REUSE-UNIQUE-SLOT — `idx_payments_order_active` is a partial UNIQUE on `payments(order_id) WHERE status != 'failed'` — at most ONE active payment per order, regardless of method. Any RPC that creates/upgrades a payment for an order (cash confirm, vietqr confirm, momo webhook) MUST first look for an existing non-failed row (`SELECT id, status FROM payments WHERE order_id=? AND status<>'failed' FOR UPDATE`) and REUSE it (UPDATE in-place) rather than blindly INSERTing — else INSERT collides with a pending row from a prior method the cashier previewed. Also: cash-specific wrappers MUST `UPDATE orders SET payment_method='cash'` themselves, because `complete_payment_and_consume_stock` is webhook-shared and (correctly) does not touch `payment_method`. Caught by: cashier tab-switch VietQR → Cash → "Đã thanh toán" → 12× `duplicate key ... idx_payments_order_active` + 7 burned payment IDs on 2026-04-24 17:37-17:39 UTC. Reference: `supabase/migrations/20260428200000_fix_confirm_cash_payment_method_swap.sql`.
- [2026-04-25] POS-SESSION-SCOPE-PER-TERMINAL-NOT-PER-USER — SUPERSEDED by [2026-04-27] POS-SESSION-SCOPE-PER-BRANCH. Keep only the surviving invariant: `fetchActiveSession` in `apps/web/app/br/[branchId]/pos/session-actions.ts` MUST query by `(tenant_id, branch_id, status='open')` and MUST NEVER re-introduce `.eq("opened_by", user.id)`. The per-terminal active-session model, multi-terminal picker UX, and `UNIQUE(terminal_id) WHERE status='open'` contract are historical context only; current runtime allows one open POS session per branch. Caught: waiter seeing "Chưa có ca mở" dead-end on 2026-04-25 after initial fix to `openPosSession` gate.
- [2026-04-25] POS-CLOSE-SHIFT-UI-GATED — "Chốt ca" button in `pos-session-header.tsx` and the F10 shortcut in `pos-desktop-shell.tsx` MUST be gated client-side by `canCloseShift` (prop threaded from `page.tsx` via `fetchPosPermissionFlags`). Server already enforces via `closePosSession` → `POS_CLOSE_SHIFT` perm, but waiter riding cashier's session would otherwise see a trigger that 403s on click. Defense in depth + clean UX. When adding new close-session entry points (dropdown menu, mobile nav), thread `canCloseShift` and render nothing when false — never fall back to server-side rejection for UI visibility decisions.
- [2026-04-25] POS-CONFIRM-CASH-GATED-BY-POS-CONFIRM-PAYMENT — `confirm_cash_payment` RPC and the `confirmCashPayment` server action (`apps/web/app/br/[branchId]/pos/payment-actions.ts`) MUST gate by `pos:confirm_payment` — cash chạm két vật lý = single-point-of-cash accountability (cashier/branch_manager+). NEVER fall back to `pos:print` (waiter has it for bill/KDS) or to the dangling `pos:payment` key (never seeded, bug source of 20260426000000). Granted templates: cashier, quan_ly_CN, quan_ly_vung, super_manager, owner. NOT granted to waiter/chef/office. VietQR/MoMo (`createPayment`/`confirmPayment`) intentionally stay on `pos:use` — e-wallet không chạm két, webhook là source of truth, waiter tại bàn vẫn confirm được QR (mobile-first flow). UI: `bill-receipt-sheet.tsx` filters `cash` method via `canConfirmCash` prop threaded from `page.tsx` → `fetchPosPermissionFlags` → `pos-desktop-shell.tsx`. Reference: `supabase/migrations/20260428210000_pos_confirm_payment_permission.sql`.
- [2026-04-25] POS-MULTI-ORDER-PER-TABLE-NEW-INTENT-EXPLICIT — POS allows multi-order per dine-in table (PR3 Gộp bàn Option A). Tap on `tables.status='occupied'` opens `MultiOrderTablePicker`; the picker is the ONLY entry point for a 2nd order on the same bàn. New-order intent on an occupied bàn MUST be tracked via the `allowOccupiedTableId` state in `pos-desktop-shell.tsx` so the auto-clear effect (line ~213, `selectedTable.status !== "available"`) does NOT reset the selection mid-flow. The flag is single-slot — one explicit-occupied selection at a time — and is cleared by an effect that fires whenever `selectedTableId` diverges from it (manual switch, post-submit reset). `selectedTableUsable = selectedTableAvailable || isExplicitOccupied` gates `orderContextReady` and `canSubmit`. NEVER re-introduce a `.eq("status","available")` block on `tables` inside `create_order` RPC — the RPC layer is multi-order-friendly today (verified [20260425020000_fix_pos_order_rpcs_auth_v2.sql:99-106](supabase/migrations/20260425020000_fix_pos_order_rpcs_auth_v2.sql:99)) and `trg_release_table_on_order_status` correctly counts active orders before releasing. `transfer_order_table` MUST also accept `tables.status IN ('available','occupied')` — blocking only `reserved`/`maintenance` — so chuyển-bàn semantics align with multi-order-per-table; rejecting transfer to an occupied bàn while ALLOWING manual creation of a 2nd order on the same bàn is incoherent UX (see migration [20260429100000_transfer_order_table_allow_occupied.sql](supabase/migrations/20260429100000_transfer_order_table_allow_occupied.sql)). Reference: `apps/web/app/br/[branchId]/pos/_components/multi-order-table-picker.tsx`.
- [2026-04-25] MV-GROUP-BY-MUST-MATCH-UNIQUE-INDEX — Materialized views with `REFRESH ... CONCURRENTLY` MUST have GROUP BY columns that are a SUBSET of the unique index. `mv_top_items` and `mv_food_cost` shipped with `GROUP BY (..., menu_item_id, oi.item_name)` but unique index keyed only `(period_start, branch_id, tenant_id, menu_item_id)`. When a menu item gets renamed (`oi.item_name` is the snapshot at order time, not the live name), two rows for the same key tuple emerge → CONCURRENTLY refresh raises `23505 duplicate key value violates unique constraint`. Result: `refresh_finance_views()` silently fails on the 2nd MV, food cost % and top-items dashboards stale forever. Fix pattern: drop the cosmetic column from GROUP BY, aggregate it via `MAX(...)` in SELECT instead. When adding a new MV with CONCURRENTLY refresh: GROUP BY ⊆ unique index columns, then add a regression test that runs `REFRESH MATERIALIZED VIEW CONCURRENTLY` after seeding rows that share the key but differ on a non-indexed column. Reference: `supabase/migrations/20260425030256_fix_mv_top_items_food_cost_dedup.sql`.
- [2026-04-25] HDDT-PAYMENT-FIRST-FAILSOFT-ORPHAN — POS HĐĐT issuance MUST run AFTER `confirm_cash_payment` returns success, NEVER atomic with payment, and HĐĐT failure MUST NOT roll back payment. Orchestrator `confirmCashPaymentWithInvoice` (`apps/web/app/br/[branchId]/pos/payment-actions.ts`) calls `confirmCashPayment` first (commercial close: cash + stock + GL post + table release + print enqueue all atomic in `confirm_cash_payment` RPC), then calls `createTaxInvoice` separately. If invoice fails, action returns `{ success: true, data: { ...payment, invoice: { status: 'failed', error } } }` so cashier sees "Đã thu tiền — HĐĐT chưa xuất được, lưu nháp Finance xử lý". Reasons: (a) MoMo-style async settlement makes 2PC across HTTP+DB+provider infeasible; (b) rolling back paid order because of e-invoice fault loses real cash for paper fault; (c) `tax_invoices.payment_status='paid'` precondition in `createTaxInvoice` already blocks issue-before-pay. Bill print fires inside `confirm_cash_payment` BEFORE invoice exists — receipt template intentionally has no mã CQT in v1; HĐĐT is digital record only. When wiring real Viettel S-invoice: keep payment-first; transition draft → signing async via Edge Function; do NOT block `confirm_cash_payment` on provider response. NEVER add `BEGIN/COMMIT` wrapping payment + invoice. Reference: `apps/web/app/br/[branchId]/pos/payment-actions.ts#confirmCashPaymentWithInvoice` + plan synthesis 2026-04-25.
- [2026-04-25] HDDT-FORM-PAYLOAD-FREEZE-AT-CLICK — POS Bill Sheet HĐĐT form (`invoice-form-section.tsx`) MUST snapshot the invoice payload via `buildInvoicePayload(invoiceForm)` at the moment cashier clicks "Đã thanh toán", NOT inside the async `startActionTransition` callback. Otherwise: cashier types MST → clicks submit → provider call in flight → cashier edits buyerName field → second render captures stale state and async submit uses stale snapshot. Same pattern: `disabled={actionPending}` on every Input/Checkbox in the form section so the form is visually locked once submit fires. The Zod refine on server (`createInvoiceSchema` in `apps/web/app/finance/actions.ts`) is the second line of defense — `MST → buyerName required` + MST regex `^\d{10}(-\d{3})?$` rejects garbage at action boundary. Reference: `apps/web/app/br/[branchId]/pos/_components/bill/bill-receipt-sheet.tsx#handleConfirmPaid`.
- [2026-04-25] REALTIME-AWAIT-AUTH-BEFORE-SUBSCRIBE — Every Supabase Realtime subscription MUST go through `useRealtimeChannel` (`apps/web/app/_hooks/use-realtime-channel.ts`) which awaits `auth.getSession()` and pins the JWT via `realtime.setAuth()` before `.subscribe()`. Calling `supabase.channel(...).subscribe()` directly inside `useEffect` mount races against auth initialization: when subscribe wins, the broker stamps `realtime.subscription.claims_role='anon'` for that row and never re-evaluates it — even after supabase-js later pushes the access token. RLS policies that gate via `has_permission()` (which needs `auth.uid()`) drop every broadcast for that subscription forever, until the channel is removed and re-subscribed. Symptom: cross-terminal sync silently breaks — POS list misses new orders from other tablets, KDS doesn't see new tickets, payment confirm doesn't propagate, but the channel reports `SUBSCRIBED` with no error. When adding a new realtime sub, NEVER bypass the helper. Caught by: POS realtime audit 2026-04-25 — `realtime.subscription` table showed leftover anon rows on dev DB even with valid logged-in session. Reference: `apps/web/app/_hooks/use-realtime-channel.ts`, callsites `useOrderSync`, `OrderDetailSheet`, `BillReceipt`, `PrinterStatusBadge`, `useKdsRealtime`, `useNotifications`.
- [2026-04-27] DIACRITIC-COMPOSED-ONLY — All Vietnamese source text MUST use composed-style (post-1984 spelling) diacritics: `Hủy`, `Xóa`, `Hóa`, `Thỏa`, `Tỏa`, `Hòa`. Old combining forms (`Huỷ`, `Xoá`, `Hoá`, `Thoả`, `Toả`, `Hoà`) are forbidden in any `.ts`/`.tsx` under `apps/`, `packages/`. Why: Phase 0 normalization 2026-04-27 standardized on composed; mixed forms cause selector drift, search-and-replace inconsistency, and grep blindness. Detect: `rg -P "[Hh]u[ỷ]\|[Xx]o[á]\|[Hh]o[á]\|[Tt]ho[ả]\|[Tt]o[ả]\|[Hh]o[à]" apps packages --type ts --type tsx` returns 0. Reference: `docs/ref/glossary.md §"Quy ước chính tả tiếng Việt"`.
- [2026-04-27] MESSAGES-SINGLE-SOURCE — Generic action verbs (`Lưu`, `Hủy`, `Xóa`, `Tạo mới`, `Xác nhận`), state copy (`Đã lưu`, `Đang tải…`, `Hoàn thành`), and generic errors (`Có lỗi xảy ra`, `Mất kết nối`) in `.tsx` MUST come from `@comtammatu/shared/messages` (`ACTIONS_VI`/`STATES_VI`/`ERRORS_VI`) or `apps/web/lib/messages/*`. Domain nouns (Order/Branch/Staff/Table/Product/Customer) MUST come from `@comtammatu/shared/messages` (`ORDER_VI`/`BRANCH_VI`/...) or module dictionaries. Legal-fixed strings (HĐĐT/MST/Tên người mua/...) live in `packages/shared/src/labels/legal-fixed.ts` (`LEGAL_FIXED_VI`) and MUST be left inline with `// vi-allow: legal-fixed` annotation — never replaced by sweep. Why: Phase 1A 2026-04-27 created shared dict; future drift breaks domain-term consistency. Complements [2026-04-15] TERMINOLOGY-SOURCE-OF-TRUTH.
- [2026-04-27] STATUS-LABEL-NO-LOGIC — Display labels for status enums (e.g., `"Đã hủy"`, `"Hoàn thành"`, `"Sẵn sàng"`, `"Đã phục vụ"`) MUST NOT appear on the right-hand side of `===` or `!==` comparisons in business logic. Branch on the canonical English/snake_case enum value (`"cancelled"`, `"completed"`, `"ready"`, `"served"`) only. Why: Phase 2 string sweep would silently change the literal and break logic. Detect: `rg -P '===\s*"(Đã \|Hoàn \|Sẵn \|Đang )"' apps packages --type ts --type tsx` returns 0.
- [2026-04-27] E2E-TEXT-FROM-DICTIONARY — Playwright selectors using Vietnamese literals (`getByText`, `getByRole({name})`, `toContainText`, `expect(locator).toHaveText`) in `apps/web/e2e/**` MUST import from `apps/web/e2e/_lib/text.ts`, which re-exports `ACTIONS_VI`/`STATES_VI`/`ERRORS_VI`/domain dicts from `@comtammatu/shared/messages`. Why: 100+ literal-text selectors would break on every copy migration; the shim keeps tests in sync with the canonical source. Detect: `rg -P 'getByText\("[^"]*[À-ỹ]' apps/web/e2e` returns 0 hits outside `_lib/`.
- [2026-04-25] PWA-SW-NETWORKONLY-MUTATIONS — `apps/web/app/sw.ts` `runtimeCaching` array MUST keep `NetworkOnly` rules ahead of any cache-strategy rules for: (a) `request.method !== "GET"`, (b) requests carrying `RSC === "1"` header, `Next-Action` header, or `?_rsc=` query, (c) any `*.supabase.co` host or `/auth/v1/` / `/rest/v1/` path. Serwist evaluates rules in array order and stops at first match — re-ordering or inserting a `CacheFirst`/`StaleWhileRevalidate` before these guards lets a Server Action POST be served from cache. A cached payment POST = cashier sees "Đã thanh toán" but `confirm_cash_payment` RPC never runs → silent double-charge or unrecoverable money discrepancy. When adding a new payment method, RPC client, or Supabase project domain, audit the matcher list before adding it. Reference: `apps/web/app/sw.ts:24-77`.
- [2026-04-25] PWA-MANIFEST-BRANCHID-INTEGER — `apps/web/app/br/[branchId]/pos/manifest.webmanifest/route.ts` (and any future per-branch dynamic Route Handler) MUST validate the `branchId` segment as a clean positive integer string with `String(branchNum) === rawBranchId`, not just `Number.isFinite(parseInt(...))`. `Number.parseInt("123abc", 10) === 123` so naive validation passes but reflecting raw `params.branchId` into `start_url` / `scope` / `id` produces a manifest pointing at `/br/123abc/pos` which doesn't exist → installed PWA silently breaks on launch. Always interpolate the parsed integer (`branchId = String(branchNum)` after validation) into the manifest JSON, never `await params` raw segment. Reference: `apps/web/app/br/[branchId]/pos/manifest.webmanifest/route.ts:9-22`.
- [2026-04-25] PWA-OFFLINE-GATE-CASH-ONLY — POS bill sheet (`apps/web/app/br/[branchId]/pos/_components/bill/bill-receipt-sheet.tsx`) MUST gate `canConfirmPaid` with `isOnline` from `useIsOnline()` AND reject non-cash payment method selection when offline (`handleSelectMethod` early-returns with toast "Mất kết nối — chuyển khoản chưa khả dụng."). Reason: `confirm_cash_payment` is the most dangerous offline failure path — physical money already in cashier's hand + RPC times out = unrecoverable trust break (cashier may pocket the difference, or customer leaves believing paid). When adding a new payment method (ZaloPay, Apple Pay, etc.), MUST add it to BOTH the `handleSelectMethod` offline guard AND any new confirm action wrapper. Source of truth for online state is `useIsOnline()` exported from `apps/web/app/br/[branchId]/pos/_components/pwa/online-status-provider.tsx`; default returns `true` when no provider is mounted (tests, Storybook, non-PWA contexts) so guard logic does not over-trip.
- [2026-04-25] PWA-HYDRATION-ONLINE-DEFAULT — `useIsOnline()` and `useInstallPrompt()` initial state in `apps/web/app/br/[branchId]/pos/_components/pwa/online-status-provider.tsx` MUST be hardcoded (`isOnline=true`, `installPrompt=null`) for the first server-rendered + first-paint render, then synced from `navigator.onLine` / `beforeinstallprompt` event inside `useEffect` after mount. Reading `navigator.onLine` inside `useState(...)` initializer breaks SSR (`navigator` undefined on server) and causes hydration mismatch when SSR returns `true` but client mounts to a freshly-offline state. Same pattern applies to any future browser-state hook (battery, geolocation, media-query, web-share availability) — pick the most common state as initial value, then sync after mount.
- [2026-04-25] REALTIME-PUB-NEEDS-REPLICA-IDENTITY-FULL — Every table added to the `supabase_realtime` publication MUST also have `REPLICA IDENTITY FULL` set in the same migration. Default `REPLICA IDENTITY` (= primary key) makes Postgres logical replication emit only the PK in DELETE payloads and only the changed columns + PK in UPDATE `payload.old`. Two failure modes follow: (a) **Filter drops on DELETE** — POS hooks subscribe with non-PK filters like `branch_id=eq.{branchId}`. With DEFAULT identity, DELETE payloads do NOT carry `branch_id`, so the Realtime broker cannot match the filter and silently drops the event — the channel still reports `SUBSCRIBED` and no error fires; (b) **UPDATE old-state truncation** — code that compares `payload.old` vs `payload.new` to detect transitions (e.g. payment_status flip, ticket bump) sees only the changed fields in `old`, masking the prior state. WAL volume grows ~30% on FULL — negligible for pilot scale; reassess at high-write tables. Tables currently FULL: `orders`, `tables`, `payments`, `kds_tickets`, `print_jobs`, `printer_agents`, `notifications` (migration `20260430000000_realtime_replica_identity_full.sql`). When publishing a new table, add `ALTER TABLE public.<name> REPLICA IDENTITY FULL;` to the SAME migration as the `ALTER PUBLICATION ... ADD TABLE` — never split across PRs. Caught by: POS realtime debate v2 audit 2026-04-25 — 7 tables in publication, 0 with REPLICA FULL, DELETE-by-branch_id silently broken.
- [2026-04-25] REALTIME-SUBSCRIBE-NEEDS-STATUS-CALLBACK — Every Supabase Realtime subscription MUST pass a `status` callback to `.subscribe()` that handles the reconnect-resync case. Supabase Realtime is **at-most-once** (no durable log) — events that fire while a client is disconnected are LOST FOREVER once the WebSocket reconnects. The broker emits `SUBSCRIBED` once on initial subscribe AND again after every successful reconnect (after `CHANNEL_ERROR`/`TIMED_OUT`/`CLOSED`); the second-and-later `SUBSCRIBED` is the only signal a client gets that "you may have missed events — refetch a snapshot now." Pattern (see `apps/web/app/br/[branchId]/pos/hooks/use-order-sync.ts:95-107`): keep a `useRef(false)` flag, skip the FIRST `SUBSCRIBED` if state is already seeded from RSC props/startup, refetch a fresh snapshot on every subsequent `SUBSCRIBED`. Naked `.subscribe()` without callback = silent reconnect-miss, papered over only by stale-poll intervals (12-30 s race window). For helpers: `useRealtimeChannel` (`apps/web/app/_hooks/use-realtime-channel.ts`) does NOT add the resync callback for you — caller is still responsible for the status handler inside the channel-builder lambda. Tables/hooks already conformant: `useOrderSync`, `useKdsRealtime`, `useNotifications`, `PrinterStatusBadge`, `apps/print-agent/src/index.ts`. When adding a new realtime sub: pair the auth-await helper (REALTIME-AWAIT-AUTH-BEFORE-SUBSCRIBE) with this status-callback pattern — both are required, neither is sufficient alone. Caught by: POS realtime debate v2 audit 2026-04-25 — 4 hooks subscribed without status callback, recovery latency 12-60 s when WS dropped mid-shift.
- [2026-04-26] PRESET-EXACT-MATCH-OR-LAYERED-OVERRIDE — `packages/ui/src/styles/globals.css` is partitioned into 3 zones; the active shadcn preset is `b6G3vbGue` (`radix-lyra` style, neutral baseColor). The generated `@import "./matu-tokens.css"` may appear before Zone A; it MUST be generated from `packages/design-tokens/tokens.json` via `pnpm tokens:build`, never hand-edited. **Zone A** (preset foundation) — `:root` + `.dark` blocks + the preset's `@theme inline` color/radius mappings + the preset's `@layer base` cursor rule remain the source surface; matu-superapp tunes values through semantic variables and generated tokens. **Zone B** (CTMT overrides) — only ADD tokens never present in preset (`--success`/`--warning`/`--info`/`--destructive-foreground` for status semantics, `--tier-elite`/`--tier-note` for tier badges); MUST be additive, MUST be OKLCH, MUST define both `:root` AND `.dark`, MUST carry a `/* Override: <reason> */` comment. **Zone C** (app utilities) — inlined byte-equivalent of `shadcn/tailwind.css` plus project additions: `--font-sans`, `--font-heading`, and `--font-matu-body` map to Be Vietnam Pro via `--font-matu-runtime`; `--font-mono` maps to JetBrains Mono; `html { font-size: 17px }`; `::selection`. NEVER reintroduce Inter/Montserrat or pilot-only `font-matu-body` semantics without changing `docs/spec/design-system.md` first. Re-syncing Zone C means diffing against `node_modules/shadcn/dist/tailwind.css` when bumping the shadcn CLI. Reference: 4-agent debate synthesis 2026-04-26; Ma Tu Concept 01 update 2026-05-05; app-wide matu-superapp baseline 2026-05-09.
- [2026-04-26] POS-DISCOUNT-RECOMPUTE-VIA-HELPER — `orders.discount_amount` is DERIVED from `(discount_type, discount_value, current_subtotal)` — never stored as a snapshot. Every RPC that touches subtotal MUST call `public.compute_discount_amount(p_type, p_value, p_subtotal)` and persist the returned value back to `orders.discount_amount` + `orders.total_amount` in the same UPDATE. Patched RPCs: `append_order_items`, `void_order_item`, `cancel_order`, `split_order`, `merge_orders`. Reasoning (owner Q3): when type='pct' the cashier-displayed rate must hold across cart changes (10% on 100k → still 10% on 150k after appending a side); when type='vnd' the helper auto-clamps to subtotal so void can never make total negative. Adding a new lifecycle RPC: copy the same `v_discount_amount := compute_discount_amount(o.discount_type, o.discount_value, v_new_subtotal); UPDATE orders SET discount_amount = v_discount_amount, total_amount = v_new_subtotal + service_charge - v_discount_amount` block. Reference: `supabase/migrations/20260430000000_pos_discount.sql`.
- [2026-04-26] POS-DISCOUNT-PAIRED-NOTE-MIN-3 — `orders` CHECK constraint `orders_discount_metadata_paired` enforces all-or-nothing: either `discount_amount = 0` AND every `discount_*` column NULL, OR `discount_amount > 0` AND `discount_type` ∈ {pct,vnd} AND `discount_value` set AND `discount_note` ≥ 3 chars after trim. Server `apply_order_discount` validates note before locking; UI `DiscountSheet` mirrors with a live counter + disabled submit. Never write a partial discount tuple (e.g. set `discount_amount` directly without the metadata) — the constraint will reject and rollback the whole transaction. To clear a discount, call `clear_order_discount` (NULLs all four fields atomically); never zero `discount_amount` while leaving the metadata.
- [2026-04-26] POS-CASH-ZERO-TOTAL-OK — `confirm_cash_payment` accepts `cash_received = 0` WHEN `total_amount = 0` (e.g. 100% discount đơn nhân viên / đền khách). The under-payment guard is `cash_received < total_amount` — strict less-than, not strict-greater-than-zero. NEVER tighten back to `cash_received <= 0 RAISE`: cashier on a paid-as-comp order would be locked out of the "Đã thanh toán" button. Sanity upper-bound `GREATEST(total*10, 50000000)` already protects against fat-finger typos at zero-total. Payments row stamped `method='cash', amount=0, status='completed'` keeps audit trail intact (zero-total comp meal is a legitimate accounting event, not a rejected one). Reference: `supabase/migrations/20260430000000_pos_discount.sql` §9.
- [2026-04-26] POS-SPLIT-IN-PLACE-NO-REPRINT — `split_order` moves items via `UPDATE order_items SET order_id = new` (in-place; row id stable) and `UPDATE kds_tickets SET order_id = new` (KDS card follows). NEVER delete-and-reinsert items, NEVER call `enqueue_kitchen_print` or `enqueue_cancel_ticket_print` from a split path: items physically exist at the kitchen, reprinting causes chef double-cooking; cancel-ticket-then-new-ticket cycle confuses station. Same rule applies to `merge_orders` (items move src→target in place, no reprint). When extending the split flow (e.g. split-by-seat), keep the move primitive; only the item-selection rules change. Reference: `supabase/migrations/20260430010000_pos_split_order.sql`.
- [2026-04-26] POS-SPLIT-PRESERVE-NON-EMPTY — `split_order` raises `split_would_empty_source` (ERRCODE 22023) if the requested move would leave the source with 0 active items. UI gating: `SplitOrderSheet` shows "còn lại N món" live + disables submit when N < 1; `OrderDetailSheet` hides the "Tách hoá đơn" entry entirely when `activeItemCount < 2`. Owner decision (Q2 4-agent debate): block, do NOT auto-cancel the source. Tách-rồi-cancel = sai mục đích — cashier muốn clear bàn thì phải dùng "Hủy đơn"; cashier muốn chuyển bàn thì dùng "Chuyển bàn". When loosening this rule (e.g. allow split-then-auto-cancel for "rebill" use case), wire a separate RPC; do NOT change `split_order` behavior in place.
- [2026-04-26] POS-MERGE-LOCK-LEAST-FIRST — `merge_orders` MUST acquire advisory locks in `LEAST(a,b)` then `GREATEST(a,b)` order — never by argument order. Two cashiers double-tapping cross-merges (`merge(A→B)` and `merge(B→A)` simultaneously) deadlock under naive ordering: T1 holds A wants B, T2 holds B wants A. With ordered locks both threads grab A first; one wins, the other queues. Same pattern applies to any future paired-order RPC (transfer-with-merge, refund-and-rebook). When adding a new such RPC: copy the `LEAST/GREATEST` block AND add `SELECT … FOR UPDATE` in lower-id order so FK-locked rows don't introduce a second deadlock surface. Reference: `supabase/migrations/20260430020000_pos_merge_orders.sql` §B.4 step 4.
- [2026-04-26] POS-MERGE-SAME-TABLE-DINE-IN-BOTH-UNPAID — `merge_orders` requires: `source.tenant_id = target.tenant_id`, `source.branch_id = target.branch_id`, `source.table_id = target.table_id` (both non-NULL), `order_type = 'dine_in'` (both), `payment_status != 'paid'` (both), `status NOT IN ('completed','cancelled')` (both), `merged_into_order_id IS NULL` (both), and NO active row in `payments` (status NOT IN failed/completed). Cross-bàn / takeaway / paid orders are out of scope per owner — when adding "transfer + merge" UX in future, that's a separate RPC composed of `transfer_order_table` then `merge_orders`, NOT a relaxed merge guard. UI MergeOrdersSheet picker server-side filters `same table_id, dine_in, non-paid, non-merged` via `fetchSiblingOrdersForTable` — never accept a target id from URL state without re-validating in the RPC.
- [2026-04-26] POS-MERGE-PCT-DISCOUNT-BLOCK — `merge_orders` raises `merge_pct_discount_blocked` (ERRCODE 22023) when EITHER source or target has `discount_type = 'pct' AND discount_amount > 0`. Owner reasoning (Q4): % discounts can't cộng dồn meaningfully (10% trên A + 15% trên B ≠ 25% trên gộp; chia subtotal nào?). Cashier must `clear_order_discount` on one side first. VND discounts DO cộng dồn into target (`v_target_discount_value := source.value + target.value` + concat notes) — auto-clamped via `compute_discount_amount` if the sum exceeds new subtotal. UI `MergeOrdersSheet` disables pct-discounted siblings (with hint label) and shows a destructive banner when the source itself has pct (every option disabled). When adding a third discount type (e.g. fixed-per-item, BOGO), update the conflict matrix in this rule + the RPC's CASE block + the UI hint logic together.
- [2026-04-26] POS-HDDT-CONDITIONAL-ON-MST — `createTaxInvoice` (`apps/web/app/finance/actions.ts`) MUST short-circuit when `buyerTaxCode` is missing/empty (after trim): insert a `tax_invoices` row with `status='not_required', provider='skipped', invoice_number=null, buyer_tax_code=null` and SKIP the provider call. Owner D4 (2026-04-26): khách comp meal / khách lẻ không nhập MST = signal khách không cần HĐĐT, không tốn provider quota, không phát sinh hoá đơn 0đ vô nghĩa. The `not_required` status is **terminal** (not in `transition_tax_invoice_state` matrix as either FROM or TO) and is **excluded from the unique-active partial index** `uq_tax_invoices_active_per_order` — nghĩa là khách quay lại sau và yêu cầu HĐĐT thì createTaxInvoice gọi lần nữa với MST sẽ insert hoá đơn thật, row 'not_required' đứng riêng làm audit trail. Duplicate check trong `createTaxInvoice` (the `not("status", "in", ...)` filter) MUST mirror this — exclude all 3 of `cancelled, replaced, not_required` — else cashier không issue được hoá đơn cho order đã có 'not_required' từ lần trước. UI side: `confirmCashPaymentWithInvoice` orchestrator passes the invoice payload through unchanged; `InvoiceOutcome.status` enum includes `'not_required'`; `bill-receipt-sheet.tsx` toast cascade has a dedicated branch "Đã thanh toán — không xuất HĐĐT (Khách không nhập MST)" so cashier không nhầm với failure path. NEVER skip the `tax_invoices` insert entirely (no row = no audit). NEVER add a `not_required → cancelled` transition path — terminal là terminal. Reference: `supabase/migrations/20260502000000_pos_hddt_not_required_d4.sql` + `tasks/owner-decisions-pos-pass-1.md`.
- [2026-04-26] POS-CLOSE-SHIFT-PAID-FILTER-AND-VARIANCE-GATE — SUPERSEDED. Current `close_pos_session` contract is split across [2026-04-27] POS-CLOSE-SHIFT-CASH-ONLY-EXPECTED (expected cash = opening cash + paid cash orders only) and [2026-04-27] POS-CLOSE-SHIFT-NO-BLOCK-ALERT (variance breach creates manager alert but MUST NOT block close). Historical D1/D3 context: unpaid orders carry forward and must not count toward closing cash; the old BM override + variance note gate is retired and MUST NOT be reintroduced without a new owner D-decision.
- [2026-04-27] POS-CLOSE-SHIFT-NO-BLOCK-ALERT — `close_pos_session` (migration `20260504000000_pos_close_shift_no_block_alert_d8.sql`) MUST allow close trong MỌI trường hợp — KHÔNG block khi `|cash_difference| > variance_threshold`. Variance breach giờ trigger `notify_pos_shift_variance_after_update` (SECURITY DEFINER trigger trên `pos_sessions` AFTER UPDATE OF status) insert vào `notifications` với kind `pos.shift_variance` và severity `warning` (≤ 5× threshold) hoặc `critical` (> 5× threshold). Owner D8 (2026-04-27, supersedes D3 2026-04-26): chặn cashier đóng ca → kẹt cuối ca, không giải quyết gốc rễ; lệch quỹ là vấn đề audit, không phải lock-down. RPC vẫn trả `variance_threshold` + `variance_breached` flag trong JSONB; UI hiện toast "đã gửi cảnh báo cho quản lý" khi breached. Permission `pos:close_shift_variance_override` còn trong DB nhưng không gate flow nào — KHÔNG drop để giữ backward-compat. UI flow `CloseSessionSheet` đã bỏ variance sub-step (varianceCtx state, varianceNote input, VARIANCE_NOTE_MIN const). Server Action `closePosSession` đã bỏ error parsing cho `variance_requires_bm_approval` / `variance_note_required`. NEVER re-introduce variance gate vào RPC mà không có owner D-decision mới — flow đã thiết kế cho human-in-the-loop hậu kiểm qua manager dashboard. Notification dedup_key = `pos.shift_variance:{session_id}` chống re-fire. Reference: `supabase/migrations/20260504000000_pos_close_shift_no_block_alert_d8.sql` + `apps/web/app/br/[branchId]/pos/{close-session-sheet,session-actions}.ts`. **Supersedes** [2026-04-26] POS-CLOSE-SHIFT-PAID-FILTER-AND-VARIANCE-GATE phần variance gate.
- [2026-04-27] POS-CLOSE-SHIFT-CASH-ONLY-EXPECTED — `close_pos_session` MUST compute `expected_cash` as `opening_cash + SUM(total_amount) FILTER (WHERE payment_status='paid' AND payment_method='cash')`. NEVER cộng đơn `payment_method IN ('vietqr','momo','bank_transfer',...)` vào `expected_cash` — chuyển khoản settle vào tài khoản ngân hàng, KHÔNG chạm két vật lý. `closing_cash` là tiền mặt cashier đếm cuối ca → so sánh phải đối xứng (cash drawer vs cash drawer). Symptom của bug: cashier đóng ca → "lệch tiền" báo thiếu đúng bằng tổng tiền chuyển khoản trong ca → `cash_difference` âm vượt ngưỡng → block close, hoặc cần BM duyệt variance vô lý. Mô hình đúng: `cash_drawer_close = opening + cash_paid_in − cash_paid_out`; tiền VietQR/MoMo paid không phải `cash_paid_in`. RPC trả về thêm `cash_revenue` + `noncash_revenue` riêng trong JSONB result để phiếu chốt ca + UI hiển thị tách bạch (chuyển khoản là doanh thu, không phải tồn quỹ). NULL `payment_method` được coi như non-cash (conservative — paid orders sau backfill `20260428200000_fix_confirm_cash_payment_method_swap.sql` luôn có method, NULL chỉ còn ở edge case orphan); nếu cashier có complaint về NULL bị bỏ thì backfill từ `payments.method` chứ KHÔNG nới lỏng filter. Owner decision D8 (2026-04-27, hotfix to D1): expected_cash = cash drawer expected, không phải total revenue. Caught: cashier production block khi đóng ca có đơn VietQR/MoMo on 2026-04-27. Reference: `supabase/migrations/20260503000000_pos_close_shift_cash_only_expected.sql`. **Supersedes** the SUM-filter clause of [2026-04-26] POS-CLOSE-SHIFT-PAID-FILTER-AND-VARIANCE-GATE; the old D3 variance gate is superseded by [2026-04-27] POS-CLOSE-SHIFT-NO-BLOCK-ALERT.
- [2026-04-26] NO-RAW-TAILWIND-PALETTE-IN-APP — Outside `packages/ui/src/styles/*.css`, code MUST NOT use Tailwind palette classes: `(bg|text|border|ring|fill|stroke|from|to|via|outline|decoration|divide|placeholder|caret|accent)-(slate|gray|zinc|neutral|stone|red|orange|amber|yellow|lime|green|emerald|teal|cyan|sky|blue|indigo|violet|purple|fuchsia|pink|rose)-\d{2,3}`. Use semantic tokens first (`bg-success/10`, `text-warning`, `border-destructive`, `bg-tier-elite/10`); generated `matu-*` utilities are app-wide brand-token escape hatches only when semantic classes are not expressive enough. Reasons: (1) palette classes bypass theme tokens so a preset bump cannot re-skin the app; (2) palette classes lack `dark:` variants so dark mode breaks (`bg-yellow-100 text-yellow-900` keeps yellow-on-yellow on dark surfaces). Tier badges that need >4 hues use Zone B tokens (`--tier-elite`, `--tier-note`) — never invent a new Tailwind palette inline. Catalog of semantic mappings: `success` (Trusted/zone-held/low-tier confirm), `warning` (variance T1/waste T1/low-stock/zone-blocked), `info` (Bootstrap/zone-acquiring), `destructive` (At-risk/variance T3/negative-stock/zone-lost), `tier-elite` (Trust Elite tier — purple), `tier-note` (variance T2/waste T2/out-of-stock — orange-mid), `muted` (idle/inactive/Bootstrap-low). Caught: 256 violations across 30+ files in `apps/web/` prior to the first shadcn preset alignment 2026-04-26 — refactor scheduled as PR-2 follow-up. ESLint guard scheduled as PR-3 follow-up. Reference: 4-agent debate synthesis 2026-04-26; app-wide matu-superapp baseline 2026-05-09.
- [2026-04-27] POS-SESSION-SCOPE-PER-BRANCH — `fetchActiveSession` query bằng `(tenant_id, branch_id, status='open')` — DB enforce `UNIQUE INDEX idx_pos_sessions_one_open_per_branch ON pos_sessions(branch_id) WHERE status='open'` (migration `20260426181829_pos_session_per_branch.sql`). Branch chỉ tồn tại 1 ca POS active cùng lúc (Owner D7, 2026-04-27) — quán Cơm Tấm 1 cashbox vật lý/branch. Cashier mở → mọi role POS (cashier/waiter/branch_manager+) cùng branch ride session đó. `MultiSessionPicker`, `?terminal=` URL param, `fetchActiveSessionsForBranch` đã retired (xoá file/code). Order attribution: `orders.created_by` = ai ring đơn (waiter/cashier), `pos_sessions.opened_by` = cashier-of-record (chịu trách nhiệm két). `pos_sessions.terminal_id` nullable từ D7 — NULL = ca chung của chi nhánh, không liên kết terminal vật lý cụ thể; FK `ON DELETE SET NULL` để admin retire pos_terminals cũ không khoá history; closed sessions trước D7 vẫn giữ terminal_id cho audit. INSERT 2nd open session same branch → `23505` từ unique partial index → UI message "Chi nhánh này đã có ca POS đang mở. Tải lại trang để vào ca hiện tại, hoặc đóng ca cũ trước khi mở ca mới.". Realtime: `pos_sessions` được publish trong `supabase_realtime` với `REPLICA IDENTITY FULL` (migration `20260426181830_pos_sessions_realtime.sql`) — `pos-desktop-shell.tsx` subscribe `UPDATE` filter `branch_id=eq.{branchId}` qua `useRealtimeChannel`, nhận `new.status='closed'` → toast warning + `router.refresh()` để rerender SessionGate / "liên hệ thu ngân"; status callback skip lần SUBSCRIBED đầu (state đã seed RSC), reconnect refresh-defensive. NEVER re-introduce `UNIQUE(terminal_id)` partial index — multi-cashier-per-branch tách két là feature out-of-scope (cần schema `pos_session_cashiers` 1-N + opening_cash split + handover RPC). NEVER filter `opened_by = user.id` trong `fetchActiveSession` — chặn waiter ride cashier session. **Supersedes** [2026-04-25] POS-SESSION-SCOPE-PER-TERMINAL-NOT-PER-USER (per-terminal model retired). Reference: `supabase/migrations/20260426181829_pos_session_per_branch.sql` + `supabase/migrations/20260426181830_pos_sessions_realtime.sql` + `apps/web/app/br/[branchId]/pos/{session-actions.ts,page.tsx,session-gate.tsx,pos-desktop-shell.tsx}`.
- [2026-04-27] POS-REDUCE-QTY-VIA-RPC-RECOMPUTE-DISCOUNT — Giảm số lượng món đã gửi PHẢI đi qua RPC `reduce_order_item_quantity(p_order_item_id, p_new_quantity, p_reason)` (migration `20260427074752_pos_reduce_order_item_qty.sql`). RPC update `order_items.quantity + subtotal`, BẮT BUỘC gọi `compute_discount_amount(o.discount_type, o.discount_value, new_subtotal)` rồi UPDATE `orders.subtotal/discount_amount/total_amount` cùng lúc — tuyệt đối không tự inline math (rule POS-DISCOUNT-RECOMPUTE-VIA-HELPER). RPC bump `kds_tickets.updated_at` của ticket non-terminal vì `order_items` không nằm trong `supabase_realtime` publication theo thiết kế (xem `20260428000000_pos_realtime_publication.sql:12-14`); nếu skip bump, KDS card render stale qty cũ cho đến khi chef refresh. Server action `reduceOrderItemQuantity` gate bằng `pos:void_order` (reuse, không tạo perm mới — cùng nhóm destructive privilege) và sau khi RPC commit thì gọi `enqueue_partial_cancel_ticket_print` để in phiếu báo bếp giảm SL khi `was_sent_to_kitchen=true`; print failure NON-FATAL (mirror void flow), surface qua `printWarning` toast — không bao giờ rollback reduce. **Phiếu in dùng `payload.kind='cancel_ticket'`** (không phải `'partial_cancel'` — print-agent chỉ switch 5 kind đã biết: `kitchen_ticket | provisional_bill | receipt | cancel_ticket | shift_close_report` ở `apps/print-agent/src/escpos.ts:898-910`; kind không khớp → `renderPayload` trả undefined → agent crash); reuse template HUỶ MÓN có sẵn, item.quantity = số phần BỊ GIẢM (delta), reason được prefix `"GIẢM SL N → M: <reason>"` để chef phân biệt với phiếu huỷ-toàn-phần (huỷ-hết render quantity=qty đầy đủ + reason không có prefix). Khi nào print-agent hỗ trợ kind mới (banner "GIẢM SỐ LƯỢNG" thay vì "HUỶ MÓN" + dòng "Còn lại: M phần") thì tách lại payload.kind — nhưng phải rebuild + redeploy agent ở mọi chi nhánh trước. Idempotency key `'order:{id}:reduce:{item_id}:{old}->{new}'` cho phép giảm nhiều lần (5→3 rồi 3→1) mà vẫn dedup double-click cùng delta. UI guard: `OrderItemActionsSheet` ẩn nút "Giảm số lượng" khi `quantity < 2` (force "Hủy món" để KDS card flip cancelled, tránh zombie qty=0 row); dialog stepper clamp `[1, qty-1]` client + RPC raise `'no reduction needed'` (22023) nếu newQty>=oldQty. Reason ≥5 ký tự (mirror `voidItemSchema`) — single-char "x" defeats audit. Block khi `payment_status='paid'`: đơn đã thanh toán = invoice locked; sửa SL sau paid sẽ rewrite revenue silently. Feature flag `pos_reduce_qty_enabled` (default true) là kill switch riêng — nếu chef phản đối flow này, owner tắt cờ mà không ảnh hưởng `pos_cancel_ticket_enabled`. Reference: `apps/web/app/br/[branchId]/pos/order-actions.ts#reduceOrderItemQuantity` + `apps/web/app/br/[branchId]/pos/_components/order-detail/reduce-quantity-dialog.tsx` + `supabase/migrations/20260427074752_pos_reduce_order_item_qty.sql`.
- [2026-04-27] NO-CLAMP-ON-KITCHEN-NOTES — KDS `item.note` (chef instruction chip in `ticket-row-meta.tsx:53`) MUST NEVER use `truncate` / `line-clamp-*` / `overflow:hidden`. Allowed: `min-w-0 break-words` only — let the ticket card grow vertically. Why: cắt một mảnh "không ớt" / "không đậu phộng" → đầu bếp ra món sai → khách dị ứng → liability event. Same rule applies to `item.item_name` `note` slot of any future kitchen-facing surface; clamp-2 is OK on `item.item_name` itself (SKU label, không phải lệnh phục vụ). KDS ticket grid must be `auto-rows-min` or equivalent so a tall note doesn't crop. When adding new note-like fields to KDS tickets (allergy tag, special prep flag), default to no-clamp + tone="warning" via `<NoteCallout>` if a chip style is needed. UI grep gate: `rg "truncate|line-clamp" apps/web/app/br/\[branchId\]/kds` MUST return at most ONE match (the existing `line-clamp-2` on `item.item_name` in `ticket-row.tsx`); any additional match on note / modifier / side / variant fields is a regression. Reference: `apps/web/app/br/[branchId]/kds/components/ticket-row-meta.tsx` + 4-agent debate synthesis 2026-04-27.
- [2026-04-27] NO-CLAMP-ON-RECEIPT-LEGAL-FIELDS — POS bill receipt 80mm (`bill-receipt-summary.tsx`, container `#pos-receipt`) MUST NEVER apply `truncate` / `line-clamp-*` / `overflow:hidden` / `max-width` to legal-required fields: `branches.address` (e-invoice cross-check / merchant identity), `order.note` (customer instruction / service contract), `branches.name`, `order_number`. Allowed: `break-words` only. `globals.css @media print` block neutralizes any inherited clamp/overflow/max-w under `#pos-receipt *` defensively — never remove the print reset block. Why: silently cut "Phường 8, Quận Phú Nhuận" to "Phường 8, …" → tax audit mismatch with HĐĐT provider payload; cut "không hành" → khách dị ứng nhận sai món + service failure. When adding new printed fields (e.g. tax invoice link, loyalty code, table number), default to no-clamp; if width pressure exists at preview stage, shorten the label not the value. UI grep gate: `rg "truncate|line-clamp" apps/web/app/br/\[branchId\]/pos/_components/bill` MUST return zero. Reference: `apps/web/app/br/[branchId]/pos/_components/bill/bill-receipt-summary.tsx` + `packages/ui/src/styles/globals.css` (@media print block) + 4-agent debate synthesis 2026-04-27.
- [2026-04-27] NO-TITLE-ATTR-OR-TOOLTIP-ON-POS-TOUCH — POS / KDS surfaces (under `apps/web/app/br/[branchId]/{pos,kds}`) MUST NOT depend on the HTML `title=""` attribute or shadcn `<Tooltip>` to reveal truncated content. PWA touch devices have no hover; `title` does nothing on tap; `<Tooltip>` opens on long-press (~500ms) which is unusable in cash-rush. Required reveal mechanisms: (a) always-visible (preferred for cash-path: KDS notes, receipt, bill summary), (b) `<NoteCallout>` framed block with full text, (c) tap-to-expand sheet/drawer, (d) multi-line layout (used in `permissions-client.tsx` SelectItem). **Carve-out:** `title` attributes that mirror already-visible text (icon-button labels, page headings, line items where the title duplicates inline text) are fine — they aid screen readers and desktop hover without being the only path to information. The rule targets `title` as a _reveal mechanism for content otherwise truncated/hidden on touch_. Admin-desktop-only surfaces (`apps/web/app/admin/*`, `apps/web/app/finance/*` table cells) MAY use `truncate + title` because hover is reliable on mouse. When in doubt, prefer `line-clamp-2 + break-words` over truncate-with-title even on admin — keeps fallback decent on tablet. Why: `branch-table.tsx` cashier-mobile copy of admin would lose address visibility; KDS staff cannot scan a tooltip during rush. Reference: 4-agent debate synthesis 2026-04-27 (QA finding §2 MAJOR touch accessibility) + code-reviewer follow-up 2026-04-27.
- [2026-04-27] POS-NETWORK-GATE-NEVER-TRUST-XFF-FIRST-HOP — Resolving a request's client IP for the POS/KDS network gate (or for any security decision) MUST go through `apps/web/lib/network/client-ip.ts#getClientIp`. NEVER read `request.headers.get("x-forwarded-for")?.split(",")[0]` directly — the leftmost XFF entry is the **client-claimed** value and is trivially spoofable by injecting an `X-Forwarded-For` request header. On Vercel the trusted source is `x-real-ip` (set from the TCP-layer source); fallback to the LAST entry of `x-forwarded-for` (most-trusted hop). Helper rejects RFC1918 private ranges (10/8, 127/8, 169.254/16, 172.16–31, 192.168/16, ::1, fe80::/10, fc00::/7) — a private IP arriving from a public proxy means misconfiguration; fail closed. NEVER read the IP from JWT `app_metadata` (would freeze it for the JWT TTL) or from a request body parameter (forgeable). Existing naive readers at `apps/web/app/api/webhooks/momo/route.ts:29`, `apps/web/app/api/auth/signout/route.ts:10`, `apps/web/app/(auth)/login/actions.ts:46` are rate-limit keys (lower stakes); migrate them to the helper opportunistically but the gate at `apps/web/proxy.ts` MUST always use it. Reference: `apps/web/lib/network/client-ip.ts` + 4-agent debate synthesis 2026-04-27.
- [2026-04-27] POS-NETWORK-GATE-PROXY-ONLY-NOT-RLS — The POS/KDS network gate (`branch_trusted_egress_ips` lookup in `apps/web/proxy.ts`) lives in middleware **only**. PostgREST / Supabase REST calls (anything the client makes via `supabase-js` after the page loads) bypass it — RLS + JWT remain the source of truth for data access. Therefore: (1) NEVER document this gate as "POS data is locked to branch wifi" — it locks the **PWA shell**, not the data; (2) any new surface that exposes branch data outside proxy (Edge Functions, Webhooks, direct REST endpoints, Server Actions on non-`/br/*` routes) MUST add its own independent IP check or be exposed only behind the proxy; (3) feature description in security narrative reads "defense-in-depth perimeter against casual cross-network access", not "authorization control". A future Phase-2 hardening (LAN cookie 2FA via native POS app, or RLS-level header passthrough via `current_setting('request.headers')`) can upgrade this to a true authz layer; until then, the proxy gate is one layer of many. Reference: `apps/web/proxy.ts` + 4-agent debate synthesis 2026-04-27.
- [2026-04-27] POS-NETWORK-GATE-AGENT-PRESENCE-TOKEN — Print-agent's IP registration (`apps/print-agent/src/index.ts#registerPresence` → `apps/web/app/api/branch-presence/route.ts`) MUST authenticate via the shared `PRINT_AGENT_PRESENCE_TOKEN` bearer (set on BOTH the agent's `.env` AND the web app's runtime env) — NOT via the Supabase service-role key. Token verification uses constant-time string compare (`timingSafeEquals` in the route file); never use `===` to compare auth secrets (timing leak). The IP itself is read SERVER-SIDE from the request via `getClientIp` — agents do NOT pass an `ip` field in the body. Any future endpoint that lets a caller assert their own IP for security purposes is a forgery vector regardless of authentication. When rotating the presence token, update agent `.env` first (push to all branches), then web env, then verify rows in `branch_trusted_egress_ips` are still being upserted (look for `last_seen_at` advancing); old token continues working briefly because of fail-open behaviour on `unauthorized` (agent retries every 5 min). Reference: `apps/web/app/api/branch-presence/route.ts` + `apps/print-agent/src/index.ts` + 4-agent debate synthesis 2026-04-27.
- [2026-04-28] HDDT-CANCEL-REASON-MIN-20 — `cancelTaxInvoice` (`apps/web/app/finance/actions.ts`) `reason` parameter is REQUIRED + ≥20 chars after trim — never `.optional()`, never substituted with a placeholder like `"Hủy theo yêu cầu"` (15 chars, useless to a tax auditor). NĐ70/2025 (and prior 13/2023) require every HĐĐT cancellation to carry a real, descriptive reason. Schema `z.string().trim().min(20).max(500)`. UI MUST collect via Textarea with live character counter (mirrors POS variance-note pattern); submit button disabled while invalid (`reasonValid` derived in `invoice-list.tsx`). Reason is propagated into `transition_tax_invoice_state(p_payload={cancel_reason}, p_note=cancelReason)` so provider cancellation payload + audit ledger both carry the actual text. When wiring real Viettel S-invoice provider: do NOT pre-fill the cancel reason from a fixed phrase even if the provider accepts it — propagate the user-typed string verbatim, that string is the legal record. Reference: `apps/web/app/finance/{actions.ts,invoice-list.tsx}` + `supabase/migrations/20260415000000_tax_invoice_state_machine.sql`.
- [2026-04-28] AUDIT-LOG-SELECT-EXPLICIT-COLUMNS — `fetchAuditLogs` and any other reader of `audit_logs` MUST use an explicit column list, NEVER `.select("*")`. Required-only columns: `id, action, entity_type, entity_id, user_id, created_at`. NEVER include `ip_address` (request-level PII) or raw `old_data`/`new_data` blobs (buyer addresses, MST, salaries depending on entity_type) in a list view. If a deeper audit drill-down needs diffs, expose a separate per-row RPC gated on a specific permission key (e.g. `audit:view_diff`) and log the access. The `audit_logs_select` policy already restricts SELECT to owner/super_manager — this rule is the second-line defense against accidental over-fetch in shared dashboards. When adding a new column to `audit_logs` (e.g. user_agent), default to NOT including it in the list selector; opt-in per use case. Reference: `apps/web/app/finance/actions.ts#fetchAuditLogs`.
- [2026-04-28] AUDIT-LOG-INSERT-RPC-ONLY — `audit_logs` rows MUST only be created via `public.log_audit(p_action, p_entity_type, p_entity_id, p_old, p_new)` SECURITY DEFINER RPC. Direct INSERT from app code (`supabase.from("audit_logs").insert(...)`) is REVOKED at GRANT level (`REVOKE INSERT ON public.audit_logs FROM authenticated`) and the broad `audit_logs_insert` policy is dropped. RPC forces `tenant_id = auth_tenant_id()` and `user_id = auth.uid()` server-side — callers cannot lie about who they are or which tenant they belong to. Caught: M6 security review 2026-04-27 — any authenticated user could `POST /rest/v1/audit_logs` with arbitrary action/entity_type/user_id to forge or smear audit history (e.g. fake "owner cancelled invoice 123"). Helper `apps/web/app/admin/_lib/audit.ts#logAudit` already wraps the RPC; callers pass `{action, entityType, entityId, oldData?, newData?}` — NEVER pass `tenantId`/`userId` (forced server-side). Anonymous callers raise `insufficient_privilege` (auth.uid() NULL); missing tenant claim raises same. When triggering audit from a Postgres trigger or another SECURITY DEFINER function, write directly to `audit_logs` from inside that function (definer privilege bypasses the GRANT) — RPC-only rule applies to APP code, not in-database triggers. Reference: `supabase/migrations/20260505020000_audit_logs_rpc_only_insert.sql` + `apps/web/app/admin/_lib/audit.ts`.
- [2026-04-27] POS-NETWORK-GATE-GRACE-IS-SECURITY-CEILING — `branch_trusted_egress_ips.last_seen_at` 30-minute grace window in `apps/web/proxy.ts` is the **security ceiling**, not a recovery buffer. Within 30 min of an agent's last successful registration, the IP remains trusted even if the agent has died, the LAN has been seized by another tenant, or the laptop running the agent has been moved off-site. Acceptable for a single-tenant pilot quán cơm tấm; explicitly NOT acceptable for multi-tenant SaaS or branches in CGNAT-shared neighborhoods (rule POS-NETWORK-GATE-PROXY-ONLY-NOT-RLS already flags CGNAT as a known limitation). When tightening: shorten the cutoff at `proxy.ts` (look for `30 * 60_000`); when loosening for an incident, prefer flipping `POS_NETWORK_GATE=off` env (kill-switch) over extending the grace — kill-switch is loud (per-cold-start `console.warn` emitted by `apps/web/proxy.ts` when `NODE_ENV=production AND POS_NETWORK_GATE=off`, format `[network-gate] disabled via POS_NETWORK_GATE=off — POS/KDS perimeter open. See regressions.md POS-NETWORK-GATE-GRACE-IS-SECURITY-CEILING.`, deduped via module-level `NETWORK_GATE_OFF_WARNED` flag — fires once per warm Edge instance, captured by Vercel log drain) while a quietly-extended grace is invisible. Future hardening: shorter grace + auto-revoke (cron job marking rows `revoked_at = now()` when `last_seen_at < now() - interval '4 hours'`) so a forgotten dev branch can't keep accepting connections forever. Reference: `apps/web/proxy.ts` + `supabase/migrations/20260428110000_branch_trusted_egress_ips.sql`.
- [2026-04-28] POS-DISCOUNT-CLEAR-METADATA-WHEN-AMOUNT-ZERO — Any RPC that drives `orders.discount_amount` to 0 MUST also set `discount_type/value/note = NULL` in the same UPDATE. The CHECK constraint `orders_discount_metadata_paired` (added 20260430000000_pos_discount.sql) demands the 4 fields move together. `clear_order_discount` honors this; the bug was that `merge_orders` (source side, items moved out → subtotal=0), `cancel_order`, `void_order_item` (auto-cancel branch + pct-FLOOR-edge), and `split_order` (pct rate × tiny remaining subtotal → FLOOR=0) silently set `discount_amount=0` while leaving the other 3 columns populated — every merge of a vnd-discounted source raised "Dữ liệu chiết khấu không nhất quán" and the action never landed; cancel/void of any discounted order had the same fate. When adding a new write path that may zero out `discount_amount`: either copy the `clear_order_discount` UPDATE shape (NULL block) when the zero is unconditional, or use `CASE WHEN v_discount = 0 THEN NULL ELSE col END` per-column when zero is one branch of a recompute. Audit trail for the prior discount lives in `order_status_history` rows (`discount_applied`/`discount_cleared`/`split_to`/`merged_into`), not in the orders columns — so clearing on collapse loses no business info. Reference: `supabase/migrations/20260430030000_pos_clear_discount_metadata_when_amount_zero.sql` + helper at `compute_discount_amount(type,value,subtotal)`.
- [2026-04-28] PERIOD-CLOSE-GUARD-ALL-MUTATIONS — Any mutation that creates/voids GL impact (`voidJournalEntry`, `cancelTaxInvoice` with cross-period reversal, future `bank_rec_match`, `expense_reverse`, `depreciation_post`, `owner_draw_reverse`, `apply_grn_amend_post_close`) MUST check `is_period_closed(branch_id, posting_date)` at the **DB trigger or RPC level**, NEVER only at the action layer. The poster path `auto_post_journal` already enforces this via `fiscal_periods` row check (FOR SHARE) at line 49-72 of `supabase/migrations/20260419210000_gl_period_enforcement.sql`. The void/cancel path lacks symmetric enforcement: today `voidJournalEntry` (apps/web/app/finance/journal-actions.ts:224) flips status without re-checking that the entry's posting date period is still open. When period status='closed', the only legitimate change is a NEW reversing entry in an OPEN period — NEVER mutate a posted entry retroactively. Spec contract for any new GL-affecting RPC: read `posting_date`, derive `(year, month)`, `SELECT status FROM fiscal_periods WHERE ... FOR SHARE`; if `closed` → `RAISE EXCEPTION 'period_closed_cannot_mutate'` ERRCODE 23514. The `closing` status permits adjusting entries (intentional). Reference: `supabase/migrations/20260419210000_gl_period_enforcement.sql` + 4-agent debate Finance enhancement 2026-04-28.
- [2026-04-28] CASHFLOW-CLASSIFY-USES-COA-FLAG — B03-DN Báo cáo lưu chuyển tiền tệ (Cashflow Statement) classifies journal lines into operating/investing/financing sections via `chart_of_accounts.cashflow_section` enum column — NEVER hardcode account number ranges (e.g. `account_code LIKE '21%'` for investing) in report queries. The flag values: `operating` (default for working capital, revenue, expense, A/R, A/P, payroll), `investing` (FA acquisition/disposal: 211, 213, 214, 217, 221, 228), `financing` (equity changes, long-term debt: 411, 414, 421, 341, 342), `none` (clearing accounts that don't represent cash movement: 911, 1381). When seeding new VAS accounts, MUST set `cashflow_section` at insert time. When user adds custom sub-account, default `cashflow_section` from parent. Reason: hardcoded ranges break when chart of accounts is customized; flag in COA is the canonical source-of-truth for both the indirect and direct method, and lets BCTC report code stay declarative (filter `WHERE cashflow_section = 'investing'`) instead of pattern-matching account codes scattered across migrations. Reference: `supabase/migrations/20260416020000_finance_chart_of_accounts.sql` + 4-agent debate Finance enhancement 2026-04-28.
- [2026-04-28] BANK-REC-MATCH-IDEMPOTENT — `match_bank_statement_line(bank_line_id, target_kind, target_id)` RPC MUST be idempotent: re-calling with the same triple → no-op (return existing match row), NEVER raise. Implementation: `SELECT ... FOR UPDATE` on the bank_statement_line row, check if `match_status='matched'` and existing target matches the new target → return existing match_id; mismatch → `RAISE EXCEPTION 'bank_line_already_matched_to_other'`. Phase 1 pilot is **manual 1-1 match only** — fuzzy matching (amount±1k, date±3day window, ML string-distance) is DEFERRED to Phase 2. NEVER let UI auto-commit fuzzy matches; require explicit human confirm. Reason: 1 bank line silently matching 2 expenses (or 1 expense matched twice) corrupts cash-flow tie-out and forces manual unwind. Concurrent match-from-two-tabs MUST surface to second tab as conflict, not silent overwrite. Reference: 4-agent debate Finance enhancement 2026-04-28 (QA risk R5).
- [2026-04-28] FA-DEPRECIATION-IDEMPOTENT-PER-PERIOD — Fixed asset depreciation posting (`fn_post_depreciation(period_year, period_month)` cron RPC) MUST be idempotent per `(fixed_asset_id, period_id)`. Schema: `asset_depreciation_schedule` has `UNIQUE(fixed_asset_id, period_id)`; `posted_journal_entry_id` nullable until posted. RPC pattern: `INSERT ... ON CONFLICT (fixed_asset_id, period_id) DO UPDATE SET posted_journal_entry_id = EXCLUDED.posted_journal_entry_id WHERE asset_depreciation_schedule.posted_journal_entry_id IS NULL` — only fills empty slot. Cron crash mid-loop + restart MUST NOT double-post the same period (no second JE Dr 6427/Cr 214). When recomputing schedule (asset cost change, useful life change, impairment), DELETE only future un-posted rows; NEVER touch rows where `posted_journal_entry_id IS NOT NULL` — voiding a posted depr requires explicit reversing entry per rule PERIOD-CLOSE-GUARD-ALL-MUTATIONS. Reference: 4-agent debate Finance enhancement 2026-04-28 (QA risk R6).
- [2026-04-28] JE-LINE-WRITE-LOCKS-HEADER — Any code path that INSERTs/UPDATEs `journal_entry_lines` MUST first acquire `SELECT ... FOR UPDATE` on the parent `journal_entries` row in the same transaction. Without it, two concurrent reverses of the same entry (e.g. user double-clicks "Đảo bút toán" + auto-trigger from cancellation flow) both see `status='posted'`, both pass the precondition, both insert reversing lines → entry header status flips twice → debit/credit total invariant breaks → trial balance mismatched without surface error. The header status check + lines write must be a single critical section. Same pattern for `voidJournalEntry`: lock header FIRST, then validate, then write reverse lines, then UPDATE header status='voided'. NEVER use `SELECT status FROM journal_entries WHERE id=?` without `FOR UPDATE` and then INSERT lines based on the result. Reference: 4-agent debate Finance enhancement 2026-04-28 (QA hidden risk #4).
- [2026-04-28] VAT-PER-LINE-NOT-PER-INVOICE — VAT amount and rate MUST be stored and recomputed PER LINE on `tax_invoices.line_items` (and on `order_items` for upstream POS), NEVER recomputed at invoice level via `total_amount / (1 + invoice_vat_rate)`. F&B mixed-VAT order is the standard case: a single đơn has cơm tấm (8% food rate per Nghị định 15/2022 chính sách giảm thuế) + bia/nước ngọt (10%) + sữa tươi đóng hộp (8%); inferring a single invoice-level VAT rate ghi sai từ tháng đầu tiên. Schema implication: `tax_invoices` keeps `vat_amount` and `subtotal` as DERIVED sums of line VAT, not as stored facts; `tax_invoices.vat_rate` is REMOVED or kept only as a UI display hint that warns "đơn có nhiều mức thuế" when lines disagree. POS `order_items` MUST already carry `vat_rate` (per item, snapshotted at order time from `menu_items.vat_rate`). When generating HĐĐT XML/payload for Viettel S-invoice (or any provider), emit per-line `<thueSuat>` and per-line `<tienThue>` elements; the invoice header `<tongTienThue>` is sum, not factor. Roundtrip rule: SUM(line.amount × line.vat_rate) — line-rounded — equals invoice.vat_amount; reconcile to NUMERIC(15,2) per line, NEVER round at invoice level. Reference: 4-agent debate Finance enhancement 2026-04-28 (BA business rule #7).
- [2026-04-28] PERIOD-FILTER-USES-LOCAL-TZ — Any query that filters journal entries / orders / payments by accounting period boundary MUST apply `AT TIME ZONE 'Asia/Ho_Chi_Minh'` before extracting month/year, NEVER use raw UTC `created_at` or `posting_date` directly. Postgres `TIMESTAMPTZ` stores UTC; a JE created at `2026-04-30T17:30:00+07:00` is stored as `10:30:00Z` — `EXTRACT(MONTH FROM created_at)` returns 4 (April UTC) but business-wise it's 30 April 23:30 local = still April. Edge case is when a transaction crosses midnight: `2026-04-30T18:30:00Z` = `01:30:00 +07` next day = May business-day. Without TZ adjustment, last 7 hours of every business day get classified into wrong calendar month near boundaries → BCTC tháng 4 thiếu/dư mất kiểm soát. Pattern: `EXTRACT(MONTH FROM (created_at AT TIME ZONE 'Asia/Ho_Chi_Minh'))` and `(created_at AT TIME ZONE 'Asia/Ho_Chi_Minh')::date BETWEEN period.start AND period.end`. Helper view `gl_entry_local_calendar` (year, month, business_date computed in Asia/Ho_Chi_Minh) recommended; report queries JOIN this view rather than recomputing. Inventory shift_key already follows this pattern (rule SHIFT-KEY-CANONICAL); finance reports MUST adopt same. Reference: 4-agent debate Finance enhancement 2026-04-28 (QA hidden risk #3).
- [2026-04-28] OWNER-DRAW-SUPER-MANAGER-ONLY — Owner draws / equity contribution mutations (`recordOwnerDraw`, `recordCapitalContribution`) MUST gate by `user_role IN ('owner','super_manager')` at THREE layers: (1) module-acl route gate at `packages/shared/src/auth/module-acl.ts`; (2) RLS policy `owner_draws_manage` on the underlying table USING + WITH CHECK; (3) Server Action `getAuthContextWithPermission(['owner','super_manager'], FINANCE_OWNER_DRAW)` at the action boundary. NEVER use `branch_manager` or below — branch managers controlling equity transactions = owner-money-leak vector. Posting rule for OWNER_DRAW_CASH: Dr 4111 (Vốn đầu tư của chủ sở hữu — withdrawal subaccount) / Cr 1111 (Tiền Việt Nam) — NEVER post to expense accounts (6xx, 8xx). Owner draws are NOT business expenses; treating them as such overstates COGS/OPEX and understates equity. Audit trail required: every owner draw posts to `audit_logs` via `log_audit()` RPC with full before/after equity balance snapshot. Reference: 4-agent debate Finance enhancement 2026-04-28 (BA business rule #5 + QA risk R7).
- [2026-04-28] SEGMENT-PNL-INVARIANT — When adding `branch_id`-aware journal entries (inter-branch transfer, branch-level OPEX, payroll allocation), the consolidated tenant P&L MUST equal SUM of segment (per-branch) P&L for any period — invariant `sum(segment_pnl WHERE period_id=X) = consolidated_pnl WHERE period_id=X`. Inter-branch transfers are the trap: a transfer JE Dr CN-A 1368 / Cr CN-B 1368 has 2 branch_ids; naive `GROUP BY branch_id` double-counts. Pattern: segment_pnl includes ONLY revenue (5xx) + expense (6xx, 8xx) accounts — NOT 1368/3368 (intra-tenant receivable/payable) which net to zero at consolidated level. Test gate: every new feature touching `journal_entries.branch_id` adds a regression test seeding 10 JE rows + asserting invariant holds. When `mv_segment_pnl` is added, MV refresh MUST recompute invariant in same transaction. Reference: 4-agent debate Finance enhancement 2026-04-28 (QA risk R11).
- [2026-04-28] EXPENSE-APPROVAL-RACE-LOCK — `approveOperatingExpense(expense_id)` RPC MUST `SELECT ... FROM operating_expenses WHERE id=? FOR UPDATE` before checking status='submitted' and inserting auto-JE. Without the lock, two concurrent approvals (owner + super_manager double-clicking) both pass `status='submitted'` precondition → 2 JEs posted → expense double-counted in P&L → trial balance off by amount. Idempotency key alternative: `UNIQUE(expense_id, posted_journal_entry_id) WHERE journal_entry_id IS NOT NULL` on a `expense_postings` audit table — second approve sees existing posting + returns success without re-posting. Same pattern applies to `submitOperatingExpense` (status='draft' → 'submitted') and `rejectOperatingExpense`. State machine: `draft → submitted → approved | rejected → posted` (terminal). When adding new state transitions (e.g. `requested_revision`), preserve the FOR UPDATE lock on every UPDATE statement. Reference: 4-agent debate Finance enhancement 2026-04-28 (QA risk R10).
- [2026-04-28] POSTING-RULES-CHECK-NOT-VALID — Adding new values to the `posting_rules.transaction_type` CHECK constraint (`gl_posting_rules.sql:14` currently enforces `IN ('sale','purchase','payroll','transfer','production')`) MUST use the two-step `ADD CONSTRAINT ... NOT VALID` + `VALIDATE CONSTRAINT` pattern, NEVER a single-step `DROP CONSTRAINT` + `ADD CONSTRAINT`. Single-step takes `ACCESS EXCLUSIVE` lock on the table for the duration of revalidation, blocking all readers/writers; on a busy tenant this stalls every server action that hits posting_rules during a multi-second migration apply. Two-step pattern: `ALTER TABLE posting_rules ADD CONSTRAINT posting_rules_transaction_type_check CHECK (...) NOT VALID;` then `ALTER TABLE posting_rules VALIDATE CONSTRAINT posting_rules_transaction_type_check;` — first statement takes brief `ACCESS EXCLUSIVE` (microseconds) for catalog change, second takes `SHARE UPDATE EXCLUSIVE` (allows concurrent reads/writes during validation). Same rule for any future CHECK constraint widening on a hot table. Reference: 4-agent debate Finance enhancement 2026-04-28 (Sr.Dev risk #2) + Postgres docs on ALTER TABLE locks.
- [2026-04-28] JE-VOID-REVERSING-ONLY — `voidJournalEntry` MUST create a NEW reversing journal_entry in the current OPEN period that mirrors the original lines with debit/credit swapped — NEVER UPDATE/DELETE the original posted entry's `journal_entry_lines`. Original entry's `status` flips `posted → voided` (header-only mutation, no line touches), gains `void_journal_entry_id` FK to the reversing entry, gains `void_reason` (≥20 chars per `JE-VOID-REASON-MIN-20`). Audit ledger captures the void via `log_audit()` RPC. Reason: VAS sequence-number continuity (Luật Kế toán 2015 Đ24) requires every posted entry to remain visible in sổ cái forever; deletion or in-place edit creates a gap → sổ không hợp lệ. The reversing entry IS the void's accounting representation, AND it gives auditor a paper trail to reconstruct decisions. NEVER allow voiding entries in CLOSED periods (rule PERIOD-CLOSE-GUARD-ALL-MUTATIONS); reversing entries land in current OPEN period regardless of original entry's posting date. Reference: 4-agent debate Finance enhancement 2026-04-28 (Sr.Dev decision #12) + Luật Kế toán 2015 Đ24.
- [2026-04-28] MV-FINANCE-CONCURRENTLY-INDEX — Every new finance materialized view (`mv_segment_pnl`, future `mv_ar_aging`, `mv_budget_variance`) MUST be created with a UNIQUE INDEX on the natural key BEFORE it is registered with `refresh_finance_views()` cron, so `REFRESH MATERIALIZED VIEW CONCURRENTLY` is supported. CONCURRENTLY uses the unique index to compute row-level diffs without locking readers; non-CONCURRENTLY refresh takes ACCESS EXCLUSIVE on the MV, blocking all dashboards for the refresh duration (seconds at our scale, minutes when MV grows). Pattern: `CREATE UNIQUE INDEX uq_mv_X_pk ON public.mv_X(natural_key_cols);` on the same migration that creates the MV. Existing rule MV-GROUP-BY-MUST-MATCH-UNIQUE-INDEX (2026-04-25) compounds: GROUP BY columns MUST be a SUBSET of the unique index columns; cosmetic columns (e.g. snapshot names) go in `MAX(...)` SELECT projection, not GROUP BY. When a new MV ships without a unique index, `refresh_finance_views()` cron silently fails on it (degrades only that MV, not the cron pipeline) — surface this in /finance MV-staleness banner. Reference: 4-agent debate Finance enhancement 2026-04-28 (Sr.Dev decision MV vs Raw) + supabase/migrations/20260425030256_fix_mv_top_items_food_cost_dedup.sql.
- [2026-05-01] DAILY-LIMIT-SIDES-MUST-COUNT — `enforce_branch_menu_daily_limit` and `decrement_branch_menu_daily_limit` MUST aggregate over both `NEW.menu_item_id` (or `OLD.menu_item_id` on cancel) AND every `sides[].side_item_id` from the JSONB column. Sides are stored as a JSONB snapshot on `order_items.sides`, NOT as separate rows — a trigger that only inspects the main item id silently lets disabled sides through and never increments quota for items sold as sides. The aggregation MUST GROUP BY `menu_item_id` (collapses duplicates and the "same item is both main and side" case into one lock + one increment of `qty_main * qty_side_per_main`) and ORDER BY `menu_item_id ASC` for deterministic lock ordering across concurrent inserts that share a side (prevents AB/BA deadlock). Side delta = `NEW.quantity (main qty) × COALESCE((s->>'quantity')::int, 1)`. Reference: supabase/migrations/20260514000000_branch_menu_daily_limit_sides.sql + 4-agent debate 2026-05-01.
- [2026-05-01] DAILY-LIMIT-SPLIT-NO-RECHARGE — `split_order` partial-clone INSERT into `order_items` MUST NOT re-charge daily quota: net qty across {source-after-decrement, clone} is unchanged from source-before, so re-incrementing `sold_today` in the trigger would over-count. The hatch is the transaction-local GUC `comtammatu.skip_quota_enforcement = 'true'` set via `set_config(name, 'true', true)` immediately before the clone INSERT and reset to `'false'` immediately after; the enforce trigger short-circuits at the top when the GUC is `'true'`. ANY future RPC that moves quota-counted rows around (re-parent, re-clone, etc.) without changing total demand MUST reuse this same hatch — never roll a per-RPC bypass column or per-RPC trigger SKIP clause, the GUC is the single contract. Full-move splits (UPDATE order_id only) are unaffected because no INSERT trigger fires on UPDATE. Reference: supabase/migrations/20260514000100_split_partial_skip_quota.sql + 4-agent debate 2026-05-01.
- [2026-05-01] DAILY-LIMIT-REDUCE-QTY-LEAK — `reduce_order_item_quantity` UPDATEs `order_items.quantity` without changing `status`, so neither `enforce_branch_menu_daily_limit` (AFTER INSERT) nor `decrement_branch_menu_daily_limit` (AFTER UPDATE OF status) fires. Reducing qty from N→M (M<N) does NOT release `(N-M) * (1 + sum(side_qty_per_main))` quota → manager dashboard shows items as "Hết suất" earlier than reality. Known gap, NOT addressed by 2026-05-01 fix (which closes only the INSERT-time sides path). Fix when prioritised: add AFTER UPDATE OF quantity trigger that computes delta `(OLD.quantity - NEW.quantity) * each-aggregated-item` and decrements via the same aggregation pattern as `decrement_branch_menu_daily_limit`. Reference: 4-agent debate 2026-05-01 (PM scope decision: deferred).
- [2026-05-01] REVENUE-BUCKET-BY-PAID-AT-LOCAL-TZ — Every revenue aggregate (MV, RPC, hand-rolled query) MUST bucket by `(payments.paid_at AT TIME ZONE 'Asia/Ho_Chi_Minh')::date` from a `payments` row with `status='completed' AND paid_at IS NOT NULL`, NEVER by `orders.created_at`. Doanh thu ngày X = đơn THU ngày X dù tạo đêm trước; bucket theo `created_at` UTC lệch 7h cuối ngày sang ngày VN sau, và đơn 23:50 paid 00:10 sẽ lệch ngày. Refunded payments (`status='refunded'`) phải bị loại khỏi gross revenue MV và đếm riêng làm "voided/refund" KPI — không được trộn vào doanh thu. Filter chuẩn ở mọi tầng: `o.status <> 'cancelled' AND o.payment_status = 'paid' AND p.status = 'completed' AND p.paid_at IS NOT NULL`. Caught by: 4-agent debate Finance reporting rebuild 2026-05-01 (BA + QA chốt fix luôn thay vì defer; tradeoff trong migration 20260512000000 đã được fix). Reference: supabase/migrations/20260514010000_revenue_paid_at_bucket_and_multi_branch_rollup.sql.
- [2026-05-07] RPC-STRUCTURED-FAILURE-DETAIL — RPC raise lỗi nghiệp vụ cần payload structured (multi-row shortage list, multi-error report, ...) MUST dùng `RAISE EXCEPTION 'short_message' USING ERRCODE = '...', DETAIL = jsonb_text` thay vì concat data vào message. Action handler đọc qua `error.details` (PostgreSQL DETAIL clause được supabase-js expose nguyên văn), parse bằng Zod schema, surface qua `ActionResult.meta` + `errorCode` ổn định để UI branch render Dialog/Sheet riêng. Lý do: (1) concat vào message gặp escape risk khi data chứa apostrophe/quote và làm UI phải parse substring fragile; (2) split message-vs-details cho phép log message ngắn vào telemetry mà vẫn payload đầy đủ cho client; (3) `errorCode` ổn định khi Vietnamese UI copy đổi (per ActionResult contract `packages/shared/src/types/action-result.ts:6-11`). Cap rows trong DETAIL (LIMIT 20 phổ biến) để tránh JSON quá lớn. Đầu tiên adopt: `confirm_production_order` shortage list (supabase/migrations/20260531030000_production_order_shortage_detail.sql:128-150). 4-agent debate 2026-05-07 (Architect + QA + BA: strict block, no force override, mobile parity bắt buộc).
- [2026-05-08] HDDT-BATCH-CRON-USES-LOCAL-TZ-BUCKET — Mọi aggregate query cho HĐ tổng hợp B2C (`aggregate_daily_b2c_invoice` RPC + future reconcile cron) MUST bucket order eligibility theo `(payments.paid_at AT TIME ZONE 'Asia/Ho_Chi_Minh')::date = p_summary_date` từ `payments` row với `status='completed' AND paid_at IS NOT NULL` — KHÔNG bucket theo `orders.created_at` hoặc `orders.paid_at` (column orders.paid_at không tồn tại). Compounds với existing `REVENUE-BUCKET-BY-PAID-AT-LOCAL-TZ` rule. Cron route handler (`apps/web/app/api/cron/hddt-daily-summary/route.ts`) compute `summary_date = getYesterdayInVietnam()` ONCE từ `Date.now() + VN_OFFSET_MS`, pass cùng giá trị cho TẤT CẢ branch loops — KHÔNG để mỗi RPC tự đọc `now()::date` (sai khi cron chạy qua midnight UTC bucket). Caught by: PR-2 smoke test 2026-05-08 (orders.paid_at column does not exist exception → fixed in 20260508055230_hddt_aggregate_rpc_fixes.sql). Detect: `grep -rnE "orders\.paid_at" apps/ packages/ supabase/migrations/20260508*` MUST 0; cron handler MUST hoist `summaryDate` outside per-branch loop.
- [2026-05-08] HDDT-BATCH-IDEMPOTENT-VIA-UNIQUE-CONSTRAINT — Daily summary HĐ idempotency enforced ở 3 lớp: (1) `pg_advisory_xact_lock(hashtext('hddt-b2c:' || branch || ':' || date)::bigint)` — blocks concurrent cron+manual race trong cùng tx; (2) `uq_tax_invoices_active_per_summary` partial UNIQUE `(tenant_id, branch_id, summary_date) WHERE invoice_kind='daily_summary' AND status NOT IN ('cancelled','replaced')` — DB-level idempotency fallback; (3) RPC short-circuit query existing active summary, return `{skipped: true, reason: 'already_exists', tax_invoice_id}` thay vì raise. Manual retry sau cancel = recreate được (UNIQUE excludes cancelled/replaced). KHÔNG dùng `pg_advisory_xact_lock(BIGINT, BIGINT)` 2-arg overload (Postgres không có signature đó — chỉ `(BIGINT)` hoặc `(INT, INT)`). Detect: `grep -rnE "pg_advisory_xact_lock\s*\([^)]*::BIGINT\s*,\s*[^)]*::BIGINT" supabase/migrations/` MUST 0; new aggregate RPC variants MUST use 1-arg hashtext pattern. Caught by: PR-2 smoke test 2026-05-08.
- [2026-05-08] HDDT-BATCH-RESERVE-DRAFT-BEFORE-PROVIDER-CALL — Aggregation RPC pattern: INSERT `tax_invoices` row at `status='draft'` + junction → COMMIT → caller (cron route hoặc admin server action) loads draft + calls Viettel S-invoice/provider externally → caller calls `transition_tax_invoice_state_as_system(.., 'signing')` → calls `transition_tax_invoice_state_as_system(.., 'issued'|'submitted'|'draft')` based on result. RPC MUST NOT call HTTP from inside Postgres (no `pg_net` or `http` extension) — 30s sync provider call inside SQL function blocks connection pool + locks transaction. UNIQUE `uq_tax_invoices_active_per_summary` claimed by draft INSERT prevents second concurrent runner from also calling provider. Detect: `grep -rnE "(pg_net\.http_post|http_post|extension\s+http)" supabase/migrations/2026050[0-9]*` related to HĐĐT → MUST 0; aggregation RPC body MUST NOT contain `pg_net` reference. Reference: `supabase/migrations/20260508055230_hddt_aggregate_rpc_fixes.sql` + `apps/web/lib/hddt-daily-summary.ts::executeSummaryRun`.
- [2026-05-08] HDDT-BATCH-NO-MISA-CALL-INSIDE-RPC — Historical rule name; read as "no provider HTTP call inside RPC". Same as HDDT-BATCH-RESERVE-DRAFT-BEFORE-PROVIDER-CALL but stated as a hard rule for review: any future "convenience" RPC that wraps "aggregate + provider call" in 1 SECURITY DEFINER function MUST be rejected. Provider HTTP belongs to Node runtime (cron route or server action) — RPC owns DB write transaction. The split lets us (a) retry provider calls without rolling back DB draft, (b) test provider mock independently of DB state, (c) avoid pg*net surface area entirely. Detect: any new SECURITY DEFINER function file matching `aggregate*_invoice_`or`_hddt_`whose body contains`http_post`, `http_get`, `pg_net`, or `dblink` MUST raise PR review block. Reference: 4-agent debate 2026-05-08 PR-2.
- [2026-05-08] HDDT-BATCH-SKIP-CONTINUE-PER-BRANCH — Cron loop trong `apps/web/app/api/cron/hddt-daily-summary/route.ts` MUST process 1 transaction per branch via separate `INSERT summary_run_queue` + `executeSummaryRun({queueId})` calls — KHÔNG wrap toàn bộ branch loop trong 1 try/catch và rollback all branches when 1 fails. `executeSummaryRun` helper internal try/catch updates queue row to `status='failed'` với `last_error`, RPC `aggregate_daily_b2c_invoice` returns `{skipped: true}` thay vì raise. Counter increments per branch outcome (`issued`/`submitted`/`skipped`/`failed`); response 207-style body summarizes. Failure of 1 branch (template not registered, provider timeout, ...) MUST NOT block other branches in same cron run. Detect: cron handler MUST have per-branch try/catch (not outer wrap); `pnpm lint` rule could enforce shape future. Reference: `apps/web/lib/hddt-daily-summary.ts:45-71` (try/catch boundary inside helper).
- [2026-05-08] HDDT-SUMMARY-CANCEL-PRESERVES-JUNCTION — `cancelTaxInvoice` action + `transition_tax_invoice_state_as_system(.., 'cancelled')` for `invoice_kind='daily_summary'` MUST NOT delete `tax_invoice_orders` junction rows. Audit trail preserves order→cancelled-HĐ link forever. Underlying orders become "re-eligible for next batch" automatically because `aggregate_daily_b2c_invoice` eligibility query checks `EXISTS junction JOIN tax_invoices WHERE status NOT IN ('cancelled','replaced')` — cancelled HĐ's junction rows are "inactive" but still queryable for forensics. Junction has `ON DELETE CASCADE` from `tax_invoices` only for HARD delete (which is itself blocked by `tax_invoice_events.tax_invoice_id` FK `ON DELETE RESTRICT`); status=cancelled is NOT a delete. Detect: any new code path that calls `DELETE FROM tax_invoice_orders` on cancel/replace flow MUST be rejected; cancel goes through state machine RPC only. Reference: `apps/web/app/finance/actions.ts::cancelTaxInvoice` + state machine matrix at migration 20260425035346 line 122-125.
- [2026-05-08] HDDT-BATCH-CRON-AUTH-VIA-BEARER-CRON-SECRET — `/api/cron/hddt-daily-summary/route.ts` MUST authenticate via Bearer `CRON_SECRET` (timing-safe compare via `node:crypto.timingSafeEqual` wrapped in fixed-length buffer) — same pattern as `feedback-daily-report/route.ts:56-64`. NEVER user JWT, NEVER unauthenticated. Vercel cron sends header `Authorization: Bearer ${env.CRON_SECRET}` automatically when configured in `vercel.json`. Cron MUST early-return `{ok:true, skipped:'feature_flag_off'}` when `HDDT_DAILY_SUMMARY_ENABLED !== 'true'` BEFORE running auth check (cheap reject for disabled deployments). Service-role client comes AFTER auth gate — never instantiate `createServiceClient()` in code path that bypasses Bearer check. Detect: `grep -rnE "createServiceClient" apps/web/app/api/cron/hddt-daily-summary/` MUST be wrapped after `timingSafeEquals` check; new HĐĐT cron variants MUST mirror feedback-daily-report's auth pattern. Reference: `apps/web/app/api/cron/hddt-daily-summary/route.ts:55-77`.
- [2026-05-08] HDDT-D4-NOT-REQUIRED-DEPRECATED-AFTER-BATCH — PILOT-ONLY / DEPRECATED AFTER PR-6 CUTOVER. D4 short-circuit (`status='not_required'` insert in `createTaxInvoice` for orders without MST, migration `20260502000000_pos_hddt_not_required_d4.sql`) remains only because the current pilot action layer and `InvoiceOutcome` enum still expect `not_required`; DROP is deferred to post-pilot. Batch eligibility query `aggregate_daily_b2c_invoice` MUST treat `not_required` as "no active per-order invoice" (i.e. INCLUDE such orders in batch) by filtering `tax_invoices.status IN ('draft','signing','submitted','issued')` and explicitly NOT `('not_required',)`. New code paths after 2026-05-08 MUST NOT introduce additional `not_required` insert sites — single source remains the existing D4 short-circuit until PR-6/post-pilot removal. Detect: `grep -rnE "status:\s*['\"]not_required" apps/web/app/finance/` MUST stay at exactly 1 site (the D4 path); new sites = block PR. Reference: `docs/plan/hddt-hybrid-sinvoice.md` D2 + `apps/web/app/finance/actions.ts:196-236`.
- [2026-05-08] HDDT-LATE-B2B-REQUEST-AFTER-BATCH-BLOCKED — BACKLOG / NOT YET IMPLEMENTED. When an order has already been included in a daily B2C summary invoice (junction row exists with active `tax_invoices.status NOT IN ('cancelled','replaced')`), `createTaxInvoice` MUST reject với message rõ ràng "Đơn này đã trong HĐ tổng hợp ngày X, vui lòng giữ biên nhận hoặc yêu cầu HĐ điều chỉnh qua kế toán". Existing existence check (`apps/web/app/finance/actions.ts:112-122`) phải extend để JOIN `tax_invoice_orders → tax_invoices` ngoài việc query `tax_invoices WHERE order_id`. Cùng ngày (chưa cron run 02:05 ICT) batch eligibility query đã exclude orders với active per-order invoice → cashier vẫn xuất HĐ B2B riêng được. Sau cron đã chạy → từ chối. Detect: `apps/web/app/finance/actions.ts::createTaxInvoice` MUST có second NOT EXISTS check joining `tax_invoice_orders` trước khi insert draft. Do not assume runtime already complies; this tracks PR-3 follow-up. Reference: D4 plan + 4-agent debate BA edge cases 2026-05-08.
- [2026-05-08] HDDT-SERVICE-ROLE-RPC-GATED-ON-CLAIM — `transition_tax_invoice_state_as_system(p_tax_invoice_id, p_to_status, p_actor, p_payload, p_note)` MUST gate first statement on `auth.role() IS DISTINCT FROM 'service_role'` → `RAISE EXCEPTION 'forbidden_service_role_only' USING ERRCODE = '42501'`. Function GRANT EXECUTE ONLY to `service_role` (REVOKE from `authenticated`, `anon`, `PUBLIC`). User JWT calls (cashier, owner manual trigger) MUST go through canonical `transition_tax_invoice_state(...)` which requires `auth.uid()` IS NOT NULL + permission split (`settings:tenant` for cancel/replace, `orders:write` else). The dual-RPC pattern lets cron route bypass user-permission checks (cron has no auth.uid()) while preserving user-path gates. Future "convenience" RPCs that allow caller to spoof actor MUST add same gate. Detect: any SECURITY DEFINER function with `_as_system` suffix or accepting `p_actor UUID` parameter MUST start with `IF auth.role() IS DISTINCT FROM 'service_role' THEN RAISE EXCEPTION ...`. Reference: `supabase/migrations/20260508055046_hddt_summary_rpcs.sql` Section 4.
- [2026-05-08] HDDT-SUMMARY-AUDIT-WHO-TRIGGERED — Mỗi summary HĐ run MUST INSERT `summary_run_queue` row với `trigger_source` ∈ `{'cron','manual'}` + `triggered_by` UUID (`auth.uid()` cho manual, `NULL` cho cron with `trigger_source='cron'` semantic). Queue table is the audit ground truth — NEVER write summary HĐ via direct `INSERT tax_invoices invoice_kind='daily_summary'` bypassing queue. Cron route + admin manual action both INSERT queue row BEFORE calling `executeSummaryRun({queueId})`. `tax_invoice_events.actor_id` for transition rows is `p_actor` arg (NULL for cron, user.id for manual); pair `events.actor_id IS NULL AND queue.trigger_source='cron'` for forensic "who triggered this batch run". Cancel of summary HĐ: `cancelTaxInvoice` action records `auth.uid()` in `tax_invoice_events` — does NOT write queue row (queue tracks issuance attempts only). Detect: `grep -rnE "INSERT INTO tax_invoices.*invoice_kind.*daily_summary" apps/ packages/` outside `aggregate_daily_b2c_invoice` RPC body MUST 0; queue insert MUST precede `executeSummaryRun` call in both cron + manual paths. Reference: `apps/web/app/api/cron/hddt-daily-summary/route.ts:117-136` + `apps/web/app/finance/summary-invoice-actions.ts:79-95`.
- [2026-05-08] HDDT-VERCEL-CRON-TIMEOUT-FANOUT-SAFE — Vercel cron function timeout: 60s (Hobby) / 300s (Pro) / 900s (Enterprise). `/api/cron/hddt-daily-summary` processes branches sequentially via `for (const branch of branches)` — 3 branches × provider 30s timeout = 90s worst case. Pro plan acceptable cho pilot ≤5 chi nhánh. Khi tenant scale ≥5 branches HOẶC provider latency >30s sustained → REFACTOR to fan-out pattern: cron route enqueues N work items vào `summary_run_queue` (status='queued'), then a separate Edge Function worker (or supabase queue) processes 1 queue row per invocation. KHÔNG `Promise.all` cấp branch để parallelize — provider rate-limits + token-cache contention sẽ bùng nổ. Hard cap detection: query `SELECT count(*) FROM branches WHERE is_active AND branch_kind='branch'` per tenant > 5 → flag in cron health report. Detect: Vercel deployment logs `Function execution timed out` for cron path → immediate ops alert; refactor to queue-based fan-out before enabling for next tenant. Reference: `apps/web/app/api/cron/hddt-daily-summary/route.ts:104-148` + `docs/plan/hddt-hybrid-sinvoice.md` follow-ups section.
- [2026-05-09] HDDT-PROVIDER-CANONICAL-SINVOICE-NOT-MISA — Viettel S-invoice/Sinvoice is the canonical production HĐĐT provider. MISA meInvoice may remain in code only as an explicit legacy/optional provider, NEVER as default, primary, or implicit fallback. Runtime provider choice MUST go through `normalizeInvoiceProviderChoice` in `packages/shared/src/providers/invoice-provider-policy.ts`; default undefined env returns `viettel`, alias `sinvoice` returns `viettel`, unknown values return `null`, and `misa` returns MISA only when explicitly configured. Detect: `pnpm --filter @comtammatu/shared test` MUST include `invoice-provider-policy.test.ts`; `apps/web/lib/invoice-provider-init.ts` MUST NOT contain `?? "misa"` or fall through to MISA for unknown env; active docs/backlog MUST reference `docs/plan/hddt-hybrid-sinvoice.md` as canonical. Archived docs may mention historical MISA decisions but must not be used as current source of truth. Reference: owner correction 2026-05-09 + `docs/ref/einvoice-tax.md` §5.
