# Regressions — Named Failure Rules

> Consult before risky work — each rule prevents a past mistake from recurring.
> The file is large: do NOT load it whole. Rule names are domain-prefixed, so
> grep by domain keyword and read only matching rows — e.g.
> `grep -niE 'RLS|DEFINER|policy' tasks/regressions.md` for RLS/grants work,
> `POS|KDS|payment` for POS/KDS, `HDDT|invoice` for e-invoice, `migration|DROP`
> for schema changes. (No static TOC by design — see retire policy item 5.)
> Format: `- [DATE] [RULE NAME] — short description`
>
> **Rule cleanup policy** — opportunistic, owner-gated, judgment-driven:
>
> 1. A regression rule is staging for a failure pattern, not a permanent design authority. If the canonical spec/rule doc has evolved, the regression row must follow it or be deleted.
> 2. When a rule's enforcement moves into `pnpm lint:*`, `scripts/check-*.mjs`, or another CI gate, the rule is redundant — delete it (or move the documentation into the relevant `docs/` file).
> 3. When a rule is explicitly superseded by a later rule (the supersedor states `Supersedes [date] OLD-NAME`), delete the old rule once the supersedor has been in place long enough to validate.
> 4. When a rule references a file or concept that no longer exists, delete the rule. This stays a manual judgment — there is no automated dead-reference gate, because a naive path scan false-positives on relative paths, column refs, and section anchors and would pressure deleting still-valid rules.
> 5. Group ad-hoc by domain only during cleanup — do not invent a TOC that re-introduces drift.
>
> Retire passes happen opportunistically (e.g. at the end of an owner-gated docs cleanup slice). A low removal count is EXPECTED and fine — most rules are still load-bearing, and the file is grepped by domain (never loaded whole), so on-disk size is not the cost metric. `pnpm regressions:retire-candidates` prints supersession and guard-covered candidates for human review (advisory, read-only, never auto-deletes). Last pass: 2026-07-18 removed UI lint-anchor policy and static utility gates that did not protect behavior. A guard must prove a deterministic runtime, accessibility, boundary, or safety outcome; it must not preserve document wording or a class allowlist by itself.
>
> **Code-enforced rules** — enforced against the codebase (not just authored) by
> `scripts/check-regression-guards.mjs` (`pnpm lint:regression-guards`, in the `lint`
> chain). Do not weaken the rule or remove its guard without updating both.
> Currently: `PROXY-NEVER-CALL-GETUSER`, `ZOMBIE-JWT-AFTER-GLOBAL-SIGNOUT`,
> `MULTI-KEY-PERMISSION-PARALLEL`, `PAYROLL-CALCULATE-MUST-BE-ATOMIC-RPC`,
> `PAYROLL-PRORATION-CAP-AT-STANDARD`, `CONFIRM-OUTSIDE-STARTTRANSITION`.
>
> **UI lookup shortcut** — not a TOC and not a second UI authority. For UI work,
> read `docs/spec/design-system.md` first, then load matching regression rows with:
> `rg -n "DESIGN-SYSTEM|UI-|PRESET-FIRST|NO-PRIMITIVE|NO-FAKE|NO-ARBITRARY|NO-SURFACE|NO-STATIC|NO-LEGACY|APP-SURFACE|STATUS|DataTable|FORMDIALOG|EMPTY|LOADER|RAW-TABLE|RAW-CARD|RAW-DIALOG|PRIMITIVE-IMPORT|PAGE-HEADER|RHYTHM|SHELL|NAV|PADDING" tasks/regressions.md`.
> Treat matches by their enforcement lane: lint-anchor rows only prove the rule
> name exists, `pnpm lint:ui-contract` rows are enforced by
> `scripts/check-ui-contract.mjs`, and review-checklist rows still require human
> inspection against the route family being changed.

- [2026-08-01] CLOCK-IN-REQUIRES-SHIFT-ASSIGNMENT — Employee/`self_service_clock_in` MUST resolve `shift_assignments` for today or overnight yesterday and freeze `scheduled_start_at`/`scheduled_end_at` on the attendance row. NEVER fall back to wall-clock `resolveDefaultShiftId` / nearest shift catalog. Unassigned → `shift_assignment_required` / Vietnamese "Chưa được phân ca…". Công uses hour-ratio on the frozen window (`attendance_shift_workdays` / `countShiftWorkdaysFromOverlap`); pending checkout → 0 công. Writers only via `reconcile_shift_assignments_week` / `copy_shift_assignments_week` with `hr:assign_shift`. Detect: `apps/web/tests/shift-roster-static.test.ts`, `packages/shared/src/auth/__tests__/hrm-truc-ngay-cong-static.test.ts`, migration `20260801131049_hr_shift_assignments_roster_and_hour_ratio_cong.sql`. ADR 0019 / D027.

- [2026-07-31] PROCUREMENT-DEMAND-PO-COVERAGE-BASE-UNITS — After approval converts demand (issue/export) qty into PO receipt units, coverage/remaining/status MUST compare in base units (or demand-unit equivalents via `entry_to_base_factor`). Never subtract raw `po_item.quantity` from `purchase_request_items.quantity` across different units — that leaves fully covered demands stuck `partially_ordered` and UI progress like `2/200 cuộn`. Detect: `supabase/tests/purchase_demand_coverage_base_units_test.sql`, `apps/web/tests/purchase-demand-progress.test.ts`, `apps/web/tests/purchase-order-workflow-static.test.ts`. Caught by: `YCM-31072026-0010` / `PO-31072026-0010-01` (200 cuộn → 2 thùng).

- [2026-07-31] SUPPLIER-INVOICE-LIST-ONE-TOOLBAR — Finance `/finance/supplier-invoices` LIST must keep view mode (`Theo NCC` / `Theo đơn mua`), search, and filter in one untitled `AppListFrame` + `AppToolbar variant="inline"` row. Do not put the active view label as `AppListFrame` `title` while also rendering the same switcher in `action` (desktop gap + duplicate chrome). Detect: `apps/web/tests/supplier-invoice-payment-static.test.ts`.
- [2026-07-30] ACCOUNTING-MONEY-MUST-NOT-ROUND-IN-FORMATTER — Values stored as `numeric(...,2)` in Finance, VAT, expenses, and supplier invoices MUST retain both decimal digits through input, arithmetic, Server Action, RPC/trigger validation, and fixed-2 display. Never use a display formatter, `Number()` aggregation, `Math.round()`, or an implicit PostgreSQL cast as the financial source of truth; reject scale above 2 before persistence. POS/menu/cash/VietQR remain whole-VND through separate semantic adapters and integer server schemas. Detect: shared money/format tests, `apps/web/tests/money-precision-*.test.ts`, expense/supplier tests, and `20260730150526_money_precision_contract.sql`.
- [2026-07-31] POSTGRES-CONDITIONAL-EXPRESSIONS-UNQUALIFIED — `COALESCE`, `GREATEST`, `LEAST`, and `NULLIF` are PostgreSQL conditional expressions, not ordinary functions, and MUST NOT be schema-qualified with `pg_catalog`. A bad expression inside PL/pgSQL may survive function creation and fail only when the execution path is first planned. Repair an already-applied definition through a forward migration; never assume replaying the source migration repairs a live function. Detect: `apps/web/tests/supplier-invoice-valuation-static.test.ts` and the catalog-definition assertion in `supabase/tests/supplier_invoice_valuation_test.sql`. Caught by: Preview-branch PostgreSQL `42601 syntax error at or near "pg_catalog"` logs on 2026-07-30 and the later supplier-invoice valuation audit.

- [2026-07-29] REALTIME-MESSAGES-POLICY-AFTER-REBASELINE — Every re-baseline (`pg_dump --schema=public --schema=private`) silently drops `realtime.messages` RLS policies because the extension-managed `realtime` schema is excluded from the dump. The `branch_ops_receive` SELECT policy (scoped to `branch:{id}:ops` via `can_read_branch_ops`) MUST be re-shipped as a forward migration after every baseline rebuild, or the branch ops bus goes dark and every authenticated client JOIN is rejected with "Unauthorized: You do not have permissions to read from this Channel topic". The forward migration is idempotent (`DROP POLICY IF EXISTS` + `CREATE POLICY`) and uses nested `CASE` guards to validate the exact topic shape and signed-bigint range before casting. Detect: `supabase/tests/branch_ops_realtime_authorization_test.sql` final-catalog guard. Caught by: Production Realtime log flood Unauthorized `branch:1:ops` 2026-07-29, continuing past the client-only claims gate in `c730c081`.
- [2026-07-29] REALTIME-PRIVATE-CHANNEL-TERMINAL-ERROR-STOP-REJOIN — Private Realtime channels rejected by RLS at JOIN leave Phoenix's `rejoinTimer` armed (`@supabase/phoenix` `channel.js`), so the supabase-js client re-JOINs the rejected topic forever and floods the broker with Unauthorized. The `.subscribe((status, err))` callback in `createBranchOpsChannel` / POS menu sync MUST detect a terminal authorization reject (`status === "CHANNEL_ERROR"` with `err.message`/`err.cause` matching `/unauthorized|permission|denied/i`) and call `supabase.removeChannel(channel)` to tear the channel down and reset the timer. Transport errors (`TIMED_OUT`, connection loss) stay retryable. Detect: `apps/web/tests/branch-ops-realtime-static.test.ts` client rejoin test. Caught by: Production Realtime log flood Unauthorized `branch:1:ops` 2026-07-29.
- [2026-07-29] REALTIME-BRANCH-OPS-CLIENT-CLAIMS-GATE — Private `branch:{id}:ops` clients MUST NOT JOIN when the JWT cannot mirror `can_read_branch_ops`: use `canSubscribeBranchOpsTopic(claims, branchId)` (Owner = any branch; non-Owner = `claims.branch_id` only) inside `createBranchOpsChannel` / POS menu sync, and skip `useRealtimeChannel` setup when `token === null` so anon JOINs never hammer Unauthorized. Inventory/operator/staff mounts are belt-and-suspenders only — `staff_permissions` and inventory branch pickers must never widen Realtime. Detect: `apps/web/tests/branch-ops-realtime-static.test.ts` + `packages/shared/src/auth/__tests__/scope.test.ts`. Caught by: Production Realtime log flood Unauthorized `branch:1:ops` / `branch:3:ops` 2026-07-29.
- [2026-07-29] SUPPLIER-INVOICES-COLUMN-GRANT-AFTER-LOCKDOWN — After `inventory_monetary_column_hardening` column-level SELECT on `supplier_invoices`, monetary/VAT columns (`total_amount`, `paid_amount`, `credit_applied_amount`, `subtotal`, `vat_amount`, `vat_rate`, `vat_breakdown`, `vat_invoice_attachment_path`) MUST be granted to `authenticated`. Omitting them makes finance cockpit AP queries fail with 42501. Detect: `apps/web/tests/supplier-invoice-payment-static.test.ts` + migration `20260729150200_grant_supplier_invoices_monetary_columns.sql`. Caught by: Production edge/postgres logs 2026-07-29.
- [2026-07-29] INGREDIENTS-COLUMN-GRANT-AFTER-LOCKDOWN — After `inventory_monetary_column_hardening` REVOKE/GRANT column-level SELECT on `ingredients`, every NEW non-monetary column (e.g. D093 `default_fulfill_site_kind`) MUST ship a follow-up `GRANT SELECT (col) ON public.ingredients TO authenticated`. Omitting it makes PostgREST `fetchIngredients` (and any query projecting the column) fail with 42501; callers that coerce failure to `[]` show empty GRN/stock/transfer ingredient pickers for `central_supply_ops` / `central_kitchen_lead` even when RLS + `inventory:read` pass. Detect: `apps/web/tests/d093-inventory-role-ops-static.test.ts` + migration `20260729140500_grant_ingredients_default_fulfill_site_kind.sql`. Caught by: owner report thuc@ GRN add-line empty on Production 2026-07-29.
- [2026-07-31] INGREDIENT-CATALOG-UNIT-REBASE-NOT-LOCK — After stock movements exist, `save_ingredient_catalog` MUST NOT permanently lock standard unit or ladder edits. Adding units, changing ratios, or swapping the sole unit is allowed; when the base unit identity changes, rebase `stock_levels` qty/WAC, ingredient thresholds, valuation account/origin quantities in the same transaction so book value stays unchanged, refresh living production-recipe and draft-document entry factors, and leave confirmed movement/document snapshots alone. UI must not disable unit fields solely because movements exist. Detect: migration `20260731220433_catalog_unit_rebase_allow_edit.sql` + `apps/web/tests/inventory-unit-system-phase-a2-static.test.ts`. Supersedes movement-lock clauses from Phase A2 unit-ladder guards.
- [2026-07-31] INGREDIENT-CATALOG-SAVE-ATOMIC-RPC — Catalog fields, unit ladder, unit roles, and `default_fulfill_site_kind` MUST save through one `save_ingredient_catalog` transaction; never follow the catalog RPC with direct `UPDATE ingredients`, because table DML is intentionally revoked and leaves a partial save. Persist receipt/issue/production roles in the INSERT, preserve `unit_cost`, and reject an invalid fulfill kind by rolling back the whole create/update. Do not expose overloaded or versioned catalog RPCs. Detect: `apps/web/tests/d093-inventory-role-ops-static.test.ts` + `supabase/tests/inventory_topology_physical_qc_test.sql`.
- [2026-07-31] PRODUCTION-RECIPE-ENTRY-UNIT-MUST-USE-PRODUCTION-ROLE — `upsert_production_recipe_lines` / import MUST send `entry_unit_id = ingredients.production_unit_id`. The production surface MUST pipe `production_unit_id` through IngredientOption → IngredientLineOption → `getDefaultProductionUnit`; Zod must require a positive `entryUnitId`; map `inventory_unit_role_mismatch` to operator copy; production quick-create MUST set `production_unit_id` to the catalog unit (never null). Detect: `apps/web/tests/production-recipe-source-static.test.ts`. Caught by: Production postgres/edge logs 2026-07-31 (`inventory_unit_role_mismatch:production` on `upsert_production_recipe_lines`).
- [2026-07-31] INVENTORY-ENTRY-SNAPSHOT-COLUMN-GRANT — After `inventory_unit_roles_and_snapshots` adds `entry_to_base_factor` / `entry_unit_code` on column-locked inventory tables, MUST `GRANT SELECT` those columns to `authenticated`. Omitting them makes PostgREST embeds (e.g. YCM list `purchase_order_items(...entry_to_base_factor)`) fail with 42501 / edge 403. Detect: migration `20260731233612_grant_inventory_entry_snapshot_columns.sql` + `apps/web/tests/purchase-order-workflow-static.test.ts`. Caught by: Production logs 2026-07-31.
- [2026-07-31] LEAVE-CHECKOUT-QUEUE-STORE-BRANCH-ONLY — `get_leave_review_queue` / `get_checkout_review_queue` require `branch_kind = 'branch'`. Operator home queue MUST pass `branchKind` and skip those RPCs for central sites so Owner browsing Kho Tổng / Bếp does not log `*_branch_not_allowed`. HR `/hr/attendance` Approvals MUST pass only store branches to `LeaveRequestsTable` (never Kho Tổng / Bếp). Operator leave-approvals MUST `notFound` for non-store; Team board MUST not link leave approvals on central sites. Detect: `apps/web/tests/command-dashboards-static.test.ts`, `apps/web/tests/hr-manager-attendance.test.ts`, `apps/web/tests/branch-leave-approvals-static.test.ts`. Caught by: Production logs 2026-07-31 (`leave_review_queue_branch_not_allowed` → "Không thể tải danh sách nghỉ phép").
- [2026-07-29] SUPPLIER-PAYMENT-UI-REQUIRES-MATCHED — Supplier invoice payment UI MUST require `matchStatus === "matched"` in addition to permission, outstanding, and VAT gates; `pending`/`unmatched` invoices expose only re-match. The payment RPC remains the final guard. Detect: `apps/web/tests/supplier-invoice-payment-static.test.ts`.
- [2026-07-29] PROCUREMENT-LIST-FIRST-DOCUMENT-ONE-ADDRESS — YCM, PO, GRN, YCH, and Transfer have one canonical query address on their LIST. Owner/Ops renders `AppDialog variant="document"`; Branch YCH/Transfer keeps its Page/fullscreen touch workflow. A linked Transfer is an internal YCH view, never a second row or overlay. Row open uses push; mode/close uses replace and retains filters/site/work/state/page. Never restore a parallel Owner detail implementation. Detect: `apps/web/tests/procurement-workflow-dialog-static.test.ts`, `apps/web/tests/record-depth-inventory-list-wave4-static.test.ts`, and `apps/web/tests/stock-fulfillment-static.test.ts`. Caught by: Owner workflow review 2026-07-29. **Supersedes** the staged-document/detail-route clause of [2026-07-28] RECORD-DEPTH-ONE-VIEW-ONE-ADDRESS.
- [2026-07-29] PROCUREMENT-PO-FIRST-ATOMIC — Warehouse PO create/edit plus supplier grouping MUST run in one `save_purchase_order_group` transaction. Missing a preferred supplier blocks the whole submission with structured ingredient evidence and leaves no partial PO. Accountant approve/return/reject stays in `review_purchase_order`; approval creates exactly one active draft GRN. GRN alone keeps a separate final confirmation because it posts stock/WAC. Detect: `supabase/tests/po_first_purchase_workflow_test.sql`, `apps/web/tests/procurement-workflow-dialog-static.test.ts`, and `apps/web/tests/purchase-order-workflow-static.test.ts`. Supersedes the YCM/direct-send contract from the 2026-07-29 workflow review.
- [2026-07-30] PROCUREMENT-MISSING-DEFAULT-SUPPLIER-FAIL-CLOSED — A PO submission with any ingredient missing an active preferred supplier MUST fail atomically and identify every missing ingredient. Never create the mapped subset or a replacement parent request. After supplier configuration is fixed, Warehouse retries the same form/group. Detect: `supabase/tests/po_first_purchase_workflow_test.sql` and `apps/web/tests/procurement-workflow-dialog-static.test.ts`. Supersedes the mixed-coverage YCM behavior found in `YCM-30072026-0001`.
- [2026-07-30] SUPPLIER-INVOICE-URL-MODE-ONE-OVERLAY — Finance supplier invoice LIST keeps filters and queue visible while URL params own `create | view | pay | credit`. Opening a row MUST push history; switching modes and closing MUST replace only workflow params. Detail Sheet and payment/credit FormDialog MUST never be open together, and the create action MUST be hidden without `procurement:invoice_create`. Detect: `apps/web/tests/supplier-invoice-payment-static.test.ts`. Caught by: Owner workflow continuity review 2026-07-30.
- [2026-07-30] SUPPLIER-INVOICE-MATCHING-ONE-HELPER — Goods invoice save and recompute MUST call the same locked DB helper over line-level allocations to confirmed GRN/PO rows from one supplier. Allocation value comes only from invoice-line quantity, price, and discount; PO `unit_price_est` is forbidden as a commercial price source. Header subtotal, document discount, VAT, and total use `±1 VND` validation. Service invoices have zero receipt allocations and require reasoned verification. Owner and Accountant with `finance:ap_pay` may record invoice-bound supplier payments; only Owner may create or allocate an advance. Both paths stay idempotent, append-only, and reduce cash/bank exactly once. Detect: `supabase/tests/po_first_purchase_workflow_test.sql`, `supabase/tests/supplier_invoice_ap_stability_test.sql`, `supabase/tests/supplier_payment_idempotency_test.sql`, and `apps/web/tests/supplier-invoice-payment-static.test.ts`.
- [2026-07-28] RECORD-DEPTH-ONE-VIEW-ONE-ADDRESS — Every LIST row has exactly one canonical view and one address (ADR 0018 / `design-system.md` § C.1). A compatibility DETAIL route may redirect but must not render a second view. D1 views use `?<entity>Id=` (Wave 3 for count `?slipId=` / `?assignmentId=`); D1 tasks stay non-addressable. Zero-action LIST is legal (C4). Overflow `⋯` must open a real `RowActionItem[]` menu — never a Link or bare icon pair. Recipes escalate from FormDialog when BOM lines > 12 (C3). Wave 4 ratchet is live: `apps/web/tests/record-depth-inventory-list-wave4-static.test.ts` (`row-open-single-path`). Detect: `docs/plan/adr/0018-record-depth-contract.md`; `docs/spec/page-archetypes.md` LIST row-actions; `docs/agent/rules/ui.md` Record Depth invariants. Caught by: Owner Wave 0 Record Depth lock 2026-07-28.
- [2026-07-28] CONFIRM-OUTSIDE-STARTTRANSITION — Client handlers that need `confirm()` from `ConfirmDialogProvider` MUST `await confirm(...)` **before** `startTransition(...)`, NEVER nest `await confirm` inside a transition callback. Transition-deferred setState can leave AlertDialog closed after DropdownMenu/Sheet teardown (PO "Duyệt mua" looked like a no-op). Pattern: `const accepted = await confirm(...); if (!accepted) return; setPending…; startTransition(async () => { …rpc… })`. Clear pending flags in `try/finally`. Detect: `pnpm lint:regression-guards` rule `CONFIRM-OUTSIDE-STARTTRANSITION` plus `apps/web/tests/purchase-order-workflow-static.test.ts`. Caught by: Inventory smoke 2026-07-27 — approve menu click never opened ConfirmDialog.
- [2026-07-27] BRANCH-SEED-NO-ON-CONFLICT-SEQUENCE-BURN — Idempotent `branches` seed MUST UPDATE existing rows then `INSERT … WHERE NOT EXISTS`, NEVER `INSERT … ON CONFLICT (name, tenant_id) DO UPDATE`. Postgres advances `branches_id_seq` before conflict resolution, so an already-present site burns the next id and shifts later site ids. Detect: `apps/web/tests/central-procurement-production-static.test.ts` asserts the NOT EXISTS seed and rejects ON CONFLICT upsert in `20260727190000_central_procurement_and_vat_evidence.sql`. Repair lineage is migration version `20260727220000`.
- [2026-07-27] CRON-HEALTH-NOTIFICATION-MUST-SET-TITLE — `public.check_cron_jobs_health()` MUST INSERT `notifications.title` (and `body`) on every `system.cron_failed` row. `notifications.title` is `NOT NULL`; omitting it raises SQLSTATE 23502 and silently drops the alert. Fresh-chain `reregister_managed_cron_jobs` MUST keep the baseline contract: `title = 'Tác vụ tự động cần kiểm tra'`, `body = v_msg`, `target_roles = ARRAY['owner']`, `SET search_path TO ''`, service_role-only EXECUTE. NEVER reintroduce a title-less INSERT when copying an older reregister template. Detect: `apps/web/tests/managed-runtime-initialization-static.test.ts` plus `supabase/tests/cron_health_notification_test.sql`. Caught by: Production postgres log 2026-07-27T12:00:00Z (`null value in column "title" of relation "notifications"`) after `20260727120004` overwrote the fixed function.
- [2026-07-26] AUTHENTICATED-RLS-OWNER-BYPASS-STAYS-BEHIND-HAS-PERMISSION — Policies evaluated as `authenticated` MUST NOT call `public.auth_is_owner(uuid)` directly: the helper intentionally revokes `EXECUTE` from `authenticated`, so PostgREST returns SQLSTATE 42501 before the policy can authorize an Owner. Reuse an authenticated-executable permission helper whose definer body owns the Owner bypass, such as `public.has_permission(branch_id, key)`, and keep the direct helper ACL restricted. Detect: `apps/web/tests/hr-payroll-enterprise.test.ts`, `supabase/tests/advisor_auth_hardening_test.sql`, and Production catalog checks for policy definitions, function ACLs, and read-only `attendance_records` grants. Caught by: `/hr/payroll` failed at the `attendance_records` read on 2026-07-25 after `tax_invoices_select`, `attendance_select`, and `attendance_checklist_items_select` called the restricted helper directly.
- [2026-07-16] REALTIME-BRANCH-OPS-ACTIVE-SCOPE — Private `branch:{id}:ops` topics carry transport refresh signals and MUST follow live operational assignment: `can_read_branch_ops(bigint)` returns true only for an active profile and active target branch in the caller's tenant; an active Owner can read active tenant branches, while every non-Owner is limited to `profiles.branch_id`. NEVER derive this audience from `staff_permissions`: a tenant-wide or cross-branch action grant must not widen Realtime visibility. Supabase caches Realtime authorization for the connection, so profile or branch deactivation is enforced when the channel next authorizes, receives a refreshed JWT, or reconnects; this function does not forcibly evict an already-authorized socket. Keep EXECUTE revoked from `PUBLIC`/`anon` and granted explicitly to `authenticated`/`service_role`. Detect: `apps/web/tests/branch-ops-realtime-static.test.ts` plus `supabase/tests/branch_ops_realtime_authorization_test.sql`. Caught by: 2026-07-16 production aggregate audit found inactive profiles and inactive branches still backed by current grants under the old permission-derived predicate.
- [2026-07-17] FRESH-CLOUD-AUTH-USERS-TRIGGER-MUST-REPLAY — A baseline limited to `public` + `private` cannot carry the `on_auth_user_created` trigger because the trigger belongs to Supabase-managed `auth.users`. Every fresh Cloud chain MUST restore `AFTER INSERT ON auth.users EXECUTE FUNCTION public.handle_new_user()` through a post-baseline forward migration; a local-only seed trigger is not runtime proof. Keep the trigger recreation transactional and data-free, configure the custom access-token hook separately, then prove a real hosted Auth login emits tenant/branch/position/access-bucket claims. Detect: `apps/web/tests/auth-rls-core-cleanup-static.test.ts` plus Cloud catalog/login smoke. Caught by: a schema-only fresh-cloud replay had the hook function but no auth-user trigger, so future hosted user creation would not create `profiles` rows.
- [2026-07-17] FRESH-BASELINE-MVS-MUST-BE-POPULATED — Schema dumps create materialized views `WITH NO DATA`; every current `REFRESH MATERIALIZED VIEW CONCURRENTLY` path fails until each view has completed one ordinary refresh. The fresh Cloud chain MUST initialize current MVs once in dependency order and skip views already populated, so applying the forward to Production does not force a redundant refresh. A managed-state reset can retain pg_cron's worker registrations even after `cron.schedule(jobname, ...)` updates `cron.job`; only in a data-empty environment, replace canonical jobs through `cron.unschedule` + `cron.schedule`, record a one-cadence health grace for the new ids, and reload pg_cron before verifying the command captured by a new `cron.job_run_details` row. Without that grace, the health checker emits false `system.cron_failed` notifications for daily and weekly jobs before their first due time. The checker MUST also treat a recent row with `end_time IS NULL` as in progress, not failed, because the monitor can start in the same tick as another job; notification dedup MUST target the partial unique index with `ON CONFLICT (tenant_id, dedup_key) WHERE dedup_key IS NOT NULL`. Do not infer a current failure by joining historical run rows to a reused `jobid`. Detect: `apps/web/tests/managed-runtime-initialization-static.test.ts` plus Cloud MV/cron smoke. Caught by: a fresh-cloud inventory refresh failed every 15 minutes with `CONCURRENTLY cannot be used when the materialized view is not populated`, two other workers kept executing commands that no longer matched their current job rows, and the first repaired health tick raced a concurrent refresh before hitting an invalid conflict target.
- [2026-07-11] INVENTORY-CATALOG-UPSERT-PRESERVE-UNIT-IDENTITY — `upsert_ingredient_catalog` and `bulk_import_ingredients` MUST sync `ingredient_units` via `INSERT … ON CONFLICT (ingredient_units_ing_unit_key) DO UPDATE` (plus selective delete of absent units), NEVER `DELETE FROM ingredient_units WHERE ingredient_id = …` then re-INSERT. `production_recipes_ingredient_entry_unit_fkey` is `ON DELETE RESTRICT`; delete-all fails with SQLSTATE 23503 and the app historically mapped that to the misleading "Đơn vị tồn chuẩn không hợp lệ". Removing a unit still referenced by `production_recipes` MUST raise `ingredient_unit_in_use_by_production_recipe` before delete; bulk import MUST reject base-unit changes (`bulk_import_base_unit_change_forbidden`) and preserve secondary ladder rows. The live 12-arg hotfix migration MUST sort before `20260710193300` (signature drop) and MUST REVOKE/GRANT the 12-arg overload explicitly. Detect: `apps/web/tests/inventory-ingredient-unit-recipe-fk-static.test.ts`. Caught by: PROD postgres logs 2026-07-11 when saving recipe-linked ingredients.
- [2026-07-04] HDDT-LEGAL-BUYER-NAME-SERVER-CONTROLLED — The no-buyer-info legal phrase on a HĐĐT ("Bán cho người tiêu dùng" per NĐ 254/2026) MUST be set server-side in the provider (`buildInvoiceBody`, `packages/shared/src/providers/impl/viettel-sinvoice.ts`): when `buyerNotGetInvoice` is true, emit the `BUYER_NOT_GET_INVOICE_NAME` constant and IGNORE `request.buyerName` — never forward a client-sent value. The POS buyer form (`invoice-form-section.tsx`) is a `"use client"` component that bundles the constant and ships `buyerName` in its payload; a terminal on a stale cached bundle (Serwist precache + long-lived tab) sends the outdated phrase, so trusting the client puts the wrong legally-mandated text on issued invoices. Real incident 2026-07-04: after the constant flip + deploy `e32344e5`, prod `buyer_name` flip-flopped between old/new across `C26MAA4544`→`4562` because un-reloaded terminals kept emitting the old string. General rule: any legally-mandated or trust-boundary invoice field is server-determined, never client-authored. Detect: `packages/shared/src/providers/__tests__/viettel-sinvoice.test.ts` "sends buyerNotGetInvoice flag" passes a stale client `buyerName` and asserts the emitted value equals `BUYER_NOT_GET_INVOICE_NAME`.
- [2026-07-03] E2E-BRINGUP-MUST-APPLY-FULL-CHAIN — The CI-only E2E provisioning path (`scripts/supabase-e2e-bringup.mjs`) MUST apply the full active migration chain — baseline + every top-level `^\d{14}_*.sql` forward file, the `copyActiveForwardMigrations` filter in `scripts/supabase-baseline-local-check.mjs` — NEVER a cherry-picked subset. The CI fixtures (`apps/web/tests/fixtures/supabase-e2e/qa-users.sql`, `apps/web/tests/fixtures/supabase-e2e/tenant.sql`) track post-forward schema semantics, so a baseline-only stack rejects them as soon as a forward migration changes seeded behavior (central-site NULL branch claims vs the baseline-era `handle_new_user` → `branch_required_for_operational_position` SQLSTATE P0001 on every PR), while Production and `baseline-replay` stay green. The CI-only E2E harness keeps storage enabled (the fold migration `20260627140000` writes `storage.buckets`/`storage.objects` policies, which need the storage schema the storage service creates) and edge runtime disabled (no edge functions in the repo; its boot fetches `@panva/jose` from jsr.io — an observed 403 CI flake source). Detect: outside CI, `node scripts/supabase-e2e-bringup.mjs` fails before `supabase start`; CI e2e-smoke reaches `Supabase e2e stack ready` without writing root or app `.env.local`. Caught by: e2e-smoke red on every PR 2026-07-02→03 (runs 28611238794 / 28610269455), fixed in PR #179.
- [2026-07-02] SEED-PERMISSION-CATALOG-SYNC — Every permission key granted in `apps/web/tests/fixtures/supabase-e2e/tenant.sql` `role_templates` arrays MUST also exist in the same file's `permission_keys` catalog INSERT: `sync_missing_permissions_from_template()` materializes template grants into `staff_permissions` at seed time, and `staff_permissions.permission_key` FKs to the catalog. A missing key kills the CI E2E bring-up with SQLSTATE 23503 BEFORE any spec runs, while prod keeps working (prod received the key via its forward migration) — so the break surfaces only in CI. When a migration adds a permission key plus a template grant, mirror the catalog row into the dev seed verbatim from that migration (and verify against prod: `inventory:count_submit` was later removed from prod, so the migration file alone is not proof). Detect: `pnpm lint:seed-permissions` (`scripts/check-seed-permission-sync.mjs`, in the lint chain). Caught by: e2e-smoke red on main 2026-07-02 (`inventory:count_assign`/`count_approve` granted to branch_manager but absent from the catalog).
- [2026-07-02] PAYROLL-CALCULATE-MUST-BE-ATOMIC-RPC — `snapshotPayrollPreview` (`apps/web/app/(protected)/hr/payroll-actions.ts`) MUST persist its approved snapshot through the atomic `snapshot_payroll_calculation` RPC, NEVER direct client-side `INSERT`/`UPDATE`/`DELETE` on `payroll_entries` or `payroll_periods` — splitting a multi-item payroll snapshot across independent calls can leave entries and its status divergent (multi-item writes MUST use a Postgres RPC, per `MIRROR:constraints`). Detect: `pnpm lint:regression-guards` guard `PAYROLL-CALCULATE-MUST-BE-ATOMIC-RPC` in `scripts/check-regression-guards.mjs` (requires the snapshot RPC and forbids direct payroll entry/period writes). Caught by: agent-framework audit 2026-07-02 (guard existed with no named rule row).
- [2026-06-22] PAYROLL-PRORATION-CAP-AT-STANDARD — `calculatePayroll` (`apps/web/app/(protected)/hr/payroll-actions.ts`) MUST cap the monthly proration ratio at 1.0: prorate base salary on `payableDays = Math.min(workingDays + paidLeaveDays, standardDays)`, NEVER raw `workingDays`. Má Tư is a 7-day/2-shift business so attendance-days plus paid annual leave can exceed `standard_days`; an uncapped ratio overpays the moment `base_salary` is populated. `working_days` still records actual completed attendance, `paid_leave_days` records approved annual leave inside quota, and only `payable_days` drives PAY. Detect: `pnpm lint:regression-guards` guard `PAYROLL-PRORATION-CAP-AT-STANDARD` in `scripts/check-regression-guards.mjs` (requires `calculatePayableDays` and its standard-day cap). Caught by: E2E audit 2026-06-22 (payroll overpay-on-activation, P0), updated for annual leave payroll on 2026-06-26.
- [2026-06-22] GUARD-MCP-WRITE-MATCH-CONNECTOR-SEP — The prod-DB guard's MCP write-tool matcher (`MCP_WRITE_TOOL` in `scripts/guard-prod-db.mjs`, mirrored in `.claude/settings.json` + `.codex/hooks.json`) MUST match both direct MCP names (`mcp__supabase__execute_sql`) and connector-wrapped names (`mcp__codex_apps__supabase._execute_sql`). Keep the `^mcp__` prefix (required by the `startsWith` check in `scripts/check-guard-sync.mjs`) and use the shared final separator `(?:__|[._]+)` before the write keyword. The older `__`-only form missed connector names; the `[._]`-only repair missed direct `mcp__supabase__*` names. Change all three mirror strings identically (`pnpm lint:guard-sync` enforces equality; its fixtures exercise both direct and connector forms). A future connector using a hyphen/space boundary would still need an explicit fixture before use. Detect: `node scripts/check-guard-sync.mjs` exit 0 + direct and connector writes on a protected ref exit 2 while connector `select` exits 0. Caught by: prod-DB guard audit 2026-06-22, repaired for direct MCP names on 2026-07-08.
- [2026-07-10] SEMANTIC-TONE-NOT-CHART-INDEX — A semantic tone (`success`, `warning`, `destructive`, `danger`, `info`) MUST resolve to its status token (`var(--success)`, `var(--warning)`, `var(--destructive)`, `var(--info)`), NEVER to a `var(--chart-N)` index. `chart-1..5` is a categorical ramp whose hues are assigned per theme, and `.dark` swaps `chart-1`/`chart-2` (gold ↔ terracotta) while `chart-4` moves from navy to cream. A tone map keyed by chart index therefore renders `success` as terracotta and `destructive` as near-invisible cream on the night theme, and was already wrong in light mode (`success → chart-2` = brand gold). Chart tokens stay reserved for genuinely categorical series (KDS side badges, finance heatmap buckets, multi-series revenue charts). Detect: `grep -rnE '(success|warning|danger|destructive|info):\s*"var\(--chart-' apps/web/app` MUST be 0. Caught by: design-system deep audit 2026-07-10 (`trend-sparkline.tsx` TONE_VAR, `inventory/_lib/ui.ts` INVENTORY_COLOR_VALUE). Reference: `docs/spec/design-system.md` § Token Contract.
- [2026-06-09] PRIVATE-HELPER-NEEDS-SCHEMA-USAGE-GRANT — A `SECURITY DEFINER` RPC that resolves a `private.*` helper (e.g. `private.staff_role_from_position_code(...)`) needs BOTH `GRANT EXECUTE ON FUNCTION private.<helper> TO service_role` AND `GRANT USAGE ON SCHEMA private TO service_role`. Granting EXECUTE alone raises `permission denied for schema private` (SQLSTATE 42501) at call time even though the function grant looks correct — PostgreSQL checks schema USAGE before function EXECUTE. The grant MUST stay scoped to `service_role` only: browser-callable `anon` and `authenticated` MUST NOT receive `USAGE ON SCHEMA private` or `EXECUTE` on private helpers (keep the matching REVOKE). Symptom: an RPC that reaches valid data then fails late with `permission denied for schema private`. Detect: any migration adding a `private` helper invoked by a definer RPC MUST pair the `GRANT EXECUTE ON FUNCTION private.<fn>` with `GRANT USAGE ON SCHEMA private TO service_role`; `grep -rn "GRANT USAGE ON SCHEMA private" supabase/migrations/` MUST cover the helper-introducing migration. Caught by: `employee_request_clock_out` hotfix 2026-06-09 (checkout-approval T3 contract).
- [2026-05-09] SECURITY-HEADERS-IN-NEXT-CONFIG — `apps/web/next.config.ts` `headers()` MUST emit (1) `Content-Security-Policy` covering Supabase REST/storage/realtime + Upstash + self, (2) `X-Frame-Options: DENY`, (3) `X-Content-Type-Options: nosniff`, (4) `Referrer-Policy: strict-origin-when-cross-origin`, (5) `Permissions-Policy: camera=(self), microphone=(), geolocation=(), interest-cohort=()` so Employee photo capture works while GPS stays disabled, (6) `Strict-Transport-Security: max-age=63072000; includeSubDomains; preload` for `source: "/:path*"`. `poweredByHeader: false` MUST stay set (drops Next.js disclosure). Removing or narrowing any of these header values is a regression of the 2026-05-07 `/qa` finding ISSUE-012/014 + the 2026-05-09 HSTS-preload hardening. CSP currently uses `'unsafe-inline'` + `'unsafe-eval'` on script/style for Next 16 RSC; nonce-based CSP is the long-term plan, not a free swap. HSTS `includeSubDomains` covers `*.comtammatu.com` — confirm no HTTP-only subdomains exist before submitting to the Chrome/Firefox preload list (preload registration is hard to undo). Detect: `curl -sI https://<deploy>/login` MUST contain `content-security-policy`, `x-frame-options: DENY`, `x-content-type-options: nosniff`, `referrer-policy`, `permissions-policy`, `strict-transport-security: max-age=63072000; includeSubDomains; preload`; MUST NOT contain `x-powered-by`. Caught by: Ralph cycles 2026-05-09 / commits 610123c8 + 9f9f424d + 60543ab8.
- [2026-05-08] MULTI-KEY-PERMISSION-PARALLEL — Multi-key permission probes (`currentUserHasAnyPermissionAny` in `apps/web/app/_lib/permissions.ts`, `getAuthContextWithAnyPermission` + `getAuthContextWithPermissions` in `apps/web/app/_lib/auth.ts`) MUST fan out via `Promise.all(keys.map(...))` and collapse with `.some(Boolean)` (OR) or `.every(Boolean)` (AND), NEVER `for…await` (sequential = N×RTT). Each underlying RPC (`has_permission_any` / `hasPermissionGrant`) already runs `_auth_is_owner` server-side per call, so parallel fan-out is correctness-preserving. Single-key helpers (`currentUserHasPermissionAny`, `currentUserHasPermission`, `hasPermissionGrant`) are wrapped in React `cache()` — primitive args value-keyed → identical `(key)` or `(branchId, key)` tuples in one render dedupe to a single RPC. NEVER wrap the multi-key helpers in `cache()` — array identity changes per render, memoization never hits. AND vs OR semantic distinction is critical: `getAuthContextWithAnyPermission` uses `.some(Boolean)` (returns ctx if ANY matches), `getAuthContextWithPermissions` uses `.every(Boolean)` (returns ctx only if ALL match). Wasted-RPC ceiling = N (caller pays N RPCs even when first key would short-circuit) — acceptable for N≤5 at single-tenant pilot scale; revisit batch RPC `has_any_permissions(p_keys TEXT[])` if Vercel→Supabase pool pressure observed at &gt;30 concurrent active users. Detect: `grep -rnE "for\\s*\\(\\s*const\\s+\\w+\\s+of\\s+\\w+\\s*\\)\\s*\\{[^}]*await\\s+(currentUserHasPermissionAny|hasPermissionGrant)" apps/web/app/_lib/` MUST 0. Caught by: 4-agent debate 2026-05-08 (PR #3 of perf series fixing 2-3s redirect Employee → POS/KDS/Inventory).
- [2026-07-27] RSC-SETTLE-SESSION-BEFORE-GETAUTHCONTEXT-FANOUT — An RSC page MUST NOT `Promise.all([loadAuthState(), fetchX(), fetchY()])` when `fetchX`/`fetchY` call `getAuthContext`/`getAuthContextWithPermission` on the shared React-`cache()`d Supabase server client. Parallel cookie-session work on that GoTrue client can race into false-null ctx → soft "Không có quyền" / load-error empty states (expenses: "Không thể tải sổ Chi phí vận hành"). Pattern: `await loadAuthState()` first, then `Promise.all` data loaders; finance loaders MUST share one `allowedRoles` ref (`MODULE_ACL.finance.allowedRoles`) so `getAuthContext` cache dedupes. Sibling: `getAuthContext` is getSession-only (no `getUser`); mutation Auth liveness lives in `withAction`; protected RSC Auth liveness lives in `loadAuthState` → `probeAuthSessionLiveness` (`ZOMBIE-JWT-AFTER-GLOBAL-SIGNOUT`). Detect: `apps/web/tests/finance-expenses.test.ts` "settles session before parallel finance getAuthContext loaders". Caught by: owner `/finance/expenses` soft load failure after VAT columns landed 2026-07-27.
- [2026-05-08] PROXY-NEVER-CALL-GETUSER — `apps/web/proxy.ts` + `packages/database/src/supabase/middleware.ts` MUST use `supabase.auth.getSession()` (cookie decode + auto-refresh via `setAll` when access token is past `EXPIRY_MARGIN_MS`), NEVER `getUser()` (HTTP roundtrip ~80-300ms per nav). JWT signature is enough for routing; cookie rotation stays on the getSession refresh path. RSC / loader `getAuthContext` (`apps/web/app/_lib/auth.ts`) is also getSession-only — reintroducing `getUser()` there mapped Auth `session_not_found` to false-null and soft "Không có quyền" while PostgREST still accepted the cookie JWT (GRN/expense). Protected RSC Auth liveness belongs in `loadAuthState` → `probeAuthSessionLiveness` (`apps/web/app/_lib/auth-session-liveness.ts`), not in `getAuthContext` or proxy. Mutation wrappers (`withAction` / `resolveActionContext` in `apps/web/app/_lib/with-action.ts`) MAY call `getUser()` for Auth liveness and MUST map revoked Auth (`AuthSessionMissingError` / `session_not_found` / terminal refresh codes) to structured `session_expired` + local `signOut`, NEVER to the forbidden copy. Avoid `session.user.*` reads in middleware/proxy (`insecureUserWarningProxy` spam); pivot on session truthy + `extractClaimsFromAccessToken`. Detect: `scripts/check-regression-guards.mjs` PROXY-NEVER-CALL-GETUSER (proxy/middleware/auth.ts absent; with-action + auth-session-liveness present). Sibling: `ZOMBIE-JWT-AFTER-GLOBAL-SIGNOUT`. Caught by: 4-agent debate 2026-05-08; GRN false-deny + zombie JWT harden 2026-07-27.
- [2026-07-27] ZOMBIE-JWT-AFTER-GLOBAL-SIGNOUT — After `signOut({ scope: "global" })` on one tab, peer tabs may keep a still-valid access JWT while the refresh token is dead. auth-js proactive-preserve then admits that JWT from `getSession()` when refresh fails inside `EXPIRY_MARGIN_MS` but `expires_at` is still in the future → pages soft-fail / mutations look like "Không có quyền". Middleware (`packages/database/src/supabase/middleware.ts`) MUST treat terminal refresh errors (incl. `session_not_found`) as anonymous and MUST override proactive-preserve on the middleware path only (force local signOut / deletion cookies when a terminal refresh was observed). Proxy MUST still never call `getUser()`. Mutating `withAction*` MUST probe Auth via `getUser` and return `session_expired`, not forbidden. Protected RSC navigations that call `loadAuthState` MUST probe Auth liveness via `probeAuthSessionLiveness` (`apps/web/app/_lib/auth-session-liveness.ts`) and redirect revoked sessions to GET `/api/auth/signout` (Route Handler Set-Cookie; RSC cookie setAll may no-op) — NEVER put that probe in `getAuthContext` (GRN false-deny) or proxy/middleware. POS station layout MUST call `loadAuthState` so shell-only stations are covered. Residual: public Runner display intentionally skips Auth; any future staff surface that skips `loadAuthState` reopens the far-from-expiry window until a mutation probe. Prefer ErrorPanel / access recovery via POST/GET `/api/auth/signout`. Detect: `apps/web/tests/middleware-auth-refresh-retry.test.ts` "still-valid access when terminal refresh would preserve"; `apps/web/tests/with-action.test.ts` session_expired mapping; `apps/web/tests/zombie-jwt-rsc-liveness.test.ts`. Caught by: T3 auth hardening 2026-07-27; far-from-expiry RSC close 2026-07-28.
- [2026-05-07] UI-RHYTHM-OWNED-BY-SURFACE — Page outer padding normally goes through `AppPage`, and repeated card padding goes through `Card`; this keeps page density coherent. A workflow-specific direct composition is valid when it is clearer than an adapter and its rendered hierarchy, touch target, keyboard behavior, and responsive density are reviewed. Static gap, padding, or radius class allowlists are not evidence of a visual defect. Detect: changed UI requires the targeted viewport/state proof in `docs/agent/rules/ui.md`. Reference: `docs/spec/design-system.md` Rhythm Contract A.
- [2026-05-27] RUNNER-BOARD-LARGE-SCAN-TOKENS — Runner/KDS customer board typography PHẢI dùng token shared theme, KHÔNG dùng `text-[...]`: column header `text-runner-header`, all four row data cells `text-runner-board`, empty secondary `text-runner-empty-secondary`, footer `text-runner-footer`. These tokens MUST be height-responsive `clamp(...dvh...)` tokens, not fixed px and not viewport-width typography, so MacBook Air, HD, FullHD, 2K, and 4K displays stay readable without overflowing. Compact desktop viewports below `xl` MUST use `px-4 py-2` cell/header/footer spacing so wrapped labels like `Mang về #041` and `2 món` do not collide with row dividers; the narrow wait-time column may use smaller horizontal padding. Status labels MUST use the same `RunnerOrderCell` text element as the other row cells and MUST NOT add separate `text-*` classes on that data-text element, because `tailwind-merge` can drop the shared size token. Board columns MUST use Tailwind built-in `grid-cols-12` + spans: Đơn `col-span-4`, Số món `col-span-3`, Trạng thái `col-span-4`, Chờ `col-span-1`; do not use custom percent grid utilities for this board. Header MUST be `Chờ`, not `Thời gian đợi`, so the wait label does not steal width from quantity/status. Wait time under 1 minute MUST show seconds (`1s`, `2s`, `15s`, `59s`) and switch to minutes from `1p`; visible wait-time cells MUST tick client-side every 1 second instead of waiting for the 15s Runner refresh. Empty state may use the decorative runner mascot `/brand/mascot/be-suon-tuoi-runner.png`, but the mascot MUST NOT replace the 4-row order board when orders exist. Detect: `apps/web/tests/runner-copy.test.ts` MUST assert the responsive token values, compact small-desktop padding, Tailwind grid split, wait header copy, mascot asset, 1s wait-time ticker, wait-time examples, and Runner page MUST not contain arbitrary `text-[...]`; status must not use `text-runner-status`, `text-warning`, or `text-success` on the row data-text element. Reference: `docs/spec/design-system.md` Rhythm Contract B.
- [2026-05-07] RSC-LUCIDE-ICON-MUST-BE-JSX — `apps/web/app/components/surface.tsx` `AppSection.icon` and `AppLinkCard.icon` are typed `ReactNode`, not `ElementType`. Server Components passing a lucide icon as a function reference (`<AppLinkCard icon={IconClock}>`) trip Next.js 16 RSC serialization: lucide icons are `forwardRef` objects (`{$$typeof, render}`) and only React **elements** (not raw component refs) survive the Server→Client boundary. ALWAYS pass JSX form: `<AppLinkCard icon={<IconClock />}>`. When building dynamic icon arrays (e.g. tile lists) keep entries as JSX elements (`{ ..., icon: <IconLayoutGrid /> }`) rather than `ElementType` references and instantiating in `.map()`. Domain wrappers like `EmployeePanel` (`apps/web/lib/staff-runtime/components/staff-runtime-page.tsx`) and same-file Server helpers like `StatCard` may keep their public `icon: ElementType` API as long as they render `<Icon />` themselves before crossing into a Client surface. Symptom (Console Error in Next 16.2 / Turbopack): `Only plain objects can be passed to Client Components from Server Components. Classes or other objects with methods are not supported. <... icon={{$$typeof: ..., render: ...}} ...>`. Detect: `grep -rEn "icon=\{[A-Z]" apps/web/app --include='*.tsx'` — any match feeding into `<AppSection>` / `<AppLinkCard>` is a violation. Caught by: 4-agent debate 2026-05-07 (architect+critic finding) on `/employee` page crash.
- [2026-05-07] RPC-DROP-MUST-SCAN-6-CHANNELS — Trước khi DROP FUNCTION migration, MUST run comprehensive caller scan ở 6 channels (JS-only grep INSUFFICIENT): (1) **JS callers** `grep -rEln 'supabase\.rpc\(\s*[\"'\'']<name>[\"'\'']' apps/ packages/`; (2) **Internal SQL `PERFORM`/`SELECT`** `grep -rEln '(PERFORM|SELECT|FROM|JOIN|=\s*)\s+(public\.)?<name>\s*\(' supabase/migrations/`; (3) **Trigger `EXECUTE FUNCTION/PROCEDURE`** `grep -rEln 'EXECUTE (FUNCTION|PROCEDURE)\s+(public\.)?<name>\b' supabase/migrations/`; (4) **RLS policy `USING/WITH CHECK`** `grep -rEln '(USING\|WITH CHECK)\s*\(.*\b<name>\s*\(' supabase/migrations/`; (5) **Column DEFAULT/CHECK** `grep -rEln 'DEFAULT\s+(public\.)?<name>\s*\(\|CHECK\s*\(.*<name>\s*\(' supabase/migrations/`; (6) **pg_cron schedules** `grep -rEln 'cron\.(schedule\|unschedule).*<name>' supabase/migrations/`. PLUS query `pg_proc` for actual existence + `pg_depend` for view/policy/trigger dependencies + `pg_stat_user_functions.calls` for runtime ground truth. Audit M7 (2026-05-07): earlier "67 dead RPCs" list từ JS-only grep had ≥3 verified false positives in 60s spot-check (`cleanup_abandoned_payments` = pg*cron, `weekly*\__report`= pg_cron,`transition_order_**status`= trigger + RPC callers). Implied false positive rate UNBOUNDED. **HARD CAP wave size ≤ 10 RPCs** per drop migration (AGENTS.md owner-apply blast radius). Pre-launch pilot: **drop ONLY Tier A** (one-shot backfills,`\_compute**`private prefix,`seed\_\_`) where 6-channel scan returns TOTAL=0. Tier B (reporting + scheduled), Tier C (lifecycle helpers), Tier D (ops emergency tools, auth hooks like `custom_access_token_hook`) — DEFER to post-pilot when `pg_stat_user_functions`provides usage telemetry. Detect: any new DROP FUNCTION migration MUST attach 6-channel scan output in PR description. Caught by: 4-agent security audit 2026-05-07 (M7 NO-GO verdict). **[2026-06-14 correction]** Channel-1's grep is line-anchored and misses the ~42 sites that call`supabase.rpc(`with the name on the NEXT line — a single-line scan undercounts callers and can mislabel a LIVE RPC as dead (verified 2026-06-14: distinct caller set 107→137 once multiline-aware). Channel-1 and every caller scan MUST be multiline-aware (ripgrep`rg -U`, or `grep -z`). Separately, Postgres `~`word boundaries are`\y`(either end), NOT`\m`— a`\m…\m`predicate matches nothing, so sanity-check any`pg_proc`/`pg_policies`reference scan against a known-referenced helper (e.g.`has_permission`) before trusting zero-result rows.
- [2026-05-07] RPC-ROLLBACK-MUST-INCLUDE-BODY — Rollback files cho DROP FUNCTION migrations PHẢI inline `CREATE OR REPLACE FUNCTION` body từ `pg_get_functiondef` snapshot, KHÔNG reference upstream migration archaeology. Lý do: staff assignment RPCs đã được amended nhiều lần. "Re-apply original migrations X, Y, Z" assumes byte-identical history — false. Rollback file shape: full function body với security flags preserved (SECURITY DEFINER + SET search_path + REVOKE/GRANT). Document `pg_get_functiondef` source snapshot timestamp trong rollback header. Caught by: 4-agent debate 2026-05-07 (architect + critic both flag).
- [2026-05-07] SELECT-STAR-ACCEPTABLE-FOR-NON-AUDIT-TABLES — `.select("*")` trong Server Actions là **acceptable** cho non-audit tables nếu consumer cần most columns + table không chứa PII fields. Audit M6 wave (#7-L4) kept the pragmatic rule for broad non-PII readers such as ingredient catalog, supplier details, transfer ledger detail, contract terms, and payroll period status. Explicit column list sẽ duplicate `database.types.ts` Row type when the consumer reads most fields. Tradeoff: mechanical refactor cost (read schemas + consumers + craft explicit lists + risk typecheck breakage) > marginal benefit (slight serialization cost). EXCEPTIONS — explicit columns REQUIRED khi: (a) table chứa PII fields nên không expose qua list view (xem **AUDIT-LOG-SELECT-EXPLICIT-COLUMNS** cho `audit_logs.ip_address/old_data/new_data`); (b) consumer chỉ cần subset nhỏ → narrow projection cải thiện perf; (c) future column additions có thể leak unintended data (e.g. internal notes column). Khi adding new column tới existing select-star table, audit consumers cho potential leak risk trước. Reference: 4-agent debate 2026-05-07 — anti-SLOP decision: don't refactor mechanical patterns without measurable benefit. Caught by: architect audit (#7-L4 finding rejected as overreach).
- [2026-05-07] EXCEPTION-WHEN-OTHERS-MUST-LOG-OR-RERAISE — Mọi `EXCEPTION WHEN OTHERS` block trong PL/pgSQL PHẢI có ít nhất 1 trong 3: (a) `RAISE EXCEPTION 'context: %', SQLERRM` (re-raise với context — preferred cho destructive ops), (b) `RAISE WARNING '[func_name] context: %', identifier, SQLERRM` (fail-soft với loud signal — preferred cho non-critical secondary ops), (c) `RAISE NOTICE` + counter accumulator (acceptable cho FOR LOOP per-item skips). KHÔNG silent swallow (assignment-only, e.g. `v_var := NULL` mà không log). Audit M5 (2026-05-07) verified existing sites and accepted only loud fail-soft paths. Critical finding: **audit_logs inserts are NEVER inside EXCEPTION WHEN OTHERS blocks** — catches surround secondary side effects only, audit always happens in main path → audit data NOT being eaten. New EXCEPTION WHEN OTHERS code PHẢI follow rule above. Detect: `grep -rn -A 3 "EXCEPTION WHEN OTHERS" supabase/migrations/` — verify next 3 lines contain RAISE EXCEPTION/WARNING/NOTICE. Caught by: 4-agent security audit 2026-05-07 (M5 finding — verified clean).
- [2026-05-07] TENANT-OWNER-USER-ID-CANONICAL — `tenants.owner_user_id UUID NOT NULL FK auth.users(id) ON DELETE RESTRICT` là canonical auth identity column cho tenant owner. PHẢI giữ separate với `tenants.representative` (TEXT người đại diện theo pháp luật) và `positions.code='owner'` (HR label, JWT user_role source). KHÔNG add UNIQUE constraint cho đến khi có business rule cấm same user owning multiple tenants. KHÔNG sync `representative` ↔ `owner_user_id` automatically — different concepts. Mọi mutation của `owner_user_id` PHẢI qua future `transfer_ownership` RPC; manual SQL UPDATE chỉ acceptable trong runbook + audit trail. Reference: `docs/plan/adr/0005-owner-identity-source-separation.md`.
- [2026-05-07] RPC-ERROR-MUST-MAP-OR-LOG-UNMAPPED — Mọi Server Action gọi `supabase.rpc(...)` PHẢI route error qua mapper helper trả về Vietnamese localized message (tham khảo `mapPaymentRpcError`, `mapDiscountRpcError`, `mapRefundRpcError`, `mapReturnRpcError`, `mapTransitionError`, `mapRpcError` × 2, `mapStationDbError`, `mapZoneDbError`, `mapTableDbError`, `mapTerminalDbError`, `mapDbError`, `mapJournalMutationError`). NEVER `return { error: rpcError.message }` — vi phạm AGENTS.md `NEVER return raw Supabase/Postgres error.message to client`. Pattern: try mapper → if matched, return mapped + log; if NULL, log unmapped (`console.error("[actionName] [unmapped] rpc error:", msg)`) THEN return generic fallback. Unmapped log là CRITICAL để iterate mapper coverage — không log → unmapped errors silently dropped, không có data để mở rộng vocabulary. Cập nhật mapper khi observed unmapped messages tích lũy. KHÔNG centralize mappers cross-domain (payment vocabulary ≠ refund ≠ inventory; mỗi domain có own RPC error keys). KHÔNG add patterns speculatively (SLOP — wait for log evidence). Inline `if (msg.includes("specific_keyword"))` checks BEFORE mapper là acceptable cho high-frequency known errors (e.g. `already_paid`, `payment_not_found`). Reference: `apps/web/app/(protected)/br/[branchId]/pos/payment-actions.ts:65-90,590-595,795-801,1041-1054,1357-1363`. Caught by: 4-agent security audit 2026-05-07 — finding outdated (mapper pattern đã in place trước session) nhưng unmapped logging gap thật sự (silent drops).
- [2026-05-07] RLS-DESTRUCTIVE-MUST-USE-HAS-PERMISSION — RLS policies cho destructive UPDATE/DELETE PHẢI gate qua `has_permission(branch_id, key)` (queries `staff_permissions` live qua DB), KHÔNG dùng `auth_role()` (đọc JWT user_role claim, cache đến token refresh ~1h). Tới khi JWT refresh, user bị revoke quyền vẫn pass `auth_role()` check → 1-hour stale-revoke window cho destructive op. Refunds_update policy đã refactor trong H2a (`20260601200000_h2a_refunds_update_perm_gate.sql`): old owner + tenant-admin role check → `has_permission(branch_id, 'orders:refund_approve')`. Owner bypass tự động qua `_auth_is_owner` trong has_permission (`20260423040000:14-18`). EXCEPTIONS (intentional, documented): (a) **scope/side guards** trong RPC bodies như `inventory_transfer_rpc_role_scope_gates.sql` — branch_manager forbidden từ inter-site ship là semantics rule, không phải revokable permission; (b) **branch-scope cross-tenant view pattern** `branch_id = auth_branch_id() OR auth_role() IN HQ_ROLES` — standard "tenant sees all branches"; (c) **named ABAC helpers** như `is_inventory_production_operator()`; (d) **module-ACL fast-path** cho non-destructive read-mostly tables (xem BMIDL-RLS-INTENTIONAL-ROLE-FASTPATH). HR payroll historical owner + retired tenant-admin role checks were backlog H2b. Detect: `grep -rnE "auth_role\(\) IN" supabase/migrations/` cho destructive policies; cross-check với 4 exceptions trên. Caught by: 4-agent security audit 2026-05-07 (architect H2 finding).
- [2026-05-07] BMIDL-RLS-INTENTIONAL-ROLE-FASTPATH — `branch_menu_item_daily_limits` (`20260510040000`) dùng route-level audience list mirror `module-acl.ts` `branch_menu_limits.allowedRoles`. Đây là intentional **module-ACL fast-path** (audience filter ⇒ access), KHÔNG phải permission gate. KHÔNG refactor sang `has_permission()` cho đến khi có sub-action distinction (ví dụ: "view limits" vs "edit limits", hay "set" vs "approve"). Nếu mechanically thêm `branch_menu_limits:write` permission key thì sẽ create bool gate identical với role list — zero functional gain, nhưng cost: catalog +1 entry, role_templates upsert, backfill staff_permissions cho all existing branch_manager/cashier/chef users (miss = lost POS access mid-shift). Defer cho đến khi business case justifies finer-grained split. Tradeoff hiện tại: 1h stale-revoke window cho daily-limit writes (ai bị demote khỏi role vẫn write được đến token refresh). Acceptable vì daily limits là branch ops control panel (POS workflow), không phải financial. Detect: nếu add column "approve_quota" hay role split → revisit. Caught by: 4-agent debate 2026-05-07 (architect+critic+PM all agree defer).
- [2026-05-07] PROFILES-POSITION-ID-MUST-NOT-NULL — `profiles.position_id` PHẢI NOT NULL. `has_permission()` / `has_permission_any()` / `auth_is_owner()` kiểm tra Owner qua INNER JOIN `positions`; nếu `position_id` NULL thì Owner bị fail closed. `handle_new_user` PHẢI yêu cầu `tenant_id` + `position_code`, resolve trực tiếp active position và RAISE khi thiếu/không khớp; tuyệt đối không có default Owner hay sentinel position. `update_staff_profile` phải có cùng guard trước UPDATE. FK là `ON DELETE RESTRICT`; xóa position khi còn profile được tham chiếu phải bị block với SQLSTATE 23503. Detect: `SELECT count(*) FROM profiles WHERE position_id IS NULL` MUST 0; `pg_get_constraintdef(profiles_position_id_fkey)` MUST contain `ON DELETE RESTRICT`. Sibling rule: PLPGSQL-RECORD-IS-NOT-NULL.
- [2026-05-07] LOGIN-MESSAGE-MUST-BE-GENERIC — `apps/web/app/(public)/(auth)/login/actions.ts` PHẢI return cùng generic message `"Email hoặc mật khẩu không đúng"` cho mọi failure mode sau Zod validate: wrong creds, no session post-signIn, no claims (hook không emit). Distinguishable copy (cũ: `"Tài khoản chưa được phân quyền. Liên hệ quản lý."`) leak credential validity → user enumeration oracle cho brute force. Khi `extractClaimsFromAccessToken` returns null hoặc `getSession` returns no session, MUST `await supabase.auth.signOut()` TRƯỚC khi return error — `signInWithPassword` đã set session cookie; thiếu signOut → user vào proxy bounce loop (`/access-denied?reason=missing-auth-context`). Pre-auth events (rate-limit fail-open) KHÔNG được dùng `log_audit` RPC: caller anonymous (auth.uid() NULL) → RPC raise `insufficient_privilege` (xem AUDIT-LOG-INSERT-RPC-ONLY). MVP dùng structured `console.error` với event name `auth.login.*` + Vercel log drain; persistent `security_events` table là follow-up wave. Detect: review code login + grep `apps/web/app/(public)/(auth)` cho hardcoded distinguishable copy. Caught by: 4-agent security audit 2026-05-07 (security-reviewer H1, architect M3 quality gate).
- [2026-05-07] POS-RESUME-MUST-REFETCH — POS/KDS poll-gated hook (`use-order-sync`, `use-kds-realtime`, `printer-status-badge`) PHẢI register `visibilitychange → "visible"` listener fire immediate refresh, song song với existing `visibilityState === "hidden" return` gate trong setInterval. iOS Safari kill WebSocket sau ~30s background → realtime events fired khi hidden có thể drop → reconnect KHÔNG replay. Pause poll mà thiếu resume-refetch = miss VietQR webhook → cashier thấy stale "chưa thanh toán" → click Cash confirm → POS-PAYMENT-REUSE-UNIQUE-SLOT replay. Pattern reference: `apps/web/app/_hooks/use-notifications.ts:131-139`. Detect: mọi `setInterval` trong `apps/web/app/(protected)/br/[branchId]/{pos,kds}/**` có hidden-gate MUST có matching `addEventListener("visibilitychange", ...)` resume-refetch. Caught by: 4-agent debate POS perf optimization 2026-05-07 (Critic) addressing incident class 2026-04-24 17:37 (12× duplicate-key burning 7 payment IDs).
- [2026-05-07] UI-PERMISSION-FLAGS-THREADED-NOT-SERVER-ONLY — Mọi destructive/financial action UI (close-shift, refund-approve, cancel-invoice, correct-payment-method, approve-expense, period-close) PHẢI có `can*` boolean prop fetched ở page.tsx (server) và threaded qua component tree đến button render. KHÔNG fallback rejection 42501 từ RPC. Detect: code-review checklist — `<Button onClick={...}>` cho destructive action MUST có `disabled={!can*}` hoặc tương đương. Mở rộng rule POS-CLOSE-SHIFT-UI-GATED (2026-04-25) và POS-CONFIRM-CASH-GATED (2026-05-02) sang Admin/Finance/HR. Caught by: pre-rebuild contract.
- [2026-05-07] UI-AUDIT-LOG-EXPLICIT-COLUMNS-IN-UI — Mở rộng `AUDIT-LOG-SELECT-EXPLICIT-COLUMNS` (2026-04-28) sang UI: mọi component đọc `audit_logs` trong `Lịch sử` tab của detail page PHẢI select explicit cols `id, action, entity_type, entity_id, user_id, created_at` qua RPC helper, không `.select("*")` hoặc include `ip_address`, `old_data`, `new_data` blobs. Detect: `grep -rnE 'audit_logs.*select' apps/web/app` mọi caller phải dùng explicit list. Caught by: pre-rebuild audit trên `_lib/audit.ts`.
- [2026-05-07] UI-TOAST-VIA-SONNER-NEVER-URL-FLASH — Non-auth success/error feedback PHẢI qua `toast.*` từ `@comtammatu/ui/components/sonner`. URL flash params (`?success=`, `?error=`) chỉ dùng cho permission/auth/scope failure → `/access-denied?reason=`. Detect: `grep -rn 'redirect.*\?error=' apps/web/app` MUST 0 (đã đạt — verified 2026-06-10). Caught by: BA audit 2026-05-07.
- [2026-05-07] UI-FORM-VIA-FORMDIALOG-FOR-CRUD — Form CRUD có line array hoặc 4+ field PHẢI dùng `FormDialog` từ `apps/web/app/components/form/form-dialog.tsx` với Zod 4 `{ error: ... }`. Plain `<form action>` chỉ cho login, sign out, single-reason confirm. Long-form line-array không vào Dialog (dùng Page hoặc Sheet — overlay decision trong `docs/modules/ui.md`). Detect: review checklist — file dùng `<Dialog>` + `useForm` + `zodResolver` mà không dùng `FormDialog` flag là vi phạm.
- [2026-05-07] UI-EMPTY-STATE-VIA-APP-EMPTY-STATE — Mọi empty/no-result/no-access/error UI ở app surface phải dùng `AppEmptyState` từ `surface.tsx` hoặc `TableEmptyStateRow` trong `Table`. Raw `Empty*` primitives chỉ được dùng trong approved wrappers (`surface.tsx`, employee surface layer) hoặc public shell đã allowlist; route code không import `@comtammatu/ui/components/empty` trực tiếp. Cấm pattern `<Empty className="border bg-card py-X">` hand-rolled. Detect: `pnpm lint:ui-contract` enforces `raw-empty-import-route-code`; targeted check `rg -n '@comtammatu/ui/components/empty' apps/web/app` chỉ còn approved allowlist. Caught by: BA audit 2026-05-07; tightened by UI/UX debt audit 2026-06-17.
- [2026-07-06] OPERATOR-NO-ADMIN-DASHBOARD-SHELL — Branch runtime, Operations, and employee-lib screens MUST NOT import/render control_surface chrome (`AppShell`, `ControlSurfaceShell`, `ManagementShell`, retired `OwnerModuleShell` / `InventoryShell` / `FinanceShell`, `resolveControlSurface*` / `resolveOwner*`, `control-surface-nav`, retired `owner-nav`) or link/redirect operator work into control_surface route roots (`/`, `/finance`, `/inventory`, `/menu`, `/orders`, `/branches`, `/hr`). Use the approved operator layout, shared `AppHeader` / `AppBottomNav`, `EmployeePage`, or `embedded` canonical `PageContent` branches instead. Detect: `pnpm lint:ui-contract` gates `operator-owner-shell-boundary` and `operator-owner-route-boundary`. Caught by: mobile/tablet operator-layout hardening request 2026-07-06; Wave2 + nav rename 2026-07-29.
- [2026-07-06] OPERATOR-EMBEDDED-NO-PAGE-HEADER — A canonical `PageContent` / client component mounted with `embedded` under `/br/[branchId]/(operator)` MUST NOT pass `AppPageHeader` through the shared `content` block. Branch runtime already owns the page header; embedded content renders the tabs/body/actions directly, while Owner surface keeps `AppPageHeader` behind `!embedded` or wraps only the Owner surface branch. Detect: `pnpm lint:ui-contract` gate `operator-embedded-page-header-boundary` scans embedded-mounted inventory/order clients and fails shared `content` `AppPageHeader` leaks. Caught by: mobile/tablet operator-layout hardening request 2026-07-06.
- [2026-05-07] UI-LOADER-VIA-SPINNER-PRIMITIVE — Cấm `Loader2 + animate-spin` ngoài `packages/ui/src/components/spinner.tsx`. Loading indicator phải dùng `Spinner` primitive. Detect: `grep -rn 'Loader2\|animate-spin' apps/web/app` MUST 0 trong app code. Caught by: BA audit 2026-05-07. Đạt 0 — verified 2026-06-13.
- [2026-05-07] UI-NO-RAW-TABLE-OUTSIDE-PRIMITIVE — Cấm element `<table>` raw ngoài `packages/ui/src/components/table.tsx` và `apps/web/app/components/data-table/`. Mọi tabular data phải dùng `Table` primitive hoặc `DataTable` adapter. Detect: `grep -rnE '<table\s' apps/web/app` MUST 0 kết quả. Caught by: design-system audit 2026-05.
- [2026-05-07] UI-PAGE-HEADER-VIA-APP-PAGE-HEADER — Mọi `app/**/page.tsx` và `*-client.tsx` không phải `(public)/(auth)/login`, `(public)/access-denied`, `(public)/payment/momo/return` PHẢI dùng `AppPageHeader` từ `apps/web/app/components/surface.tsx`. KHÔNG được tự dựng `<header><h1>` hoặc `<h1 className="font-heading text-2xl/3xl">`. Detect: `grep -rln 'font-heading text-2xl\|font-heading text-3xl' apps/web/app | grep -v "components/\(surface\|app-shell\)"`. Caught by: BA audit 2026-05-07 — chỉ 5/114 pages dùng adapter.
- [2026-07-29] PAGE-HEADER-NO-MODULE-EYEBROW — On control_surface Owner routes under `AppShell`, `AppPageHeader.eyebrow` MUST NOT repeat the sidebar module label or a marketing synonym (`messages.inventory.shell.moduleName`, literal `Kho hàng`, `INVENTORY_VI.warehouse`, `MENU_VI.eyebrow`, `ORDERS_COPY.eyebrow`, HR workspace module labels, and similar). Sidebar + deep-nav own module context; title is the page job. Contextual eyebrows (e.g. inventory dashboard site-kind) stay allowlisted. Detect: `pnpm lint:ui-contract` gate `owner-page-header-no-module-eyebrow`. Caught by: Owner right-panel header redundancy cleanup 2026-07-29.
- [2026-04-29] AUDIT-NEVER-LOG-CREDENTIALS — Every `log_audit()` / `logAudit()` call carrying any payload with secret fields (`apiKey`, `secretKey`, `accessKey`, `password`, `token`, `webhook_secret`, `signing_key`, top-level `credentials`) MUST run `oldData` and `newData` through `redactCredentials()` from `@comtammatu/shared/utils/redact-credentials` before passing to the audit helper. Otherwise raw secrets land in `audit_logs.new_data` JSONB and become readable by anyone with `finance:view`. The utility uses an exact case-insensitive key allowlist — substring matches are intentionally avoided so harmless fields like `partnerCode` (merchant ID, not a secret) are NOT redacted. Detect via grep for any `logAudit({` call site that touches `provider_configs` / payments credentials and verify `redactCredentials` is invoked first. Caught by: D011/m4 4-agent debate (QA finding), 2026-04-29.
- [2026-04-29] WEBHOOK-MUST-IDEMPOTENT — Every payment provider webhook handler MUST INSERT into `public.webhook_events(provider, request_id, ...)` before calling any payment-state RPC. The `UNIQUE (provider, request_id)` constraint converts duplicate deliveries / replay into ON CONFLICT no-ops, so payment/order side effects cannot run twice. Detect via grep `from("payments").update` or payment completion RPC calls inside `apps/web/app/api/webhooks/*/route.ts` without a preceding `webhook_events` insert. Caught by: pre-pilot security review 2026-04-27.
- [2026-04-29] WEBHOOK-MUST-BIND-TENANT — Every payment webhook MUST resolve `tenant_id` from a SIGNED, server-validated source (HMAC-verified payload + decoded `extraData` JSON), not from the `provider_ref` lookup alone. The `payments` SELECT MUST include `.eq("tenant_id", extracted_tenant_id)` so a leaked partner secret in tenant A cannot be replayed against a forged `provider_ref` in tenant B. Detect via grep `from("payments")` in webhook routes for any `.eq("provider_ref", ...)` without `.eq("tenant_id", ...)`. Caught by: pre-pilot security review 2026-04-27 (M4 P0-2).
- [2026-07-03] SEPAY-WEBHOOK-RETRY-RECEIPT-PRINT — `confirm_sepay_payment` MUST enqueue the receipt fail-soft not only on fresh completion/cash→VietQR correction, but also on the VietQR `already_completed` branch. A webhook can arrive after another completion path or after a prior print enqueue failed; returning `already_completed` with `print.failed=false` while no receipt job exists leaves POS showing paid with no payment receipt. `enqueue_receipt_print` is the idempotency boundary: service-role calls may insert/retry missing/failed/expired jobs but MUST NOT flip an already-`printed` receipt job back to `pending`. Detect: active migration replacing `confirm_sepay_payment` includes `enqueue_receipt_print` inside the `already_completed` return branch, and `enqueue_receipt_print` keeps `WHEN public.print_jobs.status = 'printed' AND NOT v_is_service THEN 'pending'`. Caught by: SePay webhook paid-without-receipt production samples 2026-07-03.
- [2026-04-29] PAYMENT-AMOUNT-MUST-RECOMPUTE-SERVER — `confirm_cash_payment` and `complete_payment_and_consume_stock` MUST recompute the order total inside the RPC from `order_items.qty * order_items.price_at_order ± discount + service + tax` and reject when `abs(amount - recomputed) > 1 ₫` with code `amount_mismatch_recomputed`. Comp / staff-meal orders bypass via `is_comp = true`. The protection sits AT payment time inside SECURITY DEFINER so no client-side amount tampering wins. Detect via grep payment RPCs for absence of `SUM(qty * price_at_order)` recompute. Caught by: pre-pilot security review 2026-04-27 (M4 P0-4). **[2026-05-28 POLICY OVERRIDE — không trừ kho]** Amount-recompute invariant vẫn ĐỨNG NGUYÊN — owner's "không trừ kho" policy chỉ tắt stock consumption, không tắt amount-recompute. `amount_mismatch_recomputed` sentinel vẫn được preserve trong `confirmCashPayment` + `createPayment` migrated handlers (batch 4b + 5a). Webhook stock leg đã disabled (`20260611001000`); RPC `complete_payment_and_consume_stock` vẫn giữ amount-recompute step. Xem `docs/plan/decisions.md` D016.
- [2026-04-29] REFUND-MUST-CHECK-PAYMENT-COMPLETED — `create_refund` (and the action wrapper `createRefund`) MUST verify `payment.status = 'completed'` BEFORE inserting the refund row. Refunds against `pending` or `failed` payments allow negative-amount injection or refund-of-nothing. The RPC is the source of truth. Detect via grep `create_refund` or `createRefund` for the precondition. Caught by: pre-pilot security review 2026-04-27 (M4 P0-5).
- [2026-04-29] REFUND-MUST-REVERSE-ATOMICALLY — Refund approval MUST go through the `reverse_payment_and_post(p_refund_id)` RPC. The action `approveRefund` MUST NOT directly UPDATE `payments.status`, `refunds`, `orders`, stock movements, or audit rows. Either ALL of {payments/refunds state flip, eligible stock restore, order payment-status invariant, audit row} land in one transaction, or none do. Detect via grep `approveRefund` for direct `.update("payments")`, `.update("refunds")`, or `.insert("stock_movements")` outside the RPC call. Caught by: pre-pilot security review 2026-04-27 (M4 P0-1), updated for D020 operating-finance scope.
- [2026-04-23] INVENTORY-BRANCH-FILTER-URL-ONLY — Inventory scope ALWAYS flows through URL `?branchId=` (see `apps/web/app/(protected)/inventory/_lib/inventory-scope.ts#resolveInventoryBranchScope`). Never persist selection to localStorage, cookies, or React context. Server Actions that mutate branch-scoped data MUST take an explicit `branchId` input and re-validate it against the resolver's `allowedBranches` — never trust URL params as a write target. Multi-item RPCs (GRN confirm, transfer commit, stocktake post, production yield) MUST receive a concrete `branchId`; never pass "all" or null. Reports and dashboard aggregation are the ONLY pages allowed to ignore the param.
- [2026-04-23] PO-STATUS-DERIVATION-IN-RPC — Purchase-order status derivation (`sent` → `partially_received` / `received`) triggered by GRN confirmation MUST live inside `confirm_goods_receipt_note` RPC, locked via `SELECT … FROM purchase_orders … FOR UPDATE`. Never re-introduce app-layer computation (pre-fetch `po_id` + read siblings + `UPDATE purchase_orders`): two concurrent confirms against the same PO race and can clobber each other's status. Derive status from ordered vs confirmed accepted quantities; do not apply a tolerance or persist a manual `accept_and_close` decision. The RPC returns `{grn_id, status, po_id, po_status}` — callers read `po_id` from the response for revalidatePath.
- [2026-04-22] DESIGN-SYSTEM-CONTRACT-FIRST — Before any UI/UX rebuild, read `docs/spec/design-system.md` and `docs/modules/ui.md`, then state the surface, primary user job, route family, change type, and Má Tư DS primitives used. Never start from a new theme, fake primitive, route-specific visual system, or vocabulary source.
- [2026-04-23] PLPGSQL-RECORD-IS-NOT-NULL — `IF record IS NOT NULL` in PL/pgSQL returns TRUE **only when every column is non-null**, not when a row was found. Tenant-level users with `branch_id IS NULL` silently fell out of the JWT hook because one nullable column tripped the check. Check a guaranteed-non-null column (`record.tenant_id IS NOT NULL`) or use `FOUND` after the SELECT. Caught by tenant-level login returning empty `app_metadata` on 2026-04-23.
- [2026-04-22] JWT-CLAIMS-NOT-IN-APP-METADATA — `session.user.app_metadata` (supabase-js) reads from the `auth.users` row, NOT from JWT claims. Hook-injected fields (`user_role`, `position`) live inside the JWT payload only. Use `extractClaimsFromAccessToken(session.access_token)` when claims include hook-added fields; `extractClaims(user.app_metadata)` still works but only returns DB-mirrored fields (`tenant_id`, `branch_id`, `role`). Caught by: `position` claim missing from UI on 2026-04-22 despite hook emitting it correctly.
- [2026-04-22] RLS-PERMISSIVE-POLICIES-OR — PostgreSQL RLS combines multiple PERMISSIVE policies on the same command with **OR**. Two permissive SELECT policies `(user_id = auth.uid())` and `(tenant_id = X)` together let anyone in the tenant see every row, not just their own. Either collapse to a single policy with the combined predicate, or mark one policy `AS RESTRICTIVE`. When writing self-scope + admin-scope policies, always gate the admin policy behind a permission check (e.g. `has_permission(NULL, 'staff:assign_permission')`). Caught by: cashier seeing 192 tenant-wide + 61 branch grants at `/employee/permissions` on 2026-04-22.
- [2026-04-02] TYPE-STUB-BEFORE-CODE — When adding a new DB table, check `database.types.ts` first. If the table isn't in the generated types, add a type stub BEFORE writing any code that calls `.from("new_table")`. Otherwise typecheck passes locally but build fails.
- [2026-04-14] NO-SURFACE-THEME-IMPORTS — Never import per-domain `theme.css` files to restyle a surface. Route all surface differentiation through shared tokens, utilities, and recipe variants instead.
- [2026-04-14] NO-STATIC-UI-INLINE-STYLES — Never add static `style={{ ... }}` blocks to foundation, shell, auth, or mobile chrome files. Move static presentation into `globals.css`, shared utilities, or recipe helpers. Inline style is allowed only for truly dynamic runtime values.
- [2026-04-14] BLOCKED-STATE-UI-IS-PRESENTATION-ONLY — Shared blocked-state helpers may parse `forbidden` / `reason` and render copy, but they must never become a second auth policy layer or change redirect targets.
- [2026-04-15] TERMINOLOGY-SOURCE-OF-TRUTH — Never introduce new domain/UI wording by ad-hoc hardcoded copy when a canonical term already exists. The copy-source ladder (which source owns which kind of copy) lives in `docs/ref/glossary.md` §"Copy source ladder"; route new/changed copy through the right source first (or in the same PR), then run `pnpm lint:copy` — enforced: it scans content for the drift-term denylist and fails on any match. Operator UI defaults to Vietnamese `long`/`short`; whitelist acronyms (`PO`, `GRN`, …) may appear only as standalone pill/badge labels, never embedded in a Vietnamese sentence and never as bilingual glosses (`Phiếu nhập (GRN)`, `Yield`, `ad-hoc`, `Online`/`Offline`, `Topping`). Caught by: copy/terminology drift audit 2026-04-15; extended 2026-07-27.
- [2026-04-16] PRIMITIVE-FIRST-UI — After `docs/spec/design-system.md` selects the pattern, use the current Má Tư DS primitive layer as the first implementation baseline. Never add UI rules, wrappers, docs, or utilities that outrun the current primitive layer, and never use runtime evidence to override the Com Tam Ma Tu Custom Theme contract. The project runtime evidence lives in `packages/ui/src/styles/globals.css`, `apps/web/app/layout.tsx`, and `packages/ui/src/components/*`, and MUST conform to `docs/spec/design-system.md`.
- [2026-04-16] NO-PRIMITIVE-DESIGN-OVERRIDE — Never redefine the visual contract of core primitives like `button`, `table`, `sidebar`, `card`, or `badge` through page-level wrappers or docs. Reuse the current Má Tư DS primitives instead of creating a parallel design system.
- [2026-04-16] DOCS-MUST-MATCH-RUNTIME — UI docs must describe the actual runtime configuration. When changing `globals.css`, `layout.tsx`, or `packages/ui/src/components/*`, update `docs/spec/design-system.md` and `docs/modules/ui.md` in the same change so docs do not drift from the live primitive layer.
- [2026-04-16] NO-RETIRED-APP-HELPERS — Do not keep or introduce retired helper classes such as `app-*`, `safe-top`, or `safe-bottom` in runtime UI. Replace them with direct Má Tư DS primitive composition and standard utility classes.
- [2026-04-16] NO-FAKE-PRIMITIVES — Never use raw `div` / `span` / `p` markup to imitate current primitives such as `Card`, `Badge`, `Button`, `Table`, `Tabs`, `Input`, or `Select`. If the UI reads like an existing primitive, use that primitive directly instead of hand-styling a clone.
- [2026-05-05] APP-SURFACE-ADAPTER-FIRST — Repeated app-level page/header/section/toolbar/empty-state/link-card patterns MUST go through `apps/web/app/components/surface.tsx`. Domain wrappers (Inventory, Employee, Admin, POS/KDS where appropriate) may keep their public API, but they must delegate to the app surface adapters instead of cloning `Card` chrome, empty-state markup, page width containers, or filter toolbars. If a new layout pattern is genuinely missing, add it to `surface.tsx` and update `docs/spec/design-system.md` / `docs/modules/ui.md` in the same change before broad rollout.
- [2026-07-29] OWNER-SHELL-STICKY-FILTER-TOP-PAD — Owner LIST sticky filters (`AppStickyFilterChrome` / `APP_PAGE_STICKY_FILTER_CLASSNAME`) MUST cancel shell `pt-3 md:pt-4` via negative sticky `top` (`top-[-0.75rem] md:top-[-1rem]`). Plain `top-0` pins to the scrollport _content_ edge below the pad, so rows scroll into the pad gap and appear above the filter. Do not use stuck-state `-mt` for the same job — it flickers the IntersectionObserver. Horizontal flush still uses stuck-state `-mx` bleed. Locked by `owner-shell-responsive-static.test.ts` + `e2e/accessibility/owner-shell-scroll.spec.ts` flush assertion.
- [2026-04-24] RLS-NOT-APPLIED-ON-MV — Postgres RLS on the base table is NOT inherited by materialized views. Inventory/Finance materialized views bypass tenant/branch RLS if queried directly by role `authenticated`. Always `REVOKE ALL ... FROM authenticated` on the MV and expose access only through a `SECURITY DEFINER` function that re-checks `tenant_id`, `branch_id`, and `has_permission(branch, key)` in SQL. Caught during inventory redesign workshop 2026-04-24 before any leak.
- [2026-04-24] SHIFT-KEY-CANONICAL — Inventory / waste anti-splitting uses the canonical string `YYYY-MM-DD_{morning|afternoon|evening}` computed against `branches.timezone` with business-day cutoff 04:00 local. Never use `pos_sessions.id`, raw `created_at`, or `date_trunc('day', now())` — those drift with timezone and don't handle overnight shifts. Helper `inventory_shift_key(branch_id, at timestamptz)` is the single source of truth.
- [2026-04-24] PERIOD-CLOSE-SOFT-HARD — Inventory mutations (waste, adjustment, stocktake finalize) respect `accounting_periods` two-stage close: soft-close day 5 of next month (back-dated posts auto-shift to current period with `originally_dated_in_prior_period=true` flag); hard-close day 15 (reject any back-dated write, `period_reopen` permission + 2FA required to override). Never silently post to a closed period — trigger `enforce_period_close()` on all write paths. Never add `ON CONFLICT DO NOTHING` to bypass the trigger.
- [2026-04-24] OFFLINE-NO-SILENT-CLIENTWINS — Offline stocktake sync conflicts MUST be materialized as `stocktake_conflicts` rows and resolved by QLV through the queue UI. Never auto-merge with "client-wins" or "last-write-wins" silently — stocktake adjustments drive accounting and shrink detection, and silent overwrite of a legitimate count destroys audit trail. Server validates `offline_created_at` within `[session_opened_at, now()]` window to block clock tamper.
- [2026-04-24] STOCKTAKE-BLIND-STRIP-SERVER-SIDE — Blind-mode counter UI MUST fetch lines through `get_stocktake_lines_blind` (SECURITY DEFINER) — never through `.from("stocktake_lines").select(...)`. The RPC strips `system_quantity` server-side before the payload crosses the RLS boundary. Client code must not re-query the base table or reference a `system_quantity` field — if the network payload contains that key, blind mode is broken and the whole monthly/quarterly/spot integrity chain collapses. Reference: S13a Day 4 count page `apps/web/app/(protected)/inventory/stocktake/[id]/count/page.tsx`.
- [2026-04-24] DRAFT-AUTOSAVE-CASCADE — Stocktake draft auto-save (`stocktake_drafts`) must CASCADE-delete when the parent `stocktake_sessions` row is removed (migration `20260425180000_s13a_stocktake_drafts.sql` uses `ON DELETE CASCADE`). Never replace with `ON DELETE SET NULL` or a manual trigger — stale drafts attach to session ids that get reused by identity sequence reset, silently feeding old counts into a new session. Debounce stays at 30 s client-side (spec §Q2); re-saves skip when serialized counts match previous save to avoid write amplification.
- [2026-04-24] ZONE-LOCK-HEARTBEAT-THIRD-TTL — `heartbeat_zone_lock` must fire at ≤ 1/3 of the TTL window, not at the TTL itself. Default TTL 1800 s → heartbeat every 600 s. Firing at or near TTL means any single network hiccup drops the lock. `ZoneLockIndicator` implements this; `onLost` callback flips the page into read-only mode — never silently retry as the holder after losing ownership.
- [2026-04-24] PAYMENT-AUTO-COMPLETES-ORDER — POS payment confirm RPCs (`confirm_payment_and_post`, `complete_payment_and_consume_stock`, `create_payment` cash branch) MUST call `finalize_paid_order(order_id, actor_id)` after `payment_status='paid'` so `orders.status='completed'` and trigger `trg_release_table_on_order_status` releases the table. Semantic: `orders.status='completed'` means "paid + closed from POS view" — NOT "kitchen done". Helper MUST NOT force `order_items.status` or `kds_tickets.status` — chef keeps cooking via KDS after payment so pay-first-cook-after (takeaway rush, customer in a hurry) still works. Bill sheet UI MUST call `onClose()` after confirm success; never re-enable the old "Hoàn tất và trả bàn" button that required `status='served'` — completion is automatic now. When adding new payment RPCs, always `PERFORM public.finalize_paid_order(order_id, actor_id)` as the last step. Reference: `supabase/migration-archive/20260426030000_auto_complete_paid_order.sql`. **[2026-05-28 POLICY NOTE — không trừ kho]** Auto-complete invariant vẫn ĐỨNG NGUYÊN; "không trừ kho" policy chỉ tắt stock consumption leg, không tắt `finalize_paid_order` call. RPC `complete_payment_and_consume_stock` vẫn phải gọi `finalize_paid_order` ngay cả khi stock leg bị disabled. Xem `docs/plan/decisions.md` D016.
- [2026-04-24] POS-SERVED-NOT-TABLE-TERMINAL — `orders.status='served'` is a fulfillment/service marker only and MUST NOT release a dine-in table. `trg_release_table_on_order_status` terminal set is `completed/cancelled`; active-count must also treat `served` as active until payment closes the order. POS UI MUST NOT show "Trả bàn" or "Hoàn tất và trả bàn" as a normal cashier step. Reference: `supabase/migration-archive/20260426040000_pos_payment_close_table_release.sql`.
- [2026-05-02] POS-SERVED-REQUIRES-ITEMS-READY — POS cashier UI MUST NOT expose an order-level "Phục vụ" action. `update_pos_order_status('served')` is retained only as a guarded compatibility/bulk-status RPC and MUST refuse to flip the order while any active `order_items.status` is still `pending`/`preparing` (raises `22023 items not terminal`, same predicate as the `complete` branch). Just as payment must NOT force `kds_tickets='served'`, any internal order-level served path must not erase in-flight tickets the chef is still cooking. The post-guard UPDATEs only flip `status='ready' → 'served'`; `served` and `cancelled` rows are preserved. Per-item served continues via `mark_order_item_served`. When adding any new bulk-status RPC that touches `kds_tickets`, replicate the `items NOT IN ('ready','served','cancelled')` guard. Reference: `supabase/migration-archive/20260502000100_pos_served_requires_items_ready.sql`.
- [2026-05-02] POS-MOBILE-BUTTON-TOUCH-TARGET — POS surfaces that show in mobile/touch context (mobile action bar, order-item actions sheet, cart pane primary CTA, item customizer qty +/- and main CTA, bill-receipt sheet primary CTA) MUST have `min-height ≥ 44px` and `min-width ≥ 44px` per Apple HIG / Google MD3. Default Má Tư DS button sizes top out at h-9 = 36px (size="lg") / size-9 = 36px (size="icon-lg"); they DO NOT meet this bar. Apply `min-h-11` (44px) override on lg buttons and `size-11` on icon buttons in mobile-touch contexts. Existing compliant patterns: `min-h-14 min-w-14` (mobile action bar), `min-h-12 w-full` (order detail primary CTA), `min-h-11 min-w-11 size-9` (cart pane delete). NEVER replace size primitive with raw inline `h-X` Tailwind — combine `size="lg"` (gives semantic font/padding) with explicit `min-h-11` override. When adding a new POS button on a surface that opens in a mobile sheet/drawer/action-bar, audit the rendered height before merging. Reference: `apps/web/app/(protected)/br/[branchId]/pos/_components/order-detail/order-item-actions-sheet.tsx`, `apps/web/app/(protected)/br/[branchId]/pos/item-customizer.tsx`.
- [2026-05-02] POS-KDS-RPC-SERVER-SIDE-ROLE-GATE — Every write-path RPC under POS/KDS (`void_order_item`, `cancel_order`, `edit_pending_order_item`, `update_pos_order_status`, `mark_order_item_served`, `bump_kds_ticket`, `recall_kds_ticket`, `transfer_order_table`, `reduce_order_item_quantity`, `merge_orders`, `split_order`, `confirm_payment_and_post`, etc) MUST gate authority server-side, never trust the UI button-disable. Preferred pattern is canonical permission check (`NOT has_permission_any('kds:recall') → 42501 not_allowed`) after `auth.uid() IS NULL → 28000`; role-set checks are allowed only as scope/side guards and must use canonical application roles. Cashier without `kds:recall` calling `recall_kds_ticket` from devtools MUST get `42501 not_allowed`. When adding any new POS/KDS write RPC: add auth context first, permission second, before any state read or lock. UI `canRecall`/`canVoid`/etc gates are convenience only — defense in depth requires the RPC itself to refuse.
- [2026-05-03] DAILY-LIMIT-REDUCE-QTY-DECREMENT — `reduce_order_item_quantity` MUST explicitly decrement `branch_menu_item_daily_limits.sold_today` by the qty delta (`v_old_qty - p_new_quantity`) for the (branch_id, menu_item_id, today) row, INSIDE the same transaction as the `order_items` UPDATE. The enforce trigger is INSERT-only and the cancel trigger fires only on `status='cancelled'` — a pure quantity reduction (5 → 2) bypasses both, leaving 3 freed portions locked in `sold_today` until end of day. Use `GREATEST(sold_today - delta, 0)` to defend against any race producing transient negative (the column has CHECK >= 0 anyway). Skip the decrement when `v_item.menu_item_id IS NULL`. When introducing any other RPC that reduces (vs cancels) committed quantities, replicate this decrement. Reference: `supabase/migration-archive/20260503000100_reduce_qty_decrement_daily_limit.sql`.
- [2026-05-03] KDS-TRANSFER-TABLE-SYNC — KDS hook (`use-kds-realtime.ts`) MUST subscribe to `orders` UPDATE with `branch_id=eq.X` filter AND re-fetch order info when `payload.old.table_id !== payload.new.table_id` AND the order is already in the client's `ordersRef` cache. Without it, `transfer_order_table` updates `orders.table_id` but the kds_tickets channel sees nothing (it's keyed on tickets, not orders) and the OrderCard renders the stale `Bàn N` indefinitely — chef bưng nhầm bàn. The double-filter (table_id-actually-changed + cached-order) avoids re-fetching on every order mutation (status/total/payment); only the genuinely-relevant subset triggers a network call. orders is in `supabase_realtime` with REPLICA IDENTITY FULL (migration 20260425024802) so `payload.old.table_id` is reliable. When adding a new RPC that relocates kitchen work between physical contexts (table, station, branch), audit whether the KDS hook needs to react.
- [2026-05-03] POS-DETAIL-REOPEN-AFTER-CUSTOMIZER — When cashier taps "Sửa món" inside `OrderDetailSheet`, the parent (pos-desktop-shell) closes the detail (mobile sheet stacking / focus-trap fight) AND opens the customizer. The detail close MUST snapshot `{id, orderNumber, summary}` into `pendingDetailReopenRef` BEFORE clearing — both `ItemCustomizer.onClose` (cancel) and the edit-sent confirm success branch route through `closeCustomizerAndMaybeReopenDetail`, which restores the detail by inlining the openDetail body (refetches the freshly-edited order; do not pass the stale pre-edit seed). Without this snapshot, every edit drops cashier to the menu — they must re-tap the order to verify the price changed. When introducing another flow that closes a detail/cart sheet to open a customizer, route teardown through this same wrapper so the reopen contract stays single-sourced. Reference: `apps/web/app/(protected)/br/[branchId]/pos/pos-desktop-shell.tsx`.
- [2026-05-03] REALTIME-CHANNEL-RESUBSCRIBE-ON-TOKEN-REFRESH — `useRealtimeChannel` MUST listen to `supabase.auth.onAuthStateChange` and tear down + resubscribe on `TOKEN_REFRESHED` and `SIGNED_IN`. Reason: the per-channel `realtime.subscription.claims_role` is stamped at subscribe time and not re-evaluated when the JWT rotates (~1h default). After rotation, RLS-protected events stop reaching the channel SILENTLY — no error, just dead subscription. POS/KDS shifts span 8h+ so token rotates ≥1× during a session; previously, the second half of the shift would not see realtime broadcasts. The teardown also fires on `SIGNED_OUT` to release server-side state. Always pair this with explicit `realtime.setAuth(token)` BEFORE re-subscribe (defense-in-depth on the JOIN frame). When auditing realtime hooks, verify they go through `useRealtimeChannel` rather than calling `supabase.channel(...).subscribe()` directly. Reference: `apps/web/app/_hooks/use-realtime-channel.ts`.
- [2026-05-03] REALTIME-EVICT-CHANNEL-SYNC-AFTER-REMOVE — Inside `useRealtimeChannel`, every `removeChannel(stale)` MUST be followed by a synchronous eviction `supabase.realtime._remove(stale)` (mirrors the `_onClose` callback that `RealtimeChannel.js:90` registers). Reason: `removeChannel` is async — it awaits the server `phx_close` ACK before evicting from `socket.channels` (`RealtimeClient.js:221-229`). realtime-js's `channel(topic)` dedups by topic and returns the existing channel from that list (`RealtimeClient.js:297-308`). Any re-subscribe attempt that runs before the leave round-trip completes — auth-event race when `TOKEN_REFRESHED` fires immediately after the initial `getSession().then(subscribeWithToken)`, navigation remount, or React Strict Mode double-invoke against the singleton client returned by `@supabase/ssr`'s `createBrowserClient` — hands back the stale, already-subscribed channel. Calling `.on('postgres_changes', ...)` on it then throws `cannot add 'postgres_changes' callbacks for realtime:<topic> after subscribe()` (`RealtimeChannel.js:378-382`). The fix evicts by topic at channel-build time (the builder's `.channel(topic)` is wrapped so it force-evicts any pre-existing same-topic channel before creating a fresh one) AND in `subscribeWithToken` + cleanup. The build-time sweep is load-bearing: the instance-local teardown only evicts the channel the current hook instance created, so a stale channel left in the singleton socket by an overlapping mount/unmount, a Fast Refresh remount, or a second live subscriber is invisible to it — only the topic sweep catches it. Do not surface the internal `_remove` call to callers, and do not change the topic naming convention (filter dedup is per-purpose semantics). Tests must exercise: (a) cold mount with token-refresh-during-init, (b) navigation away→back faster than the leave RTT, (c) `SIGNED_IN`/`TOKEN_REFRESHED` mid-session. Reference: `apps/web/app/_hooks/use-realtime-channel.ts`.
- [2026-04-24] R4-ESCALATE-NOTE-MIN-CHARS — `escalate_round_4` RPC enforces `length(note) >= 20` server-side (raises `22023`). UI MUST mirror that check in both the Zod schema (`z.string().min(20, ...)`) AND the form submit guard before the action call so the counter never gets a stale 500. Drop to 10 chars or skip the note → next audit cannot reconstruct why QLV+Admin overrode R3 counts. When changing the threshold, update the RPC, the Zod schema, AND the `(N/20)` char counter label together. Reference: `escalate-client.tsx` + `supabase/migration-archive/20260425120000_s5_stocktake_blind_recount_abc.sql`.
- [2026-04-24] CONFLICT-RESOLVE-VIA-RPC-ONLY — Stocktake conflict resolution MUST go through `resolve_stocktake_conflict` RPC. Never direct-update `stocktake_lines.counted_quantity` from the queue UI to "apply client" — bypasses the conflict ledger update (`resolved_at`, `resolution`, `resolution_qty`, `resolution_note`), breaks audit trail, and lets a second QLV re-resolve the same row. The 4 resolution modes (`keep_server` / `apply_client` / `manual_value` / `reject`) are enumerated server-side; adding a 5th mode requires RPC change first, then client. `manual_value` without `p_manual_qty` raises `22023` — client must enforce before dispatch.
- [2026-04-24] FINALIZE-R1-FINAL-GATE — `finalize_stocktake` refuses to complete a session while any R1 line has `is_final=false` (raises `22023 cannot finalize: X round-1 line(s) still not final`). Never bypass by direct `UPDATE stocktake_sessions SET status='completed'` — R4 escalation is the ONLY legitimate path from R3 → R1 final. The escalate RPC writes a round-4 line AND sets the round-1 line's `is_final=true` with the escalated qty, so after all escalations the gate passes automatically. UI finalize button MUST be disabled while `needEscalation.length > 0`.
- [2026-04-24] AUTO-WASTE-NON-FATAL — POS void + KDS cancel auto-generated waste (`create_waste_from_order` RPC + `createWasteFromOrder` action) MUST NOT fail the parent void/cancel operation. The action always returns `{ success: true }`; failures surface via `data.softFailed=true` with `data.softError` message so the host shows a warning toast ("admin sẽ xử lý") while the underlying void/cancel completes. Never rewrite the action to raise / return `{ success: false }` — a cashier confirming payment void cannot be blocked because photo-EXIF validation or permission gate rejected the waste row. Pair with `showAutoWasteToast` for uniform messaging. Reference: `apps/web/app/(protected)/inventory/waste-actions.ts#createWasteFromOrder`.
- [2026-04-24] KDS-CANCEL-STAGE-STEP-5 — `KdsCancelStagePicker` ratio slider for `mid_cook` uses **step=5**, not 1. Finer steps (1%) let a malicious chef split 30% into 6×5% fragments each under the anti-split rolling-sum threshold. Step=5 makes "split-into-smaller" still register as one event in the 15-minute rolling window. When adding new stage types (e.g. `parbaked`, `par_cooked`) keep the same step. Before `after_cook` is the 100% waste; `before_cook` emits `wasteSourceType=null` so the host short-circuits the RPC entirely (no phantom 0-qty rows).
- [2026-04-24] GET-AUTH-CONTEXT-EMPTY-ARRAY-REJECTS — `getAuthContext([])` in `apps/web/app/_lib/auth.ts` rejects **every** authenticated user because `allowedRoles.includes(claims.user_role)` is false on an empty array. When an action only needs "authenticated, any role", pass `STAFF_ROLES` from `@comtammatu/shared/auth` — NOT `[]`. Symptom: correct-looking permission code but always returns `{ success: false, error: "Chưa đăng nhập" }` even for owners.
- [2026-04-24] MCP-APPLY-VS-CLI-PUSH — MCP `apply_migration` derives the migration version from wall-clock `applied_at`, while `supabase db push` uses the filename timestamp; applying the same migration through different tools drifts `schema_migrations` and breaks `db pull`/`db reset`. Production is applied owner-only via the file→PR→owner flow (no dev Supabase target currently exists). Before any DB action, resolve the target ref against the Environment Registry in `docs/agent/rules/database.md` — that registry is the SSoT and overrides any ref label elsewhere; never apply or write to a production ref without explicit in-session owner authorization.
- [2026-04-25] INVENTORY-MOVEMENT-SUBTYPE-DERIVED — `confirm_stock_issue` RPC (migration `20260426100000_stock_issue_wac_strict_and_subtype.sql`) derives `stock_movements.movement_subtype` from `stock_issues.issue_type` at confirm time. Mapping: `consumption` → `sale_consumption`; `writeoff` → `writeoff`; `other` → `other`. Never hardcode the subtype in application code or store the label as a literal on `stock_issues` — the label is presentation, derived from this column. Reports that split food-cost by category MUST read `stock_movements.movement_subtype`, not `stock_movements.type` (which stays `consumption` for every issue flavor).
- [2026-04-25] INVENTORY-WAC-STRICT-OVERRIDE — `confirm_stock_issue` MUST read `stock_levels.avg_unit_cost` under `FOR UPDATE` keyed by `(tenant_id, branch_id, location_id, ingredient_id)` at confirm time and overwrite BOTH `stock_issue_items.unit_cost` AND the inserted `stock_movements.unit_cost` with that WAC. NULL WAC (ingredient never GRN'd) MUST raise `wac_not_ready_for_<ingredient_id>` with ERRCODE 22023 — NEVER silently fall back to 0 or to caller-provided unit_cost. UI MUST NOT expose an editable `unit_cost` field on issue lines; the draft DEFAULT 0 is only a placeholder overwritten at confirm. Reason: caller-provided costs drift food-cost reports and lose the audit tie between voucher and ledger.
- [2026-04-25] INVENTORY-ISSUE-TYPE-NO-KITCHEN-USE — `stock_issues.issue_type` CHECK constraint is `consumption | writeoff | other`. Any PR reintroducing `kitchen_use` (Zod enum, UI dropdown option, SQL constraint seed, backfill) MUST be rejected at review. D091 has no Branch stock location Bếp; do not create Kho→Bếp transfers as an issue type or stock location.
- [2026-04-27] INVENTORY-CURRENT-CONTRACT — Current external purchasing is `purchase demand → supplier resolution → supplier-specific PO → delivery GRN → supplier invoice`; auto-resolve the full remaining quantity when every ingredient has exactly one active supplier, and show allocation only for multiple or missing supplier mappings. One PO can have multiple confirmed GRNs but only one active draft. Keep one warehouse per site, central-only GRN, physical rejection reason/photo, and no manual `quality_status`, price-QC, lot/HSD/temperature, promotion engine, or `accept_and_close`. New UI MUST NOT create PO from GRN or create GRN outside a PO. Detect with `apps/web/tests/inventory-current-contract-static.test.ts` and the canonical Inventory reference.
- [2026-04-25] INVENTORY-ISSUE-LABEL-BY-BRANCH-KIND — The UI label for `issue_type='consumption'` is "Tiêu hao". Dialog titles, list columns, CSV exports, and sidebar headings MUST resolve labels through the inventory dictionary/helpers instead of ad-hoc literals.
- [2026-04-25] POS-PAYMENT-REUSE-UNIQUE-SLOT — `idx_payments_order_active` is a partial UNIQUE on `payments(order_id) WHERE status != 'failed'` — at most ONE active payment per order, regardless of method. Any RPC that creates/upgrades a payment for an order (cash confirm, vietqr confirm, momo webhook) MUST first look for an existing non-failed row (`SELECT id, status FROM payments WHERE order_id=? AND status<>'failed' FOR UPDATE`) and REUSE it (UPDATE in-place) rather than blindly INSERTing — else INSERT collides with a pending row from a prior method the cashier previewed. Also: cash-specific wrappers MUST `UPDATE orders SET payment_method='cash'` themselves, because `complete_payment_and_consume_stock` is webhook-shared and (correctly) does not touch `payment_method`. Caught by: cashier tab-switch VietQR → Cash → "Đã thanh toán" → 12× `duplicate key ... idx_payments_order_active` + 7 burned payment IDs on 2026-04-24 17:37-17:39 UTC. Reference: `supabase/migration-archive/20260428200000_fix_confirm_cash_payment_method_swap.sql`.
- [2026-04-25] POS-CLOSE-SHIFT-UI-GATED — "Chốt ca" button in `pos-session-header.tsx` and the F10 shortcut in `pos-desktop-shell.tsx` MUST be gated client-side by `canCloseShift` (prop threaded from `page.tsx` via `fetchPosPermissionFlags`). Server already enforces via `closePosSession` → `POS_CLOSE_SHIFT` perm, but service staff riding cashier's session would otherwise see a trigger that 403s on click. Defense in depth + clean UX. When adding new close-session entry points (dropdown menu, mobile nav), thread `canCloseShift` and render nothing when false — never fall back to server-side rejection for UI visibility decisions.
- [2026-04-25] POS-CONFIRM-CASH-GATED-BY-POS-CONFIRM-PAYMENT — `confirm_cash_payment` RPC and the `confirmCashPayment` server action (`apps/web/app/(protected)/br/[branchId]/pos/payment-actions.ts`) MUST gate by `pos:confirm_payment` — cash chạm két vật lý = single-point-of-cash accountability (cashier/branch_manager+). NEVER fall back to `pos:print` (bill/KDS print) or to the dangling `pos:payment` key (never seeded, bug source of 20260426000000). Granted templates: cashier, branch_manager, owner. NOT granted to chef/office. VietQR/MoMo (`createPayment`/`confirmPayment`) intentionally stay on `pos:use` — e-wallet không chạm két, webhook là source of truth, service staff at table can still confirm QR in the mobile-first flow. UI: `bill-receipt-sheet.tsx` filters `cash` method via `canConfirmCash` prop threaded from `page.tsx` → `fetchPosPermissionFlags` → `pos-desktop-shell.tsx`. Reference: `supabase/migration-archive/20260428210000_pos_confirm_payment_permission.sql`.
- [2026-04-25] POS-MULTI-ORDER-PER-TABLE-NEW-INTENT-EXPLICIT — POS allows multi-order per dine-in table (PR3 Gộp bàn Option A). Tap on `tables.status='occupied'` opens `MultiOrderTablePicker`; the picker is the ONLY entry point for a 2nd order on the same bàn. New-order intent on an occupied bàn MUST be tracked via the `allowOccupiedTableId` state in `pos-desktop-shell.tsx` so the auto-clear effect (line ~213, `selectedTable.status !== "available"`) does NOT reset the selection mid-flow. The flag is single-slot — one explicit-occupied selection at a time — and is cleared by an effect that fires whenever `selectedTableId` diverges from it (manual switch, post-submit reset). `selectedTableUsable = selectedTableAvailable || isExplicitOccupied` gates `orderContextReady` and `canSubmit`. NEVER re-introduce a `.eq("status","available")` block on `tables` inside `create_order` RPC — the RPC layer is multi-order-friendly today (verified [20260425020000_fix_pos_order_rpcs_auth.sql](../supabase/migration-archive/20260425020000_fix_pos_order_rpcs_auth_v2.sql) lines 99-106) and `trg_release_table_on_order_status` correctly counts active orders before releasing. `transfer_order_table` MUST also accept `tables.status IN ('available','occupied')` — blocking only `reserved`/`maintenance` — so chuyển-bàn semantics align with multi-order-per-table; rejecting transfer to an occupied bàn while ALLOWING manual creation of a 2nd order on the same bàn is incoherent UX (see migration [20260429100000_transfer_order_table_allow_occupied.sql](../supabase/migration-archive/20260429100000_transfer_order_table_allow_occupied.sql)). Reference: `apps/web/app/(protected)/br/[branchId]/pos/_components/multi-order-table-picker.tsx`.
- [2026-04-25] MV-GROUP-BY-MUST-MATCH-UNIQUE-INDEX — Materialized views with `REFRESH ... CONCURRENTLY` MUST have GROUP BY columns that are a SUBSET of the unique index. `mv_top_items` and `mv_food_cost` shipped with `GROUP BY (..., menu_item_id, oi.item_name)` but unique index keyed only `(period_start, branch_id, tenant_id, menu_item_id)`. When a menu item gets renamed (`oi.item_name` is the snapshot at order time, not the live name), two rows for the same key tuple emerge → CONCURRENTLY refresh raises `23505 duplicate key value violates unique constraint`. Result: `refresh_finance_views()` silently fails on the 2nd MV, food cost % and top-items dashboards stale forever. Fix pattern: drop the cosmetic column from GROUP BY, aggregate it via `MAX(...)` in SELECT instead. When adding a new MV with CONCURRENTLY refresh: GROUP BY ⊆ unique index columns, then add a regression test that runs `REFRESH MATERIALIZED VIEW CONCURRENTLY` after seeding rows that share the key but differ on a non-indexed column. Reference: `supabase/migration-archive/20260425030256_fix_mv_top_items_food_cost_dedup.sql`.
- [2026-04-25] HDDT-PAYMENT-FIRST-FAILSOFT-ORPHAN — POS HĐĐT issuance MUST run AFTER `confirm_cash_payment` returns success, NEVER atomic with payment, and HĐĐT failure MUST NOT roll back payment. Orchestrator `confirmCashPaymentWithInvoice` (`apps/web/app/(protected)/br/[branchId]/pos/payment-actions.ts`) calls `confirmCashPayment` first (commercial close: cash, order close, table release, and print enqueue land through the payment RPC; stock leg follows D016), then calls `createTaxInvoice` separately. If invoice fails, action returns `{ success: true, data: { ...payment, invoice: { status: 'failed', error } } }` so cashier sees "Đã thu tiền — HĐĐT chưa xuất được, lưu nháp Finance xử lý". Reasons: (a) MoMo-style async settlement makes 2PC across HTTP+DB+provider infeasible; (b) rolling back paid order because of e-invoice fault loses real cash for paper fault; (c) `tax_invoices.payment_status='paid'` precondition in `createTaxInvoice` already blocks issue-before-pay. Bill print fires inside `confirm_cash_payment` BEFORE invoice exists — receipt template intentionally has no mã CQT in v1; HĐĐT is digital record only. With real Sinvoice: keep payment-first; transition draft → signing async via Edge Function; do NOT block `confirm_cash_payment` on provider response. NEVER add `BEGIN/COMMIT` wrapping payment + invoice. Reference: `apps/web/app/(protected)/br/[branchId]/pos/payment-actions.ts#confirmCashPaymentWithInvoice` + plan synthesis 2026-04-25.
- [2026-04-25] HDDT-FORM-PAYLOAD-FREEZE-AT-CLICK — POS Bill Sheet HĐĐT form (`invoice-form-section.tsx`) MUST snapshot the invoice payload via `buildInvoicePayload(invoiceForm)` at the moment cashier clicks "Đã thanh toán", NOT inside the async `startActionTransition` callback. Otherwise: cashier types MST → clicks submit → provider call in flight → cashier edits buyerName field → second render captures stale state and async submit uses stale snapshot. Same pattern: `disabled={actionPending}` on every Input/Checkbox in the form section so the form is visually locked once submit fires. The Zod refine on server (`createInvoiceSchema` in `apps/web/app/(protected)/finance/actions.ts`) is the second line of defense — `MST → buyerName required` + MST regex `^\d{10}(-\d{3})?$` rejects garbage at action boundary. Reference: `apps/web/app/(protected)/br/[branchId]/pos/_components/bill/bill-receipt-sheet.tsx#handleConfirmPaid`.
- [2026-07-10] HDDT-SELF-ORDER-CASH-BINDS-PERSISTED-BUYER — A POS cash confirmation for an active self-order `cash_call` MUST issue HĐĐT from that request's immutable normalized `invoice_payload`, never from the cashier bill sheet's local empty/default form. The atomic DB boundary locks the order, rejects any active `vietqr_pending` request with `self_order_payment_cancel_staff_required` (staff must verify/cancel before switching method), delegates the commercial close to `confirm_cash_payment`, and returns only the exact bound request ID. After payment, the server action may load buyer data only by the full `{request, tenant, branch, order, payment, method='cash_call', status='completed'}` key; it MUST NOT expose buyer PII in the staff queue. A post-payment payload read failure remains HĐĐT-fail-soft and MUST NOT roll back cash/payment/order/table/receipt state. Detect: `apps/web/tests/self-order-cash-invoice-binding.test.ts`.
- [2026-07-28] HDDT-PROVIDER-DATA-REVIEW-MUST-NOT-RESYNC-ISSUE — `review_completed_vietqr_bank_webhook` writes `provider_data.bankWebhookReview`. The AFTER UPDATE trigger on `payments.provider_data` MUST NOT re-enter `private.sync_tax_invoice_issue_job_for_payment` when `status`, `invoiceSnapshot`, and `invoicePayload` are unchanged; otherwise already-issued invoices raise `tax_invoice_issue_active_invoice_not_draft` and roll back the review. Defense in depth: `private.upsert_tax_invoice_issue_job` returns the existing issue job (or `already_issued`) instead of raising when the active invoice is non-draft. Detect: `supabase/migrations/20260728170010_skip_tax_invoice_sync_on_non_invoice_provider_data.sql`, `apps/web/tests/payment-rpc-only-ledger.test.ts`.
- [2026-04-25] REALTIME-AWAIT-AUTH-BEFORE-SUBSCRIBE — Every Supabase Realtime subscription MUST go through `useRealtimeChannel` (`apps/web/app/_hooks/use-realtime-channel.ts`) which awaits `auth.getSession()` and pins the JWT via `realtime.setAuth()` before `.subscribe()`. Calling `supabase.channel(...).subscribe()` directly inside `useEffect` mount races against auth initialization: when subscribe wins, the broker stamps `realtime.subscription.claims_role='anon'` for that row and never re-evaluates it — even after supabase-js later pushes the access token. RLS policies that gate via `has_permission()` (which needs `auth.uid()`) drop every broadcast for that subscription forever, until the channel is removed and re-subscribed. Symptom: cross-terminal sync silently breaks — POS list misses new orders from other tablets, KDS doesn't see new tickets, payment confirm doesn't propagate, but the channel reports `SUBSCRIBED` with no error. When adding a new realtime sub, NEVER bypass the helper. Caught by: POS realtime audit 2026-04-25 — `realtime.subscription` table showed leftover anon rows on dev DB even with valid logged-in session. Reference: `apps/web/app/_hooks/use-realtime-channel.ts`, callsites `useOrderSync`, `OrderDetailSheet`, `BillReceipt`, `PrinterStatusIndicator`, `useKdsRealtime`, `useNotifications`.
- [2026-04-27] DIACRITIC-COMPOSED-ONLY — All Vietnamese source text MUST use composed-style (post-1984 spelling) diacritics: `Hủy`, `Xóa`, `Hóa`, `Thỏa`, `Tỏa`, `Hòa`. Old combining forms (`Huỷ`, `Xoá`, `Hoá`, `Thoả`, `Toả`, `Hoà`) are forbidden in any `.ts`/`.tsx` under `apps/`, `packages/`. Why: Phase 0 normalization 2026-04-27 standardized on composed; mixed forms cause selector drift, search-and-replace inconsistency, and grep blindness. Detect: `rg -P "[Hh]u[ỷ]\|[Xx]o[á]\|[Hh]o[á]\|[Tt]ho[ả]\|[Tt]o[ả]\|[Hh]o[à]" apps packages --type ts --type tsx` returns 0. Reference: `docs/ref/glossary.md §"Quy ước chính tả tiếng Việt"`.
- [2026-04-27] STATUS-LABEL-NO-LOGIC — Display labels for status enums (e.g., `"Đã hủy"`, `"Hoàn thành"`, `"Sẵn sàng"`, `"Đã phục vụ"`) MUST NOT appear on the right-hand side of `===` or `!==` comparisons in business logic. Branch on the canonical English/snake_case enum value (`"cancelled"`, `"completed"`, `"ready"`, `"served"`) only. Why: Phase 2 string sweep would silently change the literal and break logic. Detect: `rg -P '===\s*"(Đã \|Hoàn \|Sẵn \|Đang )"' apps packages --type ts --type tsx` returns 0.
- [2026-04-25] PWA-SW-NETWORKONLY-MUTATIONS — `apps/web/app/sw.ts` `runtimeCaching` array MUST keep `NetworkOnly` rules ahead of any cache-strategy rules for: (a) `request.method !== "GET"`, (b) requests carrying `RSC === "1"` header, `Next-Action` header, or `?_rsc=` query, (c) any `*.supabase.co` host or `/auth/v1/` / `/rest/v1/` path. Serwist evaluates rules in array order and stops at first match — re-ordering or inserting a `CacheFirst`/`StaleWhileRevalidate` before these guards lets a Server Action POST be served from cache. A cached payment POST = cashier sees "Đã thanh toán" but `confirm_cash_payment` RPC never runs → silent double-charge or unrecoverable money discrepancy. When adding a new payment method, RPC client, or Supabase project domain, audit the matcher list before adding it. Reference: `apps/web/app/sw.ts:24-77`.
- [2026-07-10] PWA-SELF-ORDER-NAV-NETWORKONLY — `/q/[token]` navigation responses MUST match a `NetworkOnly` rule before the generic public `NetworkFirst` page cache. The stable printed URL outlives a seating while SSR embeds `initialSnapshot` with the current session, batches, and bill; an offline/timeout cache fallback can otherwise show the previous guest's bill to the next seating on the same browser. Keep the generic cache for non-sensitive public pages, purge the old `pages` cache on activation, and guard matcher ordering in `apps/web/tests/pwa-manifest.test.ts`. Reference: `apps/web/app/sw.ts` and `apps/web/app/q/[token]/page.tsx`.
- [2026-04-25] PWA-MANIFEST-BRANCHID-INTEGER — `apps/web/app/(protected)/br/[branchId]/pos/manifest.webmanifest/route.ts` (and any future per-branch dynamic Route Handler) MUST validate the `branchId` segment as a clean positive integer string with `String(branchNum) === rawBranchId`, not just `Number.isFinite(parseInt(...))`. `Number.parseInt("123abc", 10) === 123` so naive validation passes but reflecting raw `params.branchId` into `start_url` / `scope` / `id` produces a manifest pointing at `/br/123abc/pos` which doesn't exist → installed PWA silently breaks on launch. Always interpolate the parsed integer (`branchId = String(branchNum)` after validation) into the manifest JSON, never `await params` raw segment. Reference: `apps/web/app/(protected)/br/[branchId]/pos/manifest.webmanifest/route.ts:9-22`.
- [2026-04-25] PWA-OFFLINE-GATE-CASH-ONLY — POS bill sheet (`apps/web/app/(protected)/br/[branchId]/pos/_components/bill/bill-receipt-sheet.tsx`) MUST gate `canConfirmPaid` with `isOnline` from `useIsOnline()` AND reject non-cash payment method selection when offline (`handleSelectMethod` early-returns with toast "Mất kết nối — chuyển khoản chưa khả dụng."). Reason: `confirm_cash_payment` is the most dangerous offline failure path — physical money already in cashier's hand + RPC times out = unrecoverable trust break (cashier may pocket the difference, or customer leaves believing paid). When adding a new non-cash payment method, MUST add it to BOTH the `handleSelectMethod` offline guard AND any new confirm action wrapper. Source of truth for online state is `useIsOnline()` exported from `apps/web/app/(protected)/br/[branchId]/pos/_components/pwa/online-status-provider.tsx`; default returns `true` when no provider is mounted (tests, Storybook, non-PWA contexts) so guard logic does not over-trip.
- [2026-04-25] PWA-HYDRATION-ONLINE-DEFAULT — `useIsOnline()` and `useInstallPrompt()` initial state in `apps/web/app/(protected)/br/[branchId]/pos/_components/pwa/online-status-provider.tsx` MUST be hardcoded (`isOnline=true`, `installPrompt=null`) for the first server-rendered + first-paint render, then synced from `navigator.onLine` / `beforeinstallprompt` event inside `useEffect` after mount. Reading `navigator.onLine` inside `useState(...)` initializer breaks SSR (`navigator` undefined on server) and causes hydration mismatch when SSR returns `true` but client mounts to a freshly-offline state. Same pattern applies to any future browser-state hook (battery, geolocation, media-query, web-share availability) — pick the most common state as initial value, then sync after mount.
- [2026-04-25] REALTIME-PUB-NEEDS-REPLICA-IDENTITY-FULL — Every table added to the `supabase_realtime` publication MUST also have `REPLICA IDENTITY FULL` set in the same migration. Default `REPLICA IDENTITY` (= primary key) makes Postgres logical replication emit only the PK in DELETE payloads and only the changed columns + PK in UPDATE `payload.old`. Two failure modes follow: (a) **Filter drops on DELETE** — POS hooks subscribe with non-PK filters like `branch_id=eq.{branchId}`. With DEFAULT identity, DELETE payloads do NOT carry `branch_id`, so the Realtime broker cannot match the filter and silently drops the event — the channel still reports `SUBSCRIBED` and no error fires; (b) **UPDATE old-state truncation** — code that compares `payload.old` vs `payload.new` to detect transitions (e.g. payment_status flip, ticket bump) sees only the changed fields in `old`, masking the prior state. WAL volume grows ~30% on FULL — negligible for pilot scale; reassess at high-write tables. Tables currently FULL: `orders`, `tables`, `payments`, `kds_tickets`, `print_jobs`, `printer_agents`, `notifications` (migration `supabase/migration-archive/20260425024802_realtime_replica_identity_full.sql`). When publishing a new table, add `ALTER TABLE public.<name> REPLICA IDENTITY FULL;` to the SAME migration as the `ALTER PUBLICATION ... ADD TABLE` — never split across PRs. Caught by: POS realtime debate v2 audit 2026-04-25 — 7 tables in publication, 0 with REPLICA FULL, DELETE-by-branch_id silently broken.
- [2026-04-25] REALTIME-SUBSCRIBE-NEEDS-STATUS-CALLBACK — Every Supabase Realtime subscription MUST pass a `status` callback to `.subscribe()` that handles the reconnect-resync case. Supabase Realtime is **at-most-once** (no durable log) — events that fire while a client is disconnected are LOST FOREVER once the WebSocket reconnects. The broker emits `SUBSCRIBED` once on initial subscribe AND again after every successful reconnect (after `CHANNEL_ERROR`/`TIMED_OUT`/`CLOSED`); the second-and-later `SUBSCRIBED` is the only signal a client gets that "you may have missed events — refetch a snapshot now." Pattern (see `apps/web/app/(protected)/br/[branchId]/pos/_hooks/use-order-sync.ts`): keep a `useRef(false)` flag, skip the FIRST `SUBSCRIBED` if state is already seeded from RSC props/startup, refetch a fresh snapshot on every subsequent `SUBSCRIBED`. Naked `.subscribe()` without callback = silent reconnect-miss, papered over only by stale-poll intervals (12-30 s race window). For helpers: `useRealtimeChannel` (`apps/web/app/_hooks/use-realtime-channel.ts`) does NOT add the resync callback for you — caller is still responsible for the status handler inside the channel-builder lambda. Tables/hooks already conformant: `useOrderSync`, `useKdsRealtime`, `useNotifications`, `PrinterStatusIndicator`, `apps/print-agent/src/index.ts`. When adding a new realtime sub: pair the auth-await helper (REALTIME-AWAIT-AUTH-BEFORE-SUBSCRIBE) with this status-callback pattern — both are required, neither is sufficient alone. Caught by: POS realtime debate v2 audit 2026-04-25 — 4 hooks subscribed without status callback, recovery latency 12-60 s when WS dropped mid-shift.
- [2026-04-26] RUNTIME-TOKEN-LAYERED-OVERRIDE — `packages/ui/src/styles/globals.css` is partitioned into base semantic tokens, CTMT status/tier additions, and app utilities. Má Tư Design System tunes values only through semantic variables (retired label: “Concept 01”). Unexplained runtime drift from `docs/spec/design-system.md` = REJECT. Additive tokens for status semantics and tier badges MUST be OKLCH and documented in the design-system contract. App utilities carry data variants, accordion keyframes, safe-area utilities, motion/elevation utilities, `--font-sans → Geist`, `--font-heading → Be Vietnam Pro`, `--font-mono → Geist Mono`, `html { font-size: 17px }`, and `::selection`. `font-mono` class is for tabular numbers (price columns, IDs, stock movements). Light border/ring and night card must keep WCAG 1.4.11 non-text contrast targets. Stitch mirror only at `.stitch/DESIGN.md` — never root `DESIGN.md`. Reference: 4-agent debate synthesis 2026-04-26; Dual Thesis / Má Tư DS update 2026-07-29.
- [2026-04-26] POS-DISCOUNT-RECOMPUTE-VIA-HELPER — `orders.discount_amount` is DERIVED from `(discount_type, discount_value, current_subtotal)` — never stored as a snapshot. Every RPC that touches subtotal MUST call `public.compute_discount_amount(p_type, p_value, p_subtotal)` and persist the returned value back to `orders.discount_amount` + `orders.total_amount` in the same UPDATE. Patched RPCs: `append_order_items`, `void_order_item`, `cancel_order`, `split_order`, `merge_orders`. Reasoning (owner Q3): when type='pct' the cashier-displayed rate must hold across cart changes (10% on 100k → still 10% on 150k after appending a side); when type='vnd' the helper auto-clamps to subtotal so void can never make total negative. Adding a new lifecycle RPC: copy the same `v_discount_amount := compute_discount_amount(o.discount_type, o.discount_value, v_new_subtotal); UPDATE orders SET discount_amount = v_discount_amount, total_amount = v_new_subtotal + service_charge - v_discount_amount` block. Reference: `supabase/migration-archive/20260430000000_pos_discount.sql`. **[2026-06-09 UPDATE — item-level discount]** Helper giờ derive thành phần order-level `orders.order_discount_amount` (tính trên `subtotal − item_discount_amount`); tổng `orders.discount_amount = order_discount_amount + item_discount_amount`. Xem POS-DISCOUNT-MONEY-INVARIANT + migration `20260609094000_pos_item_level_discount.sql`.
- [2026-06-09] POS-DISCOUNT-MONEY-INVARIANT — `orders.discount_amount = order_discount_amount + item_discount_amount` (CHECK `orders_discount_amount_source_check`); `total_amount = subtotal + service_charge − discount_amount`; item discount ≤ subtotal của chính item đó (CHECK `order_items_discount_amount_check`); discount chỉ áp pre-payment (block khi `payment_status='paid'`). Order-level metadata pairing sống trên `order_discount_amount` (CHECK `orders_order_discount_metadata_paired` — xem POS-DISCOUNT-PAIRED-NOTE-MIN-3). Reference: `supabase/migration-archive/20260609094000_pos_item_level_discount.sql`.
- [2026-04-26] POS-DISCOUNT-PAIRED-NOTE-MIN-3 — `orders` CHECK constraint `orders_order_discount_metadata_paired` (renamed từ `orders_discount_metadata_paired`, repointed sang `order_discount_amount` ở migration `20260609094000_pos_item_level_discount.sql`) enforces all-or-nothing trên thành phần order-level: either `order_discount_amount = 0` AND every `discount_*` column NULL, OR `order_discount_amount > 0` AND `discount_type` ∈ {pct,vnd} AND `discount_value` set AND `discount_note` ≥ 3 chars after trim. Server `apply_order_discount` validates note before locking; UI `DiscountSheet` mirrors with a live counter + disabled submit. Never write a partial discount tuple (e.g. set `order_discount_amount` directly without the metadata) — the constraint will reject and rollback the whole transaction. To clear a discount, call `clear_order_discount` (NULLs all four fields atomically); never zero `order_discount_amount` while leaving the metadata. Item-level discount có pairing riêng trên `order_items` (`order_items_discount_metadata_paired`).
- [2026-04-26] POS-CASH-ZERO-TOTAL-OK — `confirm_cash_payment` accepts `cash_received = 0` WHEN `total_amount = 0` (e.g. 100% discount đơn nhân viên / đền khách). The under-payment guard is `cash_received < total_amount` — strict less-than, not strict-greater-than-zero. NEVER tighten back to `cash_received <= 0 RAISE`: cashier on a paid-as-comp order would be locked out of the "Đã thanh toán" button. Sanity upper-bound `GREATEST(total*10, 50000000)` already protects against fat-finger typos at zero-total. Payments row stamped `method='cash', amount=0, status='completed'` keeps audit trail intact (zero-total comp meal is a legitimate accounting event, not a rejected one). Reference: `supabase/migration-archive/20260430000000_pos_discount.sql` §9.
- [2026-04-26] POS-SPLIT-IN-PLACE-NO-REPRINT — `split_order` moves items via `UPDATE order_items SET order_id = new` (in-place; row id stable) and `UPDATE kds_tickets SET order_id = new` (KDS card follows). NEVER delete-and-reinsert items, NEVER call `enqueue_kitchen_print` or `enqueue_cancel_ticket_print` from a split path: items physically exist at the kitchen, reprinting causes chef double-cooking; cancel-ticket-then-new-ticket cycle confuses station. Same rule applies to `merge_orders` (items move src→target in place, no reprint). When extending the split flow (e.g. split-by-seat), keep the move primitive; only the item-selection rules change. Reference: `supabase/migration-archive/20260430010000_pos_split_order.sql`.
- [2026-04-26] POS-SPLIT-PRESERVE-NON-EMPTY — `split_order` raises `split_would_empty_source` (ERRCODE 22023) if the requested move would leave the source with 0 active items. UI gating: `SplitOrderSheet` shows "còn lại N món" live + disables submit when N < 1; `OrderDetailSheet` hides the "Tách hoá đơn" entry entirely when `activeItemCount < 2`. Owner decision (Q2 4-agent debate): block, do NOT auto-cancel the source. Tách-rồi-cancel = sai mục đích — cashier muốn clear bàn thì phải dùng "Hủy đơn"; cashier muốn chuyển bàn thì dùng "Chuyển bàn". When loosening this rule (e.g. allow split-then-auto-cancel for "rebill" use case), wire a separate RPC; do NOT change `split_order` behavior in place.
- [2026-04-26] POS-MERGE-LOCK-LEAST-FIRST — `merge_orders` MUST acquire advisory locks in `LEAST(a,b)` then `GREATEST(a,b)` order — never by argument order. Two cashiers double-tapping cross-merges (`merge(A→B)` and `merge(B→A)` simultaneously) deadlock under naive ordering: T1 holds A wants B, T2 holds B wants A. With ordered locks both threads grab A first; one wins, the other queues. Same pattern applies to any future paired-order RPC (transfer-with-merge, refund-and-rebook). When adding a new such RPC: copy the `LEAST/GREATEST` block AND add `SELECT … FOR UPDATE` in lower-id order so FK-locked rows don't introduce a second deadlock surface. Reference: `supabase/migration-archive/20260430020000_pos_merge_orders.sql` §B.4 step 4.
- [2026-04-26] POS-MERGE-SAME-TABLE-DINE-IN-BOTH-UNPAID — `merge_orders` requires: `source.tenant_id = target.tenant_id`, `source.branch_id = target.branch_id`, `source.table_id = target.table_id` (both non-NULL), `order_type = 'dine_in'` (both), `payment_status != 'paid'` (both), `status NOT IN ('completed','cancelled')` (both), `merged_into_order_id IS NULL` (both), and NO active row in `payments` (status NOT IN failed/completed). Cross-bàn / takeaway / paid orders are out of scope per owner — when adding "transfer + merge" UX in future, that's a separate RPC composed of `transfer_order_table` then `merge_orders`, NOT a relaxed merge guard. UI MergeOrdersSheet picker server-side filters `same table_id, dine_in, non-paid, non-merged` via `fetchSiblingOrdersForTable` — never accept a target id from URL state without re-validating in the RPC.
- [2026-04-26] POS-MERGE-PCT-DISCOUNT-BLOCK — `merge_orders` raises `merge_pct_discount_blocked` (ERRCODE 22023) when EITHER source or target has `discount_type = 'pct' AND discount_amount > 0`. Owner reasoning (Q4): % discounts can't cộng dồn meaningfully (10% trên A + 15% trên B ≠ 25% trên gộp; chia subtotal nào?). Cashier must `clear_order_discount` on one side first. VND discounts DO cộng dồn into target (`v_target_discount_value := source.value + target.value` + concat notes) — auto-clamped via `compute_discount_amount` if the sum exceeds new subtotal. UI `MergeOrdersSheet` disables pct-discounted siblings (with hint label) and shows a destructive banner when the source itself has pct (every option disabled). When adding a third discount type (e.g. fixed-per-item, BOGO), update the conflict matrix in this rule + the RPC's CASE block + the UI hint logic together.
- [2026-04-27] POS-CLOSE-SHIFT-NO-BLOCK-ALERT — `close_pos_session` (migration `20260504000000_pos_close_shift_no_block_alert_d8.sql`) MUST allow close trong MỌI trường hợp — KHÔNG block khi `|cash_difference| > variance_threshold`. Variance breach giờ trigger `notify_pos_shift_variance_after_update` (SECURITY DEFINER trigger trên `pos_sessions` AFTER UPDATE OF status) insert vào `notifications` với kind `pos.shift_variance` và severity `warning` (≤ 5× threshold) hoặc `critical` (> 5× threshold). Owner D8 (2026-04-27, supersedes D3 2026-04-26): chặn cashier đóng ca → kẹt cuối ca, không giải quyết gốc rễ; lệch quỹ là vấn đề audit, không phải lock-down. RPC vẫn trả `variance_threshold` + `variance_breached` flag trong JSONB; UI hiện toast "đã gửi cảnh báo cho quản lý" khi breached. Permission `pos:close_shift_variance_override` còn trong DB nhưng không gate flow nào — KHÔNG drop để giữ backward-compat. UI flow `CloseSessionSheet` đã bỏ variance sub-step (varianceCtx state, varianceNote input, VARIANCE_NOTE_MIN const). Server Action `closePosSession` đã bỏ error parsing cho `variance_requires_bm_approval` / `variance_note_required`. NEVER re-introduce variance gate vào RPC mà không có owner D-decision mới — flow đã thiết kế cho human-in-the-loop hậu kiểm qua manager dashboard. Notification dedup_key = `pos.shift_variance:{session_id}` chống re-fire. Reference: `supabase/migration-archive/20260504000000_pos_close_shift_no_block_alert_d8.sql` + `apps/web/app/(protected)/br/[branchId]/pos/{close-session-sheet,session-actions}.ts`. **Supersedes** [2026-04-26] POS-CLOSE-SHIFT-PAID-FILTER-AND-VARIANCE-GATE phần variance gate.
- [2026-04-27] POS-CLOSE-SHIFT-CASH-ONLY-EXPECTED — `close_pos_session` MUST compute `expected_cash` as `opening_cash + SUM(total_amount) FILTER (WHERE payment_status='paid' AND payment_method='cash')`. NEVER cộng đơn `payment_method IN ('vietqr','momo','bank_transfer',...)` vào `expected_cash` — chuyển khoản settle vào tài khoản ngân hàng, KHÔNG chạm két vật lý. `closing_cash` là tiền mặt cashier đếm cuối ca → so sánh phải đối xứng (cash drawer vs cash drawer). Symptom của bug: cashier đóng ca → "lệch tiền" báo thiếu đúng bằng tổng tiền chuyển khoản trong ca → `cash_difference` âm vượt ngưỡng → block close, hoặc cần BM duyệt variance vô lý. Mô hình đúng: `cash_drawer_close = opening + cash_paid_in − cash_paid_out`; tiền VietQR/MoMo paid không phải `cash_paid_in`. RPC trả về thêm `cash_revenue` + `noncash_revenue` riêng trong JSONB result để phiếu chốt ca + UI hiển thị tách bạch (chuyển khoản là doanh thu, không phải tồn quỹ). NULL `payment_method` được coi như non-cash (conservative — paid orders sau backfill `20260428200000_fix_confirm_cash_payment_method_swap.sql` luôn có method, NULL chỉ còn ở edge case orphan); nếu cashier có complaint về NULL bị bỏ thì backfill từ `payments.method` chứ KHÔNG nới lỏng filter. Owner decision D8 (2026-04-27, hotfix to D1): expected_cash = cash drawer expected, không phải total revenue. Caught: cashier production block khi đóng ca có đơn VietQR/MoMo on 2026-04-27. Reference: `supabase/migration-archive/20260503000000_pos_close_shift_cash_only_expected.sql`. **Supersedes** the SUM-filter clause of [2026-04-26] POS-CLOSE-SHIFT-PAID-FILTER-AND-VARIANCE-GATE; D3 variance gate logic still applies unchanged.
- [2026-04-26] NO-RAW-TAILWIND-PALETTE-IN-APP — Outside `packages/ui/src/styles/*.css`, code MUST NOT use Tailwind palette classes: `(bg|text|border|ring|fill|stroke|from|to|via|outline|decoration|divide|placeholder|caret|accent)-(slate|gray|zinc|neutral|stone|red|orange|amber|yellow|lime|green|emerald|teal|cyan|sky|blue|indigo|violet|purple|fuchsia|pink|rose)-\d{2,3}`. Use semantic tokens: `bg-success/10`, `text-warning`, `border-destructive`, `bg-tier-elite/10`. Reasons: (1) palette classes bypass theme tokens so a preset bump cannot re-skin the app; (2) palette classes lack `dark:` variants so dark mode breaks (`bg-yellow-100 text-yellow-900` keeps yellow-on-yellow on dark surfaces). Tier badges that need >4 hues use Zone B tokens (`--tier-elite`, `--tier-note`) — never invent a new Tailwind palette inline. Catalog of semantic mappings: `success` (Trusted/zone-held/low-tier confirm), `warning` (variance T1/waste T1/low-stock/zone-blocked), `info` (Bootstrap/zone-acquiring), `destructive` (At-risk/variance T3/negative-stock/zone-lost), `tier-elite` (Trust Elite tier — purple), `tier-note` (variance T2/waste T2/out-of-stock — orange-mid), `muted` (idle/inactive/Bootstrap-low). Caught: 256 violations across 30+ files in `apps/web/` before semantic-token alignment — refactor scheduled as PR-2 follow-up. ESLint guard scheduled as PR-3 follow-up. Reference: 4-agent debate synthesis 2026-04-26.
- [2026-04-27] POS-SESSION-SCOPE-PER-BRANCH — `fetchActiveSession` query bằng `(tenant_id, branch_id, status='open')` — DB enforce `UNIQUE INDEX idx_pos_sessions_one_open_per_branch ON pos_sessions(branch_id) WHERE status='open'` (migration `20260426181829_pos_session_per_branch.sql`). Branch chỉ tồn tại 1 ca POS active cùng lúc (Owner D7, 2026-04-27) — quán Cơm Tấm 1 cashbox vật lý/branch. Cashier mở → staff cùng branch ride session đó theo permission/route gate. Order attribution: `orders.created_by` = ai ring đơn, `pos_sessions.opened_by` = cashier-of-record (chịu trách nhiệm két). `pos_sessions.terminal_id` nullable từ D7 — NULL = ca chung của chi nhánh, không liên kết terminal vật lý cụ thể; FK `ON DELETE SET NULL` để admin retire pos_terminals cũ không khoá history; closed sessions trước D7 vẫn giữ terminal_id cho audit. INSERT 2nd open session same branch → `23505` từ unique partial index → UI message "Chi nhánh này đã có ca POS đang mở. Tải lại trang để vào ca hiện tại, hoặc đóng ca cũ trước khi mở ca mới.". Realtime: `pos_sessions` được publish trong `supabase_realtime` với `REPLICA IDENTITY FULL` (migration `20260426181830_pos_sessions_realtime.sql`) — `pos-desktop-shell.tsx` subscribe `UPDATE` filter `branch_id=eq.{branchId}` qua `useRealtimeChannel`, nhận `new.status='closed'` → toast warning + `router.refresh()` để rerender SessionGate / "liên hệ thu ngân"; status callback skip lần SUBSCRIBED đầu (state đã seed RSC), reconnect refresh-defensive. NEVER re-introduce `UNIQUE(terminal_id)` partial index — multi-cashier-per-branch tách két là feature out-of-scope (cần schema `pos_session_cashiers` 1-N + opening_cash split + handover RPC). NEVER filter `opened_by = user.id` trong `fetchActiveSession` — service staff must be able to ride the cashier session when authorized. Reference: `supabase/migration-archive/20260426181829_pos_session_per_branch.sql` + `supabase/migration-archive/20260426181830_pos_sessions_realtime.sql` + `apps/web/app/(protected)/br/[branchId]/pos/{session-actions.ts,page.tsx,session-gate.tsx,pos-desktop-shell.tsx}`.
- [2026-04-27] POS-REDUCE-QTY-VIA-RPC-RECOMPUTE-DISCOUNT — Giảm số lượng món đã gửi PHẢI đi qua RPC `reduce_order_item_quantity(p_order_item_id, p_new_quantity, p_reason)` (migration `20260427074752_pos_reduce_order_item_qty.sql`). RPC update `order_items.quantity + subtotal`, BẮT BUỘC gọi `compute_discount_amount(o.discount_type, o.discount_value, new_subtotal)` rồi UPDATE `orders.subtotal/discount_amount/total_amount` cùng lúc — tuyệt đối không tự inline math (rule POS-DISCOUNT-RECOMPUTE-VIA-HELPER). RPC bump `kds_tickets.updated_at` của ticket non-terminal vì `order_items` không nằm trong `supabase_realtime` publication theo thiết kế (xem `20260428000000_pos_realtime_publication.sql:12-14`); nếu skip bump, KDS card render stale qty cũ cho đến khi chef refresh. Server action `reduceOrderItemQuantity` gate bằng `pos:void_order` (reuse, không tạo perm mới — cùng nhóm destructive privilege) và sau khi RPC commit thì gọi `enqueue_partial_cancel_ticket_print` để in phiếu báo bếp giảm SL khi `was_sent_to_kitchen=true`; print failure NON-FATAL (mirror void flow), surface qua `printWarning` toast — không bao giờ rollback reduce. **Phiếu in dùng `payload.kind='cancel_ticket'`** (không phải `'partial_cancel'` — union `PrintPayload.kind` chỉ có 5 kind: `kitchen_ticket | provisional_bill | receipt | cancel_ticket | shift_close_report` (`apps/print-agent/src/index.ts` + `packages/print-render/src/payloads.ts`); kind lạ → `materializeDocument` đọc `DEFAULT_TEMPLATE_CONTENT[kind]` undefined → renderer throw → job fail); reuse template HUỶ MÓN có sẵn, item.quantity = số phần BỊ GIẢM (delta), reason được prefix `"GIẢM SL N → M: <reason>"` để chef phân biệt với phiếu huỷ-toàn-phần (huỷ-hết render quantity=qty đầy đủ + reason không có prefix). Khi nào print-agent hỗ trợ kind mới (banner "GIẢM SỐ LƯỢNG" thay vì "HUỶ MÓN" + dòng "Còn lại: M phần") thì tách lại payload.kind — nhưng phải rebuild + redeploy agent ở mọi chi nhánh trước. Idempotency key `'order:{id}:reduce:{item_id}:{old}->{new}'` cho phép giảm nhiều lần (5→3 rồi 3→1) mà vẫn dedup double-click cùng delta. UI guard: `OrderItemActionsSheet` ẩn nút "Giảm số lượng" khi `quantity < 2` (force "Hủy món" để KDS card flip cancelled, tránh zombie qty=0 row); dialog stepper clamp `[1, qty-1]` client + RPC raise `'no reduction needed'` (22023) nếu newQty>=oldQty. Reason ≥5 ký tự (mirror `voidItemSchema`) — single-char "x" defeats audit. Block khi `payment_status='paid'`: đơn đã thanh toán = invoice locked; sửa SL sau paid sẽ rewrite revenue silently. Feature flag `pos_reduce_qty_enabled` (default true) là kill switch riêng — nếu chef phản đối flow này, owner tắt cờ mà không ảnh hưởng `pos_cancel_ticket_enabled`. Reference: `apps/web/app/(protected)/br/[branchId]/pos/order-actions.ts#reduceOrderItemQuantity` + `apps/web/app/(protected)/br/[branchId]/pos/_components/order-detail/reduce-quantity-dialog.tsx` + `supabase/migration-archive/20260427074752_pos_reduce_order_item_qty.sql`.
- [2026-04-27] NO-CLAMP-ON-KITCHEN-NOTES — KDS `item.note` (chef instruction chip in `ticket-row-meta.tsx:53`) MUST NEVER use `truncate` / `line-clamp-*` / `overflow:hidden`. Allowed: `min-w-0 break-words` only — let the ticket card grow vertically. Why: cắt một mảnh "không ớt" / "không đậu phộng" → đầu bếp ra món sai → khách dị ứng → liability event. Same rule applies to `item.item_name` `note` slot of any future kitchen-facing surface; clamp-2 is OK on `item.item_name` itself (SKU label, không phải lệnh phục vụ). KDS ticket grid must be `auto-rows-min` or equivalent so a tall note doesn't crop. When adding new note-like fields to KDS tickets (allergy tag, special prep flag), default to no-clamp + tone="warning" via `<NoteCallout>` if a chip style is needed. UI grep gate: `rg "truncate|line-clamp" apps/web/app/(protected)/br/\[branchId\]/kds` MUST return at most ONE match (only a `line-clamp-2` on `item.item_name`, the SKU label, is permitted); any match on note / modifier / side / variant fields is a regression. Reference: `apps/web/app/(protected)/br/[branchId]/kds/_components/ticket-row-meta.tsx` + 4-agent debate synthesis 2026-04-27.
- [2026-04-27] NO-CLAMP-ON-RECEIPT-LEGAL-FIELDS — POS bill receipt 80mm (`bill-receipt-summary.tsx`, container `#pos-receipt`) MUST NEVER apply `truncate` / `line-clamp-*` / `overflow:hidden` / `max-width` to legal-required fields: `branches.address` (e-invoice cross-check / merchant identity), `order.note` (customer instruction / service contract), `branches.name`, `order_number`. Allowed: `break-words` only. `globals.css @media print` block neutralizes any inherited clamp/overflow/max-w under `#pos-receipt *` defensively — never remove the print reset block. Why: silently cut "Phường 8, Quận Phú Nhuận" to "Phường 8, …" → tax audit mismatch with HĐĐT provider payload; cut "không hành" → khách dị ứng nhận sai món + service failure. When adding new printed fields (e.g. tax invoice link, loyalty code, table number), default to no-clamp; if width pressure exists at preview stage, shorten the label not the value. UI grep gate: `rg "truncate|line-clamp" apps/web/app/(protected)/br/\[branchId\]/pos/_components/bill` MUST return zero. Reference: `apps/web/app/(protected)/br/[branchId]/pos/_components/bill/bill-receipt-summary.tsx` + `packages/ui/src/styles/globals.css` (@media print block) + 4-agent debate synthesis 2026-04-27.
- [2026-04-27] NO-TITLE-ATTR-OR-TOOLTIP-ON-POS-TOUCH — POS / KDS surfaces (under `apps/web/app/(protected)/br/[branchId]/{pos,kds}`) MUST NOT depend on the HTML `title=""` attribute or `<Tooltip>` to reveal truncated content. PWA touch devices have no hover; `title` does nothing on tap; `<Tooltip>` opens on long-press (~500ms) which is unusable in cash-rush. Required reveal mechanisms: (a) always-visible (preferred for cash-path: KDS notes, receipt, bill summary), (b) `<NoteCallout>` framed block with full text, (c) tap-to-expand sheet/drawer, (d) multi-line layout (used in `permissions-client.tsx` SelectItem). **Carve-out:** `title` attributes that mirror already-visible text (icon-button labels, page headings, line items where the title duplicates inline text) are fine — they aid screen readers and desktop hover without being the only path to information. The rule targets `title` as a _reveal mechanism for content otherwise truncated/hidden on touch_. Admin-desktop-only surfaces (`apps/web/app/(protected)/*`, `apps/web/app/(protected)/finance/*` table cells) MAY use `truncate + title` because hover is reliable on mouse. When in doubt, prefer `line-clamp-2 + break-words` over truncate-with-title even on admin — keeps fallback decent on tablet. Why: `branch-table.tsx` cashier-mobile copy of admin would lose address visibility; KDS staff cannot scan a tooltip during rush. Reference: 4-agent debate synthesis 2026-04-27 (QA finding §2 MAJOR touch accessibility) + code-reviewer follow-up 2026-04-27.
- [2026-04-27] POS-NETWORK-GATE-NEVER-TRUST-XFF-FIRST-HOP — Resolving a request's client IP for the POS/KDS network gate (or for any security decision) MUST go through `apps/web/lib/network/client-ip.ts#getClientIp`. NEVER read `request.headers.get("x-forwarded-for")?.split(",")[0]` directly — the leftmost XFF entry is the **client-claimed** value and is trivially spoofable by injecting an `X-Forwarded-For` request header. On Vercel the trusted source is `x-real-ip` (set from the TCP-layer source); fallback to the LAST entry of `x-forwarded-for` (most-trusted hop). Helper rejects RFC1918 private ranges (10/8, 127/8, 169.254/16, 172.16–31, 192.168/16, ::1, fe80::/10, fc00::/7) — a private IP arriving from a public proxy means misconfiguration; fail closed. NEVER read the IP from JWT `app_metadata` (would freeze it for the JWT TTL) or from a request body parameter (forgeable). Existing naive readers at `apps/web/app/api/webhooks/momo/route.ts:29`, `apps/web/app/api/auth/signout/route.ts:10`, `apps/web/app/(public)/(auth)/login/actions.ts:46` are rate-limit keys (lower stakes); migrate them to the helper opportunistically but the gate at `apps/web/proxy.ts` MUST always use it. Reference: `apps/web/lib/network/client-ip.ts` + 4-agent debate synthesis 2026-04-27.
- [2026-04-27] POS-NETWORK-GATE-PROXY-ONLY-NOT-RLS — The POS/KDS network gate (`branch_trusted_egress_ips` lookup in `apps/web/proxy.ts`) lives in middleware **only**. PostgREST / Supabase REST calls (anything the client makes via `supabase-js` after the page loads) bypass it — RLS + JWT remain the source of truth for data access. Therefore: (1) NEVER document this gate as "POS data is locked to branch wifi" — it locks the **PWA shell**, not the data; (2) any new surface that exposes branch data outside proxy (Edge Functions, Webhooks, direct REST endpoints, Server Actions on non-`/br/*` routes) MUST add its own independent IP check or be exposed only behind the proxy; (3) feature description in security narrative reads "defense-in-depth perimeter against casual cross-network access", not "authorization control". Runner display is an explicit public customer board exception and must not be described as Account Login protected. A future Phase-2 hardening (LAN cookie 2FA via native POS app, or RLS-level header passthrough via `current_setting('request.headers')`) can upgrade this to a true authz layer; until then, the proxy gate is one layer of many. Reference: `apps/web/proxy.ts` + 4-agent debate synthesis 2026-04-27.
- [2026-04-27] POS-NETWORK-GATE-AGENT-PRESENCE-TOKEN — Print-agent's IP registration (`apps/print-agent/src/index.ts#registerPresence` → `apps/web/app/api/branch-presence/route.ts`) MUST authenticate with a per-agent raw `PRINT_AGENT_PRESENCE_TOKEN` stored only in that branch agent's `.env`; the web app MUST NOT accept a global shared presence token from runtime env. Web route hashes the bearer with SHA-256 and calls service-role-only RPC `register_branch_presence`, which validates the hash against `printer_agent_presence_tokens` for the exact `(tenant_id, branch_id, agent_id)` tuple. DB stores only `token_sha256`, never the raw token. `AGENT_ID` is required when presence is enabled because token binding and audit depend on a stable id. The RPC enforces durable 1 request / 30s per active token and returns `skipped=true` for same-IP fresh rows (`last_seen_at < 60s`) without rewriting trust rows. The IP itself is read SERVER-SIDE from the request via `getClientIp` — agents do NOT pass an `ip` field in the body. Agent heartbeat MUST NEVER clear `branch_trusted_egress_ips.revoked_at`; revoked rows return `ip_revoked` until an explicit admin re-enable path runs. Any future endpoint that lets a caller assert their own IP for security purposes is a forgery vector regardless of authentication. Token lifecycle operations MUST go through the repo-owned `pnpm --filter @comtammatu/print-agent presence:provision -- {create|rotate|revoke|status}` CLI, which requires explicit project-ref confirmation for writes and never stores the raw token. Do not reintroduce a SOP that tells operators to hash then insert/update this table by hand in Supabase Dashboard. When rotating a presence token, use the CLI `rotate`, deploy the raw token to the branch agent, restart the agent, and verify `branch_trusted_egress_ips.last_seen_at` advances. Reference: `supabase/migration-archive/20260601870000_network_gate_presence_token_registry.sql` + `apps/web/app/api/branch-presence/route.ts` + `apps/print-agent/src/index.ts` + `apps/print-agent/src/ops/provision-presence-token.ts`.
- [2026-04-28] HDDT-CANCEL-REASON-MIN-20 — `cancelTaxInvoice` (`apps/web/app/(protected)/finance/actions.ts`) `reason` parameter is REQUIRED + ≥20 chars after trim — never `.optional()`, never substituted with a placeholder like `"Hủy theo yêu cầu"` (15 chars, useless to a tax auditor). NĐ 254/2026 + TT 32/2025 require every HĐĐT cancellation to carry a real, descriptive reason. Schema `z.string().trim().min(20).max(500)`. UI MUST collect via Textarea with live character counter (mirrors POS variance-note pattern); submit button disabled while invalid (`reasonValid` derived in `invoice-list.tsx`). Reason is propagated into `transition_tax_invoice_state(p_payload={cancel_reason}, p_note=cancelReason)` so provider cancellation payload + audit ledger both carry the actual text. When calling Sinvoice cancel: do NOT pre-fill the cancel reason from a fixed phrase even if the provider accepts it — propagate the user-typed string verbatim, that string is the legal record. Reference: `apps/web/app/(protected)/finance/{actions.ts,invoice-list.tsx}` + `supabase/migration-archive/20260425035346_tax_invoice_state_machine.sql`.
- [2026-04-28] AUDIT-LOG-SELECT-EXPLICIT-COLUMNS — `fetchEntityAuditLogs` and any other reader of `audit_logs` MUST use an explicit column list, NEVER `.select("*")`. Required-only columns: `id, action, entity_type, entity_id, user_id, created_at`. NEVER include `ip_address` (request-level PII) or raw `old_data`/`new_data` blobs (buyer addresses, MST, salaries depending on entity_type) in a list view. If a deeper audit drill-down needs diffs, expose a separate per-row RPC gated on a specific permission key (e.g. `audit:view_diff`) and log the access. The `audit_logs_select` policy already restricts SELECT to owner-tier access — this rule is the second-line defense against accidental over-fetch in shared dashboards. When adding a new column to `audit_logs` (e.g. user_agent), default to NOT including it in the list selector; opt-in per use case. Reference: `apps/web/app/_lib/audit.ts#fetchEntityAuditLogs`.
- [2026-04-28] AUDIT-LOG-INSERT-RPC-ONLY — `audit_logs` rows MUST only be created via `public.log_audit(p_action, p_entity_type, p_entity_id, p_old, p_new)` SECURITY DEFINER RPC. Direct INSERT from app code (`supabase.from("audit_logs").insert(...)`) is REVOKED at GRANT level (`REVOKE INSERT ON public.audit_logs FROM authenticated`) and the broad `audit_logs_insert` policy is dropped. RPC forces `tenant_id = auth_tenant_id()` and `user_id = auth.uid()` server-side — callers cannot lie about who they are or which tenant they belong to. Caught: M6 security review 2026-04-27 — any authenticated user could `POST /rest/v1/audit_logs` with arbitrary action/entity_type/user_id to forge or smear audit history (e.g. fake "owner cancelled invoice 123"). Helper `apps/web/app/_lib/audit.ts#logAudit` already wraps the RPC; callers pass `{action, entityType, entityId, oldData?, newData?}` — NEVER pass `tenantId`/`userId` (forced server-side). Anonymous callers raise `insufficient_privilege` (auth.uid() NULL); missing tenant claim raises same. When triggering audit from a Postgres trigger or another SECURITY DEFINER function, write directly to `audit_logs` from inside that function (definer privilege bypasses the GRANT) — RPC-only rule applies to APP code, not in-database triggers. Reference: `supabase/migration-archive/20260505020000_audit_logs_rpc_only_insert.sql` + `apps/web/app/_lib/audit.ts`.
- [2026-04-27] POS-NETWORK-GATE-GRACE-IS-SECURITY-CEILING — `branch_trusted_egress_ips.last_seen_at` 30-minute grace window in `apps/web/proxy.ts` is the **security ceiling**, not a recovery buffer. Within 30 min of an agent's last successful registration, the IP remains trusted even if the agent has died, the LAN has been seized by another tenant, or the laptop running the agent has been moved off-site. Acceptable for a single-tenant pilot quán cơm tấm; explicitly NOT acceptable for multi-tenant SaaS or branches in CGNAT-shared neighborhoods (rule POS-NETWORK-GATE-PROXY-ONLY-NOT-RLS already flags CGNAT as a known limitation). When tightening: shorten the cutoff at `proxy.ts` (look for `30 * 60_000`); when loosening for an incident, prefer flipping `POS_NETWORK_GATE=off` env (kill-switch) over extending the grace — kill-switch is loud (per-cold-start `console.warn` emitted by `apps/web/proxy.ts` when `NODE_ENV=production AND POS_NETWORK_GATE=off`, format `[network-gate] disabled via POS_NETWORK_GATE=off — POS/KDS perimeter open. See regressions.md POS-NETWORK-GATE-GRACE-IS-SECURITY-CEILING.`, deduped via module-level `NETWORK_GATE_OFF_WARNED` flag — fires once per warm Edge instance, captured by Vercel log drain) while a quietly-extended grace is invisible. Future hardening: shorter grace + auto-revoke (cron job marking rows `revoked_at = now()` when `last_seen_at < now() - interval '4 hours'`) so a forgotten dev branch can't keep accepting connections forever. Reference: `apps/web/proxy.ts` + `supabase/migration-archive/20260428110000_branch_trusted_egress_ips.sql`.
- [2026-04-28] POS-DISCOUNT-CLEAR-METADATA-WHEN-AMOUNT-ZERO — Any RPC that drives `orders.order_discount_amount` to 0 MUST also set `discount_type/value/note = NULL` in the same UPDATE. The CHECK constraint `orders_order_discount_metadata_paired` (gốc `orders_discount_metadata_paired` 20260430000000_pos_discount.sql; renamed + repointed sang `order_discount_amount` ở `20260609094000_pos_item_level_discount.sql`) demands the 4 fields move together. `clear_order_discount` honors this; the bug was that `merge_orders` (source side, items moved out → subtotal=0), `cancel_order`, `void_order_item` (auto-cancel branch + pct-FLOOR-edge), and `split_order` (pct rate × tiny remaining subtotal → FLOOR=0) silently set `discount_amount=0` while leaving the other 3 columns populated — every merge of a vnd-discounted source raised "Dữ liệu chiết khấu không nhất quán" and the action never landed; cancel/void of any discounted order had the same fate. When adding a new write path that may zero out `discount_amount`: either copy the `clear_order_discount` UPDATE shape (NULL block) when the zero is unconditional, or use `CASE WHEN v_discount = 0 THEN NULL ELSE col END` per-column when zero is one branch of a recompute. Audit trail for the prior discount lives in `order_status_history` rows (`discount_applied`/`discount_cleared`/`split_to`/`merged_into`), not in the orders columns — so clearing on collapse loses no business info. Reference: `supabase/migration-archive/20260430030000_pos_clear_discount_metadata_when_amount_zero.sql` + helper at `compute_discount_amount(type,value,subtotal)`.
- [2026-04-28] VAT-PER-LINE-NOT-PER-INVOICE — In VAT-mode/template `1/...`, VAT amount and rate MUST be stored and recomputed PER LINE on invoice snapshots and `order_items`, NEVER recomputed at invoice level via `total_amount / (1 + invoice_vat_rate)`. F&B mixed-VAT orders may combine reduced-rate and excluded/standard-rate lines. Direct-sales template `2/...` remains the no-VAT-split path. POS `order_items` MUST snapshot `menu_items.vat_rate`; Sinvoice VAT-mode payloads emit per-line tax percentage and amount; header total tax is the sum.
- [2026-04-28] PERIOD-FILTER-USES-LOCAL-TZ — Any query that filters orders, payments, HĐĐT, expenses, or accountant-export rows by business period MUST apply `AT TIME ZONE 'Asia/Ho_Chi_Minh'` before extracting month/year, NEVER use raw UTC `created_at` directly. Postgres `TIMESTAMPTZ` stores UTC; a transaction created at `2026-04-30T17:30:00+07:00` is stored as `10:30:00Z` — `EXTRACT(MONTH FROM created_at)` returns 4 (April UTC) but business-wise it is still April at the branch. Edge case is when a transaction crosses midnight: `2026-04-30T18:30:00Z` = `01:30:00 +07` next day = May business-day. Without TZ adjustment, last 7 hours of every business day get classified into the wrong month near boundaries. Pattern: `EXTRACT(MONTH FROM (created_at AT TIME ZONE 'Asia/Ho_Chi_Minh'))` and `(created_at AT TIME ZONE 'Asia/Ho_Chi_Minh')::date BETWEEN period.start AND period.end`. Inventory shift_key already follows this pattern (rule SHIFT-KEY-CANONICAL); finance reports MUST adopt same. Reference: 4-agent debate Finance enhancement 2026-04-28 (QA hidden risk #3).
- [2026-04-28] MV-FINANCE-CONCURRENTLY-INDEX — Every finance materialized view used by dashboards or reporting MUST be created with a UNIQUE INDEX on the natural key BEFORE it is registered with any concurrent refresh job. CONCURRENTLY uses the unique index to compute row-level diffs without locking readers; non-CONCURRENTLY refresh takes ACCESS EXCLUSIVE on the MV, blocking dashboards for the refresh duration. Pattern: `CREATE UNIQUE INDEX uq_mv_X_pk ON public.mv_X(natural_key_cols);` on the same migration that creates the MV. Existing rule MV-GROUP-BY-MUST-MATCH-UNIQUE-INDEX (2026-04-25) compounds: GROUP BY columns MUST be a SUBSET of the unique index columns; cosmetic columns (e.g. snapshot names) go in `MAX(...)` SELECT projection, not GROUP BY. When a new MV ships without a unique index, the refresh cron silently fails on it (degrades only that MV, not the cron pipeline) — surface this in /finance MV-staleness banner. Reference: supabase/migration-archive/20260425030256_fix_mv_top_items_food_cost_dedup.sql.
- [2026-05-01] DAILY-LIMIT-SIDES-MUST-COUNT — `enforce_branch_menu_daily_limit` and `decrement_branch_menu_daily_limit` MUST aggregate over both `NEW.menu_item_id` (or `OLD.menu_item_id` on cancel) AND every `sides[].side_item_id` from the JSONB column. Sides are stored as a JSONB snapshot on `order_items.sides`, NOT as separate rows — a trigger that only inspects the main item id silently lets disabled sides through and never increments quota for items sold as sides. The aggregation MUST GROUP BY `menu_item_id` (collapses duplicates and the "same item is both main and side" case into one lock + one increment of `qty_main * qty_side_per_main`) and ORDER BY `menu_item_id ASC` for deterministic lock ordering across concurrent inserts that share a side (prevents AB/BA deadlock). Side delta = `NEW.quantity (main qty) × COALESCE((s->>'quantity')::int, 1)`. Reference: supabase/migration-archive/20260514000000_branch_menu_daily_limit_sides.sql + 4-agent debate 2026-05-01.
- [2026-05-01] DAILY-LIMIT-SPLIT-NO-RECHARGE — `split_order` partial-clone INSERT into `order_items` MUST NOT re-charge daily quota: net qty across {source-after-decrement, clone} is unchanged from source-before, so re-incrementing `sold_today` in the trigger would over-count. The hatch is the transaction-local GUC `comtammatu.skip_quota_enforcement = 'true'` set via `set_config(name, 'true', true)` immediately before the clone INSERT and reset to `'false'` immediately after; the enforce trigger short-circuits at the top when the GUC is `'true'`. ANY future RPC that moves quota-counted rows around (re-parent, re-clone, etc.) without changing total demand MUST reuse this same hatch — never roll a per-RPC bypass column or per-RPC trigger SKIP clause, the GUC is the single contract. Full-move splits (UPDATE order_id only) are unaffected because no INSERT trigger fires on UPDATE. Reference: supabase/migration-archive/20260514000100_split_partial_skip_quota.sql + 4-agent debate 2026-05-01.
- [2026-05-01] DAILY-LIMIT-REDUCE-QTY-LEAK — `reduce_order_item_quantity` releases `sold_today` for the main `menu_item_id` only (per DAILY-LIMIT-REDUCE-QTY-DECREMENT); it does NOT release the `sides[].side_item_id` portions of the reduced row, so reducing qty N→M leaves `(N-M) * sum(side_qty_per_main)` side quota locked → those side items show "Hết suất" earlier than reality. When releasing side quota on reduction, aggregate over `sides[].side_item_id` using the same pattern as DAILY-LIMIT-SIDES-MUST-COUNT.
- [2026-05-01] REVENUE-BUCKET-BY-PAID-AT-LOCAL-TZ — Every revenue aggregate (MV, RPC, hand-rolled query) MUST bucket by `(payments.paid_at AT TIME ZONE 'Asia/Ho_Chi_Minh')::date` from a `payments` row with `status='completed' AND paid_at IS NOT NULL`, NEVER by `orders.created_at`. Doanh thu ngày X = đơn THU ngày X dù tạo đêm trước; bucket theo `created_at` UTC lệch 7h cuối ngày sang ngày VN sau, và đơn 23:50 paid 00:10 sẽ lệch ngày. Refunded payments (`status='refunded'`) phải bị loại khỏi gross revenue MV và đếm riêng làm "voided/refund" KPI — không được trộn vào doanh thu. Filter chuẩn ở mọi tầng: `o.status <> 'cancelled' AND o.payment_status = 'paid' AND p.status = 'completed' AND p.paid_at IS NOT NULL`. Caught by: 4-agent debate Finance reporting rebuild 2026-05-01 (BA + QA chốt fix luôn thay vì defer; tradeoff trong migration 20260512000000 đã được fix). Reference: supabase/migration-archive/20260514010000_revenue_paid_at_bucket_and_multi_branch_rollup.sql.
- [2026-05-07] RPC-STRUCTURED-FAILURE-DETAIL — RPC raise lỗi nghiệp vụ cần payload structured (multi-row shortage list, multi-error report, ...) MUST dùng `RAISE EXCEPTION 'short_message' USING ERRCODE = '...', DETAIL = jsonb_text` thay vì concat data vào message. Action handler đọc qua `error.details` (PostgreSQL DETAIL clause được supabase-js expose nguyên văn), parse bằng Zod schema, surface qua `ActionResult.meta` + `errorCode` ổn định để UI branch render Dialog/Sheet riêng. Lý do: (1) concat vào message gặp escape risk khi data chứa apostrophe/quote và làm UI phải parse substring fragile; (2) split message-vs-details cho phép log message ngắn vào telemetry mà vẫn payload đầy đủ cho client; (3) `errorCode` ổn định khi Vietnamese UI copy đổi (per ActionResult contract `packages/shared/src/types/action-result.ts:6-11`). Cap rows trong DETAIL (LIMIT 20 phổ biến) để tránh JSON quá lớn. Đầu tiên adopt: `confirm_production_order` shortage list (supabase/migration-archive/20260531030000_production_order_shortage_detail.sql:128-150). 4-agent debate 2026-05-07 (Architect + QA + BA: strict block, no force override, mobile parity bắt buộc).
- [2026-05-23] HDDT-SINVOICE-ONLY-RUNTIME — Runtime HĐĐT provider is Viettel S-invoice only. Do NOT reintroduce `MisaProvider`, MISA env vars, `INVOICE_PROVIDER` switching, LocalMisaProvider, or docs that describe MISA as an active path. Runtime code and active docs must use `provider='viettel'`, `SINVOICE_*`, and `ViettelSinvoiceProvider`.
- [2026-05-30] ATTENDANCE-INSERT-SERVICE-ROLE-ONLY — INSERT on `attendance_records` is REVOKED from `authenticated` AND `anon`; the service role is the only Employee attendance insert path. Floor-staff clock-in MUST go through `staff-runtime/clock/actions.ts::clockInWithPhoto`, which validates the staff context/photo and calls `employee_clock_in_with_checklist` so the attendance row and checklist snapshot are created atomically. Floor-staff checkout MUST remain an approval request, not a client-side attendance update. The assigned Branch Manager may approve same-branch branch staff; Owner may approve across the tenant and must handle Branch Manager requests. Self-approval, peer-manager approval, and cross-branch approval fail closed. Branch Manager self-attendance is the explicit exception: `clockInWithPhoto` inserts their own attendance without checklist snapshot, and `clockOutManagerShift` directly closes only their own open attendance for the assigned branch via service role. Background: a permissive `attendance_self_checkin` policy (2026-04-17, employee-ownership only) OR'd away stricter checks and, with INSERT granted to authenticated, let any logged-in employee POST a fabricated attendance row straight to PostgREST. NEVER re-GRANT INSERT or DELETE on attendance_records to authenticated/anon and NEVER add a permissive FOR INSERT policy on it. Detect: any `GRANT INSERT ... ON ... attendance_records ... TO (authenticated|anon)` or any new permissive FOR INSERT policy on the table. Reference: 2026-05-30 RLS audit; owner chose A1 (REVOKE + service-client), updated 2026-06-09 for Employee photo/checklist/checkout-approval flow, 2026-06-10 for Branch Manager direct self checkout, and 2026-07-18 for branch-scoped approval authority.
- [2026-05-30] REQUIRE-BRANCH-SCOPE-ON-BRANCH-MUTATIONS — any `withAction`/`withActionPositional` that (a) allows `branch_manager` or `cashier` in its roles AND (b) performs a BRANCH-SCOPED mutation (writes data keyed to a single branch) MUST set `requireBranchScope: true` (or own its scope via `customAuth`). Without it, a branch_manager/cashier whose `claims.branch_id` is null widens a branch-scoped write to tenant-wide — several actions gate the branch with `if (claims.branch_id)`, a no-op when null. This includes branch Inventory adjustments, GRN, stock issue, transfer, count and correction actions. EXCLUDE: genuinely tenant-wide data such as menu catalog, tenant-only role sets, reads, and POS actions that use `customAuth`. Detect: a new branch-scoped mutating action allowing branch_manager/cashier with NEITHER `requireBranchScope` NOR `customAuth`. Reference: 2026-05-30 enumeration of 111 withAction sites; foundation = m4 P1-E (`with-action.ts` requireBranchScope option).
- [2026-05-30] NO-SUPERSEDED-PERMISSIVE-POLICY — When hardening an RLS policy (narrowing access / adding a permission or branch gate), you MUST DROP the older broad policy it replaces in the SAME migration. Postgres OR's permissive policies for a command, so a leftover broad policy silently NEUTRALISES the hardened one. Confirmed instances (all "added strict, forgot to drop broad"): `attendance_self_checkin` (vs `attendance_write` shift gate), `pe_employee_self_select` (vs `payroll_entries_select` paid-only), `sti_select`/`sti_manage` tenant-only (vs `stock_transfer_items_select`/`_write` inventory:read/write gate — was a real any-tenant-member read+write bypass), `pp_select`/`pp_manage` (dup of `payroll_periods_write`). Fixed in migrations 20260602008000 / 009000 / 010000. Detect (run BEFORE any RLS-hardening migration ships): `select polrelid::regclass, polcmd, count(*) from pg_policy where polpermissive group by 1,2 having count(*)>1` — for each table+command with >1 permissive policy, confirm none is a strict superset/duplicate of another. Two genuinely-distinct audiences (admin-vs-self, per-storage-bucket) are fine; a broad tenant-only policy sitting next to a permission-gated one is NOT.
- [2026-05-30] DROP-PUBLIC-CASCADE-DROPS-STORAGE-POLICIES — `DROP SCHEMA public CASCADE` (or any rebuild that drops `public`) ALSO drops every `storage.objects` RLS policy whose USING/WITH CHECK references a public function (`has_permission`, `auth_tenant_id`, `has_permission_any`, …) — they depend on those functions, so CASCADE takes them. A `--schema=public` baseline does NOT recreate them (storage-schema objects). Verified 2026-05-30: rebuilding matu-dev's public schema silently dropped public-function-dependent `storage.objects` policies; a counts-only check missed it. After a public rebuild, replay the chain — the fold migration `20260627140000_fold_managed_surfaces.sql` Section C re-creates the `storage.objects` policies — and verify `select count(*) from pg_policy where polrelid='storage.objects'::regclass` matches the manifest count (currently 12). Detect: a public rebuild/restore PR that doesn't re-run the storage-policy section, or a storage-policy count below the manifest.
- [2026-05-13] SINVOICE-DERIVE-LINENET-FROM-ROUNDED-UNITPRICE — Viettel Sinvoice per-line math (`buildSinvoiceItemInfo` in `packages/shared/src/providers/impl/viettel-sinvoice.ts`) MUST round `netUnitPrice` FIRST, then derive `lineNet = qty × netUnitPrice` (exact integer multiplication). NEVER round `lineNet` and `netUnitPrice` independently — Sinvoice validator 43 (`|qty × unitPrice − itemTotalAmountWithoutTax| < 1`) is STRICT and fails when drift ≥ 1₫. Counterexample: lineGross=100₫, qty=7, vatRate=8 → old impl `lineNet = round(100/1.08) = 93`, `netUnitPrice = round(93/7) = 13`, diff = |7×13 − 93| = 2 → reject. Validator 44 (`|(lineNet − discount) × taxPct/100 − taxAmount| < 1`) is satisfied by rounding `taxAmount = round(lineNet × vatRate / 100)` (drift ≤ 0.5). Sum invariants (validators 49/87) hold because `summarizeInfo.{sumOfTotalLineAmountWithoutTax,totalTaxAmount}` are computed as `Σ itemInfo.{itemTotalAmountWithoutTax,taxAmount}` — exact reduce, not from request totals. Detect: any new line-math helper in `viettel-sinvoice.ts` MUST follow `netUnitPrice → lineNet = qty × netUnitPrice → taxAmount = round(lineNet × rate / 100)` order; randomised fuzz test in `packages/shared/src/providers/__tests__/viettel-sinvoice.test.ts` ("validators hold across 200 random trios") is the canonical regression gate. Reference: `packages/shared/src/providers/impl/viettel-sinvoice.ts::buildSinvoiceItemInfo` + audit 2026-05-13.
- [2026-05-13] HDDT-REPLACE-ATOMIC-DUAL-TRANSITION — `replace_tax_invoice` SECURITY DEFINER RPC (`supabase/migration-archive/20260517020000_hddt_replace_flow.sql`) MUST perform OLD `issued→replaced` flip + NEW row INSERT + `replaced_by/replaced_for` linkage + 2 `tax_invoice_events` writes — ALL inside a SINGLE Postgres tx. NEVER orchestrate via 2 separate `.rpc()` calls from server action — a crash between the calls would leave the illegal "both rows active" state which violates `uq_tax_invoices_active_per_order` partial UNIQUE and corrupts the chain. Ordering inside the RPC MUST be: flip OLD status first (removes from uq partial index that excludes 'replaced'), THEN INSERT NEW draft (slot now free), THEN UPDATE OLD.replaced_by = NEW.id. Reverse order causes 23505 because both rows would be in active states simultaneously. Detect: `apps/web/app/(protected)/finance/replace-invoice-actions.ts::replaceTaxInvoice` MUST call exactly one `replace_tax_invoice` RPC for the dual transition (subsequent `transition_tax_invoice_state` calls are only for the NEW row's lifecycle draft→signing→issued, never for OLD). Reference: `supabase/migration-archive/20260517020000_hddt_replace_flow.sql::replace_tax_invoice` + 4-agent debate 2026-05-13.
- [2026-05-13] HDDT-REPLACE-CHAIN-MAX-3 — Replace chain depth MUST be capped at 3 levels (`A → B → C` allowed; `A → B → C → D` rejected with `replacement_chain_too_deep_max_3` ERRCODE 22023). Check enforced inside `replace_tax_invoice` RPC via RECURSIVE CTE walking `replaced_for` backwards. Pilot operator oversight makes >3 chains a sign of process breakdown rather than legitimate need. Detect: RPC body MUST contain `WITH RECURSIVE chain` with `v_chain_depth >= 3` early-exit; admin UI MUST surface error message clearly. Reference: `supabase/migration-archive/20260517020000_hddt_replace_flow.sql::replace_tax_invoice` lines 95-110.
- [2026-05-13] HDDT-REPLACE-NEW-UUID-FROM-NEW-INVOICE-ID — Replacement provider call (`createInvoice` with `replacement: ctx`) MUST set `request.orderId` to the NEW row's `tax_invoices.id`, NEVER to the original `orderId`. `buildSinvoiceTransactionUuid(invoiceId)` is deterministic — same input = same uuid. Reusing the original's `orderId` would generate the same transactionUuid as the original invoice → Viettel returns `TRANSACTION_IS_BEING_PROCESSED` and the replacement fails. Server action `replaceTaxInvoice` MUST pass `newId` (returned from `replace_tax_invoice` RPC) as `orderId` to provider. Detect: `apps/web/app/(protected)/finance/replace-invoice-actions.ts` MUST have `orderId: newId` in the provider call, NOT `orderId: order.id` or `orderId: parsed.data.originalId`. Reference: `apps/web/app/(protected)/finance/replace-invoice-actions.ts::replaceTaxInvoice` STEP C.
- [2026-05-13] POS-TABLE-STATUS-NO-PERSISTENT-CACHE — POS table status (`tables.status`) MUST be fetched live in `apps/web/app/(protected)/br/[branchId]/pos/session-actions.ts::fetchTablesForBranch`; NEVER wrap it in `unstable_cache`, `cacheTag`, or any persistent revalidate window. A POS terminal that loads after another terminal creates orders does not receive past realtime events, so caching `occupied/available` can hide occupied tables until TTL expiry even while `fetchActiveOrders` shows the correct order count. Static table metadata may be cached only if split from live status and recombined with a fresh status source. Detect: `rg -n "unstable_cache|cacheTag|revalidate:.*300|tags: \\[\\\"tables\\\"\\]" apps/web/app/(protected)/br/[branchId]/pos/session-actions.ts` MUST 0. Reference: live floor bug 2026-05-13 — one POS showed 4 active orders but only 2 occupied tables because the old tables snapshot was cached for 300s.
- [2026-05-13] HDDT-POS-MODIFIERS-SIDES-MUST-BE-LINES — Per-order HĐĐT provider payload MUST use `buildInvoiceLineItemsFromOrderItems` from `@comtammatu/shared/hddt`, and the Supabase order fetch MUST include `order_items.modifiers` + `order_items.sides`. POS stores `order_items.unit_price = base + modifier_sum + sides_sum`; sending that as one line creates wrong legal item declarations (e.g. Cơm sườn cốt lết kèm Bì/Chả/Trứng must invoice as 35.000 + 7.000 + 7.000 + 5.000, not one 54.000 line). Replacement invoices MUST use the same helper. Detect: shared test `packages/shared/src/hddt/__tests__/invoice-line-items.test.ts` covers the 35k/7k/7k/5k case; static test `pos-mandatory-invoice.test.ts` blocks app actions from bypassing the helper.
- [2026-06-16] PAYROLL-2026-FIVE-BRACKET-AND-BHXH-CAP-STEP — The 2026 tax year uses the 5-bracket PIT schedule (`PIT_BRACKETS_2026`) for EVERY month from kỳ tính thuế 2026 = `2026-01-01`, NOT a 7-bracket H1 / 5-bracket H2 split. Luật TNCN 109/2025/QH15 has general effect 01/07/2026 but its wage/business-income provisions apply from kỳ tính thuế 2026 (authoritative reading). Separately, BHXH `insuranceCap` steps `46_800_000 → 50_600_000` at `effectiveFrom: 2026-07-01` (NĐ 161/2026/NĐ-CP: lương cơ sở 2.34M → 2.53M; cap = 20× base). In `packages/shared/src/payroll/legal-versions.ts` the `2026-01-01` and `2026-07-01` versions MUST differ ONLY by `insuranceCap`; both carry `pitBrackets: PIT_BRACKETS_2026` and deductions 15.5M/6.2M. Caveat (owner/accountant call, NOT code): H1-2026 monthly WITHHOLDING may legitimately stay at old 7-bracket rates pending the law's 01/07 general-effect date and true up at finalization — annual liability is identical either way. If the accountant elects conservative H1 withholding, point the `2026-01-01` version back to `PIT_BRACKETS_2007` (one line); do not also change deductions or caps. Detect: `getLegalVersionForPeriod(2026, m).pitBrackets.length` MUST be 5 for all m∈[1..12]; `insuranceCap` MUST be 46_800_000 for periods ≤ 2026-06 and 50_600_000 for ≥ 2026-07; pre-2026 versions MUST stay 7-bracket + old caps (29.8M @ 2020-07, 46.8M @ 2024-07) for closed-period reproducibility. Test: `packages/shared/src/payroll/__tests__/legal-versions.test.ts`.
- [2026-07-11] SHEET-CLOSE-HEADER-ALIGN — `SheetHeader` MUST NOT hardcode `pr-16`. That padding only reserves space for the absolute default close (`showCloseButton`). Always-on `pr-16` pushes an inline `SheetClose` (item-customizer, cart-sheet) left into a dead gap. Contract: `SheetContent` sets `data-close-button` + `group/sheet`; `SheetHeader` applies `group-data-[close-button=true]/sheet:pr-16` only. Absolute default close MUST use `top-[env(safe-area-inset-top,0px)]`, NEVER `chrome-safe-top` (the `max(0.5rem, …)` floor drops the X below the title on desktop/zero-inset). Detect: static test `apps/web/tests/sheet-close-header-align.test.ts`. Reference: `packages/ui/src/components/sheet.tsx`.
- [2026-07-11] POS-CUSTOMIZER-MOBILE-SCROLL — `ItemCustomizer` (`item-customizer.tsx`) is a bottom `Sheet` with long modifier/side lists. RULE: bottom full-height POS/self-order sheets MUST use the shared `SheetContent fullscreen` mode; do not layer `h-dvh`/`max-h-dvh` in `className` because the custom `max-h-dvh-95` utility is not safely resolved by tailwind-merge. Wrap body as `flex h-full min-h-0 flex-col overflow-hidden`, put options in a `min-h-0 flex-1 overflow-y-auto` body (prefer plain overflow over `ScrollArea` here — SCROLLAREA-NEEDS-DEFINITE-HEIGHT), and pin qty/total/confirm in `SheetFooter` OUTSIDE that scroll body so the CTA stays on the viewport. Detect: static tests `apps/web/tests/pos-item-customizer-mobile-scroll.test.ts` and `apps/web/tests/sheet-close-header-align.test.ts`.
- [2026-06-28] HDDT-DISCOUNT-RATE-ROUNDED-2DP — The Viettel `itemInfo[].discount` RATE (% of line) MUST be rounded to ≤2 decimal places via `roundDiscountRate` in `buildSinvoiceItemInfo` (`packages/shared/src/providers/impl/viettel-sinvoice.ts`). A raw `(lineDiscount/lineAmount)*100` (e.g. 10.000/30.000 = 33.3333…%) is rejected by the strict Viettel validator with `BAD_REQUEST_INVALID_DECIMAL_POINT_DISCOUNT` (HTTP 400), leaving the paid order's invoice stuck `draft` — payment is failure-isolated, NOT rolled back (see HDDT-PAYMENT-FIRST-FAILSOFT-ORPHAN). Prod audit 2026-06: 72 draft invoices from this + the earlier amount-as-rate variant (`DISCOUNT_INVALID`); meanwhile 252 discounted orders whose rate was already ≤2dp issued fine on the same strict account. The authoritative discount is `itemDiscount` (VND amount) — all strict total validators (43/44/49/87) key off the amount, never the rate — so rounding the rate cannot disturb totals. NEVER send the raw division result for either pricing mode. Detect: both `discount:` sites in `buildSinvoiceItemInfo` MUST wrap the rate in `roundDiscountRate`; `packages/shared/src/providers/__tests__/viettel-sinvoice.test.ts` asserts `Math.round(rate*100)/100 === rate` for every line. Reference: prod `tax_invoices.provider_data` error audit 2026-06-28.
- [2026-07-07] THEME-NIGHT-MODE-WARM-DARK — Night mode uses the `.dark` CSS selector (so existing `dark:` variants + chart THEMES map resolve correctly) carrying a WARM-DARK "gạo cháy" palette (hue ~50, not navy). Trigger: auto 18:00–06:00 local (`new Date().getHours()`) when `matu-theme` cookie absent; override via cookie `matu-theme` (`light` | `night`, SameSite=Lax, 1y max-age) set by `ThemeToggle`. NEVER depend on `prefers-color-scheme` / `matchMedia` — shift-aware is OS-independent and timezone-stable. Receipts MUST stay protected by the `@media print` guard in `packages/ui/src/styles/globals.css` (force `color: black; background: white` on `#pos-receipt` + descendants) — night must not leak into printed receipts or ESC/POS thermal output. The ESC/POS pipeline (`packages/print-render`, `apps/print-agent`) is bitmap/byte-stream based and theme-independent; do not add theme coupling there. `global-error.tsx` is the single file allowed inline `style` for presentation (named exception per design-system.md) — it renders outside ThemeProvider so it MUST read the `matu-theme` cookie server-side and apply warm values for both modes (no neutral greys). Never add a route-local theme toggle, a second theme context, or a localStorage theme key (scope storage rule). Reference: D069.
- [2026-07-08] SECTION-LABEL-SSOT — Panel/field/section uppercase eyebrow labels (the `text-xs font-medium uppercase tracking-wide text-muted-foreground` role, or the dense `text-2xs ... tracking-wider` KDS variant) MUST render via `SectionLabel` from `@comtammatu/ui/components/section-label`. Never copy-paste the class string inline. The primitive emits the contract-locked role (`font-medium`, NOT `font-semibold`; `tracking-wide`/`tracking-wider`, NOT `tracking-widest`); deviations are bug. Do NOT use SectionLabel for: page-header eyebrow (owned by `AppPageHeader.eyebrow`), table column headers (`TableHead`), or form-field labels (`Label` primitive). Local `SectionLabel` clones in app code are ratcheted by `card-title-classname-baseline`; migrate to the shared primitive. Reference: `docs/spec/design-system.md`.
- [2026-07-26] FINANCE-OPENING-IS-IMMUTABLE-CUTOFF — Tenant-wide current funds MUST use the single `finance_fund_entries` opening and include canonical movements only when their accounting timestamp is `>= opening.effective_at`. Never replay movements before the verified opening boundary, never coerce a same-day cutover to 00:00, never fall back to `cash_opening_balance` / `bank_opening_balance` / `cash_opening_date`, and never expose an opening overwrite. Interactive cutover uses the exact server timestamp; a project-start day uses 00:00 Vietnam time only with evidence for that exact boundary. Legacy opening settings remain evidence and force a controlled cutover. Corrections are signed append-only adjustments with actor, reason, server timestamp, and idempotency key. POS sessions are reconciliation evidence only: `staff_repaid` and `accepted_adjustment` both create zero book delta. Cash supplier payments reduce cash; bank-transfer supplier payments reduce bank only through canonical `bank_transactions.out`, and reconciliation links create no delta. `/finance` period and branch filters never change current funds. Detect: `supabase/tests/finance_current_funds_test.sql` and `apps/web/tests/finance-cash-bank-fund.test.ts`.

- [2026-07-29] BRANCH-REVENUE-MONTHLY-TARGET — Monthly Doanh thu thuần targets live in `branch_revenue_targets` and compare only to `subtotal - discount` (never money collected). Missing target MUST NOT render as 0%. `reward_tiers` is Owner-only configuration on `/finance/targets`: unique KPI thresholds, highest achieved tier only (not cumulative), fixed VND or a percentage of monthly Doanh thu thuần; it does not auto-post to payroll. Branch Manager may read MTD progress for the assigned branch on `/br/[id]` home only; never read reward configuration, upsert, or open tenant `/finance`. Competition/pace stays on `/finance/revenue` when the range is a single calendar month. Branch Dashboard and Owner `/` stay free of this KPI. Detect: `supabase/tests/branch_revenue_targets_test.sql`, `apps/web/tests/finance-revenue-target.test.ts`.
- [2026-07-31] SUPPLIER-INVOICE-NET-UNIT-PRICE-ADDITIVE — Supplier invoice line entry is additive VAT (Vietnam HĐĐT convention): `unit_price` is the NET (pre-VAT) unit price, `line_total = greatest(round(quantity × unit_price, 2) − line_discount_amount, 0)` is the net line total, `vat_amount` is additive (`round(line_total × vat_rate / 100, 2)` for automatic mode, authoritative when manual), and `gross_line_total = line_total + vat_amount` is the VAT-inclusive total. The additive invariant `gross_line_total = line_total + vat_amount` must hold at client, Server Action, and deferred DB constraint; VAT is NEVER reverse-extracted from gross with `rate / (100 + rate)` and never added to gross a second time. The former `pricing_mode` selector (`gross_total` vs `unit_price`) and `gross_unit_price` mirror column are removed; entry is a single fixed column set: Đơn giá (NET) | Chiết khấu | Thuế suất | Thuế GTGT | Tổng tiền. Matching still allocates the net `line_total`; supplier price history keeps the effective pre-VAT net unit price; this aligns the persisted line shape with `SINVOICE-DERIVE-LINENET-FROM-ROUNDED-UNITPRICE`. The constraint trigger and RPC validate the net total within a 1đ tolerance (rounding drift on backfilled historical lines), but the `gross = line_total + vat` invariant is strict. Detect: `apps/web/tests/supplier-invoice-money.test.ts`, `apps/web/tests/supplier-invoice-gross-*-static.test.ts`, and `supabase/tests/supplier_invoice_gross_vat_entry_test.sql`. Supersedes [2026-07-30] SUPPLIER-INVOICE-GROSS-VAT-INVARIANT.

- [2026-08-01] INVENTORY-LEDGER-RPC-ONLY — Runtime application code MUST NOT mutate `stock_movements` directly. Document stock corrections and future ledger writes route through an authenticated, atomic Postgres RPC that validates source state, tenant/branch scope, inventory permission, actor identity, and available stock before appending a movement. Detect: `corepack pnpm lint:regression-guards` and `apps/web/tests/inventory-ledger-business-logic-static.test.ts`.
