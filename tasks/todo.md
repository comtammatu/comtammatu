# Current Tasks

> Active work tracker for the in-place `comtammatu` production track. v1.0.0 is operating on real branches; ongoing work is hardening + feature follow-ups. Retired docs are not retained in this repo; current scope lives here plus canonical docs under `docs/`.

## Module status (snapshot)

M0–M7 + Auth v2 + POS PWA + Realtime hardening + Shadcn primitive migration M1–M9 — **all SHIPPED**. External integrations VietQR + Momo are wired with production credentials and active in real branches. HĐĐT is active through Viettel S-invoice only.

## Pilot hardening execution — 2026-05-24

**Report:** `docs/worklog/pilot-hardening-readiness-2026-05-24.md`

- [x] Refresh generated snapshot docs: current checkout has 109 page routes, 13 API route handlers, 116 DB tables, 9 DB views, 241 DB functions, 0 enums, 366 SQL migrations, and 40 test/spec files. Generated by `node scripts/project-snapshot.mjs`.
- [x] Replace stale active DB schema spec with source ladder + current snapshot. Early-2026 schema table list removed; generated types and applied DB state are now the schema authorities.
- [x] Split/stage App Router route-group migration safely. Closed on 2026-05-24: route migration reconciled into `HEAD`, route audit is clean (`0` staged moves / `0` unstaged counterparts), and `pnpm typecheck && pnpm lint && pnpm build` passed.
- [x] Confirm prod apply status for payment/hardening migrations. Closed by production go-live of VietQR + Momo: `webhook_events`, `complete_payment_and_consume_stock`, `confirm_cash_payment`, and `confirm_vietqr_payment` are applied and exercised by live traffic.
- [x] Close network-gate hardening: per-agent/per-branch presence token, `/api/branch-presence` rate limit, and revoked-row heartbeat behavior. Shipped via `printer_agent_presence_tokens` + `register_branch_presence` RPC, applied to dev/type-source Cloud, generated types refreshed, and repo-owned `@comtammatu/print-agent` `presence:provision` CLI added for create/rotate/revoke/status. Branch rollout still must provision one raw token per agent before live smoke.
- [ ] Run real POS → payment → KDS/print → HĐĐT smoke in approved dev/test or staging with live provider credentials. [2026-05-28] `stock` leg dropped from chain per owner policy "không trừ kho" (see memory `project_pos_action_helper_refactor.md` "Owner policy 2026-05-28" + commit `e4eb93bc`). Action-layer enforcement landed in commit `9ba83205`; webhook stock-leg disable migration still pending owner authorize.

## Shell helpers refactor — 2026-05-27

**Report:** `docs/worklog/shell-helpers-refactor-plan-2026-05-27.md` (APPROVED 2026-05-27)

- [ ] **WS-0** — Extend `apps/web/app/_lib/with-action.ts` with `withActionPositional` + `customAuth` + `afterSuccess`; add `_lib/rpc-error-map.ts`. Zero callers migrated.
- [ ] **WS-1a** — Migrate `voidOrderItem` only (proving slice; exercises 4/4 new helper features).
- [ ] **WS-1b** — Migrate remaining POS actions (`order-actions.ts` + `payment-actions.ts`). Closes the `POS → payment → KDS/print → HĐĐT` smoke item above. In progress 17/23 (2026-05-28): batches 1–4b + 5a createPayment + reads sub-batch all landed; 1 dead action dropped (fetchDailyReconciliation, commit `9225001d`). Remaining: confirmPayment + confirmVietQrPayment + confirmVietQrPaymentWithInvoice (last 3 callers of local `mapPaymentRpcError`; once migrated, the helper can be dropped) + confirmCashPaymentWithInvoice (orchestrator, minimal extraction win) + 2 order-actions.ts reads with `probePermission` composite auth.
- [ ] **WS-2** — Migrate inventory actions (`grn-actions.ts`, `production-actions.ts`, `actions.ts`). Wire `lib/messages/inventory.ts` through `rpc-error-map.ts`.
- [ ] **WS-3** — Decompose client shells (pos-desktop-shell, grn-detail-client, order-detail-sheet) one PR each. Retire parallel `pos/hooks/` folder.
- [ ] **WS-4** — Version-naming cleanup: delete dead `S12_DASHBOARD_V2`, rename `S13A_STOCKTAKE_V2` → `INVENTORY_STOCKTAKE_REDESIGNED` (+ DB migration of `branch_feature_flags.flag_key`), fix orphan `waste_v2_not_enabled` URL token.

## Known issues

### Interface closure — from 2026-05-24 source-of-truth audit

- [x] **IF-001** — Retire Inventory pilot visual layer (`matu-surface`, `matu-*`, `font-matu-body`, kitchen sink) after current route migration is stabilized. Shipped locally by deleting the retired route/adapter/token package, removing the Be Vietnam Pro hook and generated token import, and tightening the UI contract guard to zero runtime hits.
- [x] **IF-002** — Stabilize or split the current App Router route-group migration before broad Interface runtime edits. Closed on 2026-05-24 after route audit returned clean and full completion gate passed.
- [x] **IF-003** — Triage and fix locked heading-scale violations (`text-4xl`, `text-5xl`, `font-black`) by route family. Closed locally by removing all runtime hits, tightening the heading-scale UI contract allowlist to zero, and rendered POS/KDS smoke evidence.
- [x] **IF-004** — Triage banned icon-size classes (`size-7/9/11/14/16`) and separate real media thumbnails from icon glyph violations. Closed locally by removing all `size-7/9/11` app-surface hits, reducing oversized icon glyphs, and narrowing the icon-size UI contract allowlist to real media thumbnails only.
- [x] **IF-005** — Convert raw button/touch heights to `Button` primitive variants (`touch`, `touch-lg`) where the element is a real action. Closed locally by moving POS/KDS/Inventory action controls to primitive touch sizing and tightening the button-height UI contract allowlist to remaining non-button containers, inputs, skeletons, and brand/media layout.
- [x] **IF-006** — Triage radius rhythm drift; fix banned radii only inside the touched route family instead of doing a bulk style rewrite. Closed locally by removing the POS route-family `rounded-xl` drawer override and adding a `radius-scale` UI contract guard with the remaining non-POS banned-radius hits allowlisted as baseline debt.
- [x] **IF-007** — Re-run Inventory UI/UX QA after the route migration, then update `INV-UIUX-*` evidence statuses atomically. Closed locally with rendered Playwright evidence: `INV-UIUX-001/002/003/004/005` fixed. Full Inventory sign-off still needs area manager, GRN detail, supplier invoice, expiry/report detail, POS bridge flows, and cashier seed-password cleanup.
- [x] **IF-008** — Finish Sprint 6 GRN draft client refactor: remove workflow `localStorage`, use server-side draft state, and replace URL flash with Sonner toast. Shipped locally via server-backed GRN draft state, `grn-draft.ts`, submit-as-review-navigation, and `GrnFromPoList` toast handling.
- [x] **IF-009** — Align Finance landing/nav to Finance Basic: four owner metrics first; advanced accounting routes remain support/hidden from pilot default. Closed locally by restoring the route-group `/finance/page.tsx` landing and rendered owner QA on `/finance`: desktop + mobile show `Doanh thu`, `Giá trị tồn kho`, `Chi vận hành`, `Lợi nhuận gộp`, and advanced accounting labels are absent from the landing/nav first screen.
- [x] **IF-010** — Classify all client storage usage: theme/PWA-dismiss may stay, but scope/workflow state must not persist in browser storage. Closed locally with `scripts/check-client-storage.mjs` wired into `pnpm lint`; only theme preference and POS PWA install-hint dismissal are allowlisted.
- [x] **IF-011** — Add an allowlist-based static UI guard before broad cleanup so new `matu-*`, banned fonts, and banned sizing classes cannot spread. Shipped locally via `scripts/check-ui-contract.mjs` + `pnpm lint:ui-contract`.
- [x] **IF-012** — Reconcile active docs/rules/memory entrypoints back to the UI SSOT after IF-001: README now points to `buFywKm`/`radix-lyra`, active scope links point to `tasks/todo.md`, MISA wording is removed from the root entrypoint, and retired UI audit notes are removed from docs.

### Feedback module — from /qa pass 2026-05-07 (production read-only)

**Report summary:** production read-only QA found 14 issues; the active follow-up list is tracked below.

**HIGH (do these first):**

- [x] **ISSUE-001** — `apps/web/app/(public)/r/[token]/actions.ts` fail-closed in production when ALLOWED_ORIGINS_FEEDBACK is empty. Shipped 2026-05-09 (8e713af8).
- [x] **ISSUE-012** — `apps/web/next.config.ts` `headers()` returns CSP + X-Frame-Options=DENY + X-Content-Type-Options=nosniff + Referrer-Policy + Permissions-Policy. Shipped 2026-05-09 (610123c8).

**MEDIUM:**

- [x] **ISSUE-002** — Photo upload IDOR: mint per-submission upload token in `submit_feedback` RPC, consume in `uploadFeedbackPhotos` (`apps/web/app/(public)/r/[token]/actions-photos.ts`). Closed 2026-05-28 with one-shot SHA-256 token, expiry, consumed timestamp, and Storage cleanup on update races.
- [x] **ISSUE-003** — Replaced with `after()` from `next/server` in submitFeedback (durable post-response). Shipped 2026-05-09 (79a30bb7).
- [ ] **ISSUE-004** — Tighten photo storage RLS to gate by branch (encode branch_id in path or JOIN to feedbacks for has_permission check). Local migration/regression added 2026-05-28; dev apply is blocked because the current MCP/linked SQL role is not owner of `storage.objects` (`supabase_storage_admin` owns it). Owner-capable apply/verify path is documented in the feedback worklog.
- [x] **ISSUE-013** — `/r/[token]/thank-you` now calls `notFound()` for invalid/unknown tokens. Shipped 2026-05-09 (939be3d9).

**LOW / INFO (nice to have):**

- [x] **ISSUE-005** — Cascade photo storage objects in `feedback_retention_cleanup()` to prevent forever-orphans. Closed locally 2026-05-28.
- [x] **ISSUE-006** — Defense-in-depth: re-check `feedback:view` permission in `getFeedbackPhotoUrls`. Closed locally 2026-05-28.
- [x] **ISSUE-007** — `console.info()` on honeypot trip with first 8 chars of token. Shipped 2026-05-09 (79a30bb7).
- [x] **ISSUE-008** — Server schema raw `.min/.max` before sanitize, distinct refine message after. Shipped 2026-05-09 (fec49ecd).
- [x] **ISSUE-009** — `?only_suspect=true` URL param on admin feedback inbox. Shipped 2026-05-09 (ee71d005).
- [x] **ISSUE-010** — Add `(tenant_id, created_at DESC)` index for tenant-wide inbox queries. Closed locally 2026-05-28.
- [x] **ISSUE-011** — Order snapshot heuristic broken for shared tables (data quality). Closed 2026-05-28 by making `submit_feedback` snapshot orders only when a table QR maps to exactly one active unpaid order; multi-order tables now leave order snapshots null and emit an ops warning.
- [x] **ISSUE-014** — `poweredByHeader: false` set in `apps/web/next.config.ts`. Shipped 2026-05-09 (610123c8).
- [x] **ISSUE-015** — 3-layer CSRF defense doc inline in `actions.ts`; warn-once log when ALLOWED_ORIGINS_FEEDBACK is unset in production. Shipped 2026-05-09 (79a30bb7).
- [x] **ISSUE-016** — Conditional `UPDATE ... WHERE photo_paths IS NULL OR '{}'` + `.select()` to detect race losers. Shipped 2026-05-09 (003224c0).

**Fix strategy:** all on a feature branch `fix/qa-feedback-2026-05-07` + PR for owner review. Each commit is atomic with regression test in `packages/shared/src/feedback/__tests__/` or `apps/web/__tests__/`.

### Other

- [ ] P3: Login rate limit fail-open khi Upstash unreachable — documented design decision, cần observability
- [ ] Legacy `auth_role()` RPC cleanup (không chặn ship — migrate dần qua batches α4b/α4c): α4b migration `20260601810000_auth_v3_cut_auth_role_rpc_batch.sql` rewrites `admin_update_profile`, `toggle_profile_active`, and `set_branch_kind`; α4c còn `can_access_branch` vì đây là RLS branch-scope predicate cần pass kiểm thử policy riêng. Active definitions của `bump_kds_ticket`, `close_fiscal_period`, `create_supplier_payment`, `gl_reconciliation`, `post_payroll_journal`, `recall_kds_ticket` đã đi qua permission-gated replacements trước đó.

## Active branches (in flight, on origin)

- `m4-payments-fix` (75e8250, 2026-04-29) — foundation slice landed in production: 2 migrations applied + with-action.ts requireBranchScope + redactCredentials utility + 7 regression rules. Follow-up slice (reverse_payment_and_post RPC, recompute_total, TS callers) is what closed the M4 Payments P0 list below.

## Pre-deploy fixes

- [x] **Employee page FK casts** (2026-04-27): `pnpm db:types` regen + treat M:1 FK joins as object (`r.shifts?.name`, not `?.[0]?.name`) in `app/(protected)/employee/{attendance,payslip,schedule}/page.tsx`. See lesson #10 — supabase-js typegen quirk: `isOneToOne: false` infers array but PostgREST runtime returns single object for M:1.
- [x] **POS network gate D9** (2026-04-27): `branch_trusted_egress_ips` + `/api/branch-presence` Bearer endpoint + admin dialog wired into branch table. Proxy bypass for presence endpoint (without it, agent POSTs were redirected to /login).
- [x] **Stale shift_assignments_select RLS** (2026-04-27): tightened to self + permission; tenant-wide leak closed.
- [x] **refunds table** (2026-04-27): table now exists; `@ts-nocheck` removed from `app/(protected)/orders/refund-actions.ts`. STORAGE ONLY — see P0 list below for correctness gaps.
- [x] **m4 P1-A webhook_events idempotency table** (2026-04-29, branch m4-payments-fix): UNIQUE(provider, request_id), RLS for finance:view, GRANTs. Applied and active in production.
- [x] **m4 P1-E with-action.ts requireBranchScope option** (2026-04-29, branch m4-payments-fix): branch_manager/cashier with null branch_id rejected before widening to tenant. Opt-in; wired 2026-05-30 into 22 branch-scoped mutating actions (see the M7-area item below + rule REQUIRE-BRANCH-SCOPE-ON-BRANCH-MUTATIONS).
- [ ] Inventory smoke regression runbook theo `docs/runbooks/inventory/pre-release-qa.md` — chạy định kỳ sau pilot launch.
- [ ] Uptime monitor on `/api/health` (UptimeRobot — ops, không phải code)
- [ ] Ops reconciliation query cho Momo — payment/order desync surfacing trong /admin/finance. Momo đã go-live, đây là post-launch reconciliation tooling.
- [x] Momo webhook atomic `complete_payment_and_consume_stock` RPC wired and production-applied. Confirmed by live Momo traffic post go-live.

## P0 from security review 2026-04-27 (T3-tier work, see workflow.md)

> Full findings under each agent in session log. Items below are either closed and production-applied, or remain as named follow-ups; new work in this section requires T3 review per `docs/agent/rules/workflow.md`.

### M4 Payments

- [x] **`approveRefund` doesn't actually refund** — RPC `reverse_payment_and_post` shipped 2026-04-30 (`20260510020000_m4_reverse_payment_and_post_rpc.sql`). Atomic: GL reversal + stock restore + payment+order status flip + audit. Hardcoded VAS accounts (5111/1111/1121); revisit posting_rules-driven mapping post-launch if needed. Production-applied; TS caller in refund-actions.ts wired.
- [x] **Refund `payment.status='completed'` precondition** — RPC `create_refund` shipped 2026-04-30 (`20260510030000_m4_create_refund_rpc.sql`). Rejects anything but completed; enforces cumulative refund cap. Production-applied; TS caller swapped from direct INSERT to RPC. **Open follow-up:** `area_manager` scope hole at the action layer (RPC delegates role/area scope to TS wrapper) — tracked under the H3 item in the Deferred section.
- [x] **MoMo webhook tenant binding hole** — webhook payment lookup is bound by signed `extraData` scope: `tenant_id + order_id + provider_ref + method='momo'`, after partnerCode + HMAC verification.
- [x] **Stock consumption fail-soft on hot-path** — webhook now treats `stock_consumed=false` / `stock_failed` as HTTP 500 and emits a critical notification. Migration `20260601780000_payment_completion_failhard_recompute.sql` keeps payment/order uncompleted when inventory consumption fails. Production-applied; types regenerated.
- [x] **Server-recompute `total_amount`** — migration `20260601780000_payment_completion_failhard_recompute.sql` recomputes active line subtotal + tax + service charge − discount inside `complete_payment_and_consume_stock` and returns `amount_mismatch_recomputed`. Production-applied; types regenerated.
- [x] **Webhook idempotency table** (2026-04-29, m4-payments-fix branch): `webhook_events(provider, request_id, ...)` with UNIQUE constraint shipped and production-applied.
- [ ] **POS calls provider before DB lock** — RPC fail = orphan gateway order. **DEFER-WITH-MITIGATION** per m4 plan.

### M6 Finance

- [x] **Finance scope reset contract** (2026-05-24). New active contract: `docs/modules/finance.md`. Pilot first screen is only 4 metrics: doanh thu, giá trị tồn kho, chi vận hành, lợi nhuận gộp. Cash sessions, payment/order reconciliation, HĐĐT, supplier payables, and accountant export are support workflows; Accounting Advanced routes remain legal/accounting continuity but must not define the pilot UX.
- [x] **Audit log INSERT REVOKE + `log_audit()` SECURITY DEFINER RPC** (2026-04-28). Migration `20260505020000_audit_logs_rpc_only_insert.sql`; helper at `apps/web/app/(protected)/admin/_lib/audit.ts` wraps the RPC; 7 callers in finance/HR drop `tenantId`/`userId` (forced server-side). See regression rule AUDIT-LOG-INSERT-RPC-ONLY.
- [x] **HĐĐT `cancel reason` min 20 chars** (2026-04-28). Schema dropped `.optional()`, UI collects via Textarea with counter, removed `"Hủy theo yêu cầu"` 15-char placeholder fallback. NĐ70/2025 compliant. See regression rule HDDT-CANCEL-REASON-MIN-20.
- [x] **`fetchAuditLogs` PII strip** (2026-04-28). Replaced `.select("*")` with explicit `id, action, entity_type, entity_id, user_id, created_at`. Drops `ip_address` + `old_data`/`new_data` blobs. See regression rule AUDIT-LOG-SELECT-EXPLICIT-COLUMNS.
- [ ] `voidJournalEntry` closed-period void still mutates signed BCTC. Current-period reversal guard EXISTS since `20260527000000` (`void_manual_journal_entry` raises `current_period_closed_cannot_reverse` for the reversal-date period). REMAINING (confirmed 2026-05-30): voiding a *posted* entry whose ORIGINAL `entry_date` falls in a CLOSED period flips that entry → `voided`; statements `apps/web/app/(protected)/finance/statement-actions.ts:67,179` filter `status='posted'` + `entry_date`, so the already-signed period changes retroactively. No trigger blocks the original UPDATE (period enforcement only gates `auto_post_journal` inserts). T3 decision needed: (A) reject void when original period closed → force manual adjustment in current open period [recommended pilot fix]; (B) keep original `posted`, only post the reversal; (C) reports count reversed-and-voided entries in their original period. DEFERRED 2026-05-30 per owner: Má Tư is currently a Hộ Kinh Doanh (simplified bookkeeping per TT 88/2021, no formal BCTC filed), so the retroactive-statement risk is moot. Revisit ONLY if converting to a company form (CTCP/TNHH) that files BCTC.

### M6 Finance — HĐĐT compliance (audit 2026-04-28)

> Compliance audit `docs/ref/einvoice-tax.md` ↔ implementation. Pilot OK với cashier issue path; các gap dưới chặn scale + production-grade NĐ70/2025.

> **2026-05-23 update:** HĐĐT is active through Viettel S-invoice only. Runtime/provider docs must point to Viettel S-invoice.

- [x] **Provider active: Viettel S-invoice only** — MISA runtime provider removed; no `INVOICE_PROVIDER` switch. HĐĐT env source is `SINVOICE_*` + `COMPANY_TAX_CODE`.
- [ ] **P0: HĐĐT reconcile cron (orphan `signing`)** — DEFERRED to post-pilot. Manual recovery via admin retry button covers pilot volume.
- [ ] **P0: HĐĐT replace flow (TT 78)** — DEFERRED post-pilot. Pilot cancel + manual Viettel S-invoice portal đủ.
- [ ] **P1: HĐĐT provider config qua `system_settings` (encrypted)** — DEFERRED post-pilot. Env-only acceptable cho single-tenant CTCP.
- [ ] **P1: HĐĐT PDF/XML persist + download UI** — DEFERRED post-pilot. Link Viettel S-invoice portal đủ.
- [ ] **P2: 3-way matching UI cho `supplier_invoices`** — bảng + columns (`matching_status`, `is_vat_deductible`, `declared_period`) đã có nhưng không có UI workflow PO ↔ GRN ↔ Supplier Invoice. Kế toán phải đối chiếu tay → không export được Tờ khai 01/GTGT đúng. Independent of HĐĐT provider cleanup.

### M7 Payroll

- [x] **`payroll_entries_select` RLS** — status='paid' self-read gate. h3c (`20260507181413`) added the gate but LEFT the older permissive `pe_employee_self_select` (any-status self read) in place → Postgres OR'd it away, so employees could still read DRAFT payroll. Migration `20260602008000` drops `pe_employee_self_select` + redundant `pe_select`/`pe_manage`; verified on matu-dev only `payroll_entries_select` (paid-only self) + `payroll_entries_write` remain. Owner applies prod.
- [x] HR payroll actions permission-gated — `createPayrollPeriod` / `calculatePayroll` / `fetchPayrollEntries` require `finance:payroll_calculate`; approve/paid transitions require `finance:payroll_approve` at the action layer.
- [x] `branch_manager` with null `branch_id` widens to tenant-wide writes — CLOSED 2026-05-30: `requireBranchScope: true` wired into **22 branch-scoped mutating actions** across 10 files (HR shifts/attendance/check-in/assignments/shift-requests, inventory adjust/stocktake/issue/transfer/correction/supplier-return, KDS station categories, delete zone/table), rejecting branch_manager/cashier with null `branch_id` before any tenant-wide write. POS mutations keep their `customAuth` inline branch guard. Tenant-wide data (menu catalog, QC settings) intentionally EXCLUDED. typecheck + eslint clean. Rule REQUIRE-BRANCH-SCOPE-ON-BRANCH-MUTATIONS.
- [x] Daily HMAC clock-in code reused all-day → HARDENED 2026-05-30 (commit pending) with a GRACEFUL shift-window guard in `clockIn` (rule CLOCK-IN-SHIFT-WINDOW-GRACEFUL): when the employee is rostered today, clock-in is restricted to their shift window ±60m via `isWithinShiftWindow`; when unrostered, falls through to code+GPS so nobody is wrongly blocked. Direct-REST BYPASS CLOSED 2026-05-30 (rule ATTENDANCE-INSERT-SERVICE-ROLE-ONLY): `attendance_records` INSERT revoked from `authenticated`+`anon` (migration `20260602009000`) and clock-in/HR check-in now write via the service client, so the action's code/GPS/shift checks can no longer be skipped by POSTing the table directly to PostgREST. (Root cause: `attendance_self_checkin` permissive policy from 2026-04-17 OR'd away h3c's shift gate.) Unrostered staff clocking in is intended graceful behavior, not a residual risk.
- [ ] **Attendance: employees can DELETE their own records** (found 2026-05-30) — `attendance_write` (FOR ALL) USING permits self rows, and `authenticated` retains DELETE on `attendance_records`, so an employee could erase their own attendance history via the REST API. Consider revoking DELETE from authenticated/anon (attendance is effectively append + manager-correct) or excluding self from the DELETE branch of `attendance_write`. Lower priority than the INSERT bypass (already fixed).
- [ ] **Broad `anon`/`authenticated` table privileges (schema-wide audit)** (found 2026-05-30) — on `attendance_records`, both `anon` and `authenticated` still hold DELETE/UPDATE/TRUNCATE/REFERENCES/TRIGGER (only INSERT was revoked for the clock-in fix). TRUNCATE/REFERENCES/TRIGGER are not reachable via PostgREST, and RLS gates DELETE/UPDATE, but this is over-granting that likely spans many public tables (probable `GRANT ALL ... TO anon, authenticated` in bootstrap). Audit the schema and tighten to least-privilege so RLS is defense-in-depth, not the sole gate.
- [ ] No audit on `insurance_base_salary`/`gross_salary` changes — BHXH compliance.
- [ ] Drop legacy `employees_manage`/`shifts_manage` if any still active (m4c3 cleanup audit).
- [ ] **`payroll-client.tsx` stale vs actions/types** (found 2026-05-30 attempting to remove its blanket `@ts-nocheck`): (1) `createPayrollPeriod({ periodMonth: <"YYYY-MM" string> })` but `createPeriodSchema` wants `{ month, year }` numbers → **creating a payroll period from the UI fails zod validation** (real runtime bug, latent while payroll is Excel-managed); (2) `formatMonth(period_month:number)` but `formatMonth` expects a `"YYYY-MM"` string; (3) the list reads `total_gross/total_si/total_pit/total_net`, absent from `PayrollPeriodRow` + the page query → always-empty columns. Fix path: decide wire per-period totals vs drop the 4 columns, fix the create-period args + formatMonth, then drop the `@ts-nocheck`. Deprioritized (HKD pilot runs payroll in Excel for <5 employees).

### Network gate (D9)

- [x] **Per-agent presence token** — global web-side `PRINT_AGENT_PRESENCE_TOKEN` retired for the route. `PRINT_AGENT_PRESENCE_TOKEN` is now agent-local raw secret; web stores SHA-256 hash in `printer_agent_presence_tokens` for `(tenant_id, branch_id, agent_id)`.
- [x] **Rate-limit on `/api/branch-presence`** — durable DB-backed 1 request / 30s per active token in `register_branch_presence`, plus fresh same-IP `last_seen_at < 60s` skip.
- [x] **`settings:branch_network` permission key enforced** — `network-config-actions.ts` passes `permission` + branch scope into `withAction`.
- [x] **Network trusted-IP DML RLS uses `has_permission()`** — `20260526010000_network_gate_branch_permission_rls.sql` replaces legacy `auth_role()` checks for insert/update.
- [x] **Soft-revoke race** — agent registration RPC rejects revoked trusted IP rows and updates active rows with `revoked_at IS NULL`; heartbeat never sets `revoked_at = NULL`.

## Payment / HĐĐT integrations (live in production)

- [x] P0: Wire VietQR real bank API — production credentials active, EMVCo QR generated locally per NAPAS, cashier-confirm path live.
- [x] P0: Wire Momo real API — production credentials active, IPN webhook handler live at `apps/web/app/api/webhooks/momo/route.ts`.
- [x] HĐĐT real API active via Viettel S-invoice (không dùng MISA)
- [x] P1: Momo webhook atomic RPC wired in app/types
- [x] P1: Momo webhook atomic RPC prod-applied and exercised by live provider callbacks.

## Branch Kitchen site split (Phase 2)

> Decision 2026-04-23 — tách tồn Kho CN / Bếp CN qua `inventory_locations`.

- [x] Migration `20260417040000_inventory_locations_phase1.sql` + compat columns APPLIED
- [ ] Phase 2 dual-write + cutover
- [ ] Seed 1 `inventory_locations` kitchen/warehouse per branch (`is_default_consumption`)
- [ ] Rút gọn state machine Kho CN → Bếp CN: `draft → confirmed` (cùng roof, không in_transit)
- [ ] Implement intra-branch transfer một bước cho `Kho CN -> Bếp CN` (`Cấp bếp`) bằng RPC atomic riêng
- [~] OBSOLETE per owner policy 2026-05-28 "không trừ kho": item này (RPC phải resolve `default_consumption` hoặc fail hard) không còn áp dụng vì stock deduction sẽ bị tắt toàn diện ở payment flow (action layer + webhook leg). RPC `consume_stock_for_order` có thể được DROP qua migration riêng khi owner cho phép — đến khi đó, action-layer callsite (`createPayment` line 768 + `confirmPayment` line 956) cần được remove ở slice riêng (separate from refactor). See memory `project_pos_action_helper_refactor.md` "Owner policy 2026-05-28" section.
- [x] Retire `stock_issue(issue_type='kitchen_use')` — runtime CHECK đã chặn; docs active phải trỏ sang intra-branch transfer

## Sprint 6 — Inventory UX follow-up

> Sprint 5 shipped #1 dead-code, #2 atomic `create_grn_from_po` RPC, #4 `formatVND` shadow consolidation. Sprint 6 shipped F-017 PO display ID + Fix #3 stage A foundation.

### ✅ Shipped in Sprint 6

- **F-017 PO display ID** (commit `b0888c96`): `next_po_display_id(tenant_id)` RPC + `display_id` column + backfill `PO-LEGACY-XXXXXX` + display layer. Year scoped to `Asia/Ho_Chi_Minh`. New PO writes get `PO-YYYY-####` zero-padded.
- **Fix #3 Stage A** (commit `3658b15c`): partial UNIQUE index `uq_grn_active_draft_per_user_supplier` + `loadActiveGrnDraft` + `discardGrnDraft` server actions + `createGrnDraft` UNIQUE_VIOLATION fallback. Foundation for server-side draft lifecycle.

### ✅ Sprint 6 Stage B + C

- [x] **#3 Stage B: Client refactor `grn-create-client.tsx`**:
  - Removed workflow `localStorage` helpers and browser storage calls.
  - RSC pre-fetches via `loadActiveGrnDraft({ supplierId })` in `/grn/new/[supplierId]/page.tsx` and passes `existingDraft`.
  - Lazy-creates server draft on first `saveLine`; subsequent saves call `upsertGrnLine`.
  - `discardDraft` calls `discardGrnDraft({ grnId })`.
  - Submit is navigation-only to review because lines are already persisted server-side.
- [x] **#3 Stage C: Drafts page + cleanup**:
  - `drafts/page.tsx` (RSC) + `page-client.tsx` consume server-side `goods_received_notes status='draft'` data.
  - Deleted retired `apps/web/app/(protected)/inventory/_lib/mobile-draft.ts`; shared pure types/helper now live in `grn-draft.ts`.
  - Replaced `startGrnFromPo` URL-flash redirect with `GrnFromPoList` Sonner toast handling.
- [ ] **#3 Optional post-deploy cleanup**:
  - Decide whether to add a one-time legacy-import shim for in-flight browser drafts.
  - Decide whether to add a stale draft cleanup job (14d retention).

### ⏸️ Blocked / deferred

- [ ] **F-018: Supplier "Khác"** — BLOCKED-PRODUCT. 0 occurrences in code (data-level only). Need product input on (a) require formal NCC, (b) "Mua ngoài" + inline note, or (c) accept generic "Khác" as catch-all.
- [ ] **F-009: Stock master-detail as drawer** — invasive refactor; current side-panel acceptable.

## Doc maintenance reminders

- Khi Inventory behavior thay đổi → update `docs/ref/inventory.md` + `inventory-sop.md` + `docs/modules/web-app.md` + `docs/worklog/inventory/adoption-matrix.md` cùng PR
- Khi triển khai phase tách tồn thật → update active Inventory docs in the same slice.

## Deferred to post-pilot

- [ ] Automated E2E POS→payment→stock (P2, trước scale 3+ chi nhánh)
- [ ] Staging env / Vercel Preview (P2, trước external users)
- [ ] M7 BHXH/PIT calc wiring (Excel cho pilot <5 nhân viên)
- [ ] M6 VAS journal entries hoàn chỉnh (CSV kế toán cho pilot)
- [ ] Refunds table + flow
- [ ] M5-Ext S8 — yield factor + AP aging + consumption variance (chưa cần ở 30-50 SKU)
- [ ] Finish remaining `can_access_branch` khỏi `auth_role()` bằng một batch RLS-policy riêng (α4c)
- [ ] **H3: `area_manager` tenant-wide access scoping** — currently `area_manager` has tenant-wide access because no area-scoping table exists. May need (a) `areas` table + `staff_areas` join + migration, or (b) confirm tenant-wide is the intended pilot semantic and rename the role. Surface from `docs/CODEBASE_MAP.md` Critical Unknowns. Independent of refund-flow `area_manager` scope hole in `apps/web/app/(protected)/orders/refund-actions.ts` (TS layer fix, see above).
- [ ] **H3b function update — has_permission() dual-source flip** (defer until 2nd silent-demote incident). H3a NOT NULL invariant đủ ngăn position_id NULL. Column `tenants.owner_user_id` đã ship via `20260601500000` làm data foundation; function update chỉ flip nếu real incident occurs. Per ADR 0005 minimum-regret synthesis.
- [ ] **transfer_ownership(p_new_user_id UUID) RPC + UI** — blocked on business design. Need decisions: instant transfer vs 2-phase (proposed→accepted), `representative` legal-name sync semantics, audit-log shape, RPC permission gate (only current owner? + ops escape hatch?). Manual SQL UPDATE acceptable cho pilot. Reference ADR 0005.
- [ ] **L6: Finance migration chain ADR** — 5 finance migrations form implicit chain (`20260506000000` cashflow + COA, `20260507000000` journal_entry period guard, `20260508000000` B03_DN cashflow indirect, `20260509000000` VAT per-line, `20260510000000` M4 refund foundation). Add ADR documenting prerequisite ordering + rollback dependencies. Audit found in 2026-05-07 wave; non-blocking but valuable cho new engineers.
- [ ] **Dead RPC drop wave 2** (post-pilot) — Tier A pilot (`20260601700000`) dropped 3 RPCs (backfill_permissions_from_role, \_auth_v2_is_tenant_wide_role, seed_posting_rules). Next wave needs `pg_stat_user_functions` telemetry from real pilot traffic. Tier B (reporting/cron-style), Tier C (lifecycle helpers), Tier D (NEVER drop — ops/auth) — see regression rule RPC-DROP-MUST-SCAN-6-CHANNELS for methodology.

## Architecture / Schema / Migration optimization (matu-dev) — 2026-05-30

> Owner approved rebuilding matu-dev ("không cần giữ nguyên"). Phased; matu-dev only, owner applies prod.

**Phase 1 — schema cleanup (DONE 2026-05-30):**
- [x] RLS policy dedup (`20260602010000`): closed `stock_transfer_items` any-tenant-member read+write bypass (SECURITY) + dropped redundant `payroll_periods` (`pp_select`/`pp_manage`) and `attendance_records` (`attendance_employee_self_select`) policies. Rule NO-SUPERSEDED-PERMISSIVE-POLICY.
- [x] Schema health verified clean on matu-dev: 0 RLS-disabled public tables, 0 SECDEF functions missing search_path, 0 tables without PK, 0 exact-duplicate indexes (greenfield P5 already deduped 2).
- [ ] Dead RPCs (13 candidates, greenfield P5) — OWNER-GATED T3, NOT dropped: all on money/auth/order paths and the monorepo grep cannot see the Flutter/external clients, so "no caller" is not conclusive. Each needs per-RPC owner sign-off + external-client confirmation.
- [ ] Unused indexes (~231 flagged on prod) — deferred: prod `stats_reset=NULL` and 77% `idx_scan=0` is implausibly high (recent stats reset), not representative. Re-assess only after ≥1 business cycle incl. month-end (`auto_close_periods`, `weekly_*_report`).

**Phase 2 — migration consolidation / baseline squash — EXTRACT DONE (Docker-free), replay confirmation pending target:**
- Runbook: `docs/runbooks/matu-dev-migration-squash-2026-05-30.md`. 2026-05-30: extract done **without Docker** via libpq `pg_dump 18.3` (CLI's Docker is bypassable) — faithful 118-table artifact reflecting Phase-1-cleaned state, minimal managed-surface noise (0 publications/extensions via `--schema=public`), only 2 trivial replay fixups. Acceptance manifest: 118 tables / 279 fns / 273 public policies / 523 indexes / 6 matviews / 10 cron / 11 realtime.
- [x] Replay confirmation — PASSED 2026-05-30 (owner chose rebuild). `DROP SCHEMA public CASCADE` + baseline + realtime re-add applied to matu-dev via `psql --single-transaction` (atomic): exit 0, rebuilt EXACTLY (118/279/273/523/6/11, auth hook, Phase-1 fixes intact); `db:types` from rebuilt matu-dev == committed types (no diff); typecheck 6/6. The baseline boots from empty — the property the 378-chain lacks. matu-dev now runs on the single baseline (confirmed == greenfield-cut schema).
- [x] **Step 6 — baseline-first repo consolidation (option X)** — DONE 2026-05-30 (commit `60c81ffd`): `supabase/migrations/00000000000000_baseline.sql` (validated public dump + fixups) is the fresh-env install path; the 379 historical files moved to `supabase/migrations/_archive/`; `supabase/migrations/README.md` documents it. Option X: prod keeps its history. `lint:db-boundary` passes.
- Phase 2 CORE COMPLETE. Remaining follow-ups (non-blocking):
  - [x] Toolchain: Docker-free libpq pg_dump engine is now the DEFAULT in `scripts/supabase-baseline-extract.mjs` (commit `f92cc4d4`; `--engine=cli` keeps the Docker path). `pnpm db:baseline:extract -- --project-ref=nikkridjukdbqvkvqlmi` reproducibly rebuilds the greenfield baseline from matu-dev, no Docker (validated: 118 tables, fixups applied).
  - [x] Footgun fixed (commit `8f73885f`): `pnpm db:types` resolves matu-dev via `.env.local` (cut dev schema) before the prod fallback; bare run now yields the committed cut types (no diff).
  - [ ] **Managed-surfaces companion** — the last gap for a true from-zero greenfield install: package extensions / storage buckets+policies / realtime publication / cron jobs from matu-dev into a clean (boundary-safe) install step. (matu-dev's rebuild kept them since only `public` was dropped; the existing `docs/plan/supabase-managed-surfaces-install-bundle.sql` has them but with greenfield wording + storage-owner privilege caveats.)
  - [ ] (cosmetic) Repair matu-dev `schema_migrations` history to baseline-first (schema already rebuilt from baseline).
  - [ ] Live-prod application of the 3 Phase-1 fixes (now under `_archive/`: `008000`/`009000`/`010000`) is OWNER-gated and NOT done by the agent — and per owner 2026-05-30 **NOT to matu-prod**. matu-dev + the baseline already include them.
- [ ] Per `docs/plan/live-schema-first-baseline-extraction.md` (owner-approved prep): the 378-file `supabase/migrations/` chain can't replay from empty (ordering bug at `20260508055046` — references `order_items.vat_rate` before `20260509000000` creates it). Squash matu-dev's cleaned schema into one baseline via `pnpm db:baseline:extract` → validate replay on empty scratch DB (`db:baseline:local-check`) → rebuild matu-dev → regen types → typecheck/build. Open blockers: POS/payment/stock mutation contract, managed surfaces (auth/storage/realtime/cron must be recreated explicitly — squash omits them), data migration kept separate.

**Phase 3 — architecture docs reconcile — NOT STARTED:**
- [ ] Sync `docs/spec/database-schema.md` + `docs/CODEBASE_MAP.md` to the consolidated baseline once Phase 2 lands.

## Post-v1.0 (Tier 2)

- [ ] Local-First Branch (mini PC + SQLite, offline POS/KDS)
- [ ] QR Self-Order tại bàn
- [ ] Loyalty / Vouchers
- [ ] CMS / CRM Foundation
- [ ] Advanced Analytics
- [ ] VNPay integration
- [ ] Employee portal full features
- [ ] **Native POS migration (PWA → Flutter Android)** — đánh giá khi pilot phát sinh: BT/USB printer fail >5%, cash drawer auto-pop, e-wallet native SDK > deeplink, scale ≥20 chi nhánh. Stepping stone: Capacitor wrap (~1-2 tuần) trước Flutter rewrite (3-6 tháng + 2× maintenance)
