# Current Tasks

> Active work tracker for the in-place `comtammatu` production track. v1.0.0 is operating on real branches; ongoing work is hardening + feature follow-ups. **Shipped history is condensed into one-line summaries** (durable detail lives in git, `tasks/regressions.md`, `tasks/lessons.md`); this file tracks ACTIVE + deferred work. Decisions referenced as `D0xx` live in `docs/plan/decisions.md`.

## Module status (snapshot)

M0–M7 + Auth + POS PWA + Realtime hardening + Shadcn primitive migration M1–M9 — **all SHIPPED**. External integrations VietQR + Momo are wired with production credentials and active in real branches. HĐĐT is active through Viettel S-invoice only.

## Now (active, agent-doable)

- [~] **HRM rework — chấm công per-shift (D027, NỀN)** — owner xác nhận MỌI NV làm 2 ca/ngày (prod: span 7h=1 ca, 49% treo). Gate typecheck/lint/build xanh. **CORE DONE:** ✅ (1) migration `20260615130000_hrm_per_shift_attendance.sql` (`shifts.branch_id` NULL=Global + seed 2 ca Global "Ca sáng"/"Ca chiều" + deactivate ca per-branch cũ; attendance unique → `(employee,date,shift,tenant)` + `shift_id` NOT NULL + backfill theo giờ check_in; FK shift_id → RESTRICT; RPC `employee_clock_in_with_checklist` validate ca global + dedupe per-ca). ✅ (2) clock-in đọc ca global (`branch_id.is.null,branch_id.eq`) + existing-check/dedupe per-ca + lỗi rõ khi chi nhánh chưa khai ca. ✅ (3) `today-work-state` 2-ca: resolve ca hiện tại theo giờ → drive state; nudge ca treo mở rộng (ngày cũ + ca khác trong ngày). ✅ (4) ngày công `Σ ngày(min(ca,2)×0.5)` ở `fetchAttendanceSummary` + payroll `working_days` + card lịch nhân viên. (5) checklist snapshot per-ca: RPC đã snapshot riêng mỗi bản ghi ca (cùng template employee default); template RIÊNG sáng/chiều = sau (gắn D026 §2). ✅ (6a) **UI Thiết lập Ca Global** — shifts CRUD → global owner-only (service client; bỏ branch); `fetchShifts()` no-arg trả ca global; `shift-form-dialog`/`shifts-table` bỏ chọn chi nhánh + `canManage` gate; hr-client shifts tab load global. ✅ (6b-home) màn Hôm nay nhân viên: panel "Ca hôm nay" hiện cả 2 ca (Ca sáng/Ca chiều + giờ thực + trạng thái) qua `todayShifts`. ✅ (6b-lịch) `/employee/schedule` lịch tháng hiện 2 ca/ngày: `ScheduleAttendance` mang tên ca + giờ ca (từ join), bỏ hẳn `ScheduleShift`/`shiftsByDate`; `attendanceByDate` → mảng; cell liệt kê từng ca (dot + tên + giờ vào), `SelectedDayDetail` liệt kê per-ca (tên · khung giờ · vào–ra · trạng thái); cập nhật cả `schedule/page.tsx` SSR. **CÒN LẠI:** (7) **owner apply migration `20260615130000` + `pnpm db:types`** rồi gỡ cast `branch_id: null as unknown as number` ở `createShift`; (8) ✅ seed checklist theo vị trí (D026 §2) — **migration `20260615160000_hrm_checklist_seed_by_position.sql`** sinh từ bản chuẩn hoá `docs/plan/hrm-checklist-chuanhoa-2026-06-15.md`: **95 việc / 7 template** Global (Phục vụ 19, Phụ bếp 19, Quầy 12, Nướng 10, Tạp vụ 12, Cửa hàng trưởng 13, Bếp trưởng 10). Owner chốt: BỎ "Chấm công" (−4), BỎ việc-tuần "Nạp doanh thu" + item "Giám sát vận hành" agent tự suy (−2), BM chốt tồn tổng/vị trí khác báo khu mình. Idempotent (replace items theo tên, GIỮ template_id → FK `default_checklist_template_id` còn nguyên; FK `template_item_id` ON DELETE SET NULL nên snapshot cũ an toàn). ✅ gán template↔vị trí (D026 §2, model **position-default + per-person override**): `130000` thêm cột `positions.default_checklist_template_id` (FK ON DELETE SET NULL) + RPC clock-in `COALESCE(employee override, position default)`. **Mapping sửa theo PROD reality** (verify: cashier=14 front, chef=9 bếp, branch_manager=3, waiter=2 sót, head_chef/kitchen_helper=0) — `20260615170000` CHỈ map 2 vị trí 1-role-1-checklist: **cashier→"Phục vụ"** (front: phục vụ + thu tiền; "Quầy" là KHÂU BẾP không phải thu ngân), **branch_manager→"Cửa hàng trưởng"**. **chef = nhiều khâu (Nướng/Phụ bếp/Quầy/Bếp trưởng) dưới 1 position → KHÔNG position-default; gán per-person** từng khâu qua override (owner làm thủ công). **Apply ORDER: `130000` → `160000` (seed 7 template) → `170000` (mapping) → `pnpm db:types`**. Còn (owner/sau apply): chuyển 2 NV waiter→cashier (role-merge D012 trong DB chưa làm — task T3 auth/ACL riêng); gán per-person 9 chef theo khâu + Tạp vụ; UI sửa position-default + UI scope field; employee-table Select hiện "Chưa gán" cho NV dùng position-default. Bản chuẩn hoá nguồn: **CHẶN trước khi seed:** ✅ (i) cột `scope` (every_shift/opening/closing/weekly) + CHECK trên `shift_checklist_template_items` + `attendance_checklist_items` — owner chốt hướng (a) (đã gấp vào migration `20260615130000`). ✅ (ii) RPC `employee_clock_in_with_checklist` lọc snapshot theo ca (ca mở=earliest start nhận every_shift+opening; ca đóng=latest nhận every_shift+closing; weekly loại) + copy scope vào snapshot. ✅ (DB enum chuẩn tiếng Anh, owner yêu cầu) `phase` đổi `dau_ca/trong_ca/cuoi_ca` → `start_of_shift/during_shift/end_of_shift` + `scope` = `every_shift/opening/closing/weekly`; gồm UPDATE data + swap CHECK (`*_phase_valid`) + default + COMMENT + CREATE OR REPLACE `upsert_shift_checklist_template` (validate EN) + 6 file app TS (`checklist-types`, `today-work-state`, `tasks-client`, `attendance-table`, `checklist-actions`, `checklist-templates-table`) — gộp vào migration `20260615130000`. **CÒN:** ✅ (iii) gate `employee_request_clock_out` ĐÃ lọc `is_required = true` sẵn (bản applied `20260610170000` — verified prod; agent trước trích nhầm bản cũ `20260609100000`). Không cần sửa. ✅ (iv-write) `upsert_shift_checklist_template` (trong `20260615130000`) đã đọc/validate/insert `scope` → write-path sẵn sàng. (iv-UI) form editor thêm field `scope` + read-select **GATED sau apply** (đọc scope cần cột tồn tại; làm write-only pre-apply sẽ reset scope khi sửa template, hoặc select cột chưa có sẽ vỡ tab) → land 1 slice sau khi owner apply + `db:types`. (v) `pnpm db:types` sau owner apply. Owner còn quyết: `cleaner` code cho Tạp vụ, cơ chế việc-tuần, 'Chấm công' có phải item checklist, phân vai tiền/tồn kho, giữ 2 việc trong_ca Phục vụ. Contract: `docs/worklog/hrm-redesign-2026-06-15.md` §Rework per-shift.
- [~] **HRM redesign — Đợt 1 (không cần owner)** — contract `docs/worklog/hrm-redesign-2026-06-15.md`. Gate typecheck/lint/build xanh. Behavior-verify runtime cho (b)(c) còn treo (env dev trỏ PROD branch 3 → không chạy daemon/app với env thật; code mirror pattern đã verify). ✅ (a) `attendance-table.tsx` SummaryView bỏ 3 cột Đi muộn/Vắng/Nửa ngày → "Số công" + "Ca chưa kết"; DetailView nhãn "Treo (chưa kết ca)" (check_in có / check_out NULL / date < hôm nay). ✅ (b) `cancelCheckoutRequest` (mirror `clockOutManagerShift`, guard check_out/approved NULL chống race) + nút "Rút yêu cầu kết ca" ở panel checkout_pending. ✅ (c) `today-work-state.ts` thêm `staleOpenShift` (query phụ additive) + nudge "Còn ca chưa kết" ở `/employee` home. ✅ (d) xóa dead code `checkIn`/`bulkCheckIn`/`checkOut` + provenance (0 caller). ✅ (e-một-phần) đổi nhãn tab "Chấm công" → "Ngày công"; card tổng Ngày công/Ca chưa kết/Nghỉ phép ở `/employee/schedule`. **Còn lại Đợt 1 — phần lớn GỘP vào rework per-shift (D027):** giờ check-in/out THỰC + 2-ca hiển thị, ngày-công đúng (`công = Σ ca/2`) → làm trong rework D027. Lưu ý: (a) nhãn "Số công" hiện = số dòng; sau rework 1 dòng = 1 CA nên phải đổi thành `công = Σ ca/2`. `standard_days` config + clamp vẫn đi cùng Đợt 3 (cần số-công-chuẩn, đếm theo NGÀY).
- [~] **WS-3 client-shell decomposition** — split the 3 monolith client shells into `_hooks/` (state / realtime / actions) + `views/`: `pos-desktop-shell` (~1.9k), `order-detail-sheet` (~1.6k), `grn-detail-client` (~1.5k). Higher risk (realtime subscribe/unsubscribe) → needs running-app verify. One PR per file. Goal = one clear concern per file (cohesion, NOT a line count). The action-file barrels (order / grn / production / inventory) are already decomposed by concern.
- [ ] **Owner metric definitions (blocks dashboard polish)** — (1) dashboard mixes two financial frames: `doanh thu` = tiền đã thu vs `lãi gộp` = subtotal trước VAT chỉ trừ nguyên liệu; chốt định nghĩa metric trước khi sửa. (2) device-signal tile chỉ xét lại nếu sau fail-silent wave vẫn sót lỗi in.
- [~] **Residual broad grants** — `anon`/`authenticated` still hold TRUNCATE/REFERENCES/TRIGGER on most tables (cosmetic over-grant from bootstrap `GRANT ALL`; NOT exploitable via PostgREST). Tidy schema-wide when convenient; `bmidl_write` legacy `auth_role()` folds into α4c. (Audited 2026-05-30: DELETE surface well-controlled — no tenant-only holes beyond the two already fixed.)
- [ ] No audit on `insurance_base_salary`/`gross_salary` changes — BHXH compliance. Low value while payroll is Excel-managed.

## Owner-gated / pre-go-live (NOT via agent)

- [ ] Real POS → payment → KDS/print → HĐĐT smoke in an approved dev/test or staging with live provider credentials. Stock leg is out of the chain per **D016**: action-layer landed, webhook stock leg disabled (`20260611001000`, applied prod); tail = remove `consume_stock_for_order` + callsites (see dead-RPC).
- [ ] **Dead-RPC candidates (owner-gated)** — 13 found by static scan (`handle_new_user`, `has_position`, `post_payroll_journal`, `release_table`, `resolve_po_price`, `resolve_po_prices_batch`, `rotate_branch_override_code`, `set_branch_kind`, `sync_missing_permissions_from_template`, `transition_order_status`, `transition_order_item_status`, `try_auto_approve_grn`, `update_my_profile`). NOT dropped — all touch money/auth/order/procurement or external-client risk; grep cannot prove no external caller. Each needs per-RPC owner sign-off + T3 migration (rule `RPC-DROP-MUST-SCAN-6-CHANNELS`). Includes the **D016** `consume_stock_for_order` removal tail.
- [ ] Apply the 3 Phase-1 RLS fixes (`20260602008000` payroll, `009000` attendance, `010000` dedup) to live prod IF still serving — OWNER-gated. Baseline already includes them.
- [ ] **Print pipeline apply** — migrations `20260611120000` (receipt VietQR) + `20260611150000` (template-editor RPC); after apply run `pnpm db:types` + drop the 2 `as never` casts in `templates/actions.ts`; deploy print-agent bundle v1.0.0 to the 3 branches (Phước Hải on 0.2.0) + smoke `PRINTER_HOST=<ip> pnpm test:print` before rollout.
- [x] **Applied super_manager removal** (D018) — migration `20260613110000_remove_super_manager_fold_owner.sql` applied to prod `iexws` 2026-06-13 (owner-delegated in-session, dry-run ROLLBACK then COMMIT, DO-block self-check passed) and verified: positions/role_templates/notifications/auth-metadata carry 0 super_manager, keeper user → owner, `staff_role_from_position_code('super_manager')` → NULL; ledger row `20260613110000` recorded. No `pnpm db:types` (data + function bodies only).
- [ ] Uptime monitor on `/api/health` (UptimeRobot — ops, not code).
- [ ] **HRM redesign — Đợt 2/3 (theo D026)** — contract `docs/worklog/hrm-redesign-2026-06-15.md`. Đợt 2: (a) tạo NV 1 bước (`createUser`+insert `employees` 1 transaction, bỏ dán UUID) + `updateEmployee` + ngưng việc (`is_active`+`end_date`) + SĐT vào bảng; (b) checklist gán theo `positions.code` mặc định + override theo người (mở rộng RPC `apply_checklist_template_to_role` khỏi chỉ-Global nếu cần); (c) nghỉ phép notification 2 chiều + quick-action "Xin nghỉ" ở Hôm nay + gộp pending toàn-CN; (d) đổi nhãn `/admin/staff` → "Tài khoản & phân quyền". Đợt 3 (payroll qua `base_salary` per D026 §3, KHÔNG khôi phục UI hợp đồng): UI nhập `base_salary`+`dependents_count` ở hồ sơ NV + gỡ phụ thuộc 0-contract trong `calculatePayroll` (eligibility theo base_salary); helper `standard_days` cố định + clamp ≤ base (D026 §1); Export Excel/CSV; view đối chiếu Ngày công trước Duyệt; PIT trên phiếu lương; gỡ orphan payroll (link `/hr`→`/hr/payroll`). IA: gom 5 tab → 3 trục Người·Ngày công·Lương, Ca+mẫu-checklist → "Thiết lập".
- [ ] **HRM redesign — câu hỏi IA còn mở (owner)** — (a) payroll thêm vào nav hay ghi quyết định ẩn-có-chủ-đích như D013 (hiện orphan vô tình); (b) gộp `/admin/staff`+`/hr`-employees ngay hay chờ W5; (c) ảnh selfie check-in có ai xem/dùng không (nếu không → cân nhắc bỏ). D026 "Còn mở".

## Deferred post-pilot

- [ ] **POS calls provider before DB lock** — RPC fail = orphan gateway order. DEFER-WITH-MITIGATION.
- [ ] **HĐĐT post-pilot (P0/P1/P2)** — reconcile cron for orphan `signing` (admin retry covers pilot); replace flow TT 78 (pilot cancel + manual portal); provider config via encrypted `system_settings` (env-only OK single-tenant); PDF/XML persist + download UI (portal link OK); 3-way matching UI for `supplier_invoices` (columns exist, no PO↔GRN↔Invoice workflow → 01/GTGT export blocked).
- [ ] **Branch Kitchen site split Phase 2** — dual-write + cutover; seed 1 `inventory_locations` kitchen/warehouse per branch; reduce Kho CN → Bếp CN state machine (`draft → confirmed`); intra-branch `Cấp bếp` atomic RPC.
- [ ] **Refund partial-refund T3 slice** — approving a partial refund flips the whole `payments.status` to `refunded`, which blocks a second refund on the remainder (`create_refund` requires `completed`) and overstates `get_revenue_kpis.voided_amount` (SUM of `p.amount`). Pre-existing.
- [ ] Remove dead branch `order_payment_status === "refunded"` in `apps/web/app/(protected)/finance/reconciliation/reconciliation-client.tsx` — unreachable after `20260612120000`.
- [ ] Refunds table + flow correctness gaps (storage shipped; tracked under M4).
- [ ] Unused indexes (~231 flagged on prod) — re-assess only after ≥1 business cycle incl. month-end (`stats_reset` was recently NULL → not representative).
- [ ] **F-018 Supplier "Khác"** — BLOCKED-PRODUCT: need owner input (require formal NCC / "Mua ngoài"+note / accept generic "Khác").
- [ ] **F-009 Stock master-detail as drawer** — invasive refactor; current side-panel acceptable.
- [ ] P3 Login rate-limit fail-open when Upstash unreachable — documented design decision, needs observability.
- [ ] Legacy `auth_role()` cleanup **α4c** — finish `can_access_branch` removal via a dedicated RLS-policy batch (α4b shipped `20260601810000`).
- [ ] Ops reconciliation query for Momo payment/order desync in `/admin/finance` (post-launch tooling).
- [ ] Automated E2E POS→payment→stock; staging env / Vercel Preview (before external users); inventory smoke runbook (`docs/runbooks/inventory/pre-release-qa.md`) run periodically.
- [ ] **H3b** `has_permission()` dual-source flip — defer until 2nd silent-demote incident (`tenants.owner_user_id` shipped `20260601500000`). Per ADR 0005.
- [ ] **transfer_ownership(p_new_user_id) RPC + UI** — blocked on business design (instant vs 2-phase, representative sync, audit shape, permission gate). Manual SQL UPDATE OK for pilot. ADR 0005.
- [ ] **L6** Finance migration-chain ADR — document the 5-migration finance ordering + rollback deps (non-blocking).
- [ ] **Dead-RPC drop wave 2** (post-pilot) — needs `pg_stat_user_functions` telemetry from real traffic. Tiers B/C/D per `RPC-DROP-MUST-SCAN-6-CHANNELS`.
- [x] **Dead role-string cleanup** — applied to prod 2026-06-13 (migration `20260613130000_drop_dead_role_strings`): regex-stripped `super_manager` + the retired area-scope role from 37 array-membership functions, hand-recreated `admin_update_profile` + `toggle_profile_active` (dead `ELSIF` branch removed, SECURITY DEFINER preserved), ALTERed 13 RLS policies. Dry-run ROLLBACK then COMMIT, self-check = 0 residue, ledger recorded. Dead app error-matcher in `admin/staff/actions.ts` removed. Behavior unchanged (tokens were unreachable).
- [ ] M5-Ext S8 (yield factor + AP aging + consumption variance), M6 VAS journal entries, M7 BHXH/PIT calc wiring — accounting-dependent; Excel/CSV for pilot.

## N/A while Má Tư is a Hộ kinh doanh (no formal BCTC)

- [ ] `voidJournalEntry` closed-period void still mutates signed BCTC — current-period reversal guard EXISTS (`20260527000000`); REMAINING: voiding a _posted_ entry whose ORIGINAL `entry_date` is in a CLOSED period flips it → `voided` and statements (`statement-actions.ts:67,179`) filter `status='posted'` + `entry_date`, so the signed period changes retroactively. T3 decision: (A) reject void when original period closed [recommended], (B) keep original posted + only post reversal, (C) reports count reversed entries in original period. **DEFERRED** per **D012/D013**: HKD files no BCTC (TT 152/2025) → moot. Revisit only on conversion to a company form.

## Post-v1.0 (Tier 2)

> Owner trim 2026-06-10 (định hướng "phần mềm hỗ trợ Hộ Kinh Doanh" — see **D012**): Local-First/offline POS, VNPay, và Native POS migration (Flutter) ĐÃ LOẠI BỎ. Không đề xuất lại các hướng này.

- [ ] QR Self-Order tại bàn
- [ ] Loyalty / Vouchers
- [ ] Advanced Analytics
- [ ] Employee portal full features

## Shipped (condensed — see git / `regressions.md` for detail)

- **2026-06-14** — Supabase advisor waves (owner-applied in-session, prod `iexws`): definer revoke **Wave 5** (3 trigger/definer-chain internal helpers `compute_user_trust_score`/`grn_is_auto_approvable`/`sync_insurance_base` → authenticated-callable SECURITY DEFINER 161→158; corrected the caller-scan methodology — line-only `.rpc()` grep had under-counted callers); **auth_rls_initplan** 20→0 (data-driven `auth.uid()`→`(SELECT auth.uid())` wrap across 20 RLS policies); **FK covering indexes** hot wave (4 indexes; unindexed_foreign_keys 145→141). Worklogs `docs/worklog/supabase-{definer-revoke-wave5,rls-initplan,fk-indexes}-2026-06-14.md`. Migration files `20260614090000/091000/092000` need committing to match the prod ledger.
- **2026-06-13** — POS/KDS/Runner fulfillment simplification (collapse `preparing`→pending at display/rank/count, Runner shows live queue only, no `ready` handoff row); **D017** steps 4–5: Admin L0 Tenant Command dashboard (KpiCard canonical + per-branch operations section) + Branch Command day-metrics dashboard.
- **2026-06-11** — UX E2E B1–B6 (ready→handoff, peak >15 đơn, fail-silent print/HĐĐT/runner alerts, fewer-taps, visual pass, W4.4 DataTable twins→0); UI molecule **D014** W0–W4 (loading/error/404 frames, StatusBadge SSOT + 3 vocab-vs-DB bug fixes, formatVND single style, KpiCard canonical, Empty/Confirm single path, DataTable canonical 6 real consumers); print-agent bitmap-only + VietQR on payment receipt; owner template editor + `packages/print-render` (agent esbuild single-file); HRM Phase 2 (drop `shift_assignments` + HR keys 9→5 + rename `hr:approve_checkout`).
- **2026-06-10** — HRM "1 trục Ngày công": leave grants backfill + drop shift-register flow + drop `shift_requests`; canonical position codes (lean to 11, English-only mappers, push-targeting fix); Branch Manager default-shift on clock-in + branch employee list; BM attendance simplification.
- **2026-06-09** — HKD Finance/Admin surface cleanup (operating finance default, accounting hidden per D013); Employee Portal mobile-first overhaul (multiple distill/motion/IA passes); per-role checklist templates; protected route-map + nav contract; checkout-approval flow.
- **2026-05-30** (git `ac95f841..43a3ec4b`) — HĐĐT B2B double-issue guard; payroll-draft + attendance-bypass + `stock_transfer_items` + RLS-policy-dedup fixes; `requireBranchScope` ×22; clock-in graceful shift-window; **baseline-first migration consolidation** (`00000000000000_baseline.sql` + forward chain, 358 archived) + managed-surfaces companion.
- **2026-05-27 — Shell helpers refactor** — `with-action.ts` (`withActionPositional`/`customAuth`/`afterSuccess`) + `rpc-error-map.ts`; all `Skip withAction` annotations removed (WS-1b/2 closed); WS-3 concern-split done for `order-actions` / `grn-actions` / `production-actions` / inventory `actions` (client-shell decomposition is the remaining tail — see Now).
- **2026-05-24** — Pilot hardening (snapshot-doc refresh, schema source-ladder, route-group migration, network-gate D9); Interface closure IF-001..012 (retired `matu-*` pilot layer, UI guards, Finance-Basic landing); Pre-deploy + M4 Payments + M6 Finance P0 (refund RPCs, webhook idempotency + MoMo tenant-binding + server-recompute total, audit-log RPC-only + PII strip, HĐĐT cancel-reason ≥20); Network gate D9 + VietQR/Momo live + HĐĐT via Viettel; Sprint 6 Inventory UX (F-017 PO display id, server-side GRN drafts).
- **Auth foundation** — H3 intermediate access scoping replaced by explicit branch grants / tenant-level permissions (no intermediate scope remains). Legacy `employees_manage`/`shifts_manage` verified absent.
