# Current Tasks

> Active work tracker for the in-place `comtammatu` production track. v1.0.0 is operating on real branches; ongoing work is hardening + feature follow-ups. **Shipped history is condensed into one-line summaries** (durable detail lives in git, `tasks/regressions.md`, `tasks/lessons.md`); this file tracks ACTIVE + deferred work.

## Module status (snapshot)

M0–M7 + Auth + POS PWA + Realtime hardening + Shadcn primitive migration M1–M9 — **all SHIPPED**. External integrations VietQR + Momo are wired with production credentials and active in real branches. HĐĐT is active through Viettel S-invoice only.

## Active implementation note — 2026-06-10

- [x] **Employee Portal + HRM leave requests:** T3 full debate captured in `docs/worklog/employee-leave-requests-2026-06-10.md` before coding.
      Skill plan: repo rules = engineering + skills + database + ui + workflow + references; external skills = supabase + supabase-postgres-best-practices + shadcn; runtime tools = local source/tests + browser smoke when auth/dev server allows; skipped = live Supabase apply until an owner-approved dev/test target is verified.
      Surface = `/employee/leave` and `/hr`; primary job = employee sends/cancels leave requests while HR/Branch Manager approves/rejects by branch; change type = T3 schema/RLS/RPC + UI wiring; primitives = existing Employee shell, HR tabs/table, and shadcn `Button`, `Badge`, `Dialog`, `Select`, `Table`, `Textarea`, `Empty`, `Item`.
      PM/BA/Dev/QA summary = keep leave secondary in Profile, enforce future inclusive date ranges and no self-review, write branch-scoped SECURITY DEFINER RPCs with explicit grants/RLS, and verify migration/action/UI/docs with static and full gates.

- [x] **POS responsive density hardening:** T2 self-review before coding.
      Skill plan: repo rules = engineering + skills + ui + workflow + design-system + POS workflow contract; external skills = shadcn + impeccable adapt/product register, with `impeccable` PRODUCT.md init skipped because repo UI SSOT is locked in `docs/spec/design-system.md`; runtime tools = local source/tests + browser smoke when auth/dev server allows; skipped = database/Supabase because this slice changes POS responsive composition only, not schema, RPC, RLS, ACL, payment, print, HĐĐT, or generated types.
      Surface = `/br/[branchId]/pos`; primary user job = staff on desktop/tablet can scan context + menu + draft without cramped panes, and staff on small phones only see tap targets that own a real next action; change type = responsive layout/touch density; primitives = existing `Button`, `Badge`, `Card`, `Drawer`, `Tabs`, `ToggleGroup`, `ScrollArea`, and POS route components.
      PM: scope = keep the anchor-first POS model but remove mobile bottom actions when there is no draft to act on, tighten menu tile density, and give the desktop work area more room; acceptance = no fake bottom CTA on empty draft, speed-entry still shows name/price/state/add affordance, and desktop/tablet panel widths do not squeeze the menu unnecessarily.
      BA: rules = table/order anchor remains mandatory, cart remains draft-only, append target stays an existing order, takeaway grid remains the Mang về anchor, and payment/print/HĐĐT behavior is unchanged.
      Dev: approach = patch responsive classes and source regression tests only; avoid new design tokens, route-local themes, primitive overrides, or workflow-state changes.
      QA: passed `pnpm --filter @comtammatu/web test -- --runInBand pos-takeaway-order-grid.test.ts` (web static suite 151/151), `pnpm typecheck && pnpm lint && pnpm build`, targeted `git diff --check`, and Playwright smoke at 390x844 + 1366x768. Unauthenticated `/br/3/pos` redirected to `/login?returnTo=%2Fbr%2F3%2Fpos` with no horizontal overflow or console errors. Seed QA login `cashier.phuochai@comtammatu.vn` was rejected by the current Supabase project, so post-auth POS shell smoke remains auth/session-dependent; no POS session was opened or mutated.

- [x] **POS/KDS operational PWA separation:** T2 self-review before coding.
      Skill plan: repo rules = engineering + skills + ui + workflow + PWA regressions; external skills = next-best-practices + browser:control-in-app-browser; runtime tools = local source/tests + in-app Browser smoke + full gates; skipped = database/Supabase because this slice changes manifest/install shell behavior only, not schema, RPC, RLS, ACL, auth, migrations, generated types, storage policy, payment, or print logic.
      Surface = `/br/[branchId]/pos` and `/br/[branchId]/kds`; primary job = staff installs and launches the correct operational app for the device: POS as portrait cashier flow, KDS as landscape kitchen board; change type = PWA manifest/shell boundary correction only; primitives = existing operational PWA provider/toolbar.
      PM: scope = keep POS and KDS as separate branch-scoped PWA entries and leave Branch Settings as a normal management hub; acceptance = POS manifest has POS-only scope + portrait, KDS manifest has KDS-only scope + landscape, and install hint dismissal is isolated per app and branch.
      BA: rules = branch id must stay normalized before entering manifest URLs, auth/ACL/proxy contracts are unchanged, service worker mutation NetworkOnly guards remain untouched, and Branch Settings does not become a fifth PWA surface.
      Dev: approach = patch the shared operational manifest builder, pass branch id into the POS/KDS PWA toolbar from route layouts, scope the install-dismiss storage key by surface and branch while preserving legacy POS dismissal, and update static tests; risk = accidentally widening manifest scope or introducing an async params mismatch in layouts.
      QA: passed `pnpm --filter @comtammatu/web test` (151), `pnpm --filter @comtammatu/shared test` (206), `pnpm typecheck`, `pnpm lint`, `pnpm build`, and targeted `git diff --check`. Runtime smoke on `pnpm dev:web`: POS manifest `/br/3/pos/manifest.webmanifest` returned 200 with `scope=/br/3/pos`, `orientation=portrait`; KDS manifest `/br/3/kds/manifest.webmanifest` returned 200 with `scope=/br/3/kds`, `orientation=landscape`; Branch Settings manifest URL redirected to login and no settings manifest route exists. In-app Browser confirmed the same POS/KDS manifest JSON and Settings redirect; Browser Statsig/Cloudflare noise was plugin telemetry, not app console/runtime failure.

- [x] **POS speed-entry workflow correction:** T2 self-review before coding.
      Skill plan: repo rules = engineering + skills + ui + workflow + design-system + POS workflow contract; external skills = shadcn + impeccable product register; runtime tools = local source/tests + browser smoke when session permits; skipped = database/Supabase/payment because this slice changes menu entry density and operational composition only, not RPC, RLS, ACL, payment, print, HĐĐT, or generated types.
      Surface = `/br/[branchId]/pos`; primary user job = phục vụ/thu ngân nhập món nhanh sau khi đã có bàn/đơn, nhìn được tên món, giá, trạng thái khóa/hết món, và thấy draft/order rail gần tay; change type = POS menu speed-entry UX + tests/docs, primitives = existing `Tabs`, `Button`, `Badge`, `InputGroup`, `ScrollArea`, `AppEmptyState`.
      PM: scope = replace catalog-style image cards with compact POS speed tiles so more items fit per viewport and item tapping is faster; acceptance = menu is dense, text-first, price/limit status visible, category/search stays sticky, and no extra workflow panel is introduced.
      BA: rules = table/order anchor still required before item taps, daily limit disabled/sold-out behavior unchanged, variants/modifiers still open customizer, cart remains draft-only, and payment/post-submit rule stays unchanged in this slice.
      Dev: approach = keep `PosMenuGrid` API and state model, change only menu tile composition/grid density plus static tests and POS contract docs; risk = over-densifying mobile, image removal expectation, and existing dirty POS worktree must be preserved.
      QA: passed focused/full web static tests (`pnpm --filter @comtammatu/web test -- --runInBand pos-takeaway-order-grid.test.ts`, 150 tests), targeted POS/message/test ESLint, targeted `git diff --check`, and full `pnpm typecheck && pnpm lint && pnpm build`. Playwright smoke on local dev server confirmed `/br/3/pos` redirects to `/login?returnTo=%2Fbr%2F3%2Fpos` without auth; did not fake login or open a POS session because that would mutate runtime sales data. Speed-entry grid is covered by source regression tests and docs contract.

- [x] **POS workflow UX anchor-first correction:** T2 self-review before coding.
      Skill plan: repo rules = engineering + skills + ui + workflow + design-system + POS workflow contract; external skills = shadcn + impeccable product register, with `impeccable` PRODUCT.md init skipped because repo UI SSOT is locked in `docs/spec/design-system.md`; runtime tools = local source/tests + browser smoke when auth/dev server allows; skipped = database/Supabase because this slice changes POS IA/layout/copy/docs only, not schema, RPC, RLS, ACL, payment, print, or generated types.
      Surface = `/br/[branchId]/pos`; primary user job = phục vụ/thu ngân chọn đúng bàn hoặc đúng đơn mang về trước, sau đó mới chọn món, gửi bếp, thêm món hoặc thanh toán; change type = UX flow/layout + docs/test reconciliation; primitives = existing `ToggleGroup`, `Button`, `Card`, `Badge`, `Sheet/Drawer`, `ScrollArea`, `AppEmptyState`, POS route components.
      PM: scope = replace the weak desktop/tablet "menu always main" behavior with anchor-first context selection until a valid table/order/draft exists; acceptance = first operational viewport prioritizes Table Grid or Takeaway Order Grid, menu only becomes primary after anchor, and mobile remains one job per screen.
      BA: rules = dine-in anchor is table, takeaway anchor is order/draft, cart stays draft-only, append targets existing order, payment/print/HĐĐT contracts stay unchanged, and reserved/unavailable table blocks remain inline/toast-safe as currently implemented.
      Dev: approach = keep existing provider/state/actions, restructure only `pos-desktop-shell` responsive composition plus matching static tests/docs; risk = string-regression tests overfit layout names, and dirty worktree has pre-existing POS edits to preserve.
      QA: passed focused POS workflow/layout tests, full web tests, targeted POS ESLint, targeted diff check, and full `pnpm typecheck && pnpm lint && pnpm build`. Browser smoke: `/br/1/pos` correctly redirected to `branch-scope-mismatch`; current authenticated session is `Chi nhánh Phước Hải`, and `/br/3/pos` is reachable but stops at the `Mở ca bán hàng` gate. Did not open a POS session because that would write runtime data; the post-session shell is covered by the anchor-first static tests.

- [x] **Employee Portal UX animation pass:** T2 self-review before coding.
      Skill plan: repo rules = engineering + skills + ui + workflow + design-system; external skills = design-review + frontend-testing-debugging (Browser-first runtime validation); runtime tools = local source/tests + in-app Browser; skipped = database/Supabase because this slice only changes UI motion/effects and does not touch schema, RPC, ACL, auth, generated types, migration, or storage policy.
      Surface = `/employee` route family; primary job = nhan vien dung tren man hinh mobile nho thay portal co cam giac app thuan hon qua touch feedback, entrance motion, preview feedback, va sheet transition ro rang ma khong lam nhay layout.
      PM: scope = add controlled UX animation/effects to Employee shell, home CTA, clock camera flow, schedule selected-day detail, profile manager sheet, checklist/action rows, and bottom nav; acceptance = motion feels useful, not decorative, and respects `motion-safe`.
      BA: rules = `TodayWorkState`, checklist binary state, schedule selected-day model, paid-only payslip, and manager ACL gating stay unchanged; reduced-motion users must not depend on animation to complete a task.
      Dev: approach = reuse existing Employee wrappers and shadcn primitives, add semantic transition/motion-safe classes only, avoid inline styles, arbitrary dimensions, new utilities, new wrappers, or primitive visual overrides.
      QA: passed `pnpm --filter @comtammatu/web test`, `pnpm --filter @comtammatu/shared test`, `pnpm typecheck`, `pnpm lint`, `pnpm build`, `git diff --check`, and Browser smoke on authenticated Branch Manager session. Browser evidence: 320x700 `/employee` has exactly one first-viewport CTA (`Chấm công vào`), 6 visible motion-safe elements, no horizontal overflow, and bottom nav 4 items; `/employee/clock` shows `Mở camera`, no disabled `Chấm công vào`, and camera motion classes; `/employee/schedule` has no horizontal overflow, zero visible `Nghỉ`, selected-day detail, selected-day motion; `/employee/profile` keeps manager tools collapsed and opens bottom sheet with `Ca / ngày công`, `Duyệt kết ca`, and `Hub chi nhánh`; bottom nav clicks `Hôm nay -> Việc -> Lịch -> Hồ sơ` set `aria-current="page"`; desktop 1280 shows desktop nav visible and bottom nav hidden. Relevant Browser console errors = none; existing Supabase `getSession()` warnings and Browser runtime Statsig/Cloudflare noise are unrelated to this Employee UI pass.

- [x] **Employee Portal mobile-first overhaul:** T2 self-review before coding.
      Skill plan: repo rules = engineering + skills + ui + workflow + design-system; external skills = qa-only + design-review + frontend-testing-debugging (Browser-first runtime validation); runtime tools = local source/tests + in-app Browser; skipped = database/Supabase because this slice does not change schema, RPC, ACL, auth, generated types, migration, or storage policy.
      Surface = `/employee` route family; primary job = nhan vien dung tren dien thoai nho van thay ro viec can bam, khong bi lap text, khong bi nav che thao tac, va Branch Manager mo cong cu quan ly tu khu phu thay vi chen vao luong ca ca nhan.
      PM: scope = mobile app-frame overhaul for Employee shell, clock, schedule, tasks, and profile; acceptance = one primary CTA per state, progressive clock actions only, schedule uses month plus selected-day detail, and profile manager tools collapse into a sheet.
      BA: rules = `TodayWorkState`, attendance/checklist/schedule data, paid-only payslip, and Branch Manager ACL gating stay unchanged; no new route tab, PWA split, schema, RPC, ACL, or auth contract.
      Dev: approach = refactor existing Employee components and route composition with `EmployeePage`, `EmployeePanel`, `ItemGroup`, `Button`, `Badge`, `Progress`, `Sheet`, and `AppSection`; risk = client/server boundary for the manager sheet and source tests drifting from copy.
      QA: passed `pnpm --filter @comtammatu/web test`, `pnpm --filter @comtammatu/shared test`, `pnpm typecheck`, `pnpm lint`, `pnpm build`, and Browser smoke on authenticated Branch Manager session at 320x700, 375x812, and 1024x768. Browser evidence: `/employee` has one primary CTA and no covered controls; `/employee/clock` shows `Mở camera` with no disabled `Chấm công vào`; `/employee/schedule` has no horizontal overflow, selected-day detail, and zero visible `Nghỉ`; `/employee/profile` collapses manager tools into `Quản lý chi nhánh` sheet with `Ca / ngày công`, `Duyệt kết ca`, and `Hub chi nhánh`; bottom nav clicks `Hôm nay -> Việc -> Lịch -> Hồ sơ` set active state. Dev smoke used web-only server because root `pnpm dev` was stopped by print-agent DNS to Supabase.

- [x] **Accounting period direct-support wording cleanup:** T2 self-review before coding.
      Skill plan: repo rules = engineering + skills + ui + workflow + references; external skills = shadcn (copy/surface guardrails, no component install); runtime tools = local source grep + focused web/shared tests + full gates; skipped = database/Supabase because this slice only changes labels/messages/tests.
      Surface = `/admin/accounting/periods`, shared module labels, breadcrumbs, and Finance period support copy; primary user job = owner/super_manager opens period-close only as direct accounting support, not as default HKD workflow.
      PM: scope = rename period-close support vocabulary away from default "Ky ke toan"; acceptance = module/shell/page copy frames it as support period close and no default discovery/nav is widened.
      BA: rules = HKD pilot keeps operating finance first; period close remains permissioned direct support for accounting/tax handoff; no schema, ACL, RPC, journal, or BCTC behavior changes.
      Dev: approach = patch shared labels, shell segment labels, Finance/Admin period messages, and static tests; risk = copy drift with route-map/accounting permission naming, which stays internal.
      QA: passed focused Finance source test, shared auth test, full web/shared tests, `pnpm typecheck`, `pnpm lint`, `pnpm build`, and `git diff --check`.

- [x] **Finance live-copy HKD wording cleanup:** T2 self-review before coding.
      Skill plan: repo rules = engineering + skills + ui + workflow + references; external skills = shadcn (copy/primitive guardrails, no component install); runtime tools = local source grep + focused web tests + full gates; skipped = database/Supabase because this slice is message/type/test cleanup only.
      Surface = `/finance` route family message dictionary and stale private Finance type; primary job = chu/quan ly HKD doc Finance nhu tai chinh van hanh truoc, khong thay product chia thanh "don gian/chuyen sau"; change type = copy/source cleanup only, no route, ACL, schema, RPC, or money mutation.
      PM: scope = remove stale two-mode labels from active Finance source; acceptance = active Finance messages no longer expose `Don gian`/`Chuyen sau` layout wording and default copy uses HKD operating/direct-support framing.
      BA: rules = HKD pilot keeps revenue, inventory value, food cost/gross profit, operating expense, HĐĐT and reconciliation as default; accounting support routes may remain reachable by direct permission but must not define the surface vocabulary.
      Dev: approach = delete unused layout mode type/message keys, adjust remaining support copy, and add a static regression test; risk = stale imports if the private type is still referenced.
      QA: passed focused Finance source test, final full `pnpm --filter @comtammatu/web test`, `pnpm typecheck`, `pnpm lint`, `pnpm build`, `git diff --check`, and source grep for stale Finance two-mode wording/type.

- [x] **Employee Portal app-frame clarity pass:** T2 self-review before coding.
      Skill plan: repo rules = engineering + skills + ui + workflow; external skills = shadcn + impeccable (repo UI SSOT wins; impeccable PRODUCT.md init skipped because this repo already locks `docs/spec/design-system.md`); runtime tools = local source/tests + in-app Browser; skipped = database/Supabase tools because this is UI shell, IA, and copy density only.
      Surface = `/employee` route family; primary job = nhan vien mo portal tren dien thoai la thay dung viec can lam, khong bi lap trang thai, khong bi day cong cu quan ly vao luong ca ca nhan; change type = shared Employee layout/composition/copy refinement only, no schema/RPC/RLS/ACL change; primitives = `EmployeePage`, `EmployeePanel`, `ItemGroup`, `Button`, `Badge`, `Progress`, `AppSection`.
      PM: scope = make each tab own one clear job: Home next action, Tasks checklist, Schedule month calendar, Profile hub; acceptance = no duplicated page/panel explanations, one primary action per state, and small-mobile controls remain touch-safe.
      BA: rules = `TodayWorkState` remains the state machine, checklist remains binary, paid-only payslip and manager ACL links stay unchanged, manager tools stay secondary in Profile.
      Dev: approach = use existing shared Employee app primitives, keep Home as next-action only, keep Tasks actions inside the checklist panel, fold Schedule month controls into the calendar panel, keep Profile as a compact hub, and update static UX regressions without creating a new theme or wrapper system.
      QA: passed focused Employee source tests, full `pnpm --filter @comtammatu/web test`, `pnpm --filter @comtammatu/shared test`, `pnpm typecheck`, `pnpm lint`, `pnpm build`, `git diff --check`, and in-app Browser authenticated smoke at 320x700 for `/employee`, `/employee/tasks`, `/employee/schedule`, `/employee/profile` with no horizontal overflow and one primary home CTA.

- [x] **Table seat-count simplification:** T2 self-review before coding.
      Skill plan: repo rules = engineering + skills + ui + database + workflow; external skills = shadcn; runtime tools = local source grep + focused type/lint/build gates; skipped = schema migration/Supabase apply because `tables.capacity` remains a backend default, not an operator-facing setting.
      Surface = `/admin/settings/tables`, `/br/[branchId]/settings/tables`, and POS table selection; primary job = quản lý/cashier chọn và quản lý bàn bằng số bàn, khu vực, trạng thái, không phải nhập/đọc trường phụ dư thừa; change type = UI/action payload simplification only; primitives = existing `FormDialog`, `TextField`, `SelectField`, `Table`, `Badge`, `Button`.
      PM: scope = remove seat-count from visible/editable table workflows while keeping table creation working; acceptance = table cards/list/form and POS user guides do not show the removed field/copy, new tables still get a valid backend default.
      BA: rules = số bàn/khu vực/trạng thái remain the operational contract; do not drop the DB column in this slice because POS/order history may still depend on table rows and schema migration is unnecessary blast radius.
      Dev: approach = stop selecting/rendering/submitting `capacity` on the table UI and POS gate, let create use the DB default, and avoid updating `capacity` on edits so old values are preserved.
      QA: passed targeted table-seat regression test, full `pnpm --filter @comtammatu/web test`, targeted grep for table-seat copy on table/POS routes and POS user guides, `pnpm typecheck`, `pnpm lint`, `pnpm build`, and `git diff --check`.

- [x] **HR Payroll direct-support cleanup for HKD pilot:** T2 self-review before coding.
      Skill plan: repo rules = engineering + skills + workflow + references + ui + database read-only; external skills = shadcn (primitive/copy guardrails, no component install); runtime tools = local source grep + focused auth/discovery tests + full gates; skipped = Supabase/browser because this slice changes HR/payroll surfacing, copy, docs, and tests only.
      Surface = `/hr` workspace + `/hr/payroll/*` direct route + auth discovery/docs; primary job = owner/super_manager and Branch Manager land on ca/phân ca/chấm công first, while payroll remains a permissioned direct-support route for đối soát/chốt lương.
      PM: scope = keep payroll reachable for authorized users but keep it out of default HR hot path/discovery; acceptance = `/hr` tabs stay nhân viên/ca/phân ca/chấm công, app discovery has `hr` not `hr_payroll`, and payroll copy reads as HKD support.
      BA: rules = HKD pilot currently handles payroll mainly outside the app; do not delete payroll data routes or change payroll actions/calculation/RLS; use `NSDLĐ`/Hộ kinh doanh wording instead of company framing for Má Tư.
      Dev: approach = patch message/copy/docs/regression tests, remove the orphan legacy payroll client, no schema, RPC, RLS, payroll tax math, or permission widening.
      QA: passed source grep for orphan payroll client/stale company wording, `pnpm --filter @comtammatu/shared test -- --runInBand`, `pnpm --filter @comtammatu/web test -- --runInBand`, `pnpm typecheck`, `pnpm lint`, `pnpm build`, and `git diff --check`. Browser smoke skipped because no Browser navigation tool was exposed in this session; production build confirmed `/hr` and `/hr/payroll/*` route compilation. Also reconciled pre-existing POS takeaway/status message-test drift that was blocking full gates.

- [x] **Accounting Advanced quarantine for HKD pilot:** T2 self-review before coding.
      Skill plan: repo rules = engineering + skills + workflow + references + ui + finance docs; external skills = shadcn (primitive/nav guardrails, no component install); runtime tools = local source grep + focused auth/nav tests + full gates; skipped = Supabase/browser because this slice changes navigation/copy/docs only, with no schema/RLS/RPC/money mutation.
      Surface = Admin sidebar/discovery + `/finance` route surface copy; primary job = owner/super_manager opens HKD operating finance first, without COA/journal/statements/period close shaping the pilot default; change type = nav/discovery/copy/docs only; primitives = existing `AppShell`/sidebar/nav config.
      PM: scope = remove Accounting Advanced from default Admin discovery/nav while preserving direct route access; acceptance = default Admin and Finance nav expose operating finance, not COA/Journal/Statements/VAS/BCTC.
      BA: rules = HKD pilot uses daily revenue, inventory value, food cost, operating expense, HĐĐT and accountant export; advanced accounting routes may stay reachable by permission for continuity and future conversion.
      Dev: approach = patch shared labels/nav config plus support-route copy/docs; do not alter `MODULE_ACL`, database, posting rules, or statement actions.
      QA: passed source grep for default-nav advanced links (remaining `Kỳ kế toán` hits are direct support/audit contexts), `pnpm --filter @comtammatu/web test -- --runInBand`, `pnpm --filter @comtammatu/shared test -- --runInBand`, `pnpm typecheck`, `pnpm lint`, `pnpm build`, and `git diff --check`.

## Active implementation note — 2026-06-10

- [x] **Branch Manager attendance simplification:** T2 self-review before coding.
      Skill plan: repo rules = engineering + skills + ui + database + workflow; external skills = shadcn + supabase; runtime tools = local source/tests/browser; skipped = schema migration because the change keeps the existing attendance table and service-role write path.
      Surface = `/employee` Branch Manager attendance flow + `/hr` attendance tab; primary job = quản lý tự ghi giờ vào/ra ca nhanh rồi quay lại quản lý chi nhánh, không đi qua checklist cá nhân hoặc duyệt kết ca của chính mình; change type = Server Action guard + UI/IA simplification, no RLS/schema/money changes; primitives = existing `EmployeePage`, `EmployeePanel`, `Button`, `Tabs`, `Select`, `Table`, `Badge`.
      PM: scope = branch_manager self-attendance only has Chấm công vào and Chấm công ra; acceptance = no Branch Manager personal CTA to Việc trong ca, no checkout approval pending state for their own shift, HR attendance stays a management read surface.
      BA: rules = floor-staff checklist/approval flow remains unchanged; Branch Manager direct checkout must only close their own open attendance in their assigned branch; old pending self-checkout can still be closed directly.
      Dev: approach = branch role branch in `today-work-state`, clock actions, clock/home UI, HR attendance table read-only status; reuse service client with tenant/employee/branch filters and keep raw DB errors hidden.
      QA: passed focused Employee/HR static tests, focused ESLint on touched Employee/HR files, `pnpm typecheck && pnpm lint && pnpm build`, and Playwright unauth smoke for `/employee/clock`, `/employee/tasks`, and `/hr` redirecting safely to login at localhost:3001. Authenticated Branch Manager smoke remains session-dependent. Separate full `@comtammatu/web test` still has unrelated POS takeaway workflow static failures outside this attendance slice.

## Active implementation note — 2026-06-09

- [x] **HKD Finance/Admin surface cleanup:** T2 self-review before coding.
      Skill plan: repo rules = engineering + skills + workflow + references + ui; external skills = shadcn; runtime tools = local source grep + focused tests; skipped = Browser because this slice changes navigation copy/links only and no dev session is available yet.
      Surface = `/admin/dashboard`, `/admin/reports`, and Finance copy; primary job = chủ/quản lý HKD mở báo cáo thấy tài chính vận hành trước, không bị kéo vào BCTC/VAS/kế toán doanh nghiệp trên bề mặt mặc định; change type = UI/copy/navigation cleanup only, no DB/RLS/ACL/schema changes; primitives = existing `SurfaceLinkCard`, `Button`, `AppPage`, `AppPageHeader`, `AppShell`.
      PM: scope = remove default Admin report shortcuts into Accounting Advanced and rename finance CTA toward operating finance; acceptance = Admin report landing points to revenue, inventory value, stock movement, Finance Basic, Inventory, HR only.
      BA: rules = HKD remains current legal frame; Accounting Advanced routes may stay accessible by direct permission but must not define pilot navigation; buyer/legal invoice copy may mention external companies only when referring to customers, not Má Tư as the subject.
      Dev: approach = patch existing copy/card arrays without changing `MODULE_ACL`, route resolution, database, or shell primitives; risk = copy drift and unused imports.
      QA: passed targeted greps for default `/finance/statements` and `/finance/chart-of-accounts` Admin links, `pnpm --filter @comtammatu/web test -- --runInBand`, `pnpm typecheck`, `pnpm lint`, `pnpm build`, and `git diff --check`. Separate `pnpm lint:baseline` remains red on pre-existing unclassified legacy/back-compat markers outside this HKD surface slice.

- [x] **Employee Portal mobile distill pass:** T2 self-review before coding.
      Skill plan: repo rules = engineering + ui + workflow + skills; external skills = impeccable product register + shadcn; runtime tools = in-app Browser + local tests + Playwright; skipped = new theme/primitive because `docs/spec/design-system.md` stays the UI authority.
      Surface = `/employee` home on small mobile; primary job = nhân viên nhìn ngay việc cần bấm, không đọc text lặp; change type = UI composition/copy density only, no DB/RLS/RPC/ACL changes; primitives = `EmployeePage`, `EmployeePanel`, `Button`, `Badge`, `Progress`, `ItemGroup`.
      PM: scope = cut duplicate page/panel copy, compress workflow, keep one CTA above the fold; acceptance = 320px first viewport has one primary action and no repeated explanatory workflow blocks.
      BA: rules = `TodayWorkState` stays source of truth; checklist remains binary; manager/operation tools cannot crowd the personal work hot path; auth/session behavior unchanged.
      Dev: approach = add compact home-only layout pieces, hide home page header on mobile, move operation handoffs below mobile fold, and update source regression tests.
      QA: passed `pnpm --filter @comtammatu/web test`, `pnpm --filter @comtammatu/shared test`, focused Employee/HR shared static tests, `pnpm typecheck`, `pnpm lint`, `pnpm build`, `git diff --check`, and Browser authenticated `/employee` smoke at 320x700: one 60px primary CTA above the fold, no horizontal overflow, no visible operation handoff links in `ready_to_checkout`.

- [x] **Employee Portal native-app mobile UX pass:** T2 self-review before coding.
      Skill plan: repo rules = engineering + ui + workflow + skills; external skills = shadcn + impeccable product register + frontend-testing-debugging fallback; runtime tools = local source/tests + Playwright smoke; skipped = impeccable PRODUCT.md init because repo UI SSOT is locked in `docs/spec/design-system.md` and repo authority wins external context docs.
      Surface = `/employee` route family; primary job = nhân viên dùng trên điện thoại thoải mái, tap-safe, ít phải nghĩ, chuyển trạng thái rõ nhưng ổn định; change type = mobile-first UI composition/motion only, no DB/RLS/RPC/ACL changes; primitives = `EmployeePage`, `EmployeePanel`, `ItemGroup`, `Button`, `Badge`, `Progress`, existing shell adapters.
      PM: scope = app-like Employee shell, stronger primary CTA, better mobile nav/list feel, no new workflow or portal clutter; acceptance = first viewport still has one main action, all nav/action controls are touch-safe, and desktop remains usable.
      BA: rules = `TodayWorkState` and binary checklist stay unchanged; manager tools remain profile-only; animation must not hide content or create unstable layout; URL remains scope source.
      Dev: approach = tune shared Employee layout/header/nav/action-row primitives and the home/tasks surfaces so every Employee screen inherits the mobile app feel; avoid new tokens/theme wrappers.
      QA: passed source contract for mobile affordances, `pnpm --filter @comtammatu/web test`, focused Employee/HR shared static tests, `pnpm typecheck`, `pnpm lint`, `pnpm build`, Browser protected-route smoke, and Playwright mobile/desktop unauth smoke with no horizontal overflow; authenticated state smoke remains session-dependent. Full shared package suite still fails on unrelated KDS migration static regressions.

- [x] **Branch Manager checklist config entry:** T2 self-review before coding.
      Skill plan: repo rules = engineering + database + ui + workflow; external skills = shadcn + supabase; runtime tools = local source/tests/browser; skipped = schema migration because role-scoped template migration already exists.
      Surface = `/br/[branchId]/settings` + checklist Server Action; primary job = branch manager edits checklist for own branch without entering tenant Admin.
      PM: scope = add branch settings entry/dialog and branch-scoped save; acceptance = Branch Manager sees Cấu hình chấm công in branch settings, saves own branch only; owner/super_manager still can use Admin.
      BA: rules = URL/JWT branch scope; no cross-branch edits; checklist remains role-scoped/binary and snapshot behavior unchanged.
      Dev: approach = reuse `AttendanceConfigDialog`, fetch `ChecklistItemsByRole` in branch settings hub, widen action role list with permission probes and branch claim guard, revalidate branch/admin settings.
      QA: passed focused Employee Daily Work static test, `pnpm typecheck`, `pnpm lint`, `pnpm build`, `git diff --check`, and Browser smoke for `/br/1/settings` redirecting to `/login?returnTo=%2Fbr%2F1%2Fsettings` when unauthenticated; authenticated Branch Manager dialog smoke remains dependent on a valid branch-manager session.

- [x] **Employee checklist templates by role:** T3 full debate before coding.
      Skill plan: repo rules = engineering + database + ui + workflow; external skills = shadcn + supabase + supabase-postgres-best-practices; runtime tools = Supabase changelog check, local source/tests/CLI; skipped = external subagents because the available spawn tool requires explicit user request, so the four-perspective debate is written here.
      Surface = `/admin/settings/branches` attendance config + `/employee` clock-in snapshot; primary job = quan ly cau hinh checklist rieng cho tung vai tro va nhan vien nhan dung checklist cua vai tro khi cham cong vao; change type = schema/RPC + Server Action + UI config, no money/RLS broadening.
      PM: scope = replace the single branch checklist with per-role active templates; acceptance = each branch stores separate lists for branch_manager/warehouse_manager/production_manager/cashier/waiter/chef/office and clock-in snapshots the current user's role list.
      BA: rules = checklist stays binary `Chua lam`/`Xong`; template edits do not mutate attendance snapshots; migration copies the old single list into each configurable role once, but runtime no longer generates hidden hardcoded checklist items; direct browser writes remain blocked.
      Dev: approach = add `role_code` to checklist templates, update unique active index and RPC signatures, thread role config through the branch settings UI, and pass `claims.user_role` into clock-in RPC; risk = old single-template data must backfill safely without widening browser writes.
      QA: passed targeted Employee Daily Work static test, `pnpm typecheck`, `pnpm lint`, `pnpm build`, and Browser smoke for `/admin/settings/branches` redirecting safely to login when unauthenticated; authenticated dialog smoke remains dependent on a valid owner/super_manager session.

- [x] **Employee Portal scientific IA pass:** T2 self-review before coding.
      Skill plan: repo rules = engineering + ui + workflow; external skills = shadcn + impeccable; runtime tools = local source/tests; skipped = database/browser tools because this is UI/IA only with no schema/RPC/ACL change.
      Surface = `/employee` route family; primary job = nhân viên vào portal là thấy bước tiếp theo của ca, còn thông tin cá nhân/phụ trợ và công cụ quản lý nằm đúng khu phụ; change type = UI/IA/copy refinement only, no DB/RLS/money changes; primitives = `EmployeePage`, `EmployeePanel`, `EmployeeActionSection`, `ItemGroup`, `Button`, `Badge`, `Progress`.
      PM: scope = keep four primary tabs (`Hôm nay`, `Việc`, `Lịch`, `Hồ sơ`), make `/employee` first viewport state-driven, and move Branch Manager tools out of the home hot path.
      BA: rules = `TodayWorkState` remains the workflow source; checklist stays binary; payslip remains paid-only; branch-manager tools must use existing ACL + JWT branch scope and must not depend on personal attendance.
      Dev: approach = reorganize only Employee home/profile/schedule composition and copy, moving secondary self-service/manager links to profile; keep bottom nav, actions, RPCs, and route contracts unchanged.
      QA: passed `pnpm --filter @comtammatu/web test`, focused shared HR static test, `pnpm typecheck`, `pnpm lint`, `pnpm build`, and unauth browser smoke for `/employee`; full shared test remains blocked by unrelated missing migration static regressions.

- [x] **Branch Manager ca / ngày công access:** T2 self-review before coding.
      Skill plan: repo rules = engineering + database + ui + workflow; external skills = shadcn + supabase + next-best-practices; runtime tools = local source/tests; skipped = browser auth smoke until a valid authenticated branch-manager session is available.
      Surface = `/employee` manager tools + `/hr`; primary job = quản lý chi nhánh vào được màn ca, phân ca, ngày công của đúng chi nhánh mình; change type = route ACL + UI gating + branch-scoped read shape, no schema/RLS/money changes; primitives = existing `Tabs`, `Select`, `Table`, `Button`, Employee action links.
      PM: scope = urgent minimum path for Branch Manager to manage shifts and attendance; acceptance = Branch Manager sees HR tool link, `/hr` opens without employee/payroll clutter, and payroll remains blocked.
      BA: rules = Branch Manager only sees their `claims.branch_id`; owner/super_manager still see full HR; Server Actions keep `staff:manage` and branch-scope checks; no cross-branch picker for Branch Manager.
      Dev: approach = split `/hr/payroll` ACL from `/hr`, filter branch options in the server page, pass role/employee-tab visibility to the client, and add source-level tests; files = auth ACL/resolution/tests, HR page/client, Employee home/copy, task note.
      QA: tests = focused auth scope test + HR source regression + full `pnpm typecheck && pnpm lint && pnpm build`; browser smoke if auth/dev server is usable.

- [x] **Employee Lịch monthly calendar correction:** T2 self-review before coding.
      Surface = `/employee/schedule`; primary job = nhân viên nhìn lịch ca/ngày công như calendar tháng thật, đủ ngày trong tháng và đúng trục Thứ 2 đến Chủ nhật; change type = UI composition + schedule query range from week to month, no schema/RLS/money changes; primitives = existing Employee wrappers, `Button`, `Table`, `Badge`, `Skeleton`.
      PM: scope = replace the weekly axis table with a monthly calendar grid; acceptance = title shows month/year, headers run Monday to Sunday, active cells cover every day of the selected month, blank alignment cells do not pretend to be days, and today/shift/attendance are visible in each day cell.
      BA: rules = `shift_assignments` and `attendance_records` remain the only data sources; one year has 12 month navigation states; month boundaries must handle 28/29/30/31 days without dropping empty days.
      Dev: approach = reuse shared VN date helpers and Employee panel shell, change only schedule page/action/client/copy plus this task note; risk = responsive density and date-boundary mistakes.
      QA: tests = targeted calendar helper/source checks, `pnpm typecheck && pnpm lint && pnpm build`, and protected-route smoke when local auth/dev server allows.

- [x] **Admin pages quick visual audit:** T2 self-review before coding.
      Surface = `/admin/**` page family; primary job = owner/super_manager rà từng màn Admin trên điện thoại vẫn thấy nhãn đúng tiếng Việt, hierarchy rõ, và tab phụ thể hiện màn hiện tại; change type = UI shell/page composition and copy refinement only, no DB/RLS/money changes; primitives = `AppShell`, `AppPage`, `AppPageHeader`, unframed sections, `SurfaceLinkCard`, link tabs.
      PM: scope = fix the highest-signal issues found by static + browser audit across Admin routes; acceptance = Admin routes smoke without overflow, page titles/breadcrumb tails stay Vietnamese, and reports landing matches dashboard cockpit clarity.
      BA: rules = existing ACL/redirects stay unchanged; legacy admin redirects may continue to workspace routes; URL remains the source of route state.
      Dev: approach = patch shared path-segment label resolver plus page-local Reports surfaces; avoid new design tokens or wrapper systems; risk = label drift only.
      QA: tests = targeted static grep/source checks, Playwright route matrix at mobile/desktop, then `pnpm typecheck && pnpm lint && pnpm build`.

- [x] **Employee Công + Lịch calendar anatomy:** T2 self-review before coding.
      Surface = `/employee/schedule` plus legacy `/employee/attendance`; primary job = nhân viên nhìn Công + Lịch như một calendar thật theo ngày, không phải danh sách/card grid cũ; change type = UI composition only, no DB/RLS/money changes; primitives = existing Employee wrappers, `Button`, `Table`, skeleton, calendar-style week navigation.
      PM: scope = replace the schedule card grid with a real weekly calendar table; acceptance = visible header has 7 day columns with date/month/year, body rows map Ca/Công/Trạng thái, and `/employee/attendance` remains merged into this route.
      BA: rules = shift assignments and attendance records remain the data source; no new status claims; empty/no-shift days must be visibly represented as calendar days, not omitted.
      Dev: approach = rewrite only the schedule client layout and copy helpers, keep fetch/action shape unchanged, reuse shared table primitive and semantic tokens; files = employee schedule client/copy plus task notes.
      QA: tests = `pnpm typecheck && pnpm lint && pnpm build`, `git diff --check`, and protected-route smoke after dev server restart; authenticated visual still depends on local login state.

- [x] **Admin Dashboard responsive cockpit:** T2 self-review before coding.
      Surface = `/admin/dashboard`; primary job = chủ/quản lý mở Dashboard trên điện thoại vẫn thấy ngay hôm nay cần xem gì, chỉ số nào quan trọng, và đi tiếp tới đúng workspace; change type = UI composition/responsive hierarchy only, no DB/RLS/money changes; primitives = `AppPage`, `AppPageHeader`, `AppSection`, `Card`, `Badge`, `Button`, `SurfaceLinkCard`.
      PM: scope = reorganize the existing admin landing into a clear mobile-first cockpit; acceptance = first viewport has next-look items before secondary chrome, KPI cards scan on mobile, quick links are ACL-gated, and recent orders do not overflow.
      BA: rules = module ACL remains source of truth; dashboard data remains read-only from existing stats action; no new business claims, no branch/scope state in storage.
      Dev: approach = keep the existing HR shortcut work, compose from shared app surface adapters and shadcn primitives, and avoid new tokens/theme wrappers; files = admin dashboard page plus source-level regression if needed.
      QA: tests = focused web test for ACL-scoped HR shortcut, `pnpm typecheck && pnpm lint && pnpm build`, plus browser smoke for `/admin/dashboard` desktop/mobile when auth/dev env allows.

- [x] **Protected route map and navigation contract:** T2 self-review before coding.
      Surface = protected route families across Admin, domain workspaces, branch operations, and Employee; primary job = user always knows which surface they are in, which route is the valid entry point, and how to leave without bouncing through an inaccessible page; change type = shared auth route contract + docs alignment, no DB/RLS/money changes; primitives = existing `MODULE_ACL`, `route-resolution`, `nav-config`, `AppShell`, shell/bottom-nav patterns.
      PM: scope = codify the route family/home/back/breadcrumb contract before broad page redesign; acceptance = docs and shared auth code agree on active route families, unknown Inventory URLs do not become active contracts, and shell links keep role-safe home behavior.
      BA: rules = URL remains the scope source; proxy/module ACL remains the permission source; branch-scoped routes require `branchId` in the path; browser history should be preserved for route changes while `router.replace` stays for same-page filter/tab state.
      Dev: approach = add a small route-map resolver derived from existing ACL/nav contracts, export and test it, then update Web/Auth docs with the route family matrix; files = shared auth route-map/tests/index plus `docs/modules/web-app.md` and current task notes; risk = stale duplication if docs overpromise beyond runtime.
      QA: tests = focused shared auth test for route family classification + role-home behavior, grep for stale route contracts, then full `pnpm typecheck && pnpm lint && pnpm build`.

- [x] **Admin Dashboard HR entry:** T2 self-review before coding.
      Surface = `/admin/dashboard`; primary job = owner/super_manager co loi vao HR tu dashboard dieu hanh ma khong phai tim trong sidebar/report; change type = UI navigation affordance only, no DB/RLS/money changes; primitives = unframed section, `SurfaceLinkCard`, ACL `canAccess`.
      PM: scope = restore HR as an admin dashboard shortcut; acceptance = role co quyen HR thay card "Nhan su" tro ve `/hr`, dashboard khong day payroll thanh hot path.
      BA: rules = ACL remains source of truth; card must hide for roles without HR access; existing `/hr` route contract unchanged.
      Dev: approach = reuse `MODULE_ACL.hr` path/label and existing surface link-card; files = admin dashboard and source-level regression test; risk = copy/nav drift only.
      QA: tests = static test for dashboard HR shortcut + `pnpm typecheck && pnpm lint && pnpm build`; unauthenticated smoke can still only verify auth redirect.

- [x] **Employee compact daily surface:** T2 self-review before coding.
      Surface = `/employee` route family; primary job = nhân viên nhìn nhanh ca hôm nay, checklist, lịch/công và hồ sơ tối thiểu mà không đọc lại cùng một trạng thái nhiều lần; change type = UI composition/copy refinement only, no DB/RLS/money changes; primitives = existing Employee wrappers, `AppPage`, `AppSection`, `Button`, `Item`, `Checkbox`.
      PM: scope = rút gọn đúng bốn vùng user nêu: Hôm nay, Việc, Lịch/Công, Hồ sơ; acceptance = ca hôm nay có ngày và grid Chi nhánh/Giờ vào/Giờ ra/Tiến độ, checklist không còn badge/text dư, lịch thể hiện ngày tháng năm và ngày công trong cùng màn, hồ sơ không còn Thuế TNCN.
      BA: rules = attendance/checklist state vẫn là dữ liệu nguồn; công không tách khỏi lịch trên hot path; route cũ có thể còn tồn tại nhưng không được đẩy như một surface trùng; không thêm claim/schema/action mới.
      Dev: approach = reuse current Employee shell/components and schedule action data flow, extend schedule read shape for attendance, remove profile tax/dependents panel; files = employee home/tasks/schedule/profile/payslip copy and task notes; risk = copy drift and responsive density.
      QA: tests = `pnpm typecheck && pnpm lint && pnpm build`, `git diff --check`, unauthenticated redirect smoke for `/employee` and `/employee/schedule`, plus Browser login-render smoke because no authenticated storage state was available.

- [x] **Role-safe shell navigation cleanup:** T2 self-review before coding.
      Surface = protected app shells and Inventory mobile entry links; primary job = user can leave a workspace through a valid role home instead of bouncing through an inaccessible admin dashboard or stale mobile route; change type = navigation affordance + route-link cleanup, no DB/RLS/money changes; primitives = `AppShell`, `Button`, `Link`, shared auth nav resolver.
      PM: scope = fix the first confusing navigation layer before redesigning pages; acceptance = non-admin roles are not shown shell/home links to `/admin/dashboard`, admin roles still return to Admin, and Inventory mobile links use canonical routes.
      BA: rules = proxy/module ACL remains the permission source; branch/tenant scope stays in URL/JWT only; removed mobile route labels must not survive as active links.
      Dev: approach = add a shared role-home resolver, wire shells through it, and remove stale Inventory link/prefix usage; files = shared auth nav helpers/tests, AppShell and workspace shells, Inventory entry components.
      QA: tests = focused shared auth route test plus targeted grep for stale/invalid shell links; `pnpm typecheck && pnpm lint && pnpm build` green after two type-only fixes in the in-progress Employee/HR checkout flow.

- [x] **Employee Portal / Interface UI framework:** T2 self-review before coding.
      Surface = `/employee` route family; primary job = nhân viên mở màn hình là biết bước tiếp theo trong ca, còn quản lý chi nhánh có entry point vận hành nhưng không lấn át work loop; change type = UI shell/component refactor + UX composition, no DB/RLS/money changes; primitives = `AppPage`, `AppPageHeader`, `AppSection`, `Button`, `Badge`, `Item`, `ItemGroup`, `Checkbox`.
      PM: scope = rebuild the Employee interface kit around daily work, tasks, and support/navigation; acceptance = one primary action per work state, reusable Employee components, mobile first viewport stays focused; priority = high because Employee is an operational surface.
      BA: rules = attendance state drives the main CTA; checklist belongs after clock-in; manager tools stay ACL/branch scoped; missing profile/branch states must stay safe and explicit.
      Dev: approach = reuse `apps/web/app/components/surface.tsx` and shadcn primitives through Employee wrappers; files = Employee shell/components, home, tasks, copy/tests as needed; risk = layout/copy regressions and accidental portal clutter.
      QA: tests = `pnpm --filter @comtammatu/web test`, touched-file eslint, `pnpm typecheck && pnpm lint && pnpm build`; Playwright unauthenticated smoke confirms `/employee` redirects to login without horizontal overflow on desktop/mobile. Authenticated visual pass remains pending a valid `.env.test.local` or storage state.

- [x] **Employee Portal / Branch Manager tools:** T2 self-review before coding.
      PM: scope = restore visible branch-management entry points for Branch Manager inside `/employee`; acceptance = Branch Manager sees real branch tools even outside personal clock-in state; priority = high because the role currently feels empty.
      BA: rules = personal attendance flow stays state-driven; management links must come from existing ACL + branch scope; no branch scope means no branch-operation links.
      Dev: approach = reuse Employee shell/action-list primitives, existing `canAccess`, and JWT `claims.branch_id` fallback; files = Employee home, employee copy, static regression test; risk = UI visibility only.
      QA: tests = source-level regression for branch-manager tools independent of attendance gating plus typecheck/lint/build; recheck employee daily-work flow still has one primary action.

- [x] **HRM cho Hộ kinh doanh:** T2 self-review before coding.
      Surface = `/employee` + `/hr` entry/copy layer; primary job = nhân viên chỉ thấy ca làm, việc trong ca, lịch ca, ngày công; chủ/quản lý thấy nhân viên, ca, chấm công trước khi chạm bảng lương; change type = UI/copy/IA refinement, no DB/RLS/money changes; primitives = existing Employee wrappers, `AppShell`, `Tabs`, `Button`, `Item`.
      PM: scope = rút HRM về daily-work + ngày công cho HKD pilot; acceptance = bottom nav không còn giống HR portal, hồ sơ chỉ giữ theo dõi thiết yếu, HR admin nói rõ trọng tâm HKD; priority = high because payroll is still Excel-managed for the pilot.
      BA: rules = không mất route dữ liệu hiện có; payroll/permissions vẫn truy cập trực tiếp khi cần nhưng không là hot path; ngày công phải dễ tìm từ hồ sơ; branch-manager operation tools vẫn ACL/branch scoped.
      Dev: approach = change copy/nav/form/table composition only, reuse existing wrappers/actions, avoid schema and DB churn; files = employee messages/nav/profile/schedule, HR shell/page/client/form/table, reports entry, shared labels, glossary/auth docs; risk = terminology drift.
      QA: tests = static copy grep, targeted ESLint, full typecheck/lint/build, unauthenticated browser smoke for `/employee` and `/hr`; manually review `/employee` first viewport, `/employee/profile`, and `/hr` labels for no portal clutter.

## Shipped (condensed — see git / `regressions.md` for detail)

- **Pilot hardening 2026-05-24**: snapshot-doc refresh, schema-spec source-ladder, App-Router route-group migration reconciled (`typecheck/lint/build` green), network-gate hardening, prod payment migrations confirmed.
- **Interface closure IF-001..012** (2026-05-24): retired `matu-*`/Be-Vietnam-Pro pilot layer; static UI guards (`lint:ui-contract`, `check-client-storage`); heading/icon/button/radius scale fixes; server-side GRN draft state; Finance-Basic landing (4 owner metrics).
- **Pre-deploy + M4 Payments + M6 Finance P0** (2026-04-27..05-24, T3): employee FK casts, POS network gate D9, refund RPCs (`reverse_payment_and_post`/`create_refund`), webhook idempotency + MoMo tenant-binding + fail-hard stock + server-recompute total, audit-log RPC-only + PII strip, HĐĐT cancel-reason ≥20, Finance-Basic scope contract. Open follow-ups below.
- **Network gate D9 + Payment/HĐĐT integrations**: per-agent presence tokens + rate-limit + `has_permission()` RLS; VietQR + Momo live (prod creds, atomic webhook RPC); HĐĐT via Viettel S-invoice.
- **Sprint 6 Inventory UX**: F-017 PO display ID (`next_po_display_id`); Fix #3 server-side GRN drafts (Stage A/B/C — removed `localStorage`, drafts page, Sonner toasts).
- **2026-05-30 session** (git `ac95f841..43a3ec4b`): HĐĐT B2B double-issue guard; payroll-draft + attendance-bypass + `stock_transfer_items` + RLS-policy-dedup fixes; `requireBranchScope` ×22; clock-in graceful shift-window; baseline-first migration consolidation + managed-surfaces companion (see Architecture section below).

## Active hardening — agent-doable (not blocked)

- [x] **Attendance: employees can DELETE their own records** — FIXED 2026-05-30 (migration `20260602011000`): revoked DELETE on `attendance_records` from `authenticated`+`anon` (verified — only service_role keeps DELETE; UPDATE/SELECT stay RLS-gated for clock-out/read). No app code deletes attendance. It is now INSERT+DELETE service-role-only. Rule ATTENDANCE-INSERT-SERVICE-ROLE-ONLY extended.
- [~] **Broad `anon`/`authenticated` table privileges — AUDITED 2026-05-30** (DELETE surface): swept every public table with a DELETE-permitting policy for authenticated/anon. Conclusion: **well-controlled** — each is permission-gated (`has_permission`/`can_access_*`/`auth_role`/`_auth_is_owner`), rpc-only/no-client-write, self-delete-own (benign), or branch-scoped app delete. **No tenant-only DELETE holes beyond the two already fixed** (attendance, `stock_transfer_items`). RESIDUAL (low priority, NOT exploitable via PostgREST): `anon`/`authenticated` still hold TRUNCATE/REFERENCES/TRIGGER on most tables — cosmetic over-grant from bootstrap `GRANT ALL`; tidy schema-wide if/when convenient. (Minor: `bmidl_write` still uses legacy `auth_role()` → folds into α4c.)
- [x] Drop legacy `employees_manage`/`shifts_manage` — verified ABSENT 2026-05-30: 0 references in app code, the active baseline, and matu-dev's policies/functions/data. Nothing to drop.
- [ ] No audit on `insurance_base_salary`/`gross_salary` changes — BHXH compliance. (Low value while payroll is Excel-managed.)

## Shell helpers refactor — 2026-05-27 current status

> Verified state 2026-05-31 (todo had drifted from code — re-audited via `Skip withAction` counts + LoC).

> **Zero active `// Skip withAction` annotations remain (verified 2026-05-31).** The boilerplate-bypass goal of WS-1b/2 is closed. **Goal of the remaining work = reduce per-file pile-up of functions/features by splitting along concern boundaries — NOT a line count.** The plan's "≤800 / ≤400 / ≥30%" numbers are rough proxies only; success = each file owns one clear responsibility. The big action files still cram many actions into one module → decompose under WS-3.

- [x] **WS-0** — DONE: `apps/web/app/_lib/with-action.ts` carries `withActionPositional` + `customAuth` + `afterSuccess` + `argsToInput`; `_lib/rpc-error-map.ts` exists (`mapRpcError`/`includesAny`). Extended 2026-05-31 with additive `forbiddenError?` (custom denial copy; defaults to `FORBIDDEN_ERROR`, backward-compat for the ~20 existing callers; 3 regression tests added → `tests/with-action.test.ts` 21/21 pass).
- [x] **WS-1a** — DONE: `voidOrderItem` migrated (it is the documented `withActionPositional` example in `with-action.ts`).
- [x] **WS-1b** — DONE 2026-05-31 (skip-removal): all POS `Skip withAction` migrated with exact behavior parity — `fetchActiveOrders` + `fetchOrderDetail` (`customAuth: posUseAuth`; fetchOrderDetail keeps the in-handler `probePermission` UI hint), `closePosSession` (standard `roles+permission`), `openPosSession` (standard path + new `forbiddenError: "Không có quyền mở ca"` + `permissionBranchId` cashbox hint). `payment-actions.ts` was already 0-skip. **Pile-up NOT yet resolved** — `order-actions.ts` still holds ~12 actions + reads in one module; the concern-split is WS-3 (not a line-count fix). Gates: typecheck 6/6, lint 0-err, build 2/2. Live POS smoke still owner-gated.
- [x] **WS-2** — DONE 2026-05-31 (skip-removal): all inventory `Skip withAction` migrated — `updatePurchaseOrderStatus` (positional, exact), `fetchPoSuggestions` (object-input `withAction`; `poSuggestionsSchema` got per-level `"Dữ liệu không hợp lệ"` so the generic validation message survives the helper's `issues[0]` surfacing), `updateSupplier` (id-first merged `z.object({ id, ...supplierSchema.shape })` preserves the `"ID không hợp lệ"`-first ordering). grn/production/actions were already 0-skip. **Pile-up NOT yet resolved** — `grn-actions`/`production-actions`/`actions` each still cram many actions per file; concern-split is WS-3. Optional follow-up: route `lib/messages/inventory.ts` through `rpc-error-map.ts`.
- [~] **WS-3 — decomposition (IN PROGRESS).** Goal: each file owns ONE clear concern, **measured by cohesion, not a line count**. Pattern (proven on order-actions): split along responsibility lines into flat `*-actions.ts` siblings + a plain re-export barrel (NO `"use server"` on the barrel — it just re-exports the leaf `"use server"` actions; **build-verified** in Next 16) so caller import paths don't change. Bodies moved byte-exact (`sed`), only headers + barrel hand-written → `git diff -w` shows pure relocation.
  - [x] **`order-actions.ts`** — DONE 2026-05-31: 1619 → 37-line barrel; split into `order-reads.ts` (6 query actions, 800), `order-lifecycle.ts` (submit/append/updateStatus/markServed, 339), `order-void-actions.ts` (void/reduce/edit/cancel, `posVoidAuth`, 393), `order-adjust-actions.ts` (priority×2 + transfer, 120). typecheck 6/6 + lint 0-err + build 2/2 + web tests 117/117. (reads is 800 LoC but ONE concern = queries; fine per the cohesion goal — split further only if a sub-concern emerges.)
  - [x] **`grn-actions.ts`** (was 1572, a 4-concern pile-up) — DONE 2026-05-31: extracted **Recipes** → `recipe-actions.ts` (684) and **Supplier-Invoices** (3-way match) → `supplier-invoice-actions.ts` (147). grn-actions now **762** = GRN-core + cross-domain `fetchRecentActivity` (one cohesive concern). Sole direct importer `procurement-actions.ts` re-points to the new files (UI callers go through that aggregator, unchanged); pruned grn's now-orphaned `addVNDateDays`/`PG_ERR`/spreadsheet imports. typecheck 6/6 + lint 0-err + build 2/2 + tests 117/117.
  - [x] **`production-actions.ts`** (was 1470) — DONE 2026-05-31: split recipes vs orders + a shared module. `_lib/production-shared.ts` (95: `PRODUCTION_ROLES`, `isCentralKitchenScopedRole`, `idSchema`, `RpcClient`, `requireCentralKitchenBranch` — verified used by both sides), `production-recipe-actions.ts` (919: 8 fns + CSV import/export), `production-order-actions.ts` (478: 4 fns + shortage parsing); `production-actions.ts` → 32-line barrel re-exporting all 17 public names (incl. types) so 6 callers unchanged. typecheck 6/6 + lint 0-err + build 2/2 + tests 117/117 (first try).
  - [x] **`actions.ts`** (was 1433) — clean part DONE 2026-05-31: extracted `ingredient-actions.ts` (719), `stock-actions.ts` (121), `alert-actions.ts` (235); `actions.ts` → 390 (stocktake only). **Barrel impossible here** — a `"use server"` file can only export async fns, so it cannot re-export; instead **20 callers repointed** directly to the new files (3 had mixed-concern imports → split). typecheck 6/6 + lint 0-err + build 2/2 + tests 117/117. **STILL DEFERRED (owner steer):** stocktake left in actions.ts; its overlap with the existing `stocktake-actions.ts` (merge vs keep) is unresolved — actions.ts is now effectively a 2nd stocktake file.
  - [ ] **Client shells** → `_hooks/` (state / realtime / actions) + `views/`: `pos-desktop-shell` (~1.7k), `grn-detail-client` (~1.5k), `order-detail-sheet` (~1.5k). Higher risk (realtime subscribe/unsubscribe) — needs running-app verify.
  - One PR per file. (Parallel `pos/hooks/` already retired under WS-4.)
- [x] **WS-4** — DONE 2026-05-31: `S12_DASHBOARD_V2` deleted + `S13A_STOCKTAKE_V2`→`INVENTORY_STOCKTAKE_REDESIGNED` (`inv_stocktake_redesigned`) enum + DB key (archived migration `20260602007000`) landed earlier; this slice finished the tail — URL token `stocktake_v2_not_enabled`→`stocktake_redesigned_not_enabled` (4 files) + merged `pos/hooks/`→`pos/_hooks/` (2 files, sole importer `pos-desktop-provider.tsx`). Exit grep clean; POS hook folder count==1; typecheck 6/6 + lint 0-err + build 2/2. Residual (out of scope): `messages.inventory.stocktake.v2` dict key; `kds/hooks/` uses a no-underscore convention (no drift — just differs from POS `_hooks/`).

## Owner-gated / pre-go-live (NOT via agent; NOT to matu-prod)

- [ ] Run real POS → payment → KDS/print → HĐĐT smoke in approved dev/test or staging with live provider credentials. [2026-05-28] `stock` leg dropped per owner policy "không trừ kho" (memory `project_pos_action_helper_refactor.md`; commit `e4eb93bc`); action-layer enforcement landed (`9ba83205`); **webhook stock-leg disable migration still pending owner authorize**.
- [ ] **Dead RPC candidates (owner-gated)** — 13 candidates were found by static DB/source scan (`handle_new_user`, `has_position`, `post_payroll_journal`, `release_table`, `resolve_po_price`, `resolve_po_prices_batch`, `rotate_branch_override_code`, `set_branch_kind`, `sync_missing_permissions_from_template`, `transition_order_status`, `transition_order_item_status`, `try_auto_approve_grn`, `update_my_profile`). NOT dropped: all touch money/auth/order/procurement or external-client risk, and monorepo grep cannot prove no Flutter/external caller. Each needs per-RPC owner sign-off + T3 migration.
- [ ] Apply the 3 Phase-1 RLS fixes (now under `supabase/migrations/_archive/`: `20260602008000` payroll, `009000` attendance, `010000` dedup) to the live prod IF it still serves — OWNER-gated, NOT via agent, **NOT to matu-prod**. matu-dev + the baseline already include them.
- [ ] Uptime monitor on `/api/health` (UptimeRobot — ops, not code).

## Architecture / Schema / Migration (matu-dev) — 2026-05-30

> Owner approved rebuilding matu-dev ("không cần giữ nguyên"). matu-dev only; never matu-prod. Current baseline contract lives in `docs/spec/database-schema.md` and `supabase/migrations/README.md`.

- ✅ **DONE (Phase 1 + 2):** RLS policy dedup (`20260602010000`, closed `stock_transfer_items` any-tenant read+write bypass) + schema-health verified clean; baseline-first consolidation — `supabase/migrations/00000000000000_baseline.sql` (validated: rebuilt matu-dev exactly, replays from empty, `db:types`/typecheck match), 379 files → `_archive/`, option X (prod keeps history); managed-surfaces companion `supabase/managed-surfaces.install.sql` (extensions/buckets/14 storage policies/realtime/cron); Docker-free libpq extract engine (default) + `db:types` matu-dev default. (git `60c81ffd`/`f92cc4d4`/`8f73885f`/`43a3ec4b`.)
- [x] **Phase 3** — DONE 2026-05-30: synced `docs/spec/database-schema.md` (snapshot → 118 tables/2 active migrations + baseline-first migration-layout section + live manifest) and `docs/CODEBASE_MAP.md` (counts 118 + baseline-first note) to the consolidated baseline.
- [ ] Unused indexes (~231 flagged on prod) — deferred: prod `stats_reset=NULL` + 77% `idx_scan=0` implausible (recent reset), not representative. Re-assess only after ≥1 business cycle incl. month-end.
- [ ] (cosmetic) Repair matu-dev `schema_migrations` history to baseline-first (schema already rebuilt from baseline).

## Deferred post-pilot

- [ ] **POS calls provider before DB lock** — RPC fail = orphan gateway order. **DEFER-WITH-MITIGATION** per m4 plan.
- [ ] **HĐĐT post-pilot (P0/P1/P2)**: reconcile cron for orphan `signing` (admin retry covers pilot); replace flow TT 78 (pilot cancel + manual portal); provider config via `system_settings` encrypted (env-only OK for single-tenant); PDF/XML persist + download UI (portal link OK); **3-way matching UI for `supplier_invoices`** (columns exist, no PO↔GRN↔Invoice workflow → 01/GTGT export blocked).
- [ ] **Branch Kitchen site split Phase 2**: dual-write + cutover; seed 1 `inventory_locations` kitchen/warehouse per branch; reduce `Kho CN → Bếp CN` state machine (`draft → confirmed`); intra-branch `Cấp bếp` atomic RPC.
- [~] OBSOLETE per owner policy 2026-05-28 "không trừ kho": the "RPC must resolve `default_consumption` or fail hard" item no longer applies (stock deduction disabled at payment flow). RPC `consume_stock_for_order` + action-layer callsites (`createPayment`/`confirmPayment`) can be removed in a separate slice when owner authorizes.
- [ ] **#3 optional post-deploy cleanup**: legacy-import shim for in-flight browser GRN drafts? stale-draft cleanup job (14d)?
- [ ] **F-018: Supplier "Khác"** — BLOCKED-PRODUCT. Need product input: (a) require formal NCC, (b) "Mua ngoài" + inline note, or (c) accept generic "Khác".
- [ ] **F-009: Stock master-detail as drawer** — invasive refactor; current side-panel acceptable.
- [ ] P3: Login rate limit fail-open when Upstash unreachable — documented design decision, needs observability.
- [ ] Legacy `auth_role()` RPC cleanup α4c — finish `can_access_branch` removal via a dedicated RLS-policy batch (α4b shipped: `20260601810000` rewrote `admin_update_profile`/`toggle_profile_active`/`set_branch_kind`).
- [ ] Inventory smoke regression runbook (`docs/runbooks/inventory/pre-release-qa.md`) — run periodically post-launch.
- [ ] Ops reconciliation query for Momo payment/order desync in `/admin/finance` (post-launch tooling).
- [ ] Automated E2E POS→payment→stock (before scale 3+ branches).
- [ ] Staging env / Vercel Preview (before external users).
- [ ] M7 BHXH/PIT calc wiring (Excel for pilot <5 employees).
- [ ] M6 VAS journal entries complete (CSV for accountant in pilot).
- [ ] Refunds table + flow (storage shipped; correctness gaps tracked under M4).
- [ ] M5-Ext S8 — yield factor + AP aging + consumption variance (not needed at 30–50 SKU).
- [x] **H3: intermediate access scoping** — replaced by explicit branch grants or tenant-level permissions. No intermediate scope remains in the active auth model.
- [ ] **H3b: `has_permission()` dual-source flip** — defer until 2nd silent-demote incident (`tenants.owner_user_id` data foundation shipped `20260601500000`). Per ADR 0005.
- [ ] **transfer_ownership(p_new_user_id) RPC + UI** — blocked on business design (instant vs 2-phase, representative sync, audit shape, permission gate). Manual SQL UPDATE OK for pilot. ADR 0005.
- [ ] **L6: Finance migration chain ADR** — document the 5-migration finance chain ordering + rollback deps (non-blocking).
- [ ] **Dead RPC drop wave 2** (post-pilot) — needs `pg_stat_user_functions` telemetry from real pilot traffic. Tiers B/C/D per rule RPC-DROP-MUST-SCAN-6-CHANNELS.

## N/A while Má Tư is a Hộ kinh doanh (no formal BCTC)

- [ ] `voidJournalEntry` closed-period void still mutates signed BCTC — current-period reversal guard EXISTS (`20260527000000`); REMAINING: voiding a _posted_ entry whose ORIGINAL `entry_date` is in a CLOSED period flips it → `voided` and statements (`statement-actions.ts:67,179`) filter `status='posted'` + `entry_date`, so the signed period changes retroactively. T3 decision: (A) reject void when original period closed [recommended], (B) keep original posted + only post reversal, (C) reports count reversed entries in original period. **DEFERRED 2026-05-30**: Hộ kinh doanh files no BCTC (TT 88/2021) → moot. Revisit only on conversion to a company form.

## Doc maintenance reminders

- When Inventory behavior changes → update `docs/ref/inventory.md`, `docs/ref/inventory-sop.md`, `docs/ref/inventory-rbac-matrix.md`, and `docs/modules/web-app.md` in the same PR.
- When the real stock-split phase ships → update active Inventory docs in the same slice.

## Post-v1.0 (Tier 2)

- [ ] Local-First Branch (mini PC + SQLite, offline POS/KDS)
- [ ] QR Self-Order tại bàn
- [ ] Loyalty / Vouchers
- [ ] Advanced Analytics
- [ ] VNPay integration
- [ ] Employee portal full features
- [ ] **Native POS migration (PWA → Flutter Android)** — evaluate when pilot triggers: BT/USB printer fail >5%, cash-drawer auto-pop, e-wallet native SDK > deeplink, scale ≥20 branches. Stepping stone: Capacitor wrap before Flutter rewrite.
