# Release Notes

This folder contains historical release notes and session summaries.
Release notes preserve shipped context and evidence, but they are not runtime
authority for current architecture, route behavior, database shape, or UI rules.

For current source-of-truth docs, start from `docs/README.md`,
`docs/CODEBASE_MAP.md`, `tasks/todo.md`, and `tasks/regressions.md`.
