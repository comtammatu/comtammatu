diff --git a/dist/main/GoTrueClient.js b/dist/main/GoTrueClient.js
index 422a07c4f3306e5f4022d1b56e8e9f44b44385a5..8b05ceb5d80b908306c517645dda07074f62325b 100644
--- a/dist/main/GoTrueClient.js
+++ b/dist/main/GoTrueClient.js
@@ -3606,10 +3606,12 @@ class GoTrueClient {
             catch (err) {
                 await ((_b = this.stateChangeEmitters.get(id)) === null || _b === void 0 ? void 0 : _b.callback('INITIAL_SESSION', null));
                 this._debug('INITIAL_SESSION', 'callback id', id, 'error', err);
-                if ((0, errors_1.isAuthSessionMissingError)(err)) {
-                    console.warn(err);
-                }
-                else {
+                const isTerminalSessionError = (0, errors_1.isAuthSessionMissingError)(err) ||
+                    ((0, errors_1.isAuthApiError)(err) &&
+                        (err.code === 'refresh_token_not_found' ||
+                            err.code === 'refresh_token_already_used' ||
+                            err.code === 'session_expired'));
+                if (!isTerminalSessionError) {
                     console.error(err);
                 }
             }
diff --git a/dist/module/GoTrueClient.js b/dist/module/GoTrueClient.js
index e6d611ef760ad9e9ee3f1689649cd819b1286765..b4be4b71087547dfc0709989929dbc396ffb3452 100644
--- a/dist/module/GoTrueClient.js
+++ b/dist/module/GoTrueClient.js
@@ -3603,10 +3603,12 @@ class GoTrueClient {
             catch (err) {
                 await ((_b = this.stateChangeEmitters.get(id)) === null || _b === void 0 ? void 0 : _b.callback('INITIAL_SESSION', null));
                 this._debug('INITIAL_SESSION', 'callback id', id, 'error', err);
-                if (isAuthSessionMissingError(err)) {
-                    console.warn(err);
-                }
-                else {
+                const isTerminalSessionError = isAuthSessionMissingError(err) ||
+                    (isAuthApiError(err) &&
+                        (err.code === 'refresh_token_not_found' ||
+                            err.code === 'refresh_token_already_used' ||
+                            err.code === 'session_expired'));
+                if (!isTerminalSessionError) {
                     console.error(err);
                 }
             }
diff --git a/src/GoTrueClient.ts b/src/GoTrueClient.ts
index 4f85623bcaa20f1076dd22b051c45b742b536912..91a6de2312ae1293eb99026fd1720a1cac8399be 100644
--- a/src/GoTrueClient.ts
+++ b/src/GoTrueClient.ts
@@ -4290,9 +4290,13 @@ export default class GoTrueClient {
       } catch (err) {
         await this.stateChangeEmitters.get(id)?.callback('INITIAL_SESSION', null)
         this._debug('INITIAL_SESSION', 'callback id', id, 'error', err)
-        if (isAuthSessionMissingError(err)) {
-          console.warn(err)
-        } else {
+        const isTerminalSessionError =
+          isAuthSessionMissingError(err) ||
+          (isAuthApiError(err) &&
+            (err.code === 'refresh_token_not_found' ||
+              err.code === 'refresh_token_already_used' ||
+              err.code === 'session_expired'))
+        if (!isTerminalSessionError) {
           console.error(err)
         }
       }
